# 测试环境 CORS 问题排查清单

## 当前状态

✅ **域名已添加**: `http://152.136.183.181:3000` (2025-12-14 21:39:09)  
❌ **CORS 仍然报错**: 访问 CloudBase API 被阻止

---

## 🔍 问题分析

### CORS 错误详情
```
Access to XMLHttpRequest at 'https://jihua-oa-dev-3goht9irae4d949f.ap-shanghai.tcb-api.tencentcloudapi.com/web?env=jihua-oa-dev-3goht9irae4d949f' 
from origin 'http://152.136.183.181:3000' 
has been blocked by CORS policy: Response to preflight request doesn't pass access control check: 
No 'Access-Control-Allow-Origin' header is present on the requested resource.
```

### 可能的原因

1. **⏰ CORS 配置延迟**  
   - CloudBase 的安全域名配置可能需要 10-15 分钟全网生效
   - **解决方案**: 等待 10-15 分钟后重试

2. **🔌 协议匹配问题**  
   - 测试环境使用 HTTP（`http://152.136.183.181:3000`）
   - CloudBase API 使用 HTTPS
   - 可能存在混合内容问题

3. **🌐 浏览器缓存**  
   - 浏览器可能缓存了旧的 CORS 策略
   - **已尝试**: 清除缓存，但未解决

---

## ✅ 快速解决方案（3选1）

### 方案 A：等待配置生效（推荐）⏰

**最简单但需要等待**

```bash
# 1. 等待 10-15 分钟
# 2. 清除浏览器缓存
# 3. 重新访问: http://152.136.183.181:3000
```

**为什么推荐**:
- ✅ 无需修改代码
- ✅ 一劳永逸
- ⏰ 需要等待 10-15 分钟

---

### 方案 B：使用 CloudBase 静态托管（最快）🚀

**立即可用，无需等待**

CloudBase 自带静态托管域名，天然支持 CORS：

```bash
# 1. 上传到 CloudBase 静态托管
# （使用 MCP 工具自动上传）

# 2. 访问 CloudBase 托管域名
https://jihua-oa-dev-3goht9irae4d949f-1301818329.ap-shanghai.app.tcloudbase.com
```

**优势**:
- ✅ 立即生效（无 CORS 问题）
- ✅ 免费 CDN 加速
- ✅ HTTPS 支持
- ✅ 自动部署

**实施步骤**:
1. 运行部署脚本上传到 CloudBase
2. 访问 CloudBase 域名
3. 完全没有 CORS 问题

---

### 方案 C：配置测试环境 HTTPS（专业）🔒

**彻底解决混合内容问题**

为测试环境配置 HTTPS（使用自签名证书）：

```bash
# 1. 生成自签名证书
openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
  -keyout /etc/nginx/ssl/jihua-dev.key \
  -out /etc/nginx/ssl/jihua-dev.crt

# 2. 修改 Nginx 配置
listen 3000 ssl;
ssl_certificate /etc/nginx/ssl/jihua-dev.crt;
ssl_certificate_key /etc/nginx/ssl/jihua-dev.key;

# 3. 重启 Nginx
systemctl restart nginx

# 4. 添加 HTTPS 域名到 CloudBase
https://152.136.183.181:3000
```

**优势**:
- ✅ 完全模拟生产环境
- ✅ 无混合内容问题
- ⏰ 配置需要 30 分钟

---

## 🎯 我的建议

### 当前立即使用：**方案 B**（CloudBase 静态托管）

**原因**:
1. ✅ **立即可用** - 无需等待，马上验证功能
2. ✅ **零配置** - CloudBase 天然支持
3. ✅ **生产级** - 和正式环境一致

### 长期使用：**方案 A**（等待配置生效）

**原因**:
1. ✅ **自定义域名** - 符合企业标准
2. ✅ **统一管理** - 和生产环境一致的部署流程

---

## 📋 立即执行：方案 B（CloudBase 静态托管）

我现在就可以帮你部署到 CloudBase 静态托管，立即解决 CORS 问题！

### 需要执行的命令

```bash
# 使用 MCP 工具上传到 CloudBase 静态托管
# （我会自动执行）
```

**是否立即执行方案 B？** 👍

---

## 🔍 验证配置是否生效

### 测试页面
访问: `http://152.136.183.181:3000/测试CORS.html`

### 预期结果
- ✅ 看到 "✅ 数据库连接成功"
- ✅ 控制台无 CORS 错误

### 当前结果
- ❌ CORS 错误（配置可能还在传播中）

---

## 📞 CloudBase 控制台

如果需要手动检查配置:

**安全域名管理**:  
https://console.cloud.tencent.com/tcb/env/safety?envId=jihua-oa-dev-3goht9irae4d949f

**静态托管**:  
https://console.cloud.tencent.com/tcb/hosting/index?envId=jihua-oa-dev-3goht9irae4d949f

---

## 🎯 下一步

请选择一个方案：

1. **方案 A**: 我等待 10-15 分钟后重试
2. **方案 B**: 立即使用 CloudBase 静态托管（推荐）
3. **方案 C**: 配置测试环境 HTTPS

**推荐选择方案 B，立即可用！** 🚀
