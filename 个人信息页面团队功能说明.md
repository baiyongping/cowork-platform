# 个人信息页面团队功能说明

## 功能概述

对个人信息页面进行了重要升级，将"个人账户设置"更名为"个人信息"，并新增"团队"模块，方便用户查看组织结构和团队成员信息。

## 修改内容

### 1. 页面标题优化
- **原标题**: "个人账户设置"
- **新标题**: "个人信息"
- **副标题**: "管理您的个人信息、安全设置和查看团队成员"

### 2. 新增"团队"标签页
在原有的"基本信息"、"修改手机号"、"修改密码"基础上，新增"团队"标签页。

### 3. 团队模块功能

#### 📊 数据展示
- **部门列表**: 显示所有部门及其成员数量
- **员工信息**: 每个部门下展示员工详细信息
  - 姓名
  - 用户名
  - 手机号
  - 角色(管理员/成员)

#### 🎯 交互特性
- **可折叠部门**: 点击部门名称可展开/折叠成员列表
- **默认展开**: 所有部门默认处于展开状态
- **优雅动画**: 展开/折叠带有平滑过渡效果
- **响应式布局**: 自适应不同屏幕尺寸

## 界面设计

### Tab导航栏
```
┌────────────────────────────────────────────────────────┐
│  [👤 基本信息] [👥 团队] [📱 修改手机号] [🔒 修改密码]  │
└────────────────────────────────────────────────────────┘
```

### 团队页面布局
```
┌──────────────────────────────────────────────────────┐
│  团队成员                                             │
│  查看各部门的团队成员信息                             │
├──────────────────────────────────────────────────────┤
│                                                       │
│  ┌─────────────────────────────────────┐            │
│  │ 🏢 销售部         5名成员     [▼]   │            │
│  ├─────────────────────────────────────┤            │
│  │  👤 张三  | zhangsan | 13800138000  │            │
│  │  👤 李四  | lisi     | 13900139000  │            │
│  │  ...                                │            │
│  └─────────────────────────────────────┘            │
│                                                       │
│  ┌─────────────────────────────────────┐            │
│  │ 🏢 技术部         8名成员     [▲]   │            │
│  └─────────────────────────────────────┘            │
│                                                       │
└──────────────────────────────────────────────────────┘
```

## 技术实现

### 状态管理
```typescript
// Tab状态
const [activeTab, setActiveTab] = useState<'info' | 'team' | 'phone' | 'password'>('info');

// 团队数据
const [departments, setDepartments] = useState<any[]>([]);
const [departmentEmployees, setDepartmentEmployees] = useState<Map<string, any[]>>(new Map());
const [loadingTeam, setLoadingTeam] = useState(false);

// 折叠状态(默认全部展开)
const [expandedDepartments, setExpandedDepartments] = useState<Set<string>>(new Set());
```

### 数据加载流程
```typescript
const loadTeamData = async () => {
  // 1. 加载所有部门
  const deptResult = await db.collection('departments').get();
  
  // 2. 加载所有已审核的员工
  const usersResult = await db.collection('users')
    .where({ approvalStatus: 'approved' })
    .get();
  
  // 3. 过滤已删除的员工
  const allUsers = usersResult.data.filter(user => user.deleted !== true);
  
  // 4. 构建部门-员工映射关系
  const deptEmpMap = new Map();
  allDepartments.forEach(dept => {
    const employees = allUsers.filter(user => 
      dept.memberIds && dept.memberIds.includes(user._id)
    );
    deptEmpMap.set(dept._id, employees);
  });
  
  // 5. 默认展开所有部门
  setExpandedDepartments(new Set(allDepartments.map(d => d._id)));
};
```

### 折叠/展开逻辑
```typescript
const toggleDepartment = (deptId: string) => {
  setExpandedDepartments(prev => {
    const newSet = new Set(prev);
    if (newSet.has(deptId)) {
      newSet.delete(deptId);  // 折叠
    } else {
      newSet.add(deptId);     // 展开
    }
    return newSet;
  });
};
```

## UI特性

### 1. 部门卡片
- **渐变背景**: 灰色渐变背景，悬停时颜色加深
- **部门图标**: 蓝色到青色渐变的建筑图标
- **成员统计**: 显示部门成员数量
- **展开指示器**: ChevronDown/ChevronUp图标

### 2. 员工卡片
- **头像**: 圆形渐变背景显示姓名首字母
- **信息网格**: 响应式3列布局(姓名、用户名、手机号)
- **角色标签**: 
  - 管理员: 紫色徽章
  - 成员: 蓝色徽章
- **悬停效果**: 背景变浅灰色

### 3. 空状态提示
- **无部门**: 显示建筑图标和提示文字
- **无成员**: 部门展开时显示"暂无成员"

### 4. 底部提示
- 蓝色渐变提示卡片
- 说明部门可折叠的操作方式

## 使用场景

### 查看团队结构
1. 点击"团队"标签
2. 自动加载所有部门和成员
3. 默认展开所有部门

### 折叠/展开部门
1. 点击部门头部任意位置
2. 部门成员列表平滑展开或折叠
3. 图标随状态切换(▼/▲)

### 查看成员信息
1. 在展开的部门中浏览成员列表
2. 查看姓名、用户名、手机号
3. 通过角色标签识别管理员

## 性能优化

1. **懒加载**: 只在切换到团队Tab时加载数据
2. **Map结构**: 使用Map存储部门-员工映射，O(1)查询
3. **Set结构**: 使用Set管理展开状态，高效增删
4. **条件渲染**: 只渲染展开的部门成员列表

## 兼容性

- ✅ 响应式设计，支持手机/平板/桌面
- ✅ 兼容旧数据(无phone字段显示username)
- ✅ 处理空部门和空成员列表
- ✅ 处理已删除员工(deleted=true)

## 后续优化建议

1. **搜索功能**: 添加成员搜索框
2. **筛选功能**: 按角色、状态筛选
3. **导出功能**: 导出部门成员通讯录
4. **联系方式**: 点击手机号直接拨打或复制
5. **批量操作**: 全部展开/全部折叠按钮
6. **成员详情**: 点击员工查看更多信息

## 测试要点

### 功能测试
- ✅ Tab切换正常
- ✅ 团队数据加载正确
- ✅ 部门折叠/展开正常
- ✅ 成员信息显示完整
- ✅ 角色标签显示正确

### 边界测试
- ✅ 无部门数据
- ✅ 部门无成员
- ✅ 员工缺少字段
- ✅ 大量部门和成员

### UI测试
- ✅ 响应式布局
- ✅ 悬停效果
- ✅ 动画流畅
- ✅ 图标显示

## 更新日期

2025-12-14

## 修改文件

- `components/pages/AccountSettings.tsx`: 新增团队模块，修改页面标题

## 截图说明

页面包含以下4个标签:
1. **基本信息**: 显示个人账户信息
2. **团队**: 显示部门及成员列表(新增)
3. **修改手机号**: 修改绑定手机号
4. **修改密码**: 修改登录密码
