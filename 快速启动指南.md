# 🚀 际华协同办公平台 - 快速启动指南

## ✅ 已完成的配置

### 1. 环境配置
- ✅ `.env.development` - 本地开发环境配置
- ✅ `.env.test` - 测试服务器环境配置  
- ✅ `.env.production` - 生产环境配置
- ✅ `lib/cloudbase.ts` - 自动环境切换代码
- ✅ `cloudbaserc.json` - CloudBase 项目配置

### 2. 云函数
- ✅ `cloudfunctions/initDatabase/` - 数据库初始化云函数

### 3. 安全域名（已添加）
- ✅ `https://152.136.183.181:3443`
- ✅ `http://152.136.183.181:3000`
- ✅ `152.136.183.181:3443`
- ✅ `152.136.183.181:3000`

---

## 🎯 初始化步骤（3步完成）

### 方法 A：使用批处理脚本（推荐）

**直接双击运行**：`init.bat`

这会自动完成：
1. 部署 `initDatabase` 云函数
2. 调用云函数初始化数据库（创建集合和管理员账号）

### 方法 B：手动执行命令

```bash
# 1. 部署云函数
cloudbase functions:deploy initDatabase --envId cowork-9gg9oocb516be5fb

# 2. 调用云函数初始化数据库
cloudbase functions:invoke initDatabase --envId cowork-9gg9oocb516be5fb
```

### 方法 C：在 CloudBase 控制台手动创建集合

如果云函数方式失败，可以手动创建：

**打开控制台**：https://tcb.cloud.tencent.com/dev?envId=cowork-9gg9oocb516be5fb#/db/doc

**创建以下集合并添加数据**：

#### 1️⃣ roles
```json
{
  "_id": "role-admin",
  "name": "超级管理员",
  "code": "super_admin",
  "permissions": [
    { "module": "all", "actions": ["view", "create", "edit", "delete"] }
  ],
  "isSystem": true,
  "status": "启用"
}
```

#### 2️⃣ departments
```json
{
  "_id": "dept-sales",
  "name": "销售部",
  "code": "SALES",
  "status": "正常"
}
```

#### 3️⃣ employees
```json
{
  "_id": "admin-001",
  "username": "admin",
  "password": "240be518fabd2724ddb6f04eeb1da5967448d7e831c08c8fa822809f74c720a9",
  "name": "系统管理员",
  "email": "admin@jihua.com",
  "roleIds": ["role-admin"],
  "role": "admin",
  "status": "在职",
  "isActive": true
}
```

#### 4️⃣ system_configs
```json
{
  "category": "system",
  "key": "system_name",
  "value": "际华定制协同办公管理平台",
  "dataType": "string",
  "isEditable": true
}
```

#### 5️⃣ 创建空集合
- tasks
- opportunities
- projects
- goals

---

## 🖥️ 本地开发

### 1️⃣ 启动开发服务器

```bash
npm run dev
```

### 2️⃣ 访问应用

**地址**：http://localhost:5173

### 3️⃣ 登录

- **用户名**：`admin`
- **密码**：`admin123`

---

## 🌐 测试环境部署

### 已完成配置

✅ 代码已指向测试环境：`cowork-9gg9oocb516be5fb`  
✅ HTTPS已配置：`https://152.136.183.181:3443`  
✅ 项目已构建（2.5 MB）

### 部署步骤

```bash
# 1. 上传文件
scp -r dist/* root@152.136.183.181:/var/www/jihua-dev/

# 2. 设置权限
ssh root@152.136.183.181 "chown -R www-data:www-data /var/www/jihua-dev && chmod -R 755 /var/www/jihua-dev"
```

### 访问测试环境

**地址**：`https://152.136.183.181:3443`

**处理证书警告**：
- 点击 **"高级"** → **"继续访问"**

**强制刷新**：
- 按 **`Ctrl + F5`**

---

## 📊 环境架构

```
本地开发 (localhost:5173)
    ↓ 连接
测试数据库 (cowork-9gg9oocb516be5fb)
    ↑ 连接
测试服务器 (152.136.183.181:3443)

           独立隔离 ⚡

生产环境 (jihuadz.xin / 152.136.183.181)
    ↓ 连接
生产数据库 (jihua-oa-dev-3goht9irae4d949f)
```

---

## 🔧 环境变量说明

### .env.development (本地开发)
```env
VITE_CLOUDBASE_ENV_ID=cowork-9gg9oocb516be5fb
VITE_API_URL=http://localhost:5173
```

### .env.test (测试服务器)
```env
VITE_CLOUDBASE_ENV_ID=cowork-9gg9oocb516be5fb
VITE_API_URL=https://152.136.183.181:3443
```

### .env.production (生产环境)
```env
VITE_CLOUDBASE_ENV_ID=jihua-oa-dev-3goht9irae4d949f
VITE_API_URL=https://jihuadz.xin
```

---

## 📝 CloudBase 控制台入口

### 测试环境
**环境管理**：https://tcb.cloud.tencent.com/dev?envId=cowork-9gg9oocb516be5fb

**快捷入口**：
- 数据库：https://tcb.cloud.tencent.com/dev?envId=cowork-9gg9oocb516be5fb#/db/doc
- 云函数：https://tcb.cloud.tencent.com/dev?envId=cowork-9gg9oocb516be5fb#/scf
- 安全域名：https://tcb.cloud.tencent.com/dev?envId=cowork-9gg9oocb516be5fb#/identity/login-manage

### 生产环境
**环境管理**：https://tcb.cloud.tencent.com/dev?envId=jihua-oa-dev-3goht9irae4d949f

---

## ⚠️ 注意事项

### 1. 数据隔离
- **测试环境**和**生产环境**使用不同的 CloudBase 环境
- 数据完全隔离，互不影响

### 2. 构建切换
```bash
# 本地开发（自动使用 .env.development）
npm run dev

# 测试环境构建（使用 .env.test）
npm run build -- --mode test

# 生产环境构建（使用 .env.production）
npm run build
```

### 3. 安全域名
- 每个环境需要单独配置安全域名
- 已为测试环境添加：`https://152.136.183.181:3443`

### 4. 云函数
- 云函数部署到特定环境
- 使用 `--envId` 参数指定环境

---

## 🎉 完成检查清单

- [ ] 数据库已初始化（集合和管理员账号已创建）
- [ ] 本地开发环境可以启动（`npm run dev`）
- [ ] 可以使用 `admin/admin123` 登录
- [ ] 测试服务器文件已上传
- [ ] 测试环境可以访问（`https://152.136.183.181:3443`）

---

## 🆘 常见问题

### Q1: CORS 错误
**原因**：安全域名未配置或未生效  
**解决**：
1. 检查 CloudBase 控制台安全域名配置
2. 等待 5-10 分钟让配置生效
3. 清除浏览器缓存（`Ctrl + F5`）

### Q2: 登录失败
**原因**：数据库未初始化或管理员账号未创建  
**解决**：
1. 检查 CloudBase 控制台是否有 `employees` 集合
2. 检查是否有 `admin-001` 文档
3. 重新运行初始化脚本

### Q3: 无法连接数据库
**原因**：环境 ID 配置错误  
**解决**：
1. 检查 `lib/cloudbase.ts` 中的环境 ID
2. 检查 `.env.development` 配置
3. 重启开发服务器

---

## 📞 技术支持

**CloudBase 文档**：https://docs.cloudbase.net/  
**CloudBase 控制台**：https://tcb.cloud.tencent.com/

---

**祝开发顺利！** 🎊
