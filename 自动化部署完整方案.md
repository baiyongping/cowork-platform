# 际华协同办公管理平台 - 自动化部署完整方案

## 🎯 部署目标

实现开发完成后的一键自动化部署，包括：
- ✅ 前端构建和部署
- ✅ 后端服务部署
- ✅ HTTPS 配置
- ✅ 服务自动重启

---

## 📋 服务器信息

**轻量应用服务器（Lighthouse）**
- **实例 ID**: `lhins-pnt984h2`
- **地域**: `ap-beijing`（北京）
- **公网 IP**: `152.136.183.181`
- **SSH 密钥**: `lhkp-bhzb91k4`
- **操作系统**: OpenCloudOS（兼容 CentOS）

**访问地址**
- HTTP: http://152.136.183.181
- HTTPS: https://152.136.183.181 ✅ （自签名证书 - 立即可用）
- HTTPS: https://jihuadz.xin ⏳ （备案完成后启用）

**端口开放状态**
- ✅ 80 (HTTP)
- ✅ 443 (HTTPS)
- ✅ 3000 (后端 API)

---

## 🚀 一键部署脚本

### 方案 A：Windows PowerShell 脚本（推荐）

**文件**: `一键部署.ps1`

```powershell
# 际华协同办公管理平台 - 一键部署脚本
# 使用方法: 右键 -> "使用 PowerShell 运行"

$ErrorActionPreference = "Stop"
$INSTANCE_ID = "lhins-pnt984h2"
$REGION = "ap-beijing"

Write-Host "==================================" -ForegroundColor Cyan
Write-Host "  际华协同办公管理平台 - 一键部署  " -ForegroundColor Cyan
Write-Host "==================================" -ForegroundColor Cyan
Write-Host ""

# 第一步：清理旧构建
Write-Host "[1/6] 清理旧构建..." -ForegroundColor Yellow
if (Test-Path "dist") {
    Remove-Item -Recurse -Force dist
    Write-Host "  ✅ 已清理旧构建" -ForegroundColor Green
}

# 第二步：安装依赖
Write-Host "[2/6] 安装依赖..." -ForegroundColor Yellow
npm install
Write-Host "  ✅ 依赖安装完成" -ForegroundColor Green

# 第三步：构建前端
Write-Host "[3/6] 构建前端..." -ForegroundColor Yellow
npm run build
Write-Host "  ✅ 前端构建完成" -ForegroundColor Green

# 第四步：构建后端
Write-Host "[4/6] 构建后端..." -ForegroundColor Yellow
cd server
npm install
npm run build
cd ..
Write-Host "  ✅ 后端构建完成" -ForegroundColor Green

# 第五步：上传前端到服务器
Write-Host "[5/6] 上传前端到服务器..." -ForegroundColor Yellow
scp -r dist/* root@152.136.183.181:/var/www/jihua/
if ($LASTEXITCODE -eq 0) {
    Write-Host "  ✅ 前端上传成功" -ForegroundColor Green
} else {
    Write-Host "  ❌ 前端上传失败" -ForegroundColor Red
    exit 1
}

# 第六步：上传并重启后端服务
Write-Host "[6/6] 上传并重启后端服务..." -ForegroundColor Yellow
scp -r server/dist/* root@152.136.183.181:/root/jihua-backend/
ssh root@152.136.183.181 "pm2 restart jihua-api || pm2 start /root/jihua-backend/index.js --name jihua-api"
if ($LASTEXITCODE -eq 0) {
    Write-Host "  ✅ 后端部署成功" -ForegroundColor Green
} else {
    Write-Host "  ❌ 后端部署失败" -ForegroundColor Red
    exit 1
}

Write-Host ""
Write-Host "==================================" -ForegroundColor Green
Write-Host "  🎉 部署完成！" -ForegroundColor Green
Write-Host "==================================" -ForegroundColor Green
Write-Host ""
Write-Host "访问地址:" -ForegroundColor Cyan
Write-Host "  HTTP:  http://152.136.183.181" -ForegroundColor White
Write-Host "  HTTPS: https://152.136.183.181" -ForegroundColor White
Write-Host ""
Write-Host "提示: HTTPS 首次访问需要点击 '高级' -> '继续前往'" -ForegroundColor Yellow
Write-Host ""
```

### 方案 B：使用 Lighthouse MCP 工具部署（推荐）

**文件**: `一键部署-MCP.ps1`

```powershell
# 际华协同办公管理平台 - MCP 工具一键部署脚本
# 使用方法: 右键 -> "使用 PowerShell 运行"

$ErrorActionPreference = "Stop"

Write-Host "==================================" -ForegroundColor Cyan
Write-Host "  际华协同办公管理平台 - 一键部署  " -ForegroundColor Cyan
Write-Host "==================================" -ForegroundColor Cyan
Write-Host ""

# 第一步：清理旧构建
Write-Host "[1/5] 清理旧构建..." -ForegroundColor Yellow
if (Test-Path "dist") {
    Remove-Item -Recurse -Force dist
}
Write-Host "  ✅ 已清理" -ForegroundColor Green

# 第二步：安装依赖
Write-Host "[2/5] 安装依赖..." -ForegroundColor Yellow
npm install
Write-Host "  ✅ 完成" -ForegroundColor Green

# 第三步：构建项目
Write-Host "[3/5] 构建项目..." -ForegroundColor Yellow
npm run build
Write-Host "  ✅ 构建完成" -ForegroundColor Green

# 第四步：构建后端
Write-Host "[4/5] 构建后端..." -ForegroundColor Yellow
cd server
npm install
npm run build
cd ..
Write-Host "  ✅ 完成" -ForegroundColor Green

# 第五步：调用 AI 工具自动部署
Write-Host "[5/5] 自动部署到服务器..." -ForegroundColor Yellow
Write-Host ""
Write-Host "  请在 AI 助手中执行以下命令:" -ForegroundColor Cyan
Write-Host "  '帮我部署最新构建到服务器'" -ForegroundColor White
Write-Host ""
Write-Host "  或者手动执行:" -ForegroundColor Cyan
Write-Host "  scp -r dist/* root@152.136.183.181:/var/www/jihua/" -ForegroundColor White
Write-Host "  scp -r server/dist/* root@152.136.183.181:/root/jihua-backend/" -ForegroundColor White
Write-Host "  ssh root@152.136.183.181 'pm2 restart jihua-api'" -ForegroundColor White
Write-Host ""

Write-Host "==================================" -ForegroundColor Green
Write-Host "  🎉 构建完成！等待部署..." -ForegroundColor Green
Write-Host "==================================" -ForegroundColor Green
```

---

## 📁 项目结构

```
e:/cowork/
├── dist/                      # 前端构建输出
│   ├── index.html
│   ├── assets/
│   └── ...
├── server/                    # 后端代码
│   ├── dist/                  # 后端构建输出
│   ├── src/
│   └── package.json
├── 一键部署.ps1              # 一键部署脚本
├── 一键部署-MCP.ps1          # MCP 工具部署脚本
└── 自动化部署完整方案.md     # 本文档
```

**服务器目录**：
```
/var/www/jihua/               # 前端文件
/root/jihua-backend/          # 后端文件
/etc/nginx/conf.d/            # Nginx 配置
/etc/nginx/ssl/               # SSL 证书
```

---

## 🔧 服务器配置

### 1. Nginx 配置

**HTTP 配置**: `/etc/nginx/conf.d/jihuadz.conf`
```nginx
server {
    listen 80;
    server_name 152.136.183.181 jihuadz.xin;
    root /var/www/jihua;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

**HTTPS 配置**: `/etc/nginx/conf.d/jihua-https.conf`
```nginx
server {
    listen 443 ssl http2;
    server_name 152.136.183.181;
    
    # 自签名证书（立即可用）
    ssl_certificate /etc/nginx/ssl/ip-selfsigned.crt;
    ssl_certificate_key /etc/nginx/ssl/ip-selfsigned.key;
    
    # SSL 配置
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    
    root /var/www/jihua;
    index index.html;
    
    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control public;
        try_files $uri =404;
    }
    
    location /assets/ {
        try_files $uri =404;
    }
    
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    location /api {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

### 2. SSL 证书

**当前配置（自签名证书）**:
- 证书: `/etc/nginx/ssl/ip-selfsigned.crt`
- 私钥: `/etc/nginx/ssl/ip-selfsigned.key`
- 有效期: 365 天
- 状态: ✅ 已配置，立即可用

**备案后配置（腾讯云证书）**:
- 证书: `/etc/nginx/ssl/jihuadz.xin_bundle.crt` ⏳
- 私钥: `/etc/nginx/ssl/jihuadz.xin.key` ⏳
- 域名: `jihuadz.xin`
- 状态: 等待备案完成

### 3. 后端服务（PM2）

**服务名**: `jihua-api`
**启动命令**:
```bash
pm2 start /root/jihua-backend/index.js --name jihua-api
```

**管理命令**:
```bash
pm2 list                    # 查看服务状态
pm2 restart jihua-api      # 重启服务
pm2 stop jihua-api         # 停止服务
pm2 logs jihua-api         # 查看日志
pm2 save                   # 保存当前配置
pm2 startup                # 开机自启
```

---

## 🎯 部署流程

### 快速部署（推荐）

1. **开发完成后**，在项目根目录执行：
   ```powershell
   # 方式 1: 右键运行脚本
   右键 "一键部署.ps1" -> "使用 PowerShell 运行"
   
   # 方式 2: 命令行运行
   powershell -ExecutionPolicy Bypass -File "一键部署.ps1"
   ```

2. **等待部署完成**，看到提示：
   ```
   🎉 部署完成！
   访问地址: https://152.136.183.181
   ```

3. **清除浏览器缓存并刷新**：
   ```
   Ctrl + F5 (Windows)
   Cmd + Shift + R (Mac)
   ```

### 手动部署（备用）

如果脚本失败，可以手动执行：

```bash
# 1. 构建前端
npm run build

# 2. 构建后端
cd server
npm run build
cd ..

# 3. 上传前端
scp -r dist/* root@152.136.183.181:/var/www/jihua/

# 4. 上传后端
scp -r server/dist/* root@152.136.183.181:/root/jihua-backend/

# 5. 重启后端服务
ssh root@152.136.183.181 "pm2 restart jihua-api"
```

---

## 🔍 部署检查清单

### 部署前检查

- [ ] 代码已提交到 Git
- [ ] 版本号已更新 (package.json)
- [ ] 依赖已安装 (node_modules)
- [ ] 环境变量已配置 (.env)

### 部署后验证

- [ ] 前端页面能访问 (https://152.136.183.181)
- [ ] 静态资源加载正常 (无 404)
- [ ] 后端 API 正常 (/api/*)
- [ ] 登录功能正常
- [ ] 数据库连接正常
- [ ] 控制台无错误

### 测试用例

**1. HTTPS 访问测试**
```
✅ 访问 https://152.136.183.181
✅ 处理安全警告（点击"高级" -> "继续前往"）
✅ 页面正常显示
✅ Console 无错误（除了可以忽略的警告）
```

**2. 登录测试**
```
✅ 访问登录页面
✅ 输入: admin / admin123
✅ 登录成功，进入主页
✅ 用户信息显示正常
```

**3. API 测试**
```
✅ GET /api/health - 健康检查
✅ POST /api/auth/login - 登录接口
✅ GET /api/users - 用户列表
```

---

## 🛠️ 故障排除

### 问题 1: 部署后页面 404

**症状**: 访问 https://152.136.183.181 显示 404

**排查步骤**:
```bash
# 1. 检查文件是否上传成功
ssh root@152.136.183.181 "ls -la /var/www/jihua/"

# 2. 检查 Nginx 配置
ssh root@152.136.183.181 "nginx -t"

# 3. 重启 Nginx
ssh root@152.136.183.181 "systemctl restart nginx"
```

### 问题 2: API 请求失败

**症状**: 前端显示 "网络错误" 或 "服务器错误"

**排查步骤**:
```bash
# 1. 检查后端服务状态
ssh root@152.136.183.181 "pm2 list"

# 2. 查看后端日志
ssh root@152.136.183.181 "pm2 logs jihua-api --lines 50"

# 3. 检查端口占用
ssh root@152.136.183.181 "ss -tlnp | grep 3000"

# 4. 重启后端服务
ssh root@152.136.183.181 "pm2 restart jihua-api"
```

### 问题 3: 静态资源 404

**症状**: Console 显示 CSS/JS 文件 404

**原因**: 构建后的文件路径不正确

**解决方案**:
```bash
# 检查 vite.config.ts 中的 base 配置
# 应该是 base: '/' 而不是 base: '/dist/'

# 重新构建
npm run build

# 重新部署
scp -r dist/* root@152.136.183.181:/var/www/jihua/
```

### 问题 4: 登录密码验证失败

**症状**: 输入正确密码仍然无法登录

**原因**: HTTP 环境下 `crypto.subtle` API 不可用

**解决方案**:
```bash
# 确保使用 HTTPS 访问
# ✅ https://152.136.183.181
# ❌ http://152.136.183.181

# 如果 HTTPS 不可用，检查证书
ssh root@152.136.183.181 "ls -la /etc/nginx/ssl/"
ssh root@152.136.183.181 "ss -tlnp | grep 443"
```

### 问题 5: SSL 证书过期

**症状**: HTTPS 访问显示 "证书已过期"

**解决方案**:
```bash
# 重新生成自签名证书
ssh root@152.136.183.181 "
cd /etc/nginx/ssl
openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
  -keyout ip-selfsigned.key \
  -out ip-selfsigned.crt \
  -subj '/C=CN/ST=Beijing/L=Beijing/O=JiHua/OU=IT/CN=152.136.183.181'
systemctl reload nginx
"
```

---

## 📊 监控和日志

### Nginx 日志

```bash
# 访问日志
tail -f /var/log/nginx/access.log

# 错误日志
tail -f /var/log/nginx/error.log

# HTTPS 访问日志
tail -f /var/log/nginx/jihua_https_access.log

# HTTPS 错误日志
tail -f /var/log/nginx/jihua_https_error.log
```

### 后端日志

```bash
# PM2 日志
pm2 logs jihua-api

# PM2 错误日志
pm2 logs jihua-api --err

# PM2 输出日志
pm2 logs jihua-api --out
```

### 系统监控

```bash
# 查看 CPU 和内存
top

# 查看磁盘空间
df -h

# 查看网络连接
ss -tlnp

# 查看进程
ps aux | grep node
ps aux | grep nginx
```

---

## 🔐 安全配置

### 1. SSL/TLS 配置

**当前配置**:
- ✅ TLS 1.2 和 TLS 1.3
- ✅ 强加密套件
- ✅ 服务器优先选择加密套件

**备案后升级**:
- 下载腾讯云证书（Nginx 格式）
- 上传到 `/etc/nginx/ssl/`
- 修改 Nginx 配置指向新证书
- 重启 Nginx

### 2. 防火墙配置

**已开放端口**:
- ✅ 22 (SSH)
- ✅ 80 (HTTP)
- ✅ 443 (HTTPS)
- ✅ 3000 (后端 API)

**查看规则**:
```bash
ssh root@152.136.183.181 "firewall-cmd --list-ports"
```

### 3. SSH 密钥

**当前配置**:
- ✅ 密钥 ID: `lhkp-bhzb91k4`
- ✅ 已绑定到实例
- ✅ 可通过 SSH 连接

**备份密钥**:
- 保存在安全位置
- 定期更新密钥

---

## 📝 版本管理

### 版本号规则

使用语义化版本 (Semantic Versioning):
- **主版本号**: 重大功能变更或不兼容更新
- **次版本号**: 新增功能，向下兼容
- **修订号**: Bug 修复，向下兼容

示例:
- v1.0.0 - 初始版本
- v1.1.0 - 新增任务管理功能
- v1.1.1 - 修复登录 Bug

### 部署记录

建议每次部署后记录：

| 日期 | 版本 | 部署人 | 变更内容 | 备注 |
|------|------|--------|----------|------|
| 2025-12-14 | v1.0.0 | AI | 初始部署 + HTTPS | 自签名证书 |
| 2025-12-15 | v1.0.1 | AI | 修复登录问题 | crypto.subtle API |

---

## 🎓 备案完成后的配置

### 启用腾讯云证书

**步骤 1: 下载证书**
1. 访问：https://console.cloud.tencent.com/ssl
2. 找到 `jihuadz.xin` 证书
3. 下载 Nginx 格式证书
4. 解压得到：
   - `jihuadz.xin_bundle.crt`
   - `jihuadz.xin.key`

**步骤 2: 上传证书**
```bash
# 上传证书文件到服务器
scp jihuadz.xin_bundle.crt root@152.136.183.181:/etc/nginx/ssl/
scp jihuadz.xin.key root@152.136.183.181:/etc/nginx/ssl/

# 设置权限
ssh root@152.136.183.181 "
chmod 644 /etc/nginx/ssl/jihuadz.xin_bundle.crt
chmod 600 /etc/nginx/ssl/jihuadz.xin.key
"
```

**步骤 3: 更新 Nginx 配置**
```bash
# 添加域名 HTTPS 配置
ssh root@152.136.183.181 "cat >> /etc/nginx/conf.d/jihua-https.conf << 'EOF'

# HTTPS - 域名访问（腾讯云证书 - 备案后生效）
server {
    listen 443 ssl http2;
    server_name jihuadz.xin;
    
    # 腾讯云证书
    ssl_certificate /etc/nginx/ssl/jihuadz.xin_bundle.crt;
    ssl_certificate_key /etc/nginx/ssl/jihuadz.xin.key;
    
    # SSL 配置
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    
    root /var/www/jihua;
    index index.html;
    
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control public;
        try_files \$uri =404;
    }
    
    location /assets/ {
        try_files \$uri =404;
    }
    
    location / {
        try_files \$uri \$uri/ /index.html;
    }
    
    location /api {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host \$host;
        proxy_set_header X-Real-IP \$remote_addr;
        proxy_set_header X-Forwarded-For \$proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto \$scheme;
    }
}

# HTTP 自动跳转 HTTPS
server {
    listen 80;
    server_name jihuadz.xin;
    return 301 https://\$server_name\$request_uri;
}
EOF
"

# 测试并重载 Nginx
ssh root@152.136.183.181 "nginx -t && systemctl reload nginx"
```

**步骤 4: 验证**
```
✅ 访问 https://jihuadz.xin
✅ 浏览器显示绿色锁标志
✅ 证书信息显示正确
✅ 无安全警告
```

---

## 📞 联系和支持

**服务器管理**:
- 腾讯云控制台: https://console.cloud.tencent.com/lighthouse
- 实例管理: https://console.cloud.tencent.com/lighthouse/instance/index

**SSL 证书管理**:
- 证书控制台: https://console.cloud.tencent.com/ssl

**域名备案**:
- 备案管理: https://console.cloud.tencent.com/beian

---

## ✅ 完成清单

**当前状态**:
- ✅ HTTP 访问正常 (http://152.136.183.181)
- ✅ HTTPS 访问正常 (https://152.136.183.181)
- ✅ 自签名证书已配置
- ✅ Nginx 配置优化
- ✅ 后端服务运行中
- ✅ PM2 进程管理
- ✅ 防火墙端口开放
- ✅ 一键部署脚本
- ✅ 自动化部署方案

**待完成**:
- ⏳ 域名备案审核
- ⏳ 腾讯云证书配置
- ⏳ 域名 HTTPS 访问

---

**文档版本**: v1.0
**最后更新**: 2025-12-14
**维护者**: AI Assistant
