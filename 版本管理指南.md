# 际华协同办公管理平台 - 版本管理和备份方案

## 🎯 核心理念

### 升级流程简化
✅ **系统升级 = 构建 + 覆盖上传**
- 不需要重启服务器
- 零停机部署
- 文件立即生效

### 版本备份策略
✅ **Git版本管理（最佳实践）**
- 每个大版本创建Git标签
- 自动备份分支
- 完整的版本历史
- 随时回滚

---

## 🚀 完整的版本发布流程

### 方式一：一键发布（推荐）

**直接双击**: `发布新版本.bat`

脚本会自动：
1. ✅ 检查Git状态
2. ✅ 输入版本号和说明
3. ✅ 提交所有更改
4. ✅ 创建版本标签（如 v2.0.0）
5. ✅ 创建备份分支（如 backup/v2.0.0）
6. ✅ 推送到远程仓库（如有配置）

### 方式二：手动发布

```bash
# 1. 提交更改
git add .
git commit -m "release: v2.0.0 - 主体功能完成"

# 2. 创建标签
git tag -a v2.0.0 -m "版本 2.0.0 - 主体功能完成"

# 3. 创建备份分支
git branch backup/v2.0.0

# 4. 推送到远程（可选）
git push origin main
git push origin v2.0.0
git push origin backup/v2.0.0
```

---

## 📦 版本管理结构

### Git标签（Tags）
```
v1.0.0 - 初始版本
v1.1.0 - 任务管理优化
v1.2.0 - 任务管理重大更新
v2.0.0 - 主体功能完成 ✅ 当前
v2.1.0 - 未来版本...
```

**优势**:
- 轻量级标记
- 指向特定提交
- 易于查看和切换
- 行业标准做法

### 备份分支（Backup Branches）
```
backup/v1.0.0
backup/v1.1.0
backup/v1.2.0
backup/v2.0.0 ✅ 最新
```

**优势**:
- 完整代码备份
- 可独立开发
- 双重保障
- 易于回滚

---

## 🔄 完整的版本升级工作流

### 步骤1: 开发完成后发布版本

```bash
# 双击运行
发布新版本.bat

# 输入版本号: 2.0.0
# 输入说明: 主体功能完成
# 确认发布: Y
```

**结果**:
- ✅ Git提交已创建
- ✅ 标签 v2.0.0 已创建
- ✅ 分支 backup/v2.0.0 已创建
- ✅ 已推送到远程（如配置）

### 步骤2: 部署到服务器

```bash
# 双击运行
快速升级.bat
```

**操作**:
1. 清理并构建项目
2. 上传文件覆盖服务器
3. 设置文件权限
4. **无需重启服务器**

### 步骤3: 验证部署

```bash
# 访问系统
https://152.136.183.181

# 强制刷新
Ctrl + F5
```

---

## 📚 版本查看和切换

### 查看所有版本

```bash
# 查看所有标签
git tag

# 查看标签详情
git show v2.0.0

# 查看所有备份分支
git branch | grep backup
```

### 切换到特定版本

```bash
# 方式1: 切换到标签（只读）
git checkout v1.2.0

# 方式2: 切换到备份分支（可修改）
git checkout backup/v1.2.0

# 方式3: 基于标签创建新分支
git checkout -b hotfix-1.2.0 v1.2.0

# 返回主分支
git checkout main
```

---

## 🔙 版本回滚方案

### 场景1: 紧急回滚（服务器文件）

如果新版本有严重问题，快速回滚到上一版本：

```bash
# 1. 切换到旧版本
git checkout v1.2.0

# 2. 重新构建
npx vite build

# 3. 上传覆盖
scp -r dist/* root@152.136.183.181:/var/www/jihua/

# 4. 设置权限
ssh root@152.136.183.181 "chown -R nginx:nginx /var/www/jihua && chmod -R 755 /var/www/jihua"

# 5. 返回主分支
git checkout main
```

**耗时**: 约2-3分钟  
**停机**: 0秒（覆盖部署）

### 场景2: 代码回滚（开发环境）

如果代码有问题，需要回退代码：

```bash
# 方式1: 软回滚（保留更改）
git reset --soft v1.2.0

# 方式2: 硬回滚（丢弃更改）
git reset --hard v1.2.0

# 方式3: 反向提交（保留历史）
git revert v2.0.0
```

---

## 🌐 远程仓库配置（推荐）

### 为什么需要远程仓库？

1. **多人协作** - 团队成员同步代码
2. **云端备份** - 防止本地数据丢失
3. **版本共享** - 其他设备访问代码
4. **持续集成** - 自动化构建和部署

### 选择远程仓库

| 平台 | 推荐度 | 说明 |
|------|--------|------|
| **GitHub** | ⭐⭐⭐⭐⭐ | 全球最大，免费私有仓库 |
| **Gitee** | ⭐⭐⭐⭐ | 国内速度快，免费 |
| **GitLab** | ⭐⭐⭐⭐ | 功能强大，可自建 |
| **腾讯工蜂** | ⭐⭐⭐ | 腾讯云集成 |

### 快速配置远程仓库

```bash
# 方式1: 使用 GitHub
git remote add origin https://github.com/your-username/jihua-oa.git

# 方式2: 使用 Gitee（推荐国内）
git remote add origin https://gitee.com/your-username/jihua-oa.git

# 验证配置
git remote -v

# 首次推送
git push -u origin main

# 推送所有标签
git push origin --tags

# 推送所有分支
git push origin --all
```

---

## 📊 版本命名规范（语义化版本）

### 格式: X.Y.Z

- **X (主版本号)**: 重大更新，不兼容的API变更
- **Y (次版本号)**: 新增功能，向下兼容
- **Z (修订号)**: Bug修复，向下兼容

### 示例

```
v1.0.0 - 初始发布
v1.0.1 - 修复登录bug
v1.1.0 - 新增任务回收站功能
v1.1.1 - 修复回收站显示问题
v2.0.0 - 主体功能全部完成（大版本）
v2.0.1 - 修复员工编辑问题
v2.1.0 - 新增数据导出功能
```

### 版本发布建议

| 版本类型 | 发布频率 | Git标签 | 备份分支 | 示例 |
|---------|---------|---------|---------|------|
| **主版本** | 每季度 | ✅ 必须 | ✅ 必须 | v2.0.0 |
| **次版本** | 每月 | ✅ 必须 | ✅ 推荐 | v2.1.0 |
| **修订版** | 随时 | ✅ 推荐 | ❌ 可选 | v2.0.1 |

---

## 🛠️ Git初始化配置

如果项目还没有初始化Git：

```bash
# 1. 初始化仓库
cd e:\cowork
git init

# 2. 配置用户信息
git config user.name "Your Name"
git config user.email "your.email@example.com"

# 3. 添加所有文件
git add .

# 4. 首次提交
git commit -m "Initial commit - 际华协同办公管理平台"

# 5. 创建当前版本标签
git tag -a v2.0.0 -m "版本 2.0.0 - 主体功能完成"
git branch backup/v2.0.0

# 6. 配置远程仓库（可选）
git remote add origin https://gitee.com/your-username/jihua-oa.git
git push -u origin main
git push origin v2.0.0
git push origin backup/v2.0.0
```

---

## 📋 .gitignore 配置

确保不提交不必要的文件：

```gitignore
# 依赖
node_modules/
package-lock.json

# 构建产物
dist/
build/

# 日志
*.log
logs/
npm-debug.log*

# 环境变量
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# 操作系统
.DS_Store
Thumbs.db
desktop.ini

# 临时文件
*.tmp
*.bak
*.old
```

---

## 🔍 版本对比和差异查看

### 查看版本差异

```bash
# 比较两个版本
git diff v1.2.0 v2.0.0

# 查看文件变更列表
git diff --name-only v1.2.0 v2.0.0

# 查看统计信息
git diff --stat v1.2.0 v2.0.0

# 查看某个文件的变更
git diff v1.2.0 v2.0.0 -- package.json
```

### 查看版本历史

```bash
# 查看所有提交
git log --oneline --graph --all

# 查看标签历史
git log --tags --simplify-by-decoration --pretty="format:%ci %d"

# 查看特定版本的详情
git show v2.0.0
```

---

## 🎯 最佳实践总结

### ✅ 推荐做法

1. **每个大版本必须创建Git标签和备份分支**
2. **使用语义化版本号（X.Y.Z）**
3. **配置远程仓库，云端备份**
4. **添加清晰的版本说明**
5. **定期推送到远程仓库**

### ❌ 避免做法

1. ❌ 不要直接修改已发布的标签
2. ❌ 不要删除备份分支
3. ❌ 不要在 main 分支直接回滚
4. ❌ 不要忘记推送标签到远程
5. ❌ 不要忽略 .gitignore 配置

---

## 📞 常用命令速查

```bash
# 版本发布
git tag -a v2.0.0 -m "描述"        # 创建标签
git branch backup/v2.0.0           # 创建备份
git push origin v2.0.0             # 推送标签

# 版本查看
git tag                            # 列出所有标签
git tag -l "v2.*"                  # 列出 v2.x 标签
git show v2.0.0                    # 查看标签详情

# 版本切换
git checkout v2.0.0                # 切换到标签
git checkout backup/v2.0.0         # 切换到分支
git checkout main                  # 返回主分支

# 版本回滚
git reset --hard v1.2.0            # 硬回滚
git revert HEAD                    # 反向提交

# 分支管理
git branch                         # 查看本地分支
git branch -a                      # 查看所有分支
git branch -d backup/v1.0.0        # 删除分支
```

---

## 🎓 进阶：自动化版本发布

### 集成 GitHub Actions（示例）

创建 `.github/workflows/release.yml`:

```yaml
name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      
      - name: Install dependencies
        run: npm install
      
      - name: Build
        run: npx vite build
      
      - name: Deploy to Server
        run: |
          scp -r dist/* root@152.136.183.181:/var/www/jihua/
          ssh root@152.136.183.181 "chown -R nginx:nginx /var/www/jihua"
```

**效果**: 推送标签后自动构建和部署！

---

## 📈 版本管理流程图

```
开发完成
    ↓
运行: 发布新版本.bat
    ↓
输入版本号和说明
    ↓
自动创建:
  - Git 提交
  - 版本标签 (v2.0.0)
  - 备份分支 (backup/v2.0.0)
  - 推送到远程
    ↓
运行: 快速升级.bat
    ↓
自动执行:
  - 清理构建
  - Vite 构建
  - 上传覆盖
  - 设置权限
    ↓
访问验证: https://152.136.183.181
    ↓
✅ 完成！
```

---

## 🎉 总结

### 核心要点

1. **升级 = 构建 + 覆盖**（无需重启）
2. **备份 = Git 标签 + 分支**（双重保障）
3. **一键脚本**（提高效率）
4. **远程仓库**（云端安全）

### 工作流程

```
发布新版本.bat → 快速升级.bat → 验证访问
     ↓               ↓              ↓
   Git备份        服务器部署      功能测试
```

### 时间成本

- **版本发布**: 30秒
- **构建部署**: 2分钟
- **验证测试**: 1分钟
- **总计**: **约3分钟** ⚡

---

**Built with ❤️ for 际华集团**  
*最后更新: 2025-12-14*
