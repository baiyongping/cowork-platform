# 目标分解功能 - 快速部署指南

## 部署前准备

### 环境要求
- Node.js >= 14
- CloudBase CLI 已安装
- CloudBase 环境已配置

### 检查环境
```bash
# 检查 Node.js
node --version

# 检查 CloudBase CLI
tcb --version

# 登录 CloudBase
tcb login
```

## 一键部署脚本

### Windows 快速部署

创建 `deploy-decomposition.ps1`:

```powershell
Write-Host "========================================" -ForegroundColor Cyan
Write-Host "  目标分解功能 - 快速部署" -ForegroundColor Cyan
Write-Host "========================================" -ForegroundColor Cyan
Write-Host ""

# 1. 初始化数据库集合
Write-Host "[1/4] 初始化数据库集合..." -ForegroundColor Yellow
node database/init-decomposition-collections.js
if ($LASTEXITCODE -eq 0) {
    Write-Host "✓ 集合创建成功" -ForegroundColor Green
} else {
    Write-Host "✗ 集合创建失败" -ForegroundColor Red
    exit 1
}
Write-Host ""

# 2. 部署云函数 - dimension-management
Write-Host "[2/4] 部署维度管理云函数..." -ForegroundColor Yellow
tcb fn deploy dimension-management
if ($LASTEXITCODE -eq 0) {
    Write-Host "✓ dimension-management 部署成功" -ForegroundColor Green
} else {
    Write-Host "✗ dimension-management 部署失败" -ForegroundColor Red
    exit 1
}
Write-Host ""

# 3. 部署云函数 - decomposition-management
Write-Host "[3/4] 部署分解管理云函数..." -ForegroundColor Yellow
tcb fn deploy decomposition-management
if ($LASTEXITCODE -eq 0) {
    Write-Host "✓ decomposition-management 部署成功" -ForegroundColor Green
} else {
    Write-Host "✗ decomposition-management 部署失败" -ForegroundColor Red
    exit 1
}
Write-Host ""

# 4. 构建前端
Write-Host "[4/4] 构建前端代码..." -ForegroundColor Yellow
npm run build
if ($LASTEXITCODE -eq 0) {
    Write-Host "✓ 前端构建成功" -ForegroundColor Green
} else {
    Write-Host "✗ 前端构建失败" -ForegroundColor Red
    exit 1
}
Write-Host ""

Write-Host "========================================" -ForegroundColor Cyan
Write-Host "  部署完成!" -ForegroundColor Green
Write-Host "========================================" -ForegroundColor Cyan
Write-Host ""
Write-Host "后续步骤:" -ForegroundColor Yellow
Write-Host "1. 配置数据库权限（参考文档）"
Write-Host "2. 部署前端到服务器"
Write-Host "3. 在目标管理页面集成分解功能"
Write-Host ""
```

运行:
```powershell
.\deploy-decomposition.ps1
```

### Linux/Mac 快速部署

创建 `deploy-decomposition.sh`:

```bash
#!/bin/bash

echo "========================================"
echo "  目标分解功能 - 快速部署"
echo "========================================"
echo ""

# 1. 初始化数据库集合
echo "[1/4] 初始化数据库集合..."
node database/init-decomposition-collections.js
if [ $? -eq 0 ]; then
    echo "✓ 集合创建成功"
else
    echo "✗ 集合创建失败"
    exit 1
fi
echo ""

# 2. 部署云函数 - dimension-management
echo "[2/4] 部署维度管理云函数..."
tcb fn deploy dimension-management
if [ $? -eq 0 ]; then
    echo "✓ dimension-management 部署成功"
else
    echo "✗ dimension-management 部署失败"
    exit 1
fi
echo ""

# 3. 部署云函数 - decomposition-management
echo "[3/4] 部署分解管理云函数..."
tcb fn deploy decomposition-management
if [ $? -eq 0 ]; then
    echo "✓ decomposition-management 部署成功"
else
    echo "✗ decomposition-management 部署失败"
    exit 1
fi
echo ""

# 4. 构建前端
echo "[4/4] 构建前端代码..."
npm run build
if [ $? -eq 0 ]; then
    echo "✓ 前端构建成功"
else
    echo "✗ 前端构建失败"
    exit 1
fi
echo ""

echo "========================================"
echo "  部署完成!"
echo "========================================"
echo ""
echo "后续步骤:"
echo "1. 配置数据库权限（参考文档）"
echo "2. 部署前端到服务器"
echo "3. 在目标管理页面集成分解功能"
echo ""
```

运行:
```bash
chmod +x deploy-decomposition.sh
./deploy-decomposition.sh
```

## 手动分步部署

### 步骤1: 初始化数据库

```bash
# 运行集合初始化脚本
node database/init-decomposition-collections.js
```

**成功标志:**
```
✓ 集合 goalDecompositionDimensions 创建成功
✓ 集合 goalDecompositions 创建成功
✓ 集合 goalDecompositionHistory 创建成功
```

**验证:**
```bash
# 在 CloudBase 控制台查看数据库
# 应该看到 3 个新集合
```

### 步骤2: 部署云函数

```bash
# 切换到云函数目录
cd cloudfunctions

# 部署维度管理云函数
tcb fn deploy dimension-management

# 部署分解管理云函数
tcb fn deploy decomposition-management
```

**验证:**
```bash
# 查看云函数列表
tcb fn list

# 应该看到:
# - dimension-management
# - decomposition-management
```

### 步骤3: 配置数据库权限

在 CloudBase 控制台配置集合权限:

**goalDecompositionDimensions:**
```json
{
  "read": "auth.loginType == 'CUSTOM'",
  "write": "auth.loginType == 'CUSTOM' && (resource.creator == auth.uid || auth.uid == 'admin')"
}
```

**goalDecompositions:**
```json
{
  "read": "auth.loginType == 'CUSTOM'",
  "write": "auth.loginType == 'CUSTOM' && (resource.creator == auth.uid || auth.uid == 'admin')"
}
```

**goalDecompositionHistory:**
```json
{
  "read": "auth.loginType == 'CUSTOM'",
  "write": "auth.loginType == 'CUSTOM' && (resource.creator == auth.uid || auth.uid == 'admin')"
}
```

### 步骤4: 构建和部署前端

```bash
# 构建前端
npm run build

# 部署到测试环境
# (使用您的现有部署流程)
```

## 功能集成

### 在目标管理页面添加分解按钮

修改 `components/pages/GoalManagement.tsx`:

```tsx
import { useState } from 'react';
import GoalDecompositionEditor from '../GoalDecompositionEditor';
import DecompositionHistoryModal from '../DecompositionHistoryModal';

// 在组件中添加状态
const [decompositionGoal, setDecompositionGoal] = useState(null);
const [showHistory, setShowHistory] = useState(false);

// 在目标列表的操作列添加按钮
<button
  onClick={() => setDecompositionGoal(goal)}
  className="text-blue-600 hover:text-blue-800"
>
  分解
</button>

// 在组件底部渲染编辑器
{decompositionGoal && (
  <GoalDecompositionEditor
    goalId={decompositionGoal._id}
    goalName={decompositionGoal.name}
    targetValue={decompositionGoal.target}
    onClose={() => setDecompositionGoal(null)}
    onSave={() => {
      setDecompositionGoal(null);
      loadGoals(); // 刷新目标列表
    }}
  />
)}

{showHistory && decompositionGoal && (
  <DecompositionHistoryModal
    goalId={decompositionGoal._id}
    onClose={() => setShowHistory(false)}
    onRestore={() => {
      setShowHistory(false);
      loadGoals(); // 刷新目标列表
    }}
  />
)}
```

### 添加维度设置入口

在目标管理页面添加维度设置按钮:

```tsx
import GoalDimensionSettings from '../GoalDimensionSettings';

// 添加状态
const [showDimensionSettings, setShowDimensionSettings] = useState(false);

// 在页面顶部添加按钮
<button
  onClick={() => setShowDimensionSettings(true)}
  className="px-4 py-2 bg-gray-600 text-white rounded-lg"
>
  维度设置
</button>

// 渲染维度设置组件
{showDimensionSettings && (
  <GoalDimensionSettings
    onClose={() => setShowDimensionSettings(false)}
  />
)}
```

## 测试验证

### 1. 测试维度管理

```bash
# 访问目标管理页面
# 点击「维度设置」按钮
# 尝试创建一个新维度
# 验证拖拽排序功能
```

### 2. 测试目标分解

```bash
# 在目标列表中选择一个目标
# 点击「分解」按钮
# 选择1-3个维度
# 点击「生成分解表格」
# 填写数据并保存
# 验证数据校验功能
```

### 3. 测试历史版本

```bash
# 在分解编辑器中点击「历史版本」
# 查看版本列表
# 点击某个版本查看详情
# 尝试回滚到旧版本
```

## 常见问题

### Q1: 集合创建失败？

**A:** 检查 CloudBase CLI 是否已登录:
```bash
tcb login
```

### Q2: 云函数部署失败？

**A:** 检查云函数配置:
```bash
# 查看云函数详情
cd cloudfunctions/dimension-management
cat config.json

# 确认环境ID正确
```

### Q3: 前端无法调用云函数？

**A:** 检查云函数权限:
```javascript
// 在云函数 config.json 中确认
{
  "permissions": {
    "openapi": [""]
  }
}
```

### Q4: 数据校验失败？

**A:** 检查数据精度:
```typescript
// 分解总和必须等于目标值（误差 < 0.01）
const diff = Math.abs(total - targetValue);
if (diff > 0.01) {
  // 校验失败
}
```

### Q5: 历史版本不显示？

**A:** 检查数据库权限:
```bash
# 确认 goalDecompositionHistory 集合权限配置正确
```

## 回滚方案

如果部署出现问题，可以按以下步骤回滚:

### 1. 删除云函数

```bash
# 删除云函数
tcb fn delete dimension-management
tcb fn delete decomposition-management
```

### 2. 删除集合

```bash
# 在 CloudBase 控制台手动删除集合
# - goalDecompositionDimensions
# - goalDecompositions
# - goalDecompositionHistory
```

### 3. 回滚前端代码

```bash
# 回到上一个版本
git checkout HEAD~1

# 重新构建
npm run build

# 重新部署
```

## 监控和维护

### 日志查看

```bash
# 查看云函数日志
tcb fn log dimension-management
tcb fn log decomposition-management
```

### 性能监控

```bash
# 在 CloudBase 控制台查看:
# - 云函数调用次数
# - 响应时间
# - 错误率
```

### 数据备份

```bash
# 定期备份集合数据
tcb db export goalDecompositionDimensions
tcb db export goalDecompositions
tcb db export goalDecompositionHistory
```

## 下一步

1. **功能测试**: 完成所有功能的全面测试
2. **用户培训**: 准备用户使用文档和培训材料
3. **生产部署**: 在生产环境部署功能
4. **监控反馈**: 收集用户反馈，持续优化

---

**部署完成后，请参考《目标分解功能_v1.0.0_完成报告.md》了解详细使用方法。**
