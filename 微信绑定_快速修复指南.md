# 微信绑定快速修复指南

## 🚨 问题症状
手机扫描二维码后显示: **"绑定失败：授权失败,请稍后重试"**

## ✅ 快速修复步骤

### 第1步: 检查云函数是否存在 (2分钟)

1. 打开CloudBase控制台: https://tcb.cloud.tencent.com
2. 选择环境: `cowork-9gg9oocb516be5fb`
3. 进入 **云函数** 页面
4. 查找 `wechat-oauth` 云函数

**如果云函数不存在**:
```bash
# 部署云函数
使用 CloudBase 工具或 CLI 部署
```

### 第2步: 配置环境变量 (3分钟) ⚠️ 最关键

1. 点击 `wechat-oauth` 云函数
2. 进入 **函数配置** 标签
3. 找到 **环境变量** 部分
4. 添加/检查以下变量:

```
WECHAT_APPID=你的微信公众号AppID
WECHAT_SECRET=你的微信公众号AppSecret
```

**获取AppID和AppSecret**:
1. 登录微信公众平台: https://mp.weixin.qq.com
2. 设置与开发 → 基本配置
3. 复制 AppID 和 AppSecret

### 第3步: 配置微信公众平台域名 (5分钟) ⚠️ 必须配置

#### A. 网页授权域名 (最重要!)
1. 登录微信公众平台: https://mp.weixin.qq.com
2. 设置与开发 → 接口权限 → 网页授权
3. 点击 "修改"
4. 填写: `jihuadz.xin` (不要加 https://)
5. 下载验证文件 `MP_verify_xxx.txt`
6. 上传到服务器根目录: `/var/www/jihua/MP_verify_xxx.txt`
7. 点击 "确定"

#### B. 业务域名
1. 设置与开发 → 公众号设置 → 功能设置
2. 业务域名 → 设置
3. 填写: `jihuadz.xin`
4. 下载验证文件,上传到服务器
5. 提交

#### C. JS接口安全域名
1. 设置与开发 → 公众号设置 → 功能设置
2. JS接口安全域名 → 设置
3. 填写: `jihuadz.xin`
4. 提交

### 第4步: 重新部署云函数 (1分钟)

**如果修改了环境变量**,必须重新部署:

```bash
# 使用 CloudBase 工具
updateFunctionCode({
  name: 'wechat-oauth',
  functionRootPath: 'd:/project/cowork12-21/cloudfunctions'
})
```

或在控制台手动上传代码。

### 第5步: 测试 (2分钟)

1. PC端: 登录系统 → 个人信息 → 扫描二维码绑定微信
2. 手机端: 微信扫描二维码
3. 预期: 看到微信授权页面或账号信息确认页
4. 点击确认绑定
5. 成功: 显示"绑定成功"

## 🔍 问题排查

### 错误1: "redirect_uri参数错误"

**原因**: 微信公众平台未配置授权回调域名

**解决**:
- 检查 "网页授权域名" 是否配置为 `jihuadz.xin`
- 验证文件是否上传成功
- 域名必须与实际访问域名完全一致

### 错误2: "invalid appid"

**原因**: WECHAT_APPID 配置错误

**解决**:
- 检查环境变量 WECHAT_APPID 是否正确
- 从微信公众平台重新复制 AppID
- 确保没有多余空格

### 错误3: "invalid code"

**原因**: code已被使用或过期

**解决**:
- 重新扫描二维码
- code只能使用一次,有效期5分钟

### 错误4: "请在微信中打开此链接"

**原因**: 不在微信浏览器中打开

**解决**:
- 确保在微信中扫码打开
- 不要复制链接到外部浏览器

## 📝 验证清单

完成修复后,检查以下项目:

- [ ] `wechat-oauth` 云函数已部署
- [ ] 环境变量 `WECHAT_APPID` 已配置
- [ ] 环境变量 `WECHAT_SECRET` 已配置
- [ ] 微信公众平台 "网页授权域名" 已配置
- [ ] 验证文件已上传到服务器
- [ ] 云函数已重新部署(如果修改了环境变量)
- [ ] 完整流程测试通过

## 🎯 测试命令

### 测试OAuth云函数

在CloudBase控制台 → 云函数 → wechat-oauth → 在线测试:

```json
{
  "action": "getAuthUrl",
  "redirectUri": "https://jihuadz.xin/wechat-bind",
  "scope": "snsapi_base"
}
```

**预期返回**:
```json
{
  "success": true,
  "data": {
    "authUrl": "https://open.weixin.qq.com/connect/oauth2/authorize?appid=..."
  }
}
```

**如果返回错误**,检查:
- WECHAT_APPID 是否正确
- redirectUri 格式是否正确

## 📞 需要帮助?

如果按照以上步骤仍然无法解决,请检查:

1. **CloudBase控制台 → 云函数 → wechat-oauth → 日志**
   - 查看详细错误信息
   
2. **微信公众平台 → 开发 → 接口权限**
   - 确认 "网页授权" 权限已开通
   
3. **浏览器开发者工具 → Console**
   - 查看前端错误日志

## 📋 相关文档

- 详细分析: `微信H5绑定_问题分析_v1.1.md`
- OAuth授权: `微信H5绑定_OAuth授权修复_v1.1.md`
- 开发报告: `微信H5绑定_开发完成报告_v1.0.md`

---

**预计修复时间**: 10-15分钟  
**难度等级**: ⭐⭐ (中等)  
**成功率**: 95% (按步骤操作)
