# 际华系统 - 服务器部署快速指南

## ✅ 第一步：环境配置（已完成）

- ✅ DNS 解析：jihuadz.xin → 152.136.183.181
- ✅ Nginx 安装并运行
- ✅ Web 目录：/var/www/jihua
- ✅ 测试页面：http://jihuadz.xin

---

## 🚀 第二步：部署项目（进行中）

### 方式A：自动化脚本（推荐）

**双击运行**：`上传到服务器.bat`

脚本会自动：
1. ✅ 检查 Node.js 环境
2. ✅ 询问是否需要构建
3. ✅ 检查并安装依赖
4. ✅ 执行 npm run build
5. ✅ 上传到服务器
6. ✅ 设置文件权限

---

### 方式B：手动执行

```bash
# 1. 本地构建
cd E:\cowork
npm install
npm run build

# 2. 上传到服务器
scp -r dist\* root@152.136.183.181:/var/www/jihua/

# 3. 设置权限
ssh root@152.136.183.181
chown -R nginx:nginx /var/www/jihua
chmod -R 755 /var/www/jihua
exit
```

---

## 🌐 第三步：访问系统

### 访问地址
- **HTTP**: http://jihuadz.xin
- **管理员登录**: admin / admin123

### ⚠️ 注意事项
1. CDN 有 3-5 分钟缓存，首次访问使用 **Ctrl+F5** 强制刷新
2. 如果看到测试页面，等待几分钟后再访问
3. 部署完成后建议修改管理员密码

---

## 🔒 第四步：配置 HTTPS（可选）

### 自动申请 SSL 证书

```bash
# 1. 上传 SSL 脚本
scp E:\cowork\SSL证书申请脚本.sh root@152.136.183.181:/root/

# 2. SSH 登录服务器
ssh root@152.136.183.181

# 3. 执行 SSL 申请
bash /root/SSL证书申请脚本.sh
```

脚本会自动：
- ✅ 申请 Let's Encrypt 免费证书
- ✅ 配置 HTTPS 访问
- ✅ 设置自动续期（每 90 天）
- ✅ 配置 HTTP 自动跳转 HTTPS

完成后访问：**https://jihuadz.xin**

---

## 📊 服务器信息

| 项目 | 信息 |
|-----|------|
| 服务器IP | 152.136.183.181 |
| 域名 | jihuadz.xin |
| SSH用户 | root |
| Web服务器 | Nginx 1.14.1 |
| Web目录 | /var/www/jihua |
| 日志目录 | /var/log/nginx/ |
| 配置文件 | /etc/nginx/conf.d/jihuadz.conf |

---

## 🛠️ 常用管理命令

### Nginx 管理
```bash
# 启动
systemctl start nginx

# 停止
systemctl stop nginx

# 重启
systemctl restart nginx

# 重载配置（推荐，不中断服务）
systemctl reload nginx

# 查看状态
systemctl status nginx

# 测试配置
nginx -t
```

### 查看日志
```bash
# 访问日志
tail -f /var/log/nginx/jihua_access.log

# 错误日志
tail -f /var/log/nginx/jihua_error.log
```

### 文件管理
```bash
# 查看Web目录
ls -la /var/www/jihua/

# 清理旧文件
rm -rf /var/www/jihua/*

# 重新上传后设置权限
chown -R nginx:nginx /var/www/jihua
chmod -R 755 /var/www/jihua
```

---

## 🔥 故障排查

### 无法访问网站

1. **检查 Nginx 状态**
   ```bash
   systemctl status nginx
   ```

2. **检查端口监听**
   ```bash
   ss -tlnp | grep :80
   ```

3. **检查防火墙**
   ```bash
   firewall-cmd --list-all
   ```

4. **检查 SELinux**
   ```bash
   getenforce
   # 如果显示 Enforcing，临时关闭
   setenforce 0
   ```

5. **检查安全组**
   - 登录腾讯云控制台
   - 检查是否开放 80、443 端口

### 403 Forbidden

```bash
# 检查文件权限
ls -la /var/www/jihua/

# 设置正确权限
chown -R nginx:nginx /var/www/jihua
chmod -R 755 /var/www/jihua

# 关闭 SELinux
setenforce 0
```

### 502 Bad Gateway

```bash
# 检查 Nginx 配置
nginx -t

# 重启 Nginx
systemctl restart nginx
```

---

## 📞 技术支持

如有问题，请检查：
1. 服务器状态：`systemctl status nginx`
2. 错误日志：`tail -50 /var/log/nginx/jihua_error.log`
3. 配置文件：`cat /etc/nginx/conf.d/jihuadz.conf`

---

**最后更新**: 2025-12-14
**版本**: v1.1.4
