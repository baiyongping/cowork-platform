# 本地开发环境配置指南

## 🎯 目标

建立本地开发环境与 cowork 云数据库的连接，实现：
- ✅ 本地开发使用 `cowork-9gg9oocb516be5fb` 测试数据库
- ✅ 生产环境使用 `jihua-oa-dev-3goht9irae4d949f` 生产数据库
- ✅ 数据完全隔离，互不影响
- ✅ 支持云函数开发和调试

---

## 📋 已完成的配置

### 1. 环境变量配置

已创建三个环境配置文件：

#### ✅ `.env.development` - 本地开发环境
```env
VITE_CLOUDBASE_ENV_ID=cowork-9gg9oocb516be5fb
VITE_API_BASE_URL=http://localhost:5173/api
VITE_ENV=development
VITE_CLOUD_FUNCTION_URL=https://cowork-9gg9oocb516be5fb.ap-shanghai.app.tcloudbase.com/
VITE_DEBUG=true
```

#### ✅ `.env.test` - 测试服务器环境
```env
VITE_CLOUDBASE_ENV_ID=cowork-9gg9oocb516be5fb
VITE_API_BASE_URL=/api
VITE_ENV=test
```

#### ✅ `.env.production` - 生产环境
```env
VITE_CLOUDBASE_ENV_ID=jihua-oa-dev-3goht9irae4d949f
VITE_API_BASE_URL=/api
VITE_ENV=production
```

### 2. CloudBase 配置更新

- ✅ `lib/cloudbase.ts` - 从环境变量读取环境 ID
- ✅ `cloudbaserc.json` - 更新为测试环境 ID

### 3. 云函数准备

- ✅ `cloudfunctions/initDatabase/` - 数据库初始化云函数

---

## 🚀 快速开始

### 步骤 1：初始化数据库

#### 方法 A：通过云函数初始化（推荐）✨

1. **部署云函数**

```bash
# Windows PowerShell
cd e:\cowork
cloudbase functions:deploy initDatabase
```

2. **调用云函数初始化数据库**

```bash
cloudbase functions:invoke initDatabase
```

**预期输出**：
```json
{
  "success": true,
  "message": "数据库初始化完成",
  "data": {
    "collections": ["roles", "departments", "employees", "system_configs", "tasks"],
    "loginInfo": {
      "username": "admin",
      "password": "admin123"
    }
  }
}
```

#### 方法 B：通过控制台手动创建

访问：https://tcb.cloud.tencent.com/dev?envId=cowork-9gg9oocb516be5fb#/db/doc

手动创建以下集合：
- `employees`
- `roles`
- `departments`
- `system_configs`
- `tasks`

---

### 步骤 2：启动本地开发服务器

```bash
cd e:\cowork

# 安装依赖（如果还没安装）
npm install

# 启动开发服务器
npm run dev
```

**访问地址**: http://localhost:5173

---

### 步骤 3：验证连接

1. **打开浏览器开发者工具**
2. **查看控制台输出**

应该看到：
```
🔧 CloudBase 环境: cowork-9gg9oocb516be5fb
🔧 当前模式: development
🔧 进行匿名登录...
✅ 匿名登录成功 - 用户UID: xxx
✓ CloudBase 自动认证完成
```

3. **测试登录**

- 用户名: `admin`
- 密码: `admin123`

---

## 🔧 云函数开发

### 查看现有云函数

```bash
cloudbase functions:list
```

### 部署云函数

```bash
# 部署单个云函数
cloudbase functions:deploy initDatabase

# 部署所有云函数
cloudbase functions:deploy
```

### 调用云函数

```bash
# 调用云函数
cloudbase functions:invoke initDatabase

# 调用云函数并传参
cloudbase functions:invoke initDatabase --params '{"action":"reset"}'
```

### 查看云函数日志

```bash
cloudbase functions:log initDatabase
```

### 在代码中调用云函数

```typescript
// lib/cloudbase.ts 中已导出 app
import app from '@/lib/cloudbase';

// 调用云函数
const result = await app.callFunction({
  name: 'initDatabase',
  data: {}
});

console.log('云函数返回:', result);
```

---

## 📊 环境切换

### 本地开发（使用测试数据库）

```bash
npm run dev
```

自动使用 `.env.development` 配置，连接到 `cowork-9gg9oocb516be5fb`

### 构建测试环境

```bash
npm run build -- --mode test
```

使用 `.env.test` 配置

### 构建生产环境

```bash
npm run build
```

使用 `.env.production` 配置，连接到 `jihua-oa-dev-3goht9irae4d949f`

---

## 🔍 数据库操作

### 在浏览器控制台调试

打开浏览器开发者工具，输入：

```javascript
// 查询用户
debug()

// 或者自定义查询
const result = await db.collection('employees').get();
console.log(result);
```

### 在代码中操作数据库

```typescript
import { db } from '@/lib/cloudbase';

// 查询
const users = await db.collection('employees').get();

// 添加
const result = await db.collection('tasks').add({
  name: '测试任务',
  status: '未开始',
  createdAt: new Date()
});

// 更新
await db.collection('tasks').doc('task-001').update({
  status: '进行中'
});

// 删除
await db.collection('tasks').doc('task-001').remove();
```

---

## 🚨 常见问题

### Q1: 本地开发无法连接数据库

**检查清单**：
1. ✅ 环境 ID 是否正确（`cowork-9gg9oocb516be5fb`）
2. ✅ 是否添加了安全域名（`localhost:5173`）
3. ✅ 是否完成了匿名登录

**解决方案**：
```bash
# 添加本地域名到安全域名列表
cloudbase env:domain:add localhost:5173
```

### Q2: 云函数部署失败

**常见原因**：
- 未安装 CloudBase CLI
- 未登录 CloudBase

**解决方案**：
```bash
# 安装 CLI
npm install -g @cloudbase/cli

# 登录
cloudbase login

# 选择环境
cloudbase env:list
```

### Q3: 数据库初始化失败

**检查**：
- 云函数是否部署成功
- 云函数日志中的错误信息

**查看日志**：
```bash
cloudbase functions:log initDatabase
```

---

## 📝 下一步

### 1. 初始化数据库

选择方法 A 或方法 B 完成数据库初始化

### 2. 启动本地开发

```bash
npm run dev
```

### 3. 测试登录

访问 http://localhost:5173，使用 `admin/admin123` 登录

### 4. 开发新功能

在本地开发环境中开发和测试新功能，数据保存在测试数据库中

---

## 🔗 相关链接

- **CloudBase 控制台**: https://tcb.cloud.tencent.com/dev?envId=cowork-9gg9oocb516be5fb
- **数据库管理**: https://tcb.cloud.tencent.com/dev?envId=cowork-9gg9oocb516be5fb#/db/doc
- **云函数管理**: https://tcb.cloud.tencent.com/dev?envId=cowork-9gg9oocb516be5fb#/scf
- **CloudBase 文档**: https://docs.cloudbase.net/

---

**更新时间**: 2025-12-14
