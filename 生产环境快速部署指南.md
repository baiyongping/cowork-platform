# 际华协同办公平台 - 生产环境快速部署指南

## 🚀 5分钟快速部署

### 前置条件检查

```bash
# ✓ Lighthouse 服务器已准备
# ✓ 域名 jihuadz.xin 已解析到服务器IP
# ✓ SSL 证书已配置在服务器
# ✓ Docker 已安装并运行
# ✓ Nginx 已安装并运行
```

---

## 📦 第一步：本地构建（1分钟）

在项目根目录执行：

```powershell
# 1. 清理旧构建
Remove-Item -Path dist -Recurse -Force -ErrorAction SilentlyContinue

# 2. 执行生产构建
npm run build

# 3. 验证构建产物
ls dist\
# 应该看到: index.html, assets\, ...
```

---

## ☁️ 第二步：上传到服务器（1分钟）

使用 AI 工具调用 Lighthouse MCP：

```
请帮我上传构建产物到生产环境:

FolderPath: d:/project/cowork12-21/dist
InstanceId: lhins-pnt984h2
Region: ap-beijing
ProjectName: jihua-prod
```

---

## 🐳 第三步：SSH连接服务器（30秒）

```bash
ssh root@<your-lighthouse-ip>
```

---

## 🎯 第四步：执行蓝绿部署（2分钟）

```bash
cd /var/www/jihua-deploy
./blue-green-deploy-production.sh deploy
```

**脚本会自动完成：**
- ✅ 清理旧文件和缓存
- ✅ 验证上传文件完整性
- ✅ 构建 Docker 镜像（无缓存）
- ✅ 启动新环境容器
- ✅ 健康检查（30次重试）
- ✅ 切换 Nginx 流量
- ✅ 清理旧环境

---

## ✅ 第五步：验证部署（30秒）

### 服务器验证

```bash
# 1. 查看容器状态
docker ps --filter "name=jihua-prod"

# 2. 测试网站访问
curl -I https://jihuadz.xin

# 3. 查看实时日志
docker logs jihua-prod-blue -f --tail 50
```

### 浏览器验证

1. 访问 https://jihuadz.xin
2. 按 **Ctrl + Shift + R** 强制刷新
3. 或使用**无痕/隐私模式**访问

---

## 🔄 常用命令速查

### 查看部署状态

```bash
./blue-green-deploy-production.sh status
```

### 快速回滚

```bash
# 回滚到蓝环境
./blue-green-deploy-production.sh rollback blue

# 回滚到绿环境
./blue-green-deploy-production.sh rollback green
```

### 清理和验证

```bash
# 仅清理旧文件
./blue-green-deploy-production.sh clean

# 仅验证上传文件
./blue-green-deploy-production.sh validate
```

### 查看日志

```bash
# 容器日志
docker logs jihua-prod-blue -f --tail 100
docker logs jihua-prod-green -f --tail 100

# Nginx 日志
tail -f /var/log/nginx/jihuadz.xin.access.log
tail -f /var/log/nginx/jihuadz.xin.error.log
```

---

## 🆘 故障处理速查

### 问题1: 部署失败，健康检查超时

```bash
# 1. 查看容器日志
docker logs jihua-prod-green --tail 50

# 2. 检查端口监听
netstat -tlnp | grep 3003

# 3. 手动测试容器
curl http://localhost:3003/

# 4. 如果容器正常，重新部署
./blue-green-deploy-production.sh deploy
```

### 问题2: 网站访问异常

```bash
# 1. 检查 Nginx 配置
nginx -t

# 2. 查看 Nginx 状态
systemctl status nginx

# 3. 重启 Nginx
systemctl restart nginx

# 4. 查看错误日志
tail -50 /var/log/nginx/jihuadz.xin.error.log
```

### 问题3: 浏览器仍显示旧版本

```bash
# 服务器端: 验证新版本已部署
curl -I https://jihuadz.xin | grep -i "last-modified"
docker ps --filter "name=jihua-prod"

# 浏览器端: 清理缓存
# 1. 按 Ctrl + Shift + R 硬刷新
# 2. 清空浏览器缓存和Cookie
# 3. 使用无痕模式访问

# CDN缓存: 等待3-5分钟自动更新
```

### 问题4: 需要紧急回滚

```bash
# 1. 查看当前状态
./blue-green-deploy-production.sh status

# 2. 立即回滚到旧版本
./blue-green-deploy-production.sh rollback blue

# 3. 验证回滚成功
curl -I https://jihuadz.xin
```

---

## 📊 部署检查清单

### 部署前检查 ✅

- [ ] 本地构建成功（`npm run build`）
- [ ] dist 目录包含完整文件
- [ ] 环境变量配置正确（.env.production）
- [ ] VERSION.md 版本号已更新
- [ ] Git 代码已提交

### 部署中检查 ✅

- [ ] 文件上传成功
- [ ] Docker 镜像构建成功
- [ ] 容器启动成功
- [ ] 健康检查通过
- [ ] Nginx 切换成功

### 部署后验证 ✅

- [ ] 网站可访问（https://jihuadz.xin）
- [ ] 登录功能正常
- [ ] 核心功能测试通过
- [ ] 浏览器控制台无错误
- [ ] 移动端访问正常

---

## ⏱️ 时间规划

| 步骤 | 预计时间 |
|-----|---------|
| 本地构建 | 1 分钟 |
| 上传文件 | 1 分钟 |
| SSH 连接 | 30 秒 |
| 蓝绿部署 | 2 分钟 |
| 验证测试 | 30 秒 |
| **总计** | **约 5 分钟** |

---

## 🔐 安全提醒

1. **权限控制**：
   - 部署脚本仅限运维人员使用
   - 使用 SSH 密钥认证，不使用密码
   - 定期更换服务器访问密钥

2. **备份策略**：
   - 脚本自动备份最近 3 个版本
   - 生产部署前建议手动备份数据库
   - 保留部署日志至少 30 天

3. **监控告警**：
   - 部署后观察服务器资源使用
   - 设置健康检查告警
   - 关注错误日志

---

## 📞 紧急联系

如遇紧急问题，请联系：

- **技术负责人**: [技术负责人联系方式]
- **服务器运维**: [运维联系方式]
- **开发团队**: [开发团队联系方式]

---

## 📚 相关文档

- [完整部署方案](./生产环境蓝绿部署方案_v3.12.0.md)
- [Nginx 配置详解](./nginx-production.conf)
- [故障排查手册](./生产环境蓝绿部署方案_v3.12.0.md#故障排查)
- [版本信息](./VERSION.md)

---

**版本**: v3.12.0  
**最后更新**: 2026-01-08  
**维护者**: 际华协同办公平台开发团队
