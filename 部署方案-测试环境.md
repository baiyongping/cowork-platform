# 际华协同办公平台 - 测试环境部署方案

## 环境信息

### 测试环境
- **服务器**: 152.136.183.181
- **前端端口**: 3443 (HTTPS)
- **前端路径**: /var/www/jihua-dev/
- **访问地址**: https://152.136.183.181:3443
- **CloudBase环境**: jihua-oa-dev-3goht9irae4d949f

---

## 1. CloudBase 存储桶配置

### 1.1 创建存储桶

```bash
# 在 CloudBase 控制台创建存储桶
存储桶名称: jihua-dev
地域: 广州 (ap-guangzhou)
访问权限: 私有读写
```

### 1.2 配置 CORS 规则

```json
[
  {
    "allowedOrigins": [
      "https://152.136.183.181:3443",
      "http://localhost:5173"
    ],
    "allowedMethods": ["GET", "POST", "PUT", "DELETE", "HEAD"],
    "allowedHeaders": ["*"],
    "exposeHeaders": ["ETag"],
    "maxAgeSeconds": 3600
  }
]
```

### 1.3 配置权限策略

```json
{
  "version": "2.0",
  "statement": [
    {
      "effect": "allow",
      "action": [
        "name/cos:PutObject",
        "name/cos:GetObject",
        "name/cos:DeleteObject"
      ],
      "resource": [
        "qcs::cos:ap-guangzhou:uid/*:jihua-dev/*"
      ]
    }
  ]
}
```

### 1.4 目录结构

```
jihua-dev/
├── attachments/           # 附件文件
│   ├── tasks/            # 任务附件
│   ├── opportunities/    # 商机附件
│   ├── projects/         # 项目附件
│   └── issues/           # 问题附件
├── avatars/              # 用户头像
└── temp/                 # 临时文件
```

---

## 2. NoSQL 数据库集合配置

### 2.1 核心业务集合

#### 2.1.1 users (用户)
```javascript
// 索引配置
db.collection('users').createIndex({ email: 1 }, { unique: true })
db.collection('users').createIndex({ phone: 1 })
db.collection('users').createIndex({ role: 1 })
db.collection('users').createIndex({ isActive: 1 })

// 权限设置: 仅创建者可读写
{
  "read": "doc._openid == auth.uid",
  "write": "doc._openid == auth.uid"
}
```

#### 2.1.2 tasks (任务)
```javascript
// 索引配置
db.collection('tasks').createIndex({ level: 1, status: 1 })
db.collection('tasks').createIndex({ ownerId: 1, status: 1 })
db.collection('tasks').createIndex({ endDate: 1 })
db.collection('tasks').createIndex({ type: 1 })

// 权限设置: 所有人可读,仅创建者可写
{
  "read": true,
  "write": "doc._openid == auth.uid"
}
```

#### 2.1.3 opportunities (商机)
```javascript
// 索引配置
db.collection('opportunities').createIndex({ status: 1 })
db.collection('opportunities').createIndex({ ownerId: 1 })
db.collection('opportunities').createIndex({ expectedAmount: 1 })
db.collection('opportunities').createIndex({ expectedDate: 1 })

// 权限设置: 所有人可读,仅创建者可写
{
  "read": true,
  "write": "doc._openid == auth.uid"
}
```

#### 2.1.4 projects (项目)
```javascript
// 索引配置
db.collection('projects').createIndex({ status: 1 })
db.collection('projects').createIndex({ ownerId: 1 })
db.collection('projects').createIndex({ startDate: 1 })
db.collection('projects').createIndex({ endDate: 1 })

// 权限设置: 所有人可读,仅创建者可写
{
  "read": true,
  "write": "doc._openid == auth.uid"
}
```

#### 2.1.5 goals (目标)
```javascript
// 索引配置
db.collection('goals').createIndex({ year: 1, level: 1 })
db.collection('goals').createIndex({ ownerId: 1 })
db.collection('goals').createIndex({ status: 1 })

// 权限设置: 所有人可读,仅创建者可写
{
  "read": true,
  "write": "doc._openid == auth.uid"
}
```

### 2.2 辅助功能集合

#### 2.2.1 issues (问题)
```javascript
// 索引配置
db.collection('issues').createIndex({ status: 1 })
db.collection('issues').createIndex({ severity: 1 })
db.collection('issues').createIndex({ reporterId: 1 })
db.collection('issues').createIndex({ createdAt: 1 })

// 权限设置: 所有人可读,仅创建者可写
{
  "read": true,
  "write": "doc._openid == auth.uid"
}
```

#### 2.2.2 messages (消息)
```javascript
// 索引配置
db.collection('messages').createIndex({ receiverId: 1, isRead: 1 })
db.collection('messages').createIndex({ senderId: 1 })
db.collection('messages').createIndex({ createdAt: -1 })

// 权限设置: 发送者和接收者可读,仅创建者可写
{
  "read": "doc.receiverId == auth.uid || doc.senderId == auth.uid",
  "write": "doc._openid == auth.uid"
}
```

#### 2.2.3 comments (评论)
```javascript
// 索引配置
db.collection('comments').createIndex({ targetId: 1, targetType: 1 })
db.collection('comments').createIndex({ userId: 1 })
db.collection('comments').createIndex({ createdAt: -1 })

// 权限设置: 所有人可读,仅创建者可写
{
  "read": true,
  "write": "doc._openid == auth.uid"
}
```

#### 2.2.4 operationLogs (操作日志)
```javascript
// 索引配置
db.collection('operationLogs').createIndex({ userId: 1, createdAt: -1 })
db.collection('operationLogs').createIndex({ module: 1 })
db.collection('operationLogs').createIndex({ action: 1 })

// 权限设置: 仅管理员可读写
{
  "read": "auth.role == 'admin'",
  "write": false
}
```

### 2.3 配置数据集合

#### 2.3.1 teams (团队)
```javascript
// 索引配置
db.collection('teams').createIndex({ name: 1 })
db.collection('teams').createIndex({ createdAt: 1 })

// 权限设置: 所有人可读,仅管理员可写
{
  "read": true,
  "write": "auth.role == 'admin'"
}
```

#### 2.3.2 departments (部门)
```javascript
// 索引配置
db.collection('departments').createIndex({ name: 1 })
db.collection('departments').createIndex({ parentId: 1 })

// 权限设置: 所有人可读,仅管理员可写
{
  "read": true,
  "write": "auth.role == 'admin'"
}
```

---

## 3. 云函数配置

### 3.1 核心云函数列表

| 云函数名 | 描述 | 运行时 | 内存 | 超时 |
|---------|------|--------|------|------|
| auth | 用户认证 | Nodejs18.15 | 256MB | 5s |
| user-management | 用户管理 | Nodejs18.15 | 256MB | 5s |
| task-management | 任务管理 | Nodejs18.15 | 256MB | 10s |
| opportunity-management | 商机管理 | Nodejs18.15 | 256MB | 10s |
| project-management | 项目管理 | Nodejs18.15 | 256MB | 10s |
| goal-management | 目标管理 | Nodejs18.15 | 256MB | 10s |
| issue-management | 问题管理 | Nodejs18.15 | 256MB | 10s |
| message-center | 消息中心 | Nodejs18.15 | 256MB | 5s |
| storage | 存储管理 | Nodejs18.15 | 256MB | 10s |
| data-export | 数据导出 | Nodejs18.15 | 512MB | 30s |

### 3.2 云函数环境变量

```bash
# 所有云函数通用环境变量
ENV=dev
TCB_ENV=jihua-oa-dev-3goht9irae4d949f
STORAGE_BUCKET=jihua-dev
CORS_ORIGIN=https://152.136.183.181:3443
```

### 3.3 云函数部署命令

```bash
# 部署单个云函数
cloudbase functions:deploy <function-name> --envId jihua-oa-dev-3goht9irae4d949f

# 批量部署所有云函数
cd cloudfunctions
for dir in */; do
  cloudbase functions:deploy ${dir%/} --envId jihua-oa-dev-3goht9irae4d949f
done
```

---

## 4. 前端部署流程

### 4.1 构建配置

#### vite.config.ts
```typescript
export default defineConfig({
  base: '/',
  build: {
    outDir: 'dist',
    sourcemap: false,
    rollupOptions: {
      output: {
        manualChunks: {
          'vendor-react': ['react', 'react-dom', 'react-router-dom'],
          'vendor-ui': ['@headlessui/react', 'lucide-react'],
          'vendor-cloudbase': ['@cloudbase/js-sdk']
        }
      }
    }
  },
  define: {
    'process.env.TCB_ENV_ID': JSON.stringify('jihua-oa-dev-3goht9irae4d949f')
  }
})
```

### 4.2 部署步骤

```bash
# 1. 构建项目
npm run build

# 2. 使用 Lighthouse MCP 工具部署
deploy_project_preparation({
  FolderPath: "d:/project/cowork12-21/dist",
  InstanceId: " lhins-pnt984h2",
  Region: "ap-beijing",
  ProjectName: "jihua-dev"
})

# 3. 验证部署
curl -I https://152.136.183.181:3443
```

### 4.3 Nginx 配置

```nginx
server {
    listen 3443 ssl;
    server_name 152.136.183.181;

    ssl_certificate /etc/nginx/ssl/test.crt;
    ssl_certificate_key /etc/nginx/ssl/test.key;

    root /var/www/jihua-dev;
    index index.html;

    # 强制防缓存配置
    location / {
        try_files $uri $uri/ /index.html;
        
        # HTML 文件不缓存
        location ~ \.html$ {
            add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate";
            add_header Pragma "no-cache";
            add_header Expires "0";
        }
        
        # JS/CSS 文件短期缓存
        location ~* \.(js|css)$ {
            add_header Cache-Control "public, max-age=300";
        }
    }

    # CORS 配置
    add_header Access-Control-Allow-Origin "*";
    add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS";
    add_header Access-Control-Allow-Headers "*";
}
```

---

## 5. 初始化数据

### 5.1 管理员账号

```javascript
// 使用 reset-admin-local.js 脚本初始化
{
  email: 'admin@jihua.com',
  password: 'admin123',  // 首次登录后请修改密码
  name: '系统管理员',
  role: 'admin',
  phone: '13800138000',
  isActive: true
}
```

### 5.2 基础配置数据

```javascript
// 团队配置
[
  { name: '销售团队', description: '负责商机管理' },
  { name: '项目团队', description: '负责项目实施' },
  { name: '管理团队', description: '负责目标管理' }
]

// 部门配置
[
  { name: '销售部', parentId: null },
  { name: '技术部', parentId: null },
  { name: '财务部', parentId: null },
  { name: '行政部', parentId: null }
]
```

---

## 6. 测试验证

### 6.1 功能测试清单

- [ ] 用户注册和登录
- [ ] 任务创建和管理
- [ ] 商机创建和管理
- [ ] 项目创建和管理
- [ ] 目标创建和管理
- [ ] 问题创建和管理
- [ ] 消息发送和接收
- [ ] 附件上传和下载
- [ ] 数据导出
- [ ] 操作日志记录

### 6.2 性能测试

```bash
# 并发测试
ab -n 1000 -c 10 https://152.136.183.181:3443/

# 接口响应时间测试
curl -w "@curl-format.txt" -o /dev/null -s https://152.136.183.181:3443/api/tasks
```

### 6.3 安全测试

- [ ] HTTPS 访问正常
- [ ] CORS 配置正确
- [ ] 云函数权限验证
- [ ] 数据库权限验证
- [ ] 存储桶权限验证

---

## 7. 监控和维护

### 7.1 日志查看

```bash
# 查看 Nginx 日志
tail -f /var/log/nginx/jihua-dev-access.log
tail -f /var/log/nginx/jihua-dev-error.log

# 查看云函数日志
cloudbase functions:log <function-name> --envId jihua-oa-dev-3goht9irae4d949f
```

### 7.2 性能监控

- CloudBase 控制台监控面板
- Nginx 访问统计
- 云函数调用统计
- 数据库操作统计

### 7.3 备份策略

```bash
# 数据库备份(每日)
cloudbase database:export --envId jihua-oa-dev-3goht9irae4d949f

# 代码备份(每周)
git tag v-dev-$(date +%Y%m%d)
git push origin --tags
```

---

## 8. 问题排查

### 问题1: 前端访问404

**原因**: Nginx 配置错误或文件未正确部署

**解决**:
```bash
# 检查文件是否存在
ls -la /var/www/jihua-dev/

# 检查 Nginx 配置
nginx -t

# 重启 Nginx
systemctl restart nginx
```

### 问题2: 云函数调用失败

**原因**: 环境ID配置错误或权限不足

**解决**:
```bash
# 检查环境ID
cloudbase env:list

# 检查云函数状态
cloudbase functions:list --envId jihua-oa-dev-3goht9irae4d949f

# 重新部署云函数
cloudbase functions:deploy <function-name> --envId jihua-oa-dev-3goht9irae4d949f
```

### 问题3: 存储桶上传失败

**原因**: CORS 配置错误或权限不足

**解决**:
1. 检查 CORS 配置是否包含测试环境域名
2. 检查存储桶权限策略
3. 检查前端环境变量配置

### 问题4: 数据库操作失败

**原因**: 权限配置错误或索引缺失

**解决**:
1. 检查集合权限配置
2. 检查索引是否创建
3. 查看云函数日志获取详细错误

### 问题5: 浏览器缓存问题

**原因**: CDN 缓存或浏览器缓存

**解决**:
```bash
# 用户端: 强制刷新
Ctrl + F5 (Windows)
Cmd + Shift + R (Mac)

# 服务端: 修改 Nginx 缓存配置(已配置无缓存)
# 或在 URL 添加随机参数: ?v=timestamp
```

### 问题6: HTTPS 证书失效

**原因**: 自签名证书过期

**解决**:
```bash
# 重新生成自签名证书
openssl req -x509 -nodes -days 365 \
  -newkey rsa:2048 \
  -keyout /etc/nginx/ssl/test.key \
  -out /etc/nginx/ssl/test.crt

# 重启 Nginx
systemctl restart nginx
```

---

## 9. 联系方式

### 技术支持
- **服务器管理员**: root@152.136.183.181
- **CloudBase 环境**: jihua-oa-dev-3goht9irae4d949f
- **文档地址**: https://github.com/your-org/jihua-oa

### 紧急联系
- 开发负责人: [联系方式]
- 运维负责人: [联系方式]

---

**最后更新**: 2025-01-01
**版本**: v2.0
**维护人**: AI Assistant
