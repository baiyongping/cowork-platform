# 离职人员自动回收站功能说明

## 功能概述

当员工离职并完成数据交接后，系统会自动将该员工移入回收站，无需手动操作。

## 工作流程

```
员工状态变更为"离职" 
  ↓
选择数据交接对象
  ↓
确认交接操作
  ↓
调用云函数转移数据权限
  ↓
✅ 数据交接成功
  ↓
🗑️ 自动移入回收站
  ↓
✅ 完成
```

## 详细说明

### 1. 触发条件

只有同时满足以下条件时，才会自动移入回收站：

- ✅ 员工状态从其他状态变更为"离职"
- ✅ 已选择数据交接对象
- ✅ 数据交接云函数执行成功

### 2. 自动操作

数据交接成功后，系统会自动执行：

1. **更新员工记录**
   ```javascript
   {
     deleted: true,      // 标记为已删除
     deletedAt: new Date() // 记录删除时间
   }
   ```

2. **记录操作日志**
   ```javascript
   {
     userId: employee._id,
     module: '员工管理',
     action: '离职移入回收站',
     content: '员工 XXX 离职交接完成后自动移入回收站',
     createdAt: new Date()
   }
   ```

### 3. 成功提示

交接和回收站操作都成功时，显示提示：

```
离职交接完成！
✅ 数据权限已转移给 [接收人姓名]
✅ [离职员工姓名] 已自动移入回收站
```

### 4. 异常处理

#### 场景1：数据交接失败
- ❌ 不会移入回收站
- 💡 提示：数据交接失败: [具体错误]
- 📝 员工状态已更新为"离职"，但数据未转移

#### 场景2：交接成功但移入回收站失败
- ✅ 数据权限转移成功
- ❌ 回收站操作失败
- 💡 提示：
  ```
  数据交接成功，但移入回收站失败
  ✅ 数据权限已转移给 [接收人姓名]
  ❌ 请手动将员工移入回收站
  ```

## 用户操作指南

### 操作步骤

1. 打开员工详情
2. 点击"编辑"按钮
3. 将员工状态改为"离职"
4. 选择"数据交接对象"（必选）
5. 点击"保存"
6. 在确认弹窗中查看交接详情
7. 点击"确认交接"
8. 等待自动完成
9. 查看成功提示

### 注意事项

- ⚠️ 离职员工必须选择数据交接对象，否则无法保存
- ⚠️ 数据交接对象必须是"在职"状态的员工
- ⚠️ 数据交接后，离职员工会自动移入回收站
- ⚠️ 回收站中的员工可以恢复，但需要重新分配数据权限

## 技术实现

### 修改文件
- `components/EmployeeDetailModal.tsx`

### 核心代码逻辑

```typescript
// 数据交接成功后
if (handoverResult.result && handoverResult.result.success) {
  console.log('✅ [数据交接] 数据权限转移成功');
  
  // 🆕 自动将离职员工移入回收站
  try {
    await db.collection('users').doc(employee._id).update({
      deleted: true,
      deletedAt: new Date()
    });

    // 记录操作日志
    await db.collection('operation_logs').add({
      userId: employee._id,
      module: '员工管理',
      action: '离职移入回收站',
      content: `员工 ${employee.name} 离职交接完成后自动移入回收站`,
      ipAddress: 'unknown',
      createdAt: new Date()
    });

    alert('离职交接完成！\n✅ 数据权限已转移\n✅ 已自动移入回收站');
  } catch (trashError) {
    alert('数据交接成功，但移入回收站失败\n❌ 请手动将员工移入回收站');
  }
}
```

### 数据库操作

1. **users 集合** - 更新员工状态
   ```javascript
   {
     status: '离职',
     deleted: true,
     deletedAt: new Date()
   }
   ```

2. **operation_logs 集合** - 记录操作日志
   ```javascript
   {
     userId: employee._id,
     module: '员工管理',
     action: '离职移入回收站',
     content: '...',
     createdAt: new Date()
   }
   ```

## 相关功能

### 员工回收站

- 📍 位置：员工管理 → 回收站
- 🔍 功能：查看已删除的员工
- ♻️ 操作：恢复员工（但不会恢复数据权限）

### 数据交接

- 📍 位置：员工详情 → 编辑 → 状态改为"离职"
- 🔄 功能：转移任务、商机、项目的owner和collaborators权限
- 📝 范围：仅限 tasks、opportunities、projects 三个集合

## 测试场景

### 正常流程测试

1. ✅ 创建测试员工A（在职）
2. ✅ 创建测试员工B（在职）
3. ✅ 为员工A创建一些任务/商机/项目
4. ✅ 将员工A状态改为"离职"，选择交接给员工B
5. ✅ 确认交接
6. ✅ 验证：
   - 数据权限已转移给员工B
   - 员工A已在回收站中
   - 操作日志已记录

### 异常场景测试

1. ❌ 测试云函数调用失败情况
2. ❌ 测试回收站操作失败情况
3. ❌ 测试网络异常情况

## 版本信息

- **功能版本**: v2.3.0
- **实现日期**: 2025-12-16
- **相关需求**: 离职人员数据交接功能优化

## 相关文档

- [离职人员数据交接功能说明.md](./离职人员数据交接功能说明.md)
- [员工回收站功能说明.md](./docs/员工回收站功能说明.md)
