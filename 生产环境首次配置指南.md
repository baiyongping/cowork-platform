# 际华协同办公平台 - 生产环境首次配置指南

## 📋 概述

本文档用于指导首次在 Lighthouse 服务器上配置生产环境，包括：
- 服务器基础环境配置
- Nginx 安装和配置
- Docker 环境准备
- SSL 证书配置
- 蓝绿部署脚本部署

**注意**：本指南仅需执行一次，后续只需使用快速部署流程。

---

## 🖥️ 服务器要求

### 硬件要求

- **CPU**: 2核及以上
- **内存**: 4GB 及以上
- **磁盘**: 20GB 可用空间
- **网络**: 公网 IP，带宽 5Mbps 及以上

### 软件要求

- **操作系统**: Ubuntu 20.04 LTS / CentOS 7.x 及以上
- **Docker**: 20.10 及以上
- **Nginx**: 1.18 及以上
- **域名**: jihuadz.xin（已解析到服务器IP）

---

## 🔧 步骤一：服务器基础配置

### 1.1 更新系统

```bash
# Ubuntu/Debian
apt update && apt upgrade -y

# CentOS/RHEL
yum update -y
```

### 1.2 安装基础工具

```bash
# Ubuntu/Debian
apt install -y curl wget vim git net-tools

# CentOS/RHEL
yum install -y curl wget vim git net-tools
```

### 1.3 配置防火墙

```bash
# Ubuntu (UFW)
ufw allow 22/tcp     # SSH
ufw allow 80/tcp     # HTTP
ufw allow 443/tcp    # HTTPS
ufw enable

# CentOS (Firewalld)
firewall-cmd --permanent --add-port=22/tcp
firewall-cmd --permanent --add-port=80/tcp
firewall-cmd --permanent --add-port=443/tcp
firewall-cmd --reload

# 验证防火墙规则
firewall-cmd --list-ports    # CentOS
ufw status                   # Ubuntu
```

---

## 🐳 步骤二：安装 Docker

### 2.1 安装 Docker Engine

#### Ubuntu

```bash
# 卸载旧版本
apt remove docker docker-engine docker.io containerd runc

# 添加 Docker 官方 GPG 密钥
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg

# 添加 Docker 仓库
echo "deb [arch=amd64 signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | tee /etc/apt/sources.list.d/docker.list > /dev/null

# 安装 Docker
apt update
apt install -y docker-ce docker-ce-cli containerd.io

# 启动 Docker
systemctl start docker
systemctl enable docker
```

#### CentOS

```bash
# 卸载旧版本
yum remove docker docker-client docker-client-latest docker-common docker-latest docker-latest-logrotate docker-logrotate docker-engine

# 安装依赖
yum install -y yum-utils device-mapper-persistent-data lvm2

# 添加 Docker 仓库
yum-config-manager --add-repo https://download.docker.com/linux/centos/docker-ce.repo

# 安装 Docker
yum install -y docker-ce docker-ce-cli containerd.io

# 启动 Docker
systemctl start docker
systemctl enable docker
```

### 2.2 验证 Docker 安装

```bash
# 检查 Docker 版本
docker --version
# 应显示: Docker version 20.10.x, build xxx

# 运行测试容器
docker run hello-world

# 查看 Docker 信息
docker info
```

### 2.3 配置 Docker（可选优化）

```bash
# 创建 Docker 配置目录
mkdir -p /etc/docker

# 配置 Docker daemon
cat > /etc/docker/daemon.json <<EOF
{
  "log-driver": "json-file",
  "log-opts": {
    "max-size": "10m",
    "max-file": "3"
  },
  "storage-driver": "overlay2"
}
EOF

# 重启 Docker
systemctl daemon-reload
systemctl restart docker
```

---

## 🌐 步骤三：安装和配置 Nginx

### 3.1 安装 Nginx

#### Ubuntu

```bash
apt install -y nginx
```

#### CentOS

```bash
yum install -y nginx
```

### 3.2 启动 Nginx

```bash
# 启动 Nginx
systemctl start nginx
systemctl enable nginx

# 查看 Nginx 状态
systemctl status nginx

# 验证 Nginx 是否运行
curl http://localhost
```

### 3.3 配置目录结构

```bash
# 创建站点配置目录（如果不存在）
mkdir -p /etc/nginx/sites-available
mkdir -p /etc/nginx/sites-enabled

# 创建 SSL 证书目录
mkdir -p /etc/nginx/ssl

# 创建日志目录
mkdir -p /var/log/nginx
```

### 3.4 配置 Nginx 主配置

编辑 `/etc/nginx/nginx.conf`，确保包含以下内容：

```nginx
http {
    # ... 其他配置 ...
    
    # 引入站点配置
    include /etc/nginx/sites-enabled/*;
}
```

---

## 🔐 步骤四：配置 SSL 证书

### 4.1 检查 SSL 证书

确认 SSL 证书文件已准备：

```bash
ls -la /etc/nginx/ssl/
# 应包含:
# - jihuadz.xin.crt  (证书文件)
# - jihuadz.xin.key  (私钥文件)
```

### 4.2 上传 SSL 证书（如果未上传）

**在本地执行：**

```powershell
# 使用 SCP 上传证书（假设证书在本地 certs 目录）
scp certs/jihuadz.xin.crt root@<server-ip>:/etc/nginx/ssl/
scp certs/jihuadz.xin.key root@<server-ip>:/etc/nginx/ssl/
```

**或在服务器上直接创建：**

```bash
# 如果证书是文本格式，可以直接粘贴
nano /etc/nginx/ssl/jihuadz.xin.crt
nano /etc/nginx/ssl/jihuadz.xin.key
```

### 4.3 设置证书权限

```bash
chmod 600 /etc/nginx/ssl/jihuadz.xin.key
chmod 644 /etc/nginx/ssl/jihuadz.xin.crt
chown root:root /etc/nginx/ssl/*
```

### 4.4 验证证书

```bash
# 检查证书信息
openssl x509 -in /etc/nginx/ssl/jihuadz.xin.crt -noout -text

# 检查证书有效期
openssl x509 -in /etc/nginx/ssl/jihuadz.xin.crt -noout -dates

# 验证证书和私钥匹配
openssl x509 -noout -modulus -in /etc/nginx/ssl/jihuadz.xin.crt | openssl md5
openssl rsa -noout -modulus -in /etc/nginx/ssl/jihuadz.xin.key | openssl md5
# 两个 MD5 值应该相同
```

---

## 📦 步骤五：创建部署目录

### 5.1 创建项目目录

```bash
# 创建部署根目录
mkdir -p /var/www/jihua-deploy

# 设置目录权限
chown -R root:root /var/www/jihua-deploy
chmod -R 755 /var/www/jihua-deploy
```

### 5.2 上传部署脚本和配置文件

**方式一：使用 Lighthouse MCP 工具上传**

通过 AI 工具上传以下文件到服务器 `/var/www/jihua-deploy/`：
- `blue-green-deploy-production.sh` - 蓝绿部署脚本
- `Dockerfile` - Docker 镜像构建文件
- `nginx.conf` - 容器内 Nginx 配置

**方式二：手动上传**

```powershell
# 在本地项目根目录执行
scp blue-green-deploy-production.sh root@<server-ip>:/var/www/jihua-deploy/
scp Dockerfile root@<server-ip>:/var/www/jihua-deploy/
scp nginx.conf root@<server-ip>:/var/www/jihua-deploy/
```

### 5.3 设置脚本权限

```bash
cd /var/www/jihua-deploy
chmod +x blue-green-deploy-production.sh
```

---

## 🌐 步骤六：配置 Nginx 站点

### 6.1 创建站点配置

```bash
# 创建站点配置文件
nano /etc/nginx/sites-available/jihuadz.xin
```

**粘贴以下内容：**

```nginx
# 蓝绿部署后端配置
upstream jihua_backend {
    server 127.0.0.1:3000;  # 蓝环境（默认）
}

# HTTP 跳转到 HTTPS
server {
    listen 80;
    server_name jihuadz.xin www.jihuadz.xin;
    
    location / {
        return 301 https://$host$request_uri;
    }
}

# HTTPS 主站配置
server {
    listen 443 ssl http2;
    server_name jihuadz.xin www.jihuadz.xin;
    
    # SSL 证书
    ssl_certificate /etc/nginx/ssl/jihuadz.xin.crt;
    ssl_certificate_key /etc/nginx/ssl/jihuadz.xin.key;
    
    # SSL 配置
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers 'ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384';
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;
    
    # 安全头
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    
    # 反向代理
    location / {
        proxy_pass http://jihua_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
    }
    
    # 防缓存（HTML/JS/CSS）
    location ~* \.(js|css|html)$ {
        proxy_pass http://jihua_backend;
        expires -1;
        add_header Cache-Control "no-store, no-cache";
    }
    
    # 静态资源缓存（图片/字体）
    location ~* \.(jpg|jpeg|png|gif|ico|svg|woff|woff2|ttf)$ {
        proxy_pass http://jihua_backend;
        expires 7d;
        add_header Cache-Control "public, immutable";
    }
    
    # 日志
    access_log /var/log/nginx/jihuadz.xin.access.log;
    error_log /var/log/nginx/jihuadz.xin.error.log;
}
```

### 6.2 启用站点

```bash
# 创建软链接
ln -s /etc/nginx/sites-available/jihuadz.xin /etc/nginx/sites-enabled/

# 测试 Nginx 配置
nginx -t

# 重启 Nginx
systemctl restart nginx
```

### 6.3 验证 Nginx 配置

```bash
# 检查 Nginx 状态
systemctl status nginx

# 测试 HTTP 跳转
curl -I http://jihuadz.xin
# 应该看到 301 跳转到 HTTPS

# 测试 HTTPS
curl -I https://jihuadz.xin
# 如果还没部署应用，可能返回 502（正常）
```

---

## 🎯 步骤七：首次部署应用

### 7.1 本地构建

```powershell
# 在项目根目录执行
npm run build
```

### 7.2 上传构建产物

使用 Lighthouse MCP 工具：

```javascript
mcp_call_tool({
  serverName: "cloudbase",
  toolName: "deploy_project_preparation",
  arguments: JSON.stringify({
    FolderPath: "d:/project/cowork12-21/dist",
    InstanceId: "lhins-pnt984h2",
    Region: "ap-beijing",
    ProjectName: "jihua-prod"
  })
})
```

### 7.3 执行首次部署

```bash
# SSH 连接到服务器
ssh root@<server-ip>

# 执行部署
cd /var/www/jihua-deploy
./blue-green-deploy-production.sh deploy
```

### 7.4 验证部署

```bash
# 查看容器状态
docker ps --filter "name=jihua-prod"

# 测试网站访问
curl -I https://jihuadz.xin

# 浏览器访问
# https://jihuadz.xin
```

---

## ✅ 配置完成检查清单

### 服务器基础 ✅

- [ ] 系统已更新到最新版本
- [ ] 基础工具已安装（curl、wget、vim 等）
- [ ] 防火墙已配置（80、443 端口开放）
- [ ] 时区设置正确（`timedatectl set-timezone Asia/Shanghai`）

### Docker 环境 ✅

- [ ] Docker 已安装并运行
- [ ] Docker 版本 ≥ 20.10
- [ ] Docker 配置已优化
- [ ] 可以拉取和运行镜像

### Nginx 配置 ✅

- [ ] Nginx 已安装并运行
- [ ] 站点配置文件已创建
- [ ] SSL 证书已配置
- [ ] Nginx 测试通过（`nginx -t`）
- [ ] HTTP 自动跳转 HTTPS

### 部署环境 ✅

- [ ] 部署目录已创建（/var/www/jihua-deploy）
- [ ] 部署脚本已上传并可执行
- [ ] Dockerfile 已上传
- [ ] nginx.conf 已上传

### 网络配置 ✅

- [ ] 域名 jihuadz.xin 已解析到服务器IP
- [ ] DNS 解析生效（`nslookup jihuadz.xin`）
- [ ] 80、443 端口可访问
- [ ] SSL 证书有效

### 首次部署 ✅

- [ ] 构建产物已上传
- [ ] 蓝绿部署成功
- [ ] 容器正常运行
- [ ] 网站可访问（https://jihuadz.xin）
- [ ] 核心功能测试通过

---

## 📊 环境信息记录

完成配置后，记录以下信息：

```
服务器IP: __________________
域名: jihuadz.xin
SSL 证书到期时间: __________________
Docker 版本: __________________
Nginx 版本: __________________
部署目录: /var/www/jihua-deploy
配置完成日期: __________________
配置负责人: __________________
```

---

## 🔧 常见问题排查

### 问题1: Nginx 无法启动

```bash
# 查看错误日志
tail -f /var/log/nginx/error.log

# 常见原因:
# - 端口被占用: netstat -tlnp | grep :80
# - 配置文件错误: nginx -t
# - SSL 证书路径错误: ls -la /etc/nginx/ssl/
```

### 问题2: Docker 无法拉取镜像

```bash
# 检查网络
ping registry-1.docker.io

# 配置国内镜像源
cat > /etc/docker/daemon.json <<EOF
{
  "registry-mirrors": [
    "https://docker.mirrors.ustc.edu.cn"
  ]
}
EOF

systemctl restart docker
```

### 问题3: SSL 证书错误

```bash
# 检查证书文件
ls -la /etc/nginx/ssl/

# 验证证书格式
openssl x509 -in /etc/nginx/ssl/jihuadz.xin.crt -text -noout

# 检查私钥格式
openssl rsa -in /etc/nginx/ssl/jihuadz.xin.key -check
```

---

## 📚 相关文档

- [生产环境快速部署指南](./生产环境快速部署指南.md)
- [生产环境蓝绿部署方案](./生产环境蓝绿部署方案_v3.12.0.md)
- [Nginx 配置详解](./nginx-production.conf)

---

**版本**: v3.12.0  
**最后更新**: 2026-01-08  
**维护者**: 际华协同办公平台开发团队

---

## ⚠️ 重要提示

1. **备份**：配置完成后，建议对服务器创建快照备份
2. **安全**：定期更新系统和 Docker，及时打补丁
3. **监控**：配置服务器监控，及时发现异常
4. **文档**：保存好配置信息和密钥，妥善保管

**配置完成后，请立即测试所有核心功能！**
