# 目标分解多表功能 - 快速部署指南

## 🔍 问题诊断

**核心问题**: 数据库集合未创建,导致分解表无法显示

### 症状
- ✅ 前端组件完整
- ✅ 云函数已部署
- ❌ 数据库集合不存在
- ❌ 无法看到构建的分解表

---

## 🎯 快速修复步骤 (5分钟)

### 步骤1: 创建数据库集合 (2分钟)

使用云函数快速创建:

```javascript
// 在 cloudfunctions/ 创建临时云函数 create-decomposition-collections/index.js
const cloud = require('wx-server-sdk');
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV });
const db = cloud.database();

exports.main = async (event, context) => {
  try {
    // 1. 创建 decompositionTables 集合
    await db.createCollection('decompositionTables');
    console.log('✅ decompositionTables 集合创建成功');

    // 2. 创建 decompositionTableConfigs 集合  
    await db.createCollection('decompositionTableConfigs');
    console.log('✅ decompositionTableConfigs 集合创建成功');

    // 3. 创建索引
    await db.collection('decompositionTables').aggregate()
      .sort({ sortOrder: 1, createdAt: -1 })
      .end();

    await db.collection('decompositionTableConfigs').aggregate()
      .sort({ goalTypeId: 1, updatedAt: -1 })
      .end();

    return {
      success: true,
      message: '数据库集合创建成功'
    };
  } catch (error) {
    console.error('创建集合失败:', error);
    return {
      success: false,
      error: error.message
    };
  }
};
```

**部署并运行**:
```bash
# 1. 创建云函数目录
mkdir cloudfunctions/create-decomposition-collections

# 2. 复制上述代码到 index.js

# 3. 创建 package.json
echo '{"name": "create-decomposition-collections", "version": "1.0.0"}' > cloudfunctions/create-decomposition-collections/package.json

# 4. 部署
cd cloudfunctions/create-decomposition-collections
npm install wx-server-sdk
# 使用腾讯云开发工具上传云函数

# 5. 在云开发控制台调用一次该云函数
```

### 步骤2: 配置集合权限 (1分钟)

在云开发控制台:

1. 进入 **数据库 > 集合列表**
2. 找到 `decompositionTables`:
   - 权限设置: **仅创建者可读写**
3. 找到 `decompositionTableConfigs`:
   - 权限设置: **仅创建者可读写**

### 步骤3: 重新构建分解表 (2分钟)

1. 进入 **目标管理 > 维度设置 > 分解表构建**
2. 点击 **"新增分解表"**
3. 填写表名、选择横纵轴维度
4. 点击 **"创建表"**
5. 启用分解表 (点击开关图标)
6. 返回 **目标管理**
7. 点击年度目标的 **"目标分解"** 按钮
8. 点击右上角 **"多表分解视图"** 按钮

**预期结果**: 🎉 能看到刚才创建的分解表!

---

## 📚 集合结构说明

### decompositionTables (分解表主表)

```javascript
{
  _id: string,              // 表ID
  name: string,             // 表名
  horizontalDimension: {    // 横轴维度
    dimensionId: string,
    dimensionName: string,
    values: string[]
  },
  verticalDimension: {      // 纵轴维度
    dimensionId: string,
    dimensionName: string,
    values: string[]
  },
  isEnabled: boolean,       // 是否启用
  sortOrder: number,        // 排序
  createdAt: Date,
  updatedAt: Date,
  createdBy: string
}
```

### decompositionTableConfigs (分解表配置关联表)

```javascript
{
  _id: string,              // 配置ID
  goalTypeId: string,       // 目标类型ID (如 'sales', 'opportunity')
  goalTypeName: string,     // 目标类型名称
  tables: [                 // 关联的分解表
    {
      tableId: string,
      tableName: string,
      sortOrder: number
    }
  ],
  createdAt: Date,
  updatedAt: Date,
  createdBy: string
}
```

---

## 🔄 完整工作流程

### 阶段1: 维度准备
1. 在 **维度设置 > 维度管理** 创建维度:
   - 时间维度 (Q1, Q2, Q3, Q4)
   - 部门维度 (销售一部, 销售二部...)
   - 产品维度 (产品A, 产品B...)

### 阶段2: 构建分解表
1. 在 **维度设置 > 分解表构建** 创建表:
   - 表名: "销售目标季度×部门分解表"
   - 横轴: 时间维度 (Q1~Q4)
   - 纵轴: 部门维度 (销售一部~五部)
   - 点击 "创建表" → 生成4×5矩阵

2. 启用分解表 (点击开关图标)

### 阶段3: 关联目标类型 (当前需要手动)
```javascript
// 调用云函数关联
callFunction({
  name: 'decomposition-table-configs',
  data: {
    action: 'saveConfig',
    data: {
      goalTypeId: 'sales',           // 销售目标
      goalTypeName: '销售目标',
      tables: [
        {
          tableId: 'table_xxx',      // 分解表ID
          tableName: '季度×部门分解表',
          sortOrder: 0
        }
      ]
    }
  }
});
```

### 阶段4: 使用分解表
1. 在 **目标管理** 点击年度销售目标
2. 点击 **"目标分解"** 按钮
3. 点击右上角 **"多表分解视图"** 按钮
4. 看到卡片式的多表展示 🎉

---

## ⚠️ 常见问题

### Q1: 创建分解表时提示"集合不存在"?
**A**: 先完成步骤1创建集合

### Q2: 分解表列表为空?
**A**: 检查:
- 集合是否已创建? (步骤1)
- 是否已构建分解表? (步骤3)
- 分解表是否已启用? (开关图标)

### Q3: 点击"多表分解视图"没有表?
**A**: 需要配置目标类型关联 (阶段3)

### Q4: 如何配置关联?
**A**: 当前需手动调用云函数,后续会在UI中添加配置入口

---

## 📊 验证清单

完成部署后,验证以下内容:

- [ ] 集合 `decompositionTables` 已创建
- [ ] 集合 `decompositionTableConfigs` 已创建
- [ ] 在"分解表构建"能创建表
- [ ] 创建的表能成功保存
- [ ] 能看到表列表
- [ ] 能启用/禁用表
- [ ] 在"目标分解"能点击"多表分解视图"
- [ ] 能看到配置的分解表

---

## 🚀 下一步优化

1. **UI优化**: 在"分解表构建"页面添加"关联目标类型"选择框
2. **自动关联**: 创建表时自动关联到目标类型
3. **批量操作**: 支持批量启用/禁用/删除
4. **数据导入**: 支持从Excel导入分解表数据

---

**部署完成时间**: 约5分钟  
**技术难度**: ⭐⭐☆☆☆  
**是否需要重启**: 否

如有问题,请查看云函数日志或联系技术支持。
