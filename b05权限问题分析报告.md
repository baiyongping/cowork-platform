# b05 用户权限问题分析报告

## 📋 问题描述

**用户**: b05 (白永平5)  
**分配角色**: assa (测试角色)  
**预期行为**: 只能查看自己相关的任务、商机、项目  
**实际行为**: 可以看到所有（或大量）任务、商机、项目数据  

---

## 🔍 问题分析

### 1. b05 用户信息

```json
{
  "_id": "d84694f269411e7407f2963c258339d8",
  "username": "b05",
  "name": "白永平5",
  "roles": ["user", "assa"],
  "departments": ["客户服务部"],
  "supervisorId": "4289fa816940d1f807e70a923b242299"
}
```

### 2. assa 角色权限配置

```json
{
  "role": "assa",
  "name": "测试",
  "permissions": {
    "tasks": {
      "view": true,
      "create": true,
      "edit": true,
      "delete": false,
      "export": false
    },
    "opportunity": {
      "view": true,
      "create": true,
      "edit": false,
      "delete": false,
      "export": false
    },
    "project": {
      "view": true,
      "create": true,
      "edit": false,
      "delete": false,
      "export": false
    }
  }
}
```

**权限配置正确**：assa 角色确实只有 `view: true`，不应该看到所有数据。

---

### 3. 根本原因：数据查询逻辑缺陷

#### 问题代码位置：`utils/permission.ts:253-304`

```typescript
export async function buildQueryConditions(
  currentUserId: string,
  currentUserRole: string,  // ⚠️ 只检查单个角色字符串
  db: any
): Promise<any> {
  // 管理员可以查看所有数据
  if (currentUserRole === 'admin') {
    return {};
  }

  // ... 省略其他代码 ...

  // ⚠️ 关键缺陷：无条件给同部门用户添加查看权限
  if (departments.length > 0) {
    const deptUsersResult = await db.collection('users')
      .where({ departments: { $in: departments } })
      .get();
    
    const deptUserIds = deptUsersResult.data.map((u: any) => u._id);
    
    // 🐛 问题：无论角色权限如何，都添加同部门查看权限
    if (isDepartmentLeader) {
      conditions.push({ owner: { $in: deptUserIds } });
    } else {
      // 🐛 普通员工也能看到同部门所有数据！
      conditions.push({ owner: { $in: deptUserIds } });
    }
  }

  return { $or: conditions };
}
```

---

### 4. 权限逻辑链条

```
用户登录 b05
  ↓
获取角色: ["user", "assa"]
  ↓
调用 buildQueryConditions
  ↓
currentUserRole = "assa" (或 "user")
  ↓
不是 admin，执行普通用户逻辑
  ↓
构建查询条件:
  ✅ owner: "b05用户ID"           (自己创建的)
  ✅ collaborators: "b05用户ID"   (作为协作人的)
  ⚠️ owner: { $in: [客户服务部所有用户ID] }  // 🐛 问题！
  ↓
结果：能看到客户服务部所有员工创建的数据
```

---

### 5. 为什么感觉是"所有数据"？

如果"客户服务部"有很多员工（例如10-20人），那么 b05 用户可以看到这些员工创建的所有任务、商机、项目，数据量可能非常大，给人感觉就是"所有数据"。

---

## ✅ 解决方案

### 方案A：基于角色权限控制数据范围（推荐）

修改 `buildQueryConditions` 函数，根据角色的**数据权限范围**来决定是否允许查看同部门数据。

#### 需要的改动

1. **定义数据范围等级**
```typescript
enum DataScope {
  SELF = 'self',           // 仅自己：只能看到自己创建的和作为协作人的
  DEPARTMENT = 'department', // 部门：可以看到同部门所有数据
  SUBORDINATES = 'subordinates', // 下级：可以看到下级的数据
  ALL = 'all'              // 全部：可以看到所有数据
}
```

2. **在角色权限中添加数据范围配置**
```typescript
{
  "role": "assa",
  "name": "测试",
  "dataScope": "self",  // 🆕 只能看自己的数据
  "permissions": { ... }
}
```

3. **修改 buildQueryConditions 函数**
```typescript
export async function buildQueryConditions(
  currentUserId: string,
  currentUserRole: string,
  db: any,
  dataScope: string = 'self'  // 🆕 新增参数
): Promise<any> {
  // 管理员：查看所有数据
  if (currentUserRole === 'admin' || dataScope === 'all') {
    return {};
  }

  const conditions: any[] = [
    { owner: currentUserId },
    { collaborators: currentUserId }
  ];

  // 🆕 根据数据范围决定是否添加额外查询条件
  if (dataScope === 'subordinates' || dataScope === 'department') {
    const subordinates = await getAllSubordinates(currentUserId, db);
    if (subordinates.length > 0) {
      conditions.push({ owner: { $in: subordinates } });
    }
  }

  if (dataScope === 'department') {
    const departments = await getUserDepartments(currentUserId, db);
    if (departments.length > 0) {
      const deptUsersResult = await db.collection('users')
        .where({ departments: { $in: departments } })
        .get();
      const deptUserIds = deptUsersResult.data.map((u: any) => u._id);
      conditions.push({ owner: { $in: deptUserIds } });
    }
  }

  return { $or: conditions };
}
```

---

### 方案B：临时快速修复（不推荐，但立即生效）

直接修改 `buildQueryConditions`，移除同部门查看权限的逻辑：

```typescript
// 注释掉同部门查看逻辑
/*
if (departments.length > 0) {
  const deptUsersResult = await db.collection('users')
    .where({ departments: { $in: departments } })
    .get();
  const deptUserIds = deptUsersResult.data.map((u: any) => u._id);
  conditions.push({ owner: { $in: deptUserIds } });
}
*/
```

这样所有非admin用户都只能看到：
- 自己创建的数据
- 自己作为协作人的数据
- 自己下级创建的数据（如果有下级）

---

## 📊 影响范围评估

### 修复后的影响

| 用户类型 | 修复前 | 修复后（方案A） |
|---------|--------|----------------|
| 管理员 (admin) | 查看所有数据 | 查看所有数据（无变化） |
| 部门经理 (manager) | 查看同部门所有数据 | 查看同部门所有数据（dataScope=department） |
| 高管 (executive) | 查看同部门所有数据 | 查看所有数据（dataScope=all） |
| 普通员工 (employee) | 查看同部门所有数据 ❌ | 只查看自己和下级的数据 ✅ |
| 测试角色 (assa) | 查看同部门所有数据 ❌ | 只查看自己的数据 ✅ |

---

## 🎯 推荐的修复步骤

### 第一步：立即临时修复（方案B）
- 移除同部门查看权限的代码
- 确保 b05 等测试用户只能看到自己的数据
- 测试验证

### 第二步：完善权限系统（方案A）
- 设计数据范围等级
- 在 role_permissions 集合中添加 dataScope 字段
- 修改 buildQueryConditions 函数支持 dataScope
- 为每个角色配置合适的 dataScope
- 全面测试各角色的数据访问权限

---

## 🧪 测试清单

修复后需要测试以下场景：

- [ ] b05 (assa角色) 只能看到自己创建的数据
- [ ] 普通员工 只能看到自己和下级的数据
- [ ] 部门经理 可以看到同部门所有数据
- [ ] 高管 可以看到全公司所有数据
- [ ] 管理员 可以看到所有数据
- [ ] 协作人功能正常（能看到自己作为协作人的数据）
- [ ] 上下级关系正常（上级能看到下级的数据）

---

## 📝 关键教训

1. **权限系统需要多维度控制**
   - 功能权限（能否操作）
   - 数据权限（能看到哪些数据）
   - 页面权限（能访问哪些页面）

2. **默认应该是最小权限原则**
   - 不确定时，应该限制而不是开放
   - 同部门查看权限不应该是默认行为

3. **角色配置需要明确数据范围**
   - 不同角色应该有不同的数据可见范围
   - 这应该是可配置的，而不是硬编码

4. **测试要覆盖权限场景**
   - 每个角色都应该测试数据访问范围
   - 不只是测试"能看到"，也要测试"不能看到"

---

## 🔗 相关文件

- `utils/permission.ts` - 权限查询条件构建
- `components/TaskManagementPage.tsx` - 任务列表查询
- `components/pages/OpportunityManagement.tsx` - 商机列表查询
- `components/pages/ProjectManagement.tsx` - 项目列表查询
- `database/role_permissions` - 角色权限配置

---

*分析时间: 2025-12-16*  
*问题严重程度: 高（数据权限泄露）*  
*建议修复优先级: 紧急*
