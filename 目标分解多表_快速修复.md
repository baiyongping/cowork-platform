# 目标分解多表 - 快速修复指南

## 🎯 问题

构建并启用的分解表在目标分解页面看不到

## ✅ 立即修复(3步)

### 步骤1: 更新 DecompositionTablesTab 组件

**文件**: `components/DecompositionTablesTab.tsx`

在表单的"基本信息"部分(表名输入框后)添加:

```tsx
{/* 在表名输入框后添加 */}
<div>
  <label className="block text-sm font-medium text-gray-700 mb-1">
    关联目标类型 <span className="text-red-500">*</span>
  </label>
  <select
    value={formData.goalTypeId || ''}
    onChange={(e) => setFormData({ ...formData, goalTypeId: e.target.value })}
    className="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
    required
  >
    <option value="">请选择目标类型</option>
    <option value="sales">销售目标</option>
    <option value="opportunity">商机目标</option>
  </select>
  <p className="text-xs text-gray-500 mt-1">
    选择此分解表用于哪种目标类型
  </p>
</div>
```

并在初始化 formData 时添加:

```tsx
const [formData, setFormData] = useState<Partial<DecompositionTable>>({
  name: '',
  goalTypeId: '', // 添加这行
  horizontalDimensionId: '',
  verticalDimensionId: '',
  columns: [],
  sortOrder: 0,
  isActive: true
});
```

### 步骤2: 更新云函数

**文件**: `cloudfunctions/decomposition-tables/index.js`

找到 `case 'query':` 部分,修改为:

```javascript
case 'query': {
  const { goalTypeId } = data || {};
  console.log('📊 查询分解表配置:', { goalTypeId });
  
  let query = db.collection('decompositionTableSettings');
  
  // 如果指定了目标类型,则筛选
  if (goalTypeId) {
    query = query.where({
      goalTypeId: goalTypeId,
      isActive: true // 只返回启用的表
    });
  } else {
    query = query.where({ isActive: true });
  }
  
  const result = await query
    .orderBy('sortOrder', 'asc')
    .get();
  
  console.log(`✅ 查询到 ${result.data.length} 个分解表`);
  
  return {
    success: true,
    data: result.data
  };
}
```

### 步骤3: 手动更新现有数据

在 CloudBase 控制台执行:

1. 打开 CloudBase 控制台
2. 进入数据库 -> `decompositionTableSettings` 集合
3. 找到您构建的分解表记录
4. 点击"编辑"
5. 添加字段:
   ```json
   {
     "goalTypeId": "sales",  // 或 "opportunity"
     "goalTypeName": "销售目标" // 或 "商机目标"
   }
   ```
6. 保存

**或者使用云函数批量更新** (创建临时云函数):

```javascript
// 临时迁移云函数
const cloud = require('wx-server-sdk');
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV });
const db = cloud.database();

exports.main = async (event, context) => {
  const tables = await db.collection('decompositionTableSettings')
    .where({ isActive: true })
    .get();
  
  console.log(`找到 ${tables.data.length} 个分解表`);
  
  for (const table of tables.data) {
    // 根据表名判断类型(您可以根据实际情况调整)
    let goalTypeId = 'sales'; // 默认销售目标
    let goalTypeName = '销售目标';
    
    if (table.name.includes('商机') || table.name.includes('opportunity')) {
      goalTypeId = 'opportunity';
      goalTypeName = '商机目标';
    }
    
    await db.collection('decompositionTableSettings')
      .doc(table._id)
      .update({
        data: {
          goalTypeId,
          goalTypeName,
          updatedAt: new Date()
        }
      });
    
    console.log(`✅ 更新: ${table.name} -> ${goalTypeName}`);
  }
  
  return { success: true, updated: tables.data.length };
};
```

## 🧪 验证

1. **刷新页面**
2. 进入"目标管理"
3. 点击年度销售目标的"目标分解"按钮
4. 点击右上角**"多表分解视图"**按钮
5. 应该能看到您构建的分解表了! 🎉

## 🔍 如果还是看不到

### 检查1: 数据库记录

```javascript
// 在浏览器控制台执行
const { callFunction } = window.tcb;

const result = await callFunction({
  name: 'decomposition-tables',
  data: { action: 'query', goalTypeId: 'sales' }
});

console.log('查询结果:', result.result);
```

### 检查2: 云函数日志

1. CloudBase 控制台 -> 云函数 -> `decomposition-tables`
2. 查看日志标签页
3. 找到最近的查询日志
4. 确认返回了数据

### 检查3: 浏览器控制台

1. 打开浏览器开发者工具 (F12)
2. 查看 Console 标签
3. 查找错误信息

## 📞 需要帮助?

如果问题仍未解决,请提供:
1. 浏览器控制台截图
2. 云函数日志截图
3. 数据库记录截图

---

**预计修复时间**: 10分钟  
**难度**: ⭐⭐☆☆☆ (简单)
