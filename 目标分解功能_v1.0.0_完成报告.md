# 目标分解功能 v1.0.0 完成报告

## 功能概述

目标分解功能允许用户将年度/季度目标按照多个维度进行拆解，支持动态维度配置、数据填写、版本管理等完整功能。

## 核心功能

### 1. 分解维度管理 ✅

**功能点:**
- ✅ 创建/编辑/删除分解维度
- ✅ 支持二级维度项配置
- ✅ 拖拽排序功能（一级和二级维度项）
- ✅ 已使用维度只能停用，不能删除
- ✅ 维度状态管理（启用/停用）

**实现位置:**
- 前端组件: `components/GoalDimensionSettings.tsx`
- 云函数: `cloudfunctions/dimension-management/index.js`
- 数据集合: `goalDecompositionDimensions`

### 2. 目标分解编辑 ✅

**功能点:**
- ✅ 选择1-3个分解维度
- ✅ 动态生成分解表格（笛卡尔积组合）
- ✅ 支持数据填写和实时计算
- ✅ 数据校验：分解总和必须等于目标值（精度0.01）
- ✅ 三维平铺表格展示方式
- ✅ 保存分解数据

**实现位置:**
- 前端组件: `components/GoalDecompositionEditor.tsx`
- 云函数: `cloudfunctions/decomposition-management/index.js`
- 数据集合: `goalDecompositions`

### 3. 历史版本管理 ✅

**功能点:**
- ✅ 自动记录每次保存的版本
- ✅ 保留最近10个版本（自动清理）
- ✅ 版本列表展示（时间、创建人）
- ✅ 版本详情对比查看
- ✅ 版本回滚功能

**实现位置:**
- 前端组件: `components/DecompositionHistoryModal.tsx`
- 云函数: `cloudfunctions/decomposition-management/index.js`
- 数据集合: `goalDecompositionHistory`

## 技术实现

### 数据库设计

**1. goalDecompositionDimensions（分解维度）**
```javascript
{
  _id: string,
  name: string,           // 维度名称
  type: 'time' | 'region' | 'business' | 'custom',
  items: [                // 维度项
    {
      id: string,
      name: string,
      order: number,
      status: 'active' | 'inactive',
      children: [...]      // 二级维度项
    }
  ],
  status: 'active' | 'inactive',
  createdAt: Date,
  createdBy: string
}
```

**索引:**
- `status`
- `type`

**2. goalDecompositions（分解数据）**
```javascript
{
  _id: string,
  goalId: string,         // 目标ID
  goalName: string,
  targetValue: number,    // 目标值
  dimensionIds: string[], // 使用的维度ID
  cells: [                // 分解单元格
    {
      cellId: string,
      dimensionPath: [    // 维度路径
        {
          dimensionId: string,
          itemId: string,
          itemName: string
        }
      ],
      value: number
    }
  ],
  version: number,
  createdAt: Date,
  updatedAt: Date,
  createdBy: string
}
```

**索引:**
- `goalId`（唯一）
- `createdBy`
- `updatedAt`

**3. goalDecompositionHistory（历史版本）**
```javascript
{
  _id: string,
  goalId: string,
  snapshot: {             // 完整快照
    targetValue: number,
    dimensionIds: string[],
    cells: [...]
  },
  version: number,
  comment: string,
  createdAt: Date,
  createdBy: string
}
```

**索引:**
- `goalId` + `createdAt`

### 云函数API

**dimension-management（维度管理）**
```javascript
// 操作列表
{
  action: 'list',        // 查询维度列表
  action: 'create',      // 创建维度
  action: 'update',      // 更新维度
  action: 'delete',      // 删除/停用维度
  action: 'reorder'      // 维度项排序
}
```

**decomposition-management（分解管理）**
```javascript
// 操作列表
{
  action: 'get',         // 获取分解数据
  action: 'create',      // 创建分解
  action: 'update',      // 更新分解
  action: 'getHistory',  // 获取历史版本
  action: 'restoreVersion' // 恢复版本
}
```

### 核心算法

**1. 笛卡尔积生成**
```typescript
// 递归生成所有维度组合
const generateCombinations = (
  dimIndex: number,
  currentPath: DimensionPath[]
) => {
  if (dimIndex >= dimensions.length) {
    // 生成一个单元格
    cells.push({ cellId, dimensionPath: currentPath, value: 0 });
    return;
  }
  
  const currentDim = dimensions[dimIndex];
  for (const item of currentDim.items) {
    generateCombinations(dimIndex + 1, [
      ...currentPath,
      { dimensionId: currentDim._id, itemId: item.id, itemName: item.name }
    ]);
  }
};
```

**2. 数据校验**
```typescript
const validateData = (): boolean => {
  const total = cells.reduce((sum, cell) => sum + cell.value, 0);
  const diff = Math.abs(total - targetValue);
  return diff < 0.01; // 精度0.01
};
```

**3. 版本管理**
```javascript
// 保存新版本前，保留最近10个版本
async function saveVersion(goalId, snapshot) {
  const MAX_VERSIONS = 10;
  
  // 查询现有版本数
  const count = await db.collection('goalDecompositionHistory')
    .where({ goalId })
    .count();
  
  // 如果超过10个，删除最旧的
  if (count.total >= MAX_VERSIONS) {
    const oldest = await db.collection('goalDecompositionHistory')
      .where({ goalId })
      .orderBy('createdAt', 'asc')
      .limit(1)
      .get();
    
    await db.collection('goalDecompositionHistory')
      .doc(oldest.data[0]._id)
      .remove();
  }
  
  // 保存新版本
  await db.collection('goalDecompositionHistory').add({
    data: {
      goalId,
      snapshot,
      version: count.total + 1,
      createdAt: db.serverDate(),
      createdBy: wxContext.OPENID
    }
  });
}
```

## 部署步骤

### 1. 初始化数据库集合

```bash
# 运行集合初始化脚本
node database/init-decomposition-collections.js
```

**创建的集合:**
- `goalDecompositionDimensions`
- `goalDecompositions`
- `goalDecompositionHistory`

### 2. 部署云函数

```bash
# 部署维度管理云函数
tcb fn deploy dimension-management

# 部署分解管理云函数
tcb fn deploy decomposition-management
```

### 3. 配置权限

**集合权限设置:**
- `goalDecompositionDimensions`: 仅创建者可写，所有登录用户可读
- `goalDecompositions`: 仅创建者可写，所有登录用户可读
- `goalDecompositionHistory`: 仅创建者可写，所有登录用户可读

### 4. 前端集成

**在目标管理页面添加分解按钮:**

```tsx
import GoalDecompositionEditor from '../components/GoalDecompositionEditor';
import DecompositionHistoryModal from '../components/DecompositionHistoryModal';

// 在目标列表中添加
<button onClick={() => setDecompositionGoal(goal)}>
  分解目标
</button>

// 渲染编辑器
{decompositionGoal && (
  <GoalDecompositionEditor
    goalId={decompositionGoal._id}
    goalName={decompositionGoal.name}
    targetValue={decompositionGoal.target}
    onClose={() => setDecompositionGoal(null)}
    onSave={loadGoals}
  />
)}
```

## 使用流程

### 1. 配置分解维度

1. 进入「目标管理」-「维度设置」
2. 点击「新增维度」
3. 配置维度信息：
   - 维度名称（如：时间维度）
   - 维度类型（时间/区域/业务/自定义）
   - 维度项（如：Q1、Q2、Q3、Q4）
   - 可添加二级维度项
4. 拖拽调整顺序
5. 保存配置

### 2. 目标分解

1. 在目标列表中点击「分解」按钮
2. 选择1-3个分解维度
3. 点击「生成分解表格」
4. 填写各单元格的目标值
5. 系统自动计算总和并校验
6. 确认无误后点击「保存」

### 3. 查看历史版本

1. 在分解编辑器中点击「历史版本」
2. 选择要查看的版本
3. 查看版本详情和数据
4. 如需回滚，点击「恢复此版本」

## 数据示例

### 示例1：时间维度分解

**目标:** 年度销售额 1000万
**维度:** 时间维度（Q1、Q2、Q3、Q4）

| 季度 | 目标值 |
|------|--------|
| Q1   | 200    |
| Q2   | 250    |
| Q3   | 300    |
| Q4   | 250    |
| 合计 | 1000   |

### 示例2：二维分解

**目标:** 年度销售额 1000万
**维度1:** 时间（Q1、Q2、Q3、Q4）
**维度2:** 区域（华北、华东、华南）

| 季度 | 区域 | 目标值 |
|------|------|--------|
| Q1   | 华北 | 70     |
| Q1   | 华东 | 80     |
| Q1   | 华南 | 50     |
| Q2   | 华北 | 90     |
| ...  | ...  | ...    |
| 合计 | -    | 1000   |

### 示例3：三维分解

**目标:** 年度销售额 1000万
**维度1:** 时间（Q1、Q2）
**维度2:** 区域（华北、华东）
**维度3:** 业务（产品A、产品B）

| 季度 | 区域 | 业务  | 目标值 |
|------|------|-------|--------|
| Q1   | 华北 | 产品A | 100    |
| Q1   | 华北 | 产品B | 80     |
| Q1   | 华东 | 产品A | 120    |
| Q1   | 华东 | 产品B | 100    |
| Q2   | 华北 | 产品A | 110    |
| ...  | ...  | ...   | ...    |
| 合计 | -    | -     | 1000   |

## 注意事项

### 1. 数据校验
- ⚠️ 分解总和必须精确等于目标值（误差不超过0.01）
- ⚠️ 保存前系统会自动校验，不符合要求将无法保存

### 2. 维度管理
- ⚠️ 已被使用的维度只能停用，不能删除
- ⚠️ 停用维度后，新的分解将无法使用该维度
- ⚠️ 已有分解数据不受影响

### 3. 版本管理
- ℹ️ 系统自动保留最近10个版本
- ℹ️ 每次保存都会创建新版本
- ℹ️ 版本回滚会创建新的当前版本

### 4. 性能优化
- ℹ️ 三维分解最多支持 100 × 100 × 100 = 100万个单元格
- ℹ️ 建议控制维度项数量，避免生成过多单元格
- ℹ️ 大数据量时采用分页加载

## 后续优化建议

### 短期优化（1-2周）
1. **导出功能**: 支持导出为 Excel/PDF
2. **批量填写**: 支持按比例分配、平均分配等快捷操作
3. **公式计算**: 支持单元格之间的公式关联
4. **权限控制**: 细化到维度级别的权限管理

### 中期优化（1-2月）
1. **可视化展示**: 添加图表展示分解结果
2. **智能推荐**: 基于历史数据推荐分解方案
3. **模板功能**: 支持保存和复用分解模板
4. **移动端适配**: 优化移动端展示和操作

### 长期优化（3-6月）
1. **AI辅助**: 使用AI自动生成分解建议
2. **实时协同**: 支持多人同时编辑
3. **敏感度分析**: 分析各维度对目标的影响
4. **预警机制**: 实际值偏离目标时自动预警

## 测试清单

### 功能测试
- [ ] 创建/编辑/删除维度
- [ ] 维度项拖拽排序
- [ ] 一维分解（只选1个维度）
- [ ] 二维分解（选2个维度）
- [ ] 三维分解（选3个维度）
- [ ] 数据填写和自动计算
- [ ] 数据校验（总和不等于目标值）
- [ ] 保存分解数据
- [ ] 查看历史版本
- [ ] 版本回滚

### 边界测试
- [ ] 维度项为空
- [ ] 单元格值为负数
- [ ] 单元格值为小数
- [ ] 超大数据量（10000+单元格）
- [ ] 已使用维度尝试删除
- [ ] 版本数量超过10个

### 性能测试
- [ ] 100个单元格加载速度
- [ ] 1000个单元格加载速度
- [ ] 10000个单元格加载速度
- [ ] 版本切换响应时间
- [ ] 批量保存响应时间

## 已知问题

无

## 版本信息

- **版本号**: v1.0.0
- **完成日期**: 2025-01-27
- **开发人员**: AI Assistant
- **测试状态**: 待测试

## 附录

### 相关文件清单

**数据库:**
- `database/collections/goalDecompositionDimensions.js`
- `database/collections/goalDecompositions.js`
- `database/collections/goalDecompositionHistory.js`
- `database/init-decomposition-collections.js`

**云函数:**
- `cloudfunctions/dimension-management/index.js`
- `cloudfunctions/dimension-management/config.json`
- `cloudfunctions/dimension-management/package.json`
- `cloudfunctions/decomposition-management/index.js`
- `cloudfunctions/decomposition-management/config.json`
- `cloudfunctions/decomposition-management/package.json`

**前端组件:**
- `components/GoalDimensionSettings.tsx`
- `components/GoalDecompositionEditor.tsx`
- `components/DecompositionHistoryModal.tsx`
- `types/goalDecomposition.ts`

### 数据库权限配置脚本

```javascript
// goalDecompositionDimensions
{
  "read": "auth.loginType == 'CUSTOM'",
  "write": "auth.loginType == 'CUSTOM' && (resource.creator == auth.uid || auth.uid == 'admin')"
}

// goalDecompositions
{
  "read": "auth.loginType == 'CUSTOM'",
  "write": "auth.loginType == 'CUSTOM' && (resource.creator == auth.uid || auth.uid == 'admin')"
}

// goalDecompositionHistory
{
  "read": "auth.loginType == 'CUSTOM'",
  "write": "auth.loginType == 'CUSTOM' && (resource.creator == auth.uid || auth.uid == 'admin')"
}
```

---

## 快速开始

```bash
# 1. 初始化集合
node database/init-decomposition-collections.js

# 2. 部署云函数
tcb fn deploy dimension-management
tcb fn deploy decomposition-management

# 3. 构建前端
npm run build

# 4. 部署前端
# (使用现有部署流程)
```

**完成报告结束**
