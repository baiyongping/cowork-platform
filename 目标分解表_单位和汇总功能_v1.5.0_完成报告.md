# 目标分解表 - 单位和汇总功能 v1.5.0

## ✅ 完成时间
2026-01-06

---

## 📋 功能概述

在目标分解表中实现了以下三个重要的显示功能：

1. **单位显示** - 在表格标题显示单位参数
2. **行汇总** - 在表格右侧显示每行的合计
3. **列总计** - 在表格底部显示每列的总计

---

## 🎯 实现效果

### 1️⃣ 单位显示

**位置**：表格标题后面

**显示格式**：
```
2026年销售目标分解--收入渠道（单位：万元）
```

**实现逻辑**：
- 如果分解表配置了 `unit` 参数，则在标题后显示单位
- 格式：`（单位：{unit}）`
- 样式：灰色小字，不影响主标题

---

### 2️⃣ 行汇总（Horizontal Total）

**位置**：表格最右侧增加一列

**显示内容**：
- 列标题：`合计`
- 单元格：该行所有数值的总和

**样式特征**：
- 列头背景：绿色（`bg-green-50`）
- 单元格背景：绿色（`bg-green-50`）
- 字体：粗体（`font-bold`）
- 数字格式化：千分位分隔符

**计算逻辑**：
```typescript
// 遍历该行所有列，累加数值
calculateRowTotal(tableId, rowIndex, colCount)
```

---

### 3️⃣ 列总计（Vertical Total）

**位置**：表格最底部增加一行

**显示内容**：
- 行标题：`总计`
- 单元格：该列所有数值的总和

**样式特征**：
- 整行背景：黄色（`bg-yellow-50`）
- 行标题和数值：粗体（`font-bold`）
- 数字格式化：千分位分隔符

**计算逻辑**：
```typescript
// 遍历该列所有行，累加数值
calculateColumnTotal(tableId, colIndex, rowCount)
```

---

### 4️⃣ 总计的总计（Grand Total）

**触发条件**：同时启用行汇总和列总计

**位置**：表格右下角（行汇总列 × 列总计行的交叉点）

**显示内容**：所有单元格数值的总和

**样式特征**：
- 背景：橙色（`bg-orange-100`）
- 字体：粗体（`font-bold`）
- 数字格式化：千分位分隔符

**计算逻辑**：
```typescript
// 遍历所有单元格，累加数值
calculateGrandTotal(tableId, rowCount, colCount)
```

---

## 🔧 技术实现

### 1. 数据结构扩展

**DecompositionTable 接口新增字段**：
```typescript
interface DecompositionTable {
  // ... 原有字段
  unit?: string;              // 单位（可选）
  showRowTotal?: boolean;     // 显示行汇总（可选）
  showColumnTotal?: boolean;  // 显示列总计（可选）
}
```

### 2. 计算函数

#### 行汇总计算
```typescript
const calculateRowTotal = (
  tableId: string, 
  rowIndex: number, 
  colCount: number
): number => {
  let total = 0;
  for (let colIndex = 0; colIndex < colCount; colIndex++) {
    const value = getCellValue(tableId, rowIndex, colIndex);
    const numValue = parseFloat(value);
    if (!isNaN(numValue)) {
      total += numValue;
    }
  }
  return total;
};
```

#### 列总计计算
```typescript
const calculateColumnTotal = (
  tableId: string, 
  colIndex: number, 
  rowCount: number
): number => {
  let total = 0;
  for (let rowIndex = 0; rowIndex < rowCount; rowIndex++) {
    const value = getCellValue(tableId, rowIndex, colIndex);
    const numValue = parseFloat(value);
    if (!isNaN(numValue)) {
      total += numValue;
    }
  }
  return total;
};
```

#### 总计计算
```typescript
const calculateGrandTotal = (
  tableId: string, 
  rowCount: number, 
  colCount: number
): number => {
  let total = 0;
  for (let rowIndex = 0; rowIndex < rowCount; rowIndex++) {
    for (let colIndex = 0; colIndex < colCount; colIndex++) {
      const value = getCellValue(tableId, rowIndex, colIndex);
      const numValue = parseFloat(value);
      if (!isNaN(numValue)) {
        total += numValue;
      }
    }
  }
  return total;
};
```

### 3. 渲染逻辑

#### 单位显示
```tsx
<h3 className="text-lg font-semibold text-gray-900">
  {table.name}
  {table.unit && (
    <span className="ml-2 text-sm font-normal text-gray-600">
      （单位：{table.unit}）
    </span>
  )}
</h3>
```

#### 行汇总列
```tsx
{/* 表头 */}
{table.showRowTotal && (
  <th className="border border-gray-300 bg-green-50 px-4 py-3 text-center text-sm font-semibold text-gray-700 min-w-[150px]">
    合计
  </th>
)}

{/* 数据行 */}
{table.showRowTotal && (
  <td className="border border-gray-300 bg-green-50 px-4 py-3 text-center text-sm font-bold text-gray-900">
    {calculateRowTotal(table._id, rowIndex, table.horizontalDimension.values.length).toLocaleString()}
  </td>
)}
```

#### 列总计行
```tsx
{table.showColumnTotal && (
  <tr className="bg-yellow-50">
    <td className="border border-gray-300 px-4 py-3 text-sm font-bold text-gray-900 sticky left-0 z-10">
      总计
    </td>
    {table.horizontalDimension.values.map((_, colIndex) => (
      <td key={colIndex} className="border border-gray-300 px-4 py-3 text-center text-sm font-bold text-gray-900">
        {calculateColumnTotal(table._id, colIndex, table.verticalDimension.values.length).toLocaleString()}
      </td>
    ))}
    {/* 总计的总计 */}
    {table.showRowTotal && (
      <td className="border border-gray-300 bg-orange-100 px-4 py-3 text-center text-sm font-bold text-gray-900">
        {calculateGrandTotal(table._id, table.verticalDimension.values.length, table.horizontalDimension.values.length).toLocaleString()}
      </td>
    )}
  </tr>
)}
```

---

## 📊 显示效果示例

### 启用所有功能的表格

```
2026年销售目标分解--收入渠道（单位：万元）

时间 × 收入渠道

              Q1      Q2      Q3      Q4     【合计】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
工装行动      100     120     150     180      550
自拓          80      90      95      100      365
区域协同      60      70      75      80       285
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【总计】      240     280     320     360     1200
                                              ↑总计的总计
```

### 只启用行汇总

```
2026年销售目标分解--收入渠道（单位：万元）

              Q1      Q2      Q3      Q4     【合计】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
工装行动      100     120     150     180      550
自拓          80      90      95      100      365
区域协同      60      70      75      80       285
```

### 只启用列总计

```
2026年销售目标分解--收入渠道（单位：万元）

              Q1      Q2      Q3      Q4
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
工装行动      100     120     150     180
自拓          80      90      95      100
区域协同      60      70      75      80
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【总计】      240     280     320     360
```

---

## 🎨 样式设计

### 颜色方案

| 元素 | 背景色 | 文字色 | 用途 |
|------|--------|--------|------|
| 行汇总列头 | `bg-green-50` | `text-gray-700` | 标识合计列 |
| 行汇总单元格 | `bg-green-50` | `text-gray-900` | 显示行总和 |
| 列总计行 | `bg-yellow-50` | `text-gray-900` | 标识总计行 |
| 总计的总计 | `bg-orange-100` | `text-gray-900` | 突出显示 |
| 单位标识 | 无 | `text-gray-600` | 辅助信息 |

### 字体和格式

- **数值字体**：粗体（`font-bold`）
- **数字格式**：千分位分隔符（`toLocaleString()`）
- **对齐方式**：居中对齐（`text-center`）

---

## ✅ 验证步骤

### 1. 准备测试数据

确保数据库中有一条完整的分解表记录：

```javascript
{
  _id: "xxx",
  name: "2026年销售目标分解--收入渠道",
  unit: "万元",
  showRowTotal: true,
  showColumnTotal: true,
  horizontalDimension: {
    dimensionName: "时间",
    values: ["Q1", "Q2", "Q3", "Q4"]
  },
  verticalDimension: {
    dimensionName: "收入渠道",
    values: ["工装行动", "自拓", "区域协同"]
  },
  isEnabled: true
}
```

### 2. 访问页面

1. 登录系统
2. 进入"目标管理"
3. 点击"目标分解"Tab

### 3. 验证功能

#### ✅ 单位显示
- 查看表格标题
- 确认显示"（单位：万元）"

#### ✅ 行汇总
- 查看表格最右侧
- 确认有"合计"列
- 填入测试数据（如：100, 200, 300）
- 确认合计显示正确（600）

#### ✅ 列总计
- 查看表格最底部
- 确认有"总计"行
- 填入测试数据
- 确认每列总计正确

#### ✅ 总计的总计
- 查看右下角单元格
- 确认背景色为橙色
- 确认数值为所有单元格之和

#### ✅ 数字格式化
- 填入大数字（如：100000）
- 确认显示为 "100,000"

---

## 🐛 边界情况处理

### 1. 空值处理
- 空单元格不参与计算
- 只累加有效数值

### 2. 非数字处理
```typescript
const numValue = parseFloat(value);
if (!isNaN(numValue)) {
  total += numValue;
}
```

### 3. 可选参数
- `unit`、`showRowTotal`、`showColumnTotal` 都是可选的
- 不启用时不显示对应功能

### 4. 实时更新
- 编辑单元格时，汇总和总计**实时计算**
- 无需点击保存即可查看计算结果

---

## 📝 数据库影响

### 现有数据兼容性

✅ **完全兼容** - 因为新增字段都是可选的：

```typescript
unit?: string;
showRowTotal?: boolean;
showColumnTotal?: boolean;
```

如果旧数据没有这些字段：
- `unit` 不显示 → 表格标题不带单位
- `showRowTotal` 为 `undefined` → 不显示行汇总列
- `showColumnTotal` 为 `undefined` → 不显示列总计行

### 新建表格

创建新分解表时，可以在 `CreateDecompositionTableModal` 中设置这些参数。

---

## 📂 修改文件

| 文件 | 修改内容 |
|------|---------|
| `components/GoalDecompositionMultiTable.tsx` | 新增单位显示、行汇总、列总计功能 |

---

## 🚀 部署状态

✅ **构建完成**

```bash
npx vite build
```

构建产物位于：`dist/` 目录

---

## 📌 后续优化建议

1. **格式化选项**
   - 支持小数位数设置
   - 支持货币符号（¥、$、€）

2. **条件样式**
   - 负数显示为红色
   - 超过目标值显示为绿色

3. **导出功能**
   - 导出时保留汇总和总计
   - Excel 格式包含计算公式

4. **百分比模式**
   - 显示占比（如：某项 / 总计）

---

## ✅ 功能完成

- ✅ 单位显示
- ✅ 行汇总计算和显示
- ✅ 列总计计算和显示
- ✅ 总计的总计
- ✅ 数字千分位格式化
- ✅ 实时计算更新
- ✅ 边界情况处理
- ✅ 样式优化

---

## 🎯 测试建议

### 测试用例 1：基本功能
1. 创建包含单位的分解表
2. 启用行汇总和列总计
3. 填入测试数据
4. 验证计算结果

### 测试用例 2：部分启用
1. 只启用行汇总
2. 填入数据
3. 确认只显示合计列

### 测试用例 3：边界情况
1. 填入空值
2. 填入非数字
3. 填入负数
4. 验证计算正确性

---

**开发完成时间**：2026-01-06  
**版本**：v1.5.0  
**开发者**：AI Assistant
