# 际华协同办公平台 - 蓝绿部署方案

## 📋 部署架构

```
┌─────────────────────────────────────────────────────────┐
│                    测试环境部署架构                       │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  公网访问 (HTTPS 3443)                                   │
│         ↓                                                │
│  ┌─────────────────┐                                    │
│  │  Nginx 反向代理  │                                    │
│  │  (SSL 终止)      │                                    │
│  └────────┬─────────┘                                    │
│           │                                              │
│           ├──→ 蓝环境 (Docker, 端口 3000) ←── 当前流量  │
│           │    └─ jihua-prod:latest                     │
│           │                                              │
│           └──→ 绿环境 (Docker, 端口 3001) ←── 升级时临时 │
│                └─ jihua-prod:latest (新版本)            │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

## 🎯 核心特性

- ✅ **零停机部署**: 用户无感知升级
- ✅ **快速回滚**: 秒级切换到旧版本
- ✅ **健康检查**: 自动验证新版本
- ✅ **统一入口**: HTTPS 3443 端口访问
- ✅ **自动化脚本**: 一键升级

## 🚀 快速开始

### 首次部署（已完成）✅

```bash
# 1. 清理现有部署
docker stop jihua-app jihua-oa jihua-cowork 2>/dev/null || true
docker rm jihua-app jihua-oa jihua-cowork 2>/dev/null || true

# 2. 上传项目文件（Lighthouse MCP Tool）
deploy_project_preparation({
  FolderPath: "d:/project/cowork12-21/dist",
  InstanceId: "lhins-pnt984h2",
  Region: "ap-beijing",
  ProjectName: "jihua-prod"
})

# 3. 执行初始部署（已完成）
✅ 蓝环境已运行在端口 3000
✅ Nginx 已配置反向代理到 3443 端口
✅ SSL 证书已配置
```

### 日常升级流程

#### 方式一：使用自动化脚本（推荐）⭐

```bash
# 步骤1: 本地构建新版本
npx vite build

# 步骤2: 上传新版本到服务器（Lighthouse MCP Tool）
deploy_project_preparation({
  FolderPath: "d:/project/cowork12-21/dist",
  InstanceId: "lhins-pnt984h2",
  Region: "ap-beijing",
  ProjectName: "jihua-prod"
})

# 步骤3: 执行自动化升级脚本
ssh root@152.136.183.181
/root/blue-green-upgrade.sh
```

#### 方式二：手动执行（调试用）

```bash
# 1. 查找最新项目路径
PROJECT_PATH=$(ls -td /root/dist_* | head -1)
echo "使用路径: $PROJECT_PATH"

# 2. 构建镜像
cd $PROJECT_PATH
cat > Dockerfile << EOF
FROM nginx:alpine
COPY . /usr/share/nginx/html
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
EOF
docker build -t jihua-prod:latest .

# 3. 启动绿环境
docker run -d --name jihua-prod-green -p 3001:80 jihua-prod:latest

# 4. 健康检查
sleep 3
curl -s http://127.0.0.1:3001/

# 5. 切换流量
sed -i 's/server 127.0.0.1:3000;/server 127.0.0.1:3001;/' /etc/nginx/conf.d/jihua-prod.conf
nginx -s reload

# 6. 清理蓝环境
docker stop jihua-prod-blue && docker rm jihua-prod-blue

# 7. 重命名绿环境为蓝环境
docker stop jihua-prod-green && docker rm jihua-prod-green
docker run -d --name jihua-prod-blue -p 3000:80 jihua-prod:latest

# 8. 恢复配置
sed -i 's/server 127.0.0.1:3001;/server 127.0.0.1:3000;/' /etc/nginx/conf.d/jihua-prod.conf
nginx -s reload
```

## 🔄 回滚方案

### 快速回滚（如果升级失败）

```bash
# 自动化脚本会在健康检查失败时自动回滚
# 手动回滚命令：
docker stop jihua-prod-green && docker rm jihua-prod-green
# 蓝环境继续运行，用户不受影响
```

### 回滚到上一个版本

```bash
# 1. 使用上一个版本的镜像
docker tag jihua-prod:latest jihua-prod:backup
docker tag jihua-prod:previous jihua-prod:latest

# 2. 重启蓝环境
docker stop jihua-prod-blue && docker rm jihua-prod-blue
docker run -d --name jihua-prod-blue -p 3000:80 jihua-prod:latest
```

## 📊 监控和验证

### 验证部署状态

```bash
# 容器状态
docker ps | grep jihua-prod

# Nginx 状态
systemctl status nginx

# 端口监听
netstat -tlnp | grep :3443

# 访问测试
curl -k -I https://127.0.0.1:3443/
```

### 查看日志

```bash
# 容器日志
docker logs jihua-prod-blue

# Nginx 日志
tail -f /var/log/nginx/access.log
tail -f /var/log/nginx/error.log
```

## 🔧 配置文件

### Nginx 反向代理配置

位置: `/etc/nginx/conf.d/jihua-prod.conf`

```nginx
upstream jihua_backend {
    server 127.0.0.1:3000;  # 蓝环境
}

server {
    listen 3443 ssl http2;
    server_name 152.136.183.181;

    # SSL 证书
    ssl_certificate /etc/nginx/ssl/ip-selfsigned.crt;
    ssl_certificate_key /etc/nginx/ssl/ip-selfsigned.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    # 反向代理
    location / {
        proxy_pass http://jihua_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_http_version 1.1;
    }

    # 防缓存配置
    location ~* \.(js|css|html)$ {
        proxy_pass http://jihua_backend;
        expires -1;
        add_header Cache-Control "no-store, no-cache";
    }
}
```

### 升级脚本

位置: `/root/blue-green-upgrade.sh`

```bash
#!/bin/bash
set -e
PROJECT_PATH=$(ls -td /root/dist_* | head -1)
BLUE_PORT=3000
GREEN_PORT=3001

# 构建 → 启动绿环境 → 健康检查 → 切换流量 → 清理蓝环境 → 重命名
# 详细逻辑见脚本文件
```

## 🎯 访问地址

- **测试环境**: https://152.136.183.181:3443
- **蓝环境直连**: http://152.136.183.181:3000
- **绿环境直连** (升级时): http://152.136.183.181:3001

## 📝 注意事项

1. **SSL 证书**: 当前使用自签名证书，浏览器会提示不安全（正常）
2. **防火墙**: 确保 3443 端口已开放
3. **健康检查**: 自动化脚本会等待 3 秒后进行 5 次重试
4. **清理**: 定期清理未使用的 Docker 镜像（自动化脚本已包含）

## 🔐 安全加固

### 推荐使用正式 SSL 证书

```bash
# 使用 Let's Encrypt 免费证书
certbot --nginx -d yourdomain.com

# 或使用腾讯云申请的证书
ssl_certificate /etc/nginx/ssl/cert.pem;
ssl_certificate_key /etc/nginx/ssl/key.pem;
```

## 📈 性能优化

### Nginx 配置优化

```nginx
# 启用 Gzip 压缩
gzip on;
gzip_types text/plain text/css application/json application/javascript;

# 静态资源缓存（根据实际情况调整）
location ~* \.(jpg|jpeg|png|gif|ico|css|js|woff2)$ {
    expires 7d;
    add_header Cache-Control "public, immutable";
}
```

## 🆘 故障排查

### 常见问题

**问题1: 无法访问 3443 端口**
```bash
# 检查防火墙
firewall-cmd --list-ports
firewall-cmd --permanent --add-port=3443/tcp
firewall-cmd --reload
```

**问题2: 健康检查失败**
```bash
# 检查容器日志
docker logs jihua-prod-green

# 手动测试
curl -v http://127.0.0.1:3001/
```

**问题3: Nginx 配置错误**
```bash
# 测试配置
nginx -t

# 查看错误日志
tail -f /var/log/nginx/error.log
```

## 📚 相关文档

- [Lighthouse MCP 工具使用](./部署方案总览.md)
- [生产环境部署](./部署方案-生产环境.md)
- [防缓存部署](./防缓存部署-快速参考.md)

---

**版本**: v1.0  
**最后更新**: 2026-01-08  
**维护者**: 际华协同办公平台开发团队
