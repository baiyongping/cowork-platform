# 际华协同办公平台 - 双环境部署方案

## 📊 环境架构

```
┌─────────────────────────────────────────────────────────┐
│                  本地开发环境                            │
│              http://localhost:5173                      │
│            (使用 .env.development)                      │
└────────────────────┬────────────────────────────────────┘
                     │
                     ↓ 连接
┌─────────────────────────────────────────────────────────┐
│              测试 CloudBase 环境                         │
│        jihua-oa-dev-3goht9irae4d949f                    │
│         (测试数据库、云函数、云存储)                     │
└────────────────────┬────────────────────────────────────┘
                     ↑
                     │ 连接
┌─────────────────────────────────────────────────────────┐
│                  测试服务器                              │
│         https://152.136.183.181:3443                    │
│             (使用 .env.test)                            │
│           目录: /var/www/jihua-dev/                     │
└─────────────────────────────────────────────────────────┘

                  验证通过后部署 ↓

┌─────────────────────────────────────────────────────────┐
│              生产 CloudBase 环境                         │
│           cowork-9gg9oocb516be5fb                       │
│         (生产数据库、云函数、云存储)                     │
└────────────────────┬────────────────────────────────────┘
                     ↑
                     │ 连接
┌─────────────────────────────────────────────────────────┐
│                  生产服务器                              │
│             https://jihuadz.xin                         │
│          (使用 .env.production)                         │
│            目录: /var/www/jihua/                        │
└─────────────────────────────────────────────────────────┘
```

## 🎯 方案优势

### ✅ 简化的环境配置
- **测试环境**：本地开发 + 测试服务器 → 共用同一个测试数据库
- **生产环境**：生产服务器 → 独立的生产数据库
- **无需频繁切换**：开发和测试都用测试库，只有生产使用生产库

### ✅ 清晰的数据隔离
- **测试数据库**：`jihua-oa-dev-3goht9irae4d949f`
  - 可以随意测试新功能
  - 可以模拟各种场景
  - 数据损坏不影响生产
- **生产数据库**：`cowork-9gg9oocb516be5fb`
  - 只存放真实业务数据
  - 严格控制访问权限
  - 完全独立不受测试影响

### ✅ 安全的部署流程
```
本地开发 → 本地测试 → 部署测试服务器 → 验证通过 → 部署生产服务器
```

## 📋 环境配置文件

### 1. `.env.development` (本地开发)
```env
VITE_CLOUDBASE_ENV_ID=jihua-oa-dev-3goht9irae4d949f
VITE_PORT=5173
VITE_API_BASE_URL=http://localhost:5173
VITE_LOG_LEVEL=debug
```

### 2. `.env.test` (测试服务器)
```env
VITE_CLOUDBASE_ENV_ID=jihua-oa-dev-3goht9irae4d949f
VITE_API_BASE_URL=https://152.136.183.181:3443
VITE_LOG_LEVEL=info
```

### 3. `.env.production` (生产服务器)
```env
VITE_CLOUDBASE_ENV_ID=cowork-9gg9oocb516be5fb
VITE_API_BASE_URL=https://jihuadz.xin
VITE_LOG_LEVEL=error
```

## 🚀 工作流程

### 日常开发流程

#### 1️⃣ 本地开发
```bash
# 启动本地开发服务器（自动使用测试数据库）
npm run dev

# 访问 http://localhost:5173
# 使用测试数据库进行开发和调试
```

#### 2️⃣ 部署到测试环境

**✨ 推荐方式：使用 Lighthouse Integration 部署**

**优势**：
- ✅ **自动化部署**：无需手动SSH登录和文件上传
- ✅ **权限管理**：自动设置正确的文件权限（nginx:nginx, 755）
- ✅ **防火墙配置**：自动开放所需端口
- ✅ **集成验证**：自动验证部署结果
- ✅ **错误处理**：智能错误检测和修复建议

**部署流程**：
1. 构建测试版本：`npx vite build --mode test`
2. 告诉 AI："请帮我在lighthouse上部署这个项目"
3. AI 自动完成：
   - 查询可用的 Lighthouse 实例
   - 上传文件到服务器 `/var/www/jihua-dev/`
   - 设置文件权限 `nginx:nginx` 和 `chmod 755`
   - 验证部署成功
4. 访问 http://152.136.183.181:3000 验证功能

**备选方式：使用传统脚本部署**
```bash
# 方法1: 使用统一管理工具
环境管理.bat
# 选择: 2. Build for Test Server
# 选择: 4. Deploy to Test Server

# 方法2: 使用专用脚本
部署到开发环境.bat

# 访问 http://152.136.183.181:3000 验证功能
```

#### 3️⃣ 部署到生产环境
```bash
# 确保测试环境验证通过后再部署生产！

# 方法1: 使用统一管理工具
环境管理.bat
# 选择: 3. Build for Production Server
# 选择: 5. Deploy to Production Server
# 输入 YES 确认

# 方法2: 使用专用脚本
部署到生产环境.bat
# 输入 YES 确认

# 访问 https://jihuadz.xin 验证生产环境
```

## 🛠️ 统一管理工具

**运行 `环境管理.bat` 可以：**

1. **启动本地开发** - 连接测试数据库
2. **构建测试版本** - 使用测试环境配置
3. **构建生产版本** - 使用生产环境配置
4. **部署测试服务器** - 一键部署到测试环境
5. **部署生产服务器** - 安全部署到生产环境（需要二次确认）
6. **打开测试数据库控制台** - 快速访问测试库
7. **打开生产数据库控制台** - 快速访问生产库

## 📊 数据库对比

| 特性 | 测试数据库 (jihua-oa-dev) | 生产数据库 (cowork) |
|-----|--------------------------|-------------------|
| **环境ID** | jihua-oa-dev-3goht9irae4d949f | cowork-9gg9oocb516be5fb |
| **使用场景** | 本地开发 + 测试服务器 | 生产服务器 |
| **数据类型** | 测试数据 | 真实业务数据 |
| **修改频率** | 高（随时可修改） | 低（严格控制） |
| **数据安全** | 可以随意测试 | 严格保护 |
| **访问权限** | 开发人员 | 限制访问 |

## 🔗 控制台链接

### 测试环境
- **数据库管理**：https://tcb.cloud.tencent.com/dev?envId=jihua-oa-dev-3goht9irae4d949f#/db/doc
- **云函数**：https://tcb.cloud.tencent.com/dev?envId=jihua-oa-dev-3goht9irae4d949f#/scf
- **云存储**：https://tcb.cloud.tencent.com/dev?envId=jihua-oa-dev-3goht9irae4d949f#/storage

### 生产环境
- **数据库管理**：https://tcb.cloud.tencent.com/dev?envId=cowork-9gg9oocb516be5fb#/db/doc
- **云函数**：https://tcb.cloud.tencent.com/dev?envId=cowork-9gg9oocb516be5fb#/scf
- **云存储**：https://tcb.cloud.tencent.com/dev?envId=cowork-9gg9oocb516be5fb#/storage

## ⚠️ 注意事项

### 开发阶段
- ✅ 本地开发直接使用测试数据库，无需切换
- ✅ 可以在测试库中自由测试各种场景
- ✅ 代码修改后本地测试，然后部署到测试服务器验证

### 测试阶段
- ✅ 测试服务器使用测试数据库
- ✅ 完整验证所有功能
- ✅ 测试各种边界情况和异常场景

### 生产部署
- ⚠️ **必须在测试环境充分验证后才能部署生产**
- ⚠️ **生产部署需要二次确认（输入 YES）**
- ⚠️ **部署后立即验证关键功能**
- ⚠️ **出现问题立即回滚到上一版本**

### 数据库操作
- ⚠️ **生产数据库严禁直接手动修改**
- ⚠️ **所有数据库变更必须通过代码和云函数**
- ⚠️ **重要操作前做好数据备份**

## 📝 服务器目录结构

```
腾讯云轻量应用服务器 (152.136.183.181)
├── /var/www/jihua-dev/        # 测试环境
│   ├── index.html
│   ├── assets/
│   └── ...
│
└── /var/www/jihua/            # 生产环境
    ├── index.html
    ├── assets/
    └── ...
```

## 🎯 快速命令参考

### 本地开发
```bash
npm run dev
```

### 构建
```bash
# 构建测试版本
npx vite build --mode test

# 构建生产版本
npx vite build --mode production
```

### 部署方式

#### ⭐ 推荐：使用 Lighthouse Integration（自动化）
```bash
# 1. 构建
npx vite build --mode test

# 2. 告诉 AI 部署
"请帮我在lighthouse上部署这个项目"

# AI 会自动：
# - 查询 Lighthouse 实例
# - 上传文件到 /var/www/jihua-dev/
# - 设置权限 nginx:nginx 755
# - 验证部署结果
```

#### 备选：使用传统方式（手动）
```bash
# 部署测试环境
scp -r dist/* root@152.136.183.181:/var/www/jihua-dev/
ssh root@152.136.183.181 "chown -R nginx:nginx /var/www/jihua-dev && chmod -R 755 /var/www/jihua-dev"

# 部署生产环境
scp -r dist/* root@152.136.183.181:/var/www/jihua/
ssh root@152.136.183.181 "chown -R nginx:nginx /var/www/jihua && chmod -R 755 /var/www/jihua"

# 使用统一管理工具（推荐）
环境管理.bat
```

## 💡 最佳实践

1. **开发新功能**
   - 在本地开发环境编码
   - 使用测试数据库进行测试
   - 确保功能完整后提交代码

2. **测试验证**
   - **✨ 推荐使用 Lighthouse Integration 部署到测试服务器**
     - 告诉 AI："请帮我在lighthouse上部署这个项目"
     - AI 自动完成所有部署步骤
   - 在测试环境进行完整测试
   - 邀请相关人员验收

3. **生产发布**
   - 测试环境验证通过
   - 选择合适的发布时间窗口
   - **建议使用传统方式部署生产环境**（需要明确确认）
   - 立即验证关键功能
   - 监控系统运行状态

4. **数据管理**
   - 定期备份生产数据库
   - 测试数据定期清理
   - 重要数据变更记录日志

## 🔧 Lighthouse Integration 使用指南

### 部署流程详解

#### 第一步：查询实例
AI 会自动调用 `analyze_lighthouse_instances` 获取可用的服务器实例：
```json
{
  "ap-beijing": 1,  // 北京地域有1个实例
  "总实例数": 1
}
```

#### 第二步：获取实例详情
AI 自动调用 `describe_running_instances` 获取运行中的实例：
```json
{
  "InstanceId": "lhins-pnt984h2",
  "InstanceName": "OpenCloudOS8-Docker26-Tbot",
  "PublicAddresses": ["152.136.183.181"],
  "Region": "ap-beijing"
}
```

#### 第三步：部署准备
AI 调用 `deploy_project_preparation` 上传文件：
```json
{
  "FolderPath": "e:/cowork/dist",
  "InstanceId": "lhins-pnt984h2",
  "Region": "ap-beijing",
  "ProjectName": "jihua-dev"
}
```

#### 第四步：配置和验证
AI 自动执行：
1. 创建目标目录：`mkdir -p /var/www/jihua-dev`
2. 复制文件：`cp -r /root/dist_*/* /var/www/jihua-dev/`
3. 设置权限：`chown -R nginx:nginx /var/www/jihua-dev`
4. 设置文件权限：`chmod -R 755 /var/www/jihua-dev`
5. 验证部署：`ls -lh /var/www/jihua-dev`

### 优势对比

| 特性 | Lighthouse Integration | 传统方式 |
|------|----------------------|---------|
| **自动化程度** | ✅ 完全自动化 | ❌ 需要手动执行多个命令 |
| **权限设置** | ✅ 自动设置 nginx:nginx 755 | ⚠️ 需要手动设置 |
| **错误处理** | ✅ 智能检测和修复建议 | ❌ 需要手动排查 |
| **部署验证** | ✅ 自动验证文件和权限 | ❌ 需要手动检查 |
| **学习成本** | ✅ 低（自然语言交互） | ⚠️ 中（需要熟悉SSH命令） |
| **安全性** | ✅ 无需暴露密码 | ⚠️ 需要SSH密钥或密码 |

### 使用建议

**适合使用 Lighthouse Integration 的场景**：
- ✅ 日常开发和测试部署
- ✅ 快速迭代验证
- ✅ 团队协作部署
- ✅ 需要自动化部署流程

**适合使用传统方式的场景**：
- ✅ 生产环境部署（需要明确确认）
- ✅ 紧急修复（需要快速手动干预）
- ✅ 特殊操作（如重启服务、修改配置）

## 🔐 安全建议

1. **访问控制**
   - 生产环境限制访问权限
   - 使用强密码和SSH密钥
   - 定期更新访问凭证

2. **数据保护**
   - 定期备份生产数据
   - 敏感数据加密存储
   - 限制数据库直接访问

3. **监控告警**
   - 监控服务器资源使用
   - 设置异常行为告警
   - 记录重要操作日志

---

**创建时间**：2025-12-15  
**最后更新**：2025-12-15
