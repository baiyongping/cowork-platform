# 际华协同办公平台 - 防缓存部署快速参考

## 🚀 快速命令

### 本地构建 (Windows)
```powershell
.\build-clean.ps1
```

### 本地构建 (Linux/Mac)
```bash
./build-clean.sh
```

### 服务器部署
```bash
# 完整部署 (自动清理+验证+构建)
./blue-green-deploy.sh deploy

# 分步执行 (可选)
./blue-green-deploy.sh clean      # 1. 清理旧文件
./blue-green-deploy.sh validate   # 2. 验证上传
./blue-green-deploy.sh deploy     # 3. 执行部署

# 查看状态
./blue-green-deploy.sh status

# 回滚 (如需要)
./blue-green-deploy.sh rollback blue
```

---

## ✅ 核心检查点

### 构建前
- [ ] 关闭开发服务器 (`npm run dev`)
- [ ] 运行清理脚本 (`./build-clean.ps1`)
- [ ] 验证 dist/ 完全删除
- [ ] 检查构建成功
- [ ] 确认 dist/.build-version 存在

### 上传后
- [ ] 验证文件完整性
- [ ] 检查 dist/ 大小正常 (2-5MB)
- [ ] 统计文件数量 (10+)

### 部署后
- [ ] **硬刷新**: `Ctrl + Shift + R` (Windows/Linux) 或 `Cmd + Shift + R` (Mac)
- [ ] 或使用隐私/无痕模式
- [ ] 打开开发者工具 → Network → 勾选 "Disable cache"
- [ ] 验证新版本生效

---

## ⚠️ 常见问题

### Q1: dist未清理干净?
```powershell
# 手动强制删除
Remove-Item -Path "dist" -Recurse -Force
Remove-Item -Path "node_modules\.vite" -Recurse -Force
```

### Q2: 浏览器仍显示旧版本?
```
1. Ctrl + Shift + R (硬刷新)
2. 清空缓存并硬性重新加载
3. 使用隐私/无痕模式访问
4. 等待CDN缓存更新 (3-5分钟)
```

### Q3: Docker缓存问题?
```bash
# 服务器端执行
./blue-green-deploy.sh clean
docker builder prune -f
```

---

## 📊 防缓存机制对照表

| 机制 | 实施位置 | 效果 |
|------|---------|------|
| **build-clean脚本** | 本地 | ⭐⭐⭐⭐⭐ |
| **emptyOutDir: true** | 本地 | ⭐⭐⭐⭐⭐ |
| **文件名hash+时间戳** | 全局 | ⭐⭐⭐⭐⭐ |
| **Docker --no-cache** | 服务器 | ⭐⭐⭐⭐⭐ |
| **Nginx Cache-Control** | 全局 | ⭐⭐⭐⭐ |
| **浏览器硬刷新** | 客户端 | ⭐⭐⭐ |

---

## 🎯 完整流程 (30秒版)

```bash
# 本地 (Windows)
.\build-clean.ps1

# 上传到服务器 (Lighthouse MCP工具)
deploy_project_preparation(...)

# 服务器
cd /var/www/jihua-deploy
mv /root/jihua-prod-dist_* ./dist
./blue-green-deploy.sh deploy

# 验证 (浏览器)
https://jihuadz.xin
Ctrl + Shift + R
```

---

## 📞 支持

遇到问题请检查:
1. 部署日志输出
2. Docker容器日志: `docker logs jihua-prod-green`
3. Nginx错误日志: `tail -f /var/log/nginx/error.log`
4. 浏览器开发者工具 Console

---

**版本**: v2.0 防缓存增强版  
**更新**: 2025-12-31
