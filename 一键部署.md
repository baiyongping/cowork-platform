# 一键部署指南

## 🚀 快速部署流程

### 方式一：完整蓝绿部署（推荐）

**步骤 1：本地构建**
```bash
npm run build
```

**步骤 2：在 AI 对话中执行部署**
```
请使用蓝绿部署方式部署到开发环境
```

AI 会自动完成：
1. ✓ 上传文件到服务器
2. ✓ 构建 Docker 镜像
3. ✓ 启动新容器（自动选择端口）
4. ✓ 健康检查
5. ✓ 更新 Nginx 配置
6. ✓ 停止旧容器

### 方式二：服务器端快速脚本

**在服务器上创建快速部署脚本：**
```bash
cat > /root/quick-deploy.sh << 'EOF'
#!/bin/bash
# 快速蓝绿部署脚本

set -e

echo "========================================"
echo "际华协同办公平台 - 快速部署"
echo "========================================"

# 检查参数
if [ -z "$1" ]; then
    echo "用法: $0 <dist目录路径>"
    echo "示例: $0 /root/dist_20241230130805"
    exit 1
fi

DIST_DIR=$1
cd $DIST_DIR

# 准备目录结构
echo "[1/6] 准备文件..."
if [ ! -d "dist" ]; then
    mkdir -p dist
    mv assets index.html logo.png dist/ 2>/dev/null || true
fi

# 检查 Dockerfile 和 nginx.conf
if [ ! -f "Dockerfile" ]; then
    cat > Dockerfile << 'DOCKERFILE'
FROM nginx:alpine
COPY dist/ /usr/share/nginx/html/
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
DOCKERFILE
fi

if [ ! -f "nginx.conf" ]; then
    cat > nginx.conf << 'NGINXCONF'
server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;
    location / { try_files $uri $uri/ /index.html; }
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
NGINXCONF
fi

# 构建镜像
echo "[2/6] 构建 Docker 镜像..."
VERSION=$(date +%Y%m%d-%H%M%S)
docker build -t jihua-dev:$VERSION -t jihua-dev:latest .

# 决定端口
echo "[3/6] 检查当前容器..."
CURRENT=$(docker ps --filter 'name=jihua-dev' --format '{{.Names}}:{{.Ports}}' | head -1)
if echo "$CURRENT" | grep -q '3001'; then
    NEW_PORT=3002
    NEW_NAME="jihua-dev-green"
    OLD_NAME="jihua-dev-blue"
else
    NEW_PORT=3001
    NEW_NAME="jihua-dev-blue"
    OLD_NAME="jihua-dev-green"
fi

echo "新容器: $NEW_NAME (端口 $NEW_PORT)"

# 启动新容器
echo "[4/6] 启动新容器..."
docker stop $NEW_NAME 2>/dev/null || true
docker rm $NEW_NAME 2>/dev/null || true
docker run -d --name $NEW_NAME -p $NEW_PORT:80 --restart unless-stopped jihua-dev:latest

# 健康检查
echo "[5/6] 健康检查..."
sleep 3
for i in {1..5}; do
    if curl -sf http://localhost:$NEW_PORT > /dev/null; then
        echo "✓ 容器响应正常"
        break
    fi
    if [ $i -eq 5 ]; then
        echo "✗ 健康检查失败"
        docker logs $NEW_NAME --tail 20
        exit 1
    fi
    sleep 2
done

# 更新 Nginx
echo "[6/6] 更新 Nginx 配置..."
cp /etc/nginx/conf.d/jihua-dev.conf /etc/nginx/conf.d/jihua-dev.conf.backup.$(date +%Y%m%d-%H%M%S)
sed -i "s/proxy_pass http:\/\/localhost:[0-9]*;/proxy_pass http:\/\/localhost:$NEW_PORT;/g" \
    /etc/nginx/conf.d/jihua-dev.conf
nginx -t && nginx -s reload

# 停止旧容器
if [ -n "$OLD_NAME" ]; then
    docker stop $OLD_NAME 2>/dev/null || true
fi

echo ""
echo "========================================"
echo "✓ 部署完成！"
echo "========================================"
echo "新容器: $NEW_NAME"
echo "端口: $NEW_PORT"
echo "访问: https://152.136.183.181:3443"
echo ""
echo "回滚命令: docker start $OLD_NAME && sed -i 's/:$NEW_PORT/:$(docker port $OLD_NAME | grep -o '300[0-9]' | head -1)/g' /etc/nginx/conf.d/jihua-dev.conf && nginx -s reload"
EOF

chmod +x /root/quick-deploy.sh
```

**使用快速脚本：**
```bash
/root/quick-deploy.sh /root/dist_XXXXXX
```

## 🔄 一键回滚

**方式一：使用 AI**
```
请回滚到上一个版本
```

**方式二：服务器端命令**
```bash
# 找到停止的容器
OLD=$(docker ps -a --filter 'name=jihua-dev' --filter 'status=exited' --format '{{.Names}}' | head -1)

# 获取旧端口
OLD_PORT=$(docker inspect $OLD | grep -o '300[0-9]' | head -1)

# 启动旧容器
docker start $OLD

# 更新 Nginx
sed -i "s/proxy_pass http:\/\/localhost:[0-9]*;/proxy_pass http:\/\/localhost:$OLD_PORT;/g" \
    /etc/nginx/conf.d/jihua-dev.conf

# 重载 Nginx
nginx -s reload

echo "✓ 回滚完成！访问 https://152.136.183.181:3443"
```

## 📊 状态检查

```bash
# 查看运行中的容器
docker ps --filter 'name=jihua-dev'

# 查看 Nginx 当前配置
grep proxy_pass /etc/nginx/conf.d/jihua-dev.conf

# 测试容器响应
curl -I http://localhost:3001
curl -I http://localhost:3002

# 测试外部访问
curl -Ik https://152.136.183.181:3443
```

## 🔧 常用命令

### 查看日志
```bash
# 容器日志
docker logs jihua-dev-blue -f --tail 100
docker logs jihua-dev-green -f --tail 100

# Nginx 日志
tail -f /var/log/nginx/access.log
tail -f /var/log/nginx/error.log
```

### 清理资源
```bash
# 清理停止的容器
docker container prune -f

# 清理未使用的镜像
docker image prune -a -f

# 清理所有未使用的资源
docker system prune -a -f
```

### 备份和恢复
```bash
# 导出容器为镜像
docker commit jihua-dev-blue jihua-dev:backup-$(date +%Y%m%d)

# 保存镜像到文件
docker save jihua-dev:latest > jihua-dev-backup.tar

# 从文件恢复镜像
docker load < jihua-dev-backup.tar
```

## 📝 部署检查清单

部署前：
- [ ] 本地构建成功 (`npm run build`)
- [ ] 检查 CloudBase 环境 ID
- [ ] 确认目标服务器和端口

部署后：
- [ ] 新容器启动成功
- [ ] 健康检查通过
- [ ] Nginx 配置更新
- [ ] 外部访问测试
- [ ] 功能验证

回滚准备：
- [ ] 旧容器保留
- [ ] 知道回滚命令
- [ ] 备份配置文件

## ⚠️ 注意事项

1. **环境确认**
   - 开发环境：jihua-oa-dev-3goht9irae4d949f
   - 生产环境：需要明确确认

2. **部署时机**
   - 避免在高峰期部署
   - 提前通知团队

3. **监控**
   - 部署后持续观察 5-10 分钟
   - 检查错误日志

4. **备份**
   - 重要数据提前备份
   - 保留至少 2 个版本的容器

---

**文档版本**: v1.0  
**最后更新**: 2024-12-30
