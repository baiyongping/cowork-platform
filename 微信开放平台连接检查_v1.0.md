# 微信开放平台连接检查报告 v1.0

## 📋 修正内容

### 1. **云函数修正** (`cloudfunctions/wechat-oauth/index.js`)

#### ❌ **修正前（错误）**
```javascript
// 使用微信公众号配置
const WECHAT_CONFIG = {
  appid: process.env.WECHAT_APPID,      // 公众号 AppID
  secret: process.env.WECHAT_SECRET     // 公众号 AppSecret
};

// 使用公众号授权端点
const authUrl = `https://open.weixin.qq.com/connect/oauth2/authorize?...&scope=snsapi_base`;
```

#### ✅ **修正后（正确）**
```javascript
// 使用微信开放平台配置
const WECHAT_CONFIG = {
  appid: process.env.WECHAT_OPEN_APPID,    // 开放平台 AppID
  secret: process.env.WECHAT_OPEN_SECRET   // 开放平台 AppSecret
};

// 使用开放平台授权端点
const authUrl = `https://open.weixin.qq.com/connect/qrconnect?...&scope=snsapi_login`;
```

---

### 2. **前端调用修正** (`components/wechat/WechatBind.tsx`)

#### ❌ **修正前（错误）**
```typescript
const result = await app.callFunction({
  name: 'wechat-oauth',
  data: {
    action: 'getAuthUrl',
    redirectUri: '...',
    scope: 'snsapi_base'  // ❌ 公众号网页授权
  }
});
```

#### ✅ **修正后（正确）**
```typescript
const result = await app.callFunction({
  name: 'wechat-oauth',
  data: {
    action: 'getAuthUrl',
    redirectUri: '...',
    scope: 'snsapi_login'  // ✅ 开放平台网站应用
  }
});
```

---

## 🔍 自检清单

### ✅ **1. 配置检查**

| 项目 | 配置位置 | 正确值 | 当前状态 |
|------|----------|--------|----------|
| **AppID** | 云函数环境变量 | `wx79afc92f6fe01a31` | ⚠️ 需确认 |
| **AppSecret** | 云函数环境变量 | `(保密)` | ⚠️ 需确认 |
| **授权端点** | 云函数代码 | `qrconnect` | ✅ 已修正 |
| **Scope** | 前端代码 | `snsapi_login` | ✅ 已修正 |

---

### ✅ **2. 环境变量配置**

**云函数需要配置以下环境变量：**

```bash
# CloudBase 云函数环境变量设置
WECHAT_OPEN_APPID=wx79afc92f6fe01a31
WECHAT_OPEN_SECRET=你的开放平台AppSecret
```

**配置步骤：**
1. 登录 CloudBase 控制台
2. 进入云函数 `wechat-oauth`
3. 配置 → 环境变量 → 添加：
   - 键：`WECHAT_OPEN_APPID`，值：`wx79afc92f6fe01a31`
   - 键：`WECHAT_OPEN_SECRET`，值：`(开放平台AppSecret)`

---

### ✅ **3. 微信开放平台配置检查**

访问：https://open.weixin.qq.com/

#### **3.1 应用基本信息**
- [x] 应用名称：际华协同办公平台
- [x] AppID：`wx79afc92f6fe01a31`
- [x] AppSecret：已配置

#### **3.2 授权回调域名配置**
- [x] 授权回调域名：`jihuadz.xin`
- [x] HTTPS 配置：已启用
- [ ] 备用域名（可选）：`152.136.183.181:3443`

**注意**：回调域名必须是已备案的一级或二级域名，不能带端口号和路径。

---

### ✅ **4. 连接测试流程**

#### **测试步骤**

**Step 1: 部署云函数**
```bash
# 使用 CloudBase MCP 工具部署
updateFunctionCode({
  name: 'wechat-oauth',
  functionRootPath: 'd:/project/cowork12-21/cloudfunctions'
})
```

**Step 2: 测试授权 URL 生成**
```javascript
// 在浏览器控制台测试
const result = await app.callFunction({
  name: 'wechat-oauth',
  data: {
    action: 'getAuthUrl',
    redirectUri: 'https://jihuadz.xin/wechat-bind?scene=test123',
    scope: 'snsapi_login'
  }
});

console.log('授权URL:', result.result.data.authUrl);
```

**Step 3: 验证 URL 格式**

✅ **正确的授权 URL 应该是：**
```
https://open.weixin.qq.com/connect/qrconnect?
  appid=wx79afc92f6fe01a31
  &redirect_uri=https%3A%2F%2Fjihuadz.xin%2Fwechat-bind%3Fscene%3Dtest123
  &response_type=code
  &scope=snsapi_login
  &state=STATE
#wechat_redirect
```

**关键特征**：
- ✅ 使用 `qrconnect` 端点（不是 `oauth2/authorize`）
- ✅ `scope=snsapi_login`（不是 `snsapi_base` 或 `snsapi_userinfo`）
- ✅ AppID 为开放平台 AppID

---

### ✅ **5. 手动测试**

#### **测试 1: 生成授权 URL**

**测试代码**：
```javascript
// 在生产环境 https://jihuadz.xin 的浏览器控制台运行
import { app } from './lib/cloudbase';

const result = await app.callFunction({
  name: 'wechat-oauth',
  data: {
    action: 'getAuthUrl',
    redirectUri: 'https://jihuadz.xin/wechat-bind?scene=manual_test',
    scope: 'snsapi_login'
  }
});

console.log('✅ 授权URL:', result.result.data.authUrl);
```

**预期结果**：
```javascript
{
  success: true,
  data: {
    authUrl: "https://open.weixin.qq.com/connect/qrconnect?appid=wx79afc92f6fe01a31&redirect_uri=..."
  }
}
```

---

#### **测试 2: 访问授权 URL**

**步骤**：
1. 复制生成的 `authUrl`
2. 在浏览器中打开（PC端）
3. 观察页面显示

**预期结果**：
- ✅ 显示微信扫码登录页面
- ✅ 页面中央有二维码
- ✅ 提示"请使用微信扫描二维码登录"

**如果出现错误**：
- ❌ "redirect_uri 参数错误"：检查微信开放平台授权回调域名配置
- ❌ "scope 参数错误"：确认使用的是 `snsapi_login`
- ❌ "appid 参数错误"：确认 AppID 是开放平台的，不是公众号的

---

#### **测试 3: 扫码授权**

**步骤**：
1. 使用微信扫描二维码
2. 在手机上点击"确认登录"
3. 观察浏览器跳转

**预期结果**：
```
浏览器跳转到:
https://jihuadz.xin/wechat-bind?scene=manual_test&code=XXX&state=STATE
```

**关键参数**：
- ✅ `code`：微信返回的授权码（有效期5分钟）
- ✅ `state`：自定义参数（原样返回）

---

#### **测试 4: 换取 Access Token**

**测试代码**：
```javascript
// 使用上一步获取的 code
const code = 'XXX'; // 从 URL 参数中获取

const result = await app.callFunction({
  name: 'wechat-oauth',
  data: {
    action: 'getOpenId',
    code: code
  }
});

console.log('✅ 用户信息:', result.result.data);
```

**预期结果**：
```javascript
{
  success: true,
  data: {
    openid: "o1234567890abcdefghijklmnopqrst",
    access_token: "...",
    expires_in: 7200,
    refresh_token: "...",
    scope: "snsapi_login",
    unionid: "U1234567890abcdefghijklmnopqrst"  // 如果有
  }
}
```

---

## 📊 三种 Scope 对比

| Scope | 适用场景 | 授权端点 | 授权方式 | 能获取的信息 |
|-------|----------|----------|----------|--------------|
| **snsapi_base** | 微信公众号网页 | `oauth2/authorize` | 无需用户同意 | 仅 openid |
| **snsapi_userinfo** | 微信公众号网页 | `oauth2/authorize` | 需用户点击"确认授权" | openid + 用户信息 |
| **snsapi_login** ⭐ | **微信开放平台-网站应用** | **qrconnect** | **PC端扫码授权** | **openid + 用户信息 + unionid** |

---

## 🎯 关键差异总结

### **微信公众号 vs 微信开放平台**

| 特性 | 微信公众号网页授权 | 微信开放平台-网站应用 |
|------|-------------------|----------------------|
| **适用场景** | 微信内置浏览器打开的网页 | 独立Web网站（PC浏览器） |
| **授权方式** | 在微信内跳转授权 | PC端显示二维码，手机扫码 |
| **授权端点** | `oauth2/authorize` | `qrconnect` |
| **Scope** | `snsapi_base` / `snsapi_userinfo` | `snsapi_login` |
| **AppID** | 公众号 AppID | 开放平台 AppID |
| **回调域名配置** | 公众号后台 | 开放平台后台 |
| **我们使用的** | ❌ **不适用** | ✅ **正确** |

---

## ⚠️ 常见错误

### **错误 1: redirect_uri 参数错误**

**原因**：
- 授权回调域名未在微信开放平台配置
- 回调域名配置错误（如带端口号、路径等）

**解决方案**：
1. 登录微信开放平台
2. 进入应用 → 网站应用 → 修改
3. 授权回调域名配置：`jihuadz.xin`（不带 https://，不带路径）

---

### **错误 2: scope 参数错误**

**原因**：
- 使用了公众号的 scope（`snsapi_base` / `snsapi_userinfo`）
- 应该使用开放平台的 scope（`snsapi_login`）

**解决方案**：
- ✅ 已在本次修正中解决

---

### **错误 3: appid 参数错误**

**原因**：
- 使用了公众号 AppID，而不是开放平台 AppID

**解决方案**：
1. 检查云函数环境变量 `WECHAT_OPEN_APPID`
2. 确认值为：`wx79afc92f6fe01a31`

---

## 📚 官方文档

- **微信开放平台-网站应用授权**：https://developers.weixin.qq.com/doc/oplatform/Website_App/WeChat_Login/Wechat_Login.html
- **微信公众号网页授权（对比参考）**：https://developers.weixin.qq.com/doc/offiaccount/OA_Web_Apps/Wechat_webpage_authorization.html

---

## ✅ 部署验证步骤

### **Step 1: 部署云函数**
```bash
# 使用 CloudBase MCP 工具
updateFunctionCode({
  name: 'wechat-oauth',
  functionRootPath: 'd:/project/cowork12-21/cloudfunctions'
})
```

### **Step 2: 配置环境变量**
- CloudBase 控制台 → 云函数 `wechat-oauth` → 配置 → 环境变量
- 添加 `WECHAT_OPEN_APPID` 和 `WECHAT_OPEN_SECRET`

### **Step 3: 测试授权 URL**
- 在生产环境 `https://jihuadz.xin` 运行测试代码
- 检查生成的 URL 是否使用 `qrconnect` 端点

### **Step 4: 扫码测试**
- 访问生成的授权 URL
- 验证是否显示微信扫码页面
- 扫码授权，检查回调是否正确

---

## 📝 总结

### **本次修正内容**
1. ✅ 修正云函数 AppID/AppSecret 环境变量名（`WECHAT_OPEN_APPID`）
2. ✅ 修正授权端点（`qrconnect` 而非 `oauth2/authorize`）
3. ✅ 修正 scope 参数（`snsapi_login` 而非 `snsapi_base`）
4. ✅ 修正文档注释（指向开放平台文档）

### **关键配置**
- **应用类型**：微信开放平台-网站应用
- **AppID**：`wx79afc92f6fe01a31`
- **授权端点**：`https://open.weixin.qq.com/connect/qrconnect`
- **Scope**：`snsapi_login`
- **回调域名**：`jihuadz.xin`

### **下一步**
1. 部署云函数
2. 配置环境变量
3. 在生产环境测试完整流程
4. 如遇问题，参考本文档的"常见错误"章节

---

**版本**: v1.0  
**更新日期**: 2025-01-26  
**维护者**: 际华协同办公平台开发团队
