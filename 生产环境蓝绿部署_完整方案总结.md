# 际华协同办公平台 - 生产环境蓝绿部署完整方案总结

## 📋 方案概述

基于 **Lighthouse 服务器** + **Docker** + **Nginx** 的蓝绿部署架构，实现零停机部署到生产域名 **jihuadz.xin**。

### 核心特性

- ✅ **零停机部署** - 用户无感知升级
- ✅ **快速回滚** - 秒级切换到旧版本
- ✅ **健康检查** - 自动验证新版本
- ✅ **防缓存机制** - 强制无缓存构建
- ✅ **自动化脚本** - 一键完成部署
- ✅ **SSL 加密** - HTTPS 443 端口访问

---

## 📦 文档清单

### 1. 生产环境蓝绿部署方案 v3.12.0 ⭐⭐⭐

**文件**: `生产环境蓝绿部署方案_v3.12.0.md`

**内容**：
- 完整的部署架构说明
- 详细的部署流程
- Nginx 配置详解
- Dockerfile 说明
- 回滚方案
- 监控和验证
- 故障排查指南

**适用场景**: 需要了解完整技术方案时查阅

---

### 2. 生产环境快速部署指南 ⭐⭐⭐

**文件**: `生产环境快速部署指南.md`

**内容**：
- 5分钟快速部署流程
- 常用命令速查
- 故障处理速查
- 部署检查清单
- 时间规划

**适用场景**: 日常部署时的快速参考手册

---

### 3. 生产环境首次配置指南 ⭐⭐

**文件**: `生产环境首次配置指南.md`

**内容**：
- 服务器基础配置
- Docker 安装和配置
- Nginx 安装和配置
- SSL 证书配置
- 首次部署步骤
- 配置完成检查清单

**适用场景**: 首次搭建生产环境时使用（仅需执行一次）

---

### 4. 部署脚本文件

#### 4.1 蓝绿部署脚本（生产环境）

**文件**: `blue-green-deploy-production.sh`

**功能**：
- 清理旧部署文件和缓存
- 验证上传文件完整性
- 强制无缓存构建 Docker 镜像
- 启动新环境容器
- 健康检查（30次重试）
- 切换 Nginx 流量
- 清理旧环境

**命令**：
```bash
./blue-green-deploy-production.sh deploy    # 执行部署
./blue-green-deploy-production.sh status    # 查看状态
./blue-green-deploy-production.sh rollback  # 回滚
```

#### 4.2 Nginx 配置文件（生产环境）

**文件**: `nginx-production.conf`

**功能**：
- 完整的 Nginx 站点配置
- SSL 证书配置
- 安全头配置
- 反向代理配置
- 缓存策略配置
- Gzip 压缩配置

**部署位置**: `/etc/nginx/sites-available/jihuadz.xin`

---

## 🚀 快速开始（日常部署）

### 前置条件

✅ 服务器已完成首次配置（参考：生产环境首次配置指南）  
✅ SSL 证书已绑定  
✅ 域名 jihuadz.xin 已解析  

### 部署步骤（5分钟）

#### 步骤1: 本地构建（1分钟）

```powershell
# 清理旧构建
Remove-Item -Path dist -Recurse -Force -ErrorAction SilentlyContinue

# 生产构建
npm run build

# 验证构建
ls dist\
```

#### 步骤2: 上传到服务器（1分钟）

使用 AI 工具调用 Lighthouse MCP：

```
请帮我上传构建产物到生产环境

FolderPath: d:/project/cowork12-21/dist
InstanceId: lhins-pnt984h2
Region: ap-beijing
ProjectName: jihua-prod
```

#### 步骤3: 执行蓝绿部署（2分钟）

```bash
# SSH 连接服务器
ssh root@<server-ip>

# 执行部署
cd /var/www/jihua-deploy
./blue-green-deploy-production.sh deploy
```

#### 步骤4: 验证部署（1分钟）

```bash
# 查看容器状态
docker ps --filter "name=jihua-prod"

# 浏览器访问（强制刷新）
# https://jihuadz.xin
# Ctrl + Shift + R
```

---

## 🏗️ 架构说明

### 部署架构图

```
公网访问 (HTTPS 443)
    ↓
┌─────────────────────┐
│   jihuadz.xin       │  SSL 已绑定
│   (域名解析)         │
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│   Nginx 反向代理     │  监听 443 端口
│   (SSL 终止)         │
└──────────┬──────────┘
           │
    ┌──────┴──────┐
    │             │
    ↓             ↓
┌────────┐   ┌────────┐
│蓝环境   │   │绿环境   │
│3000端口│   │3003端口│  Docker 容器
│活跃中  │   │待命中  │
└────────┘   └────────┘
```

### 端口分配

| 服务 | 端口 | 说明 |
|-----|------|------|
| HTTPS | 443 | 公网访问入口 |
| HTTP | 80 | 自动跳转到 HTTPS |
| 蓝环境 | 3000 | Docker 容器（仅内部访问） |
| 绿环境 | 3003 | Docker 容器（仅内部访问） |

### 文件结构

```
服务器文件结构:
/var/www/jihua-deploy/
├── dist/                     # 当前构建产物
├── dist-backup-*/            # 自动备份（保留3个）
├── Dockerfile                # Docker 镜像构建文件
├── nginx.conf                # 容器内 Nginx 配置
└── blue-green-deploy-production.sh  # 部署脚本

/etc/nginx/
├── sites-available/
│   └── jihuadz.xin          # 站点配置
├── sites-enabled/
│   └── jihuadz.xin -> ...   # 软链接
└── ssl/
    ├── jihuadz.xin.crt      # SSL 证书
    └── jihuadz.xin.key      # SSL 私钥
```

---

## 🔄 部署流程详解

### 蓝绿切换流程

```
1. 检查当前活跃环境（蓝）
2. 确定目标环境（绿）
3. 清理旧文件和缓存
4. 验证上传文件完整性
5. 强制无缓存构建镜像
6. 启动绿环境容器（3003端口）
7. 健康检查（30次重试，60秒）
   ├─ 成功 → 继续
   └─ 失败 → 自动回滚，保持蓝环境
8. 切换 Nginx 配置（3000 → 3003）
9. Nginx 配置测试
   ├─ 成功 → 重载 Nginx
   └─ 失败 → 恢复配置，回滚
10. 等待30秒（确保切换稳定）
11. 清理蓝环境容器和镜像
12. 部署完成
```

### 回滚流程

```
1. 检测到问题（健康检查失败）
2. 停止新环境容器
3. 保持旧环境继续运行
4. Nginx 配置不变
5. 用户不受影响
```

---

## ⚙️ 核心功能说明

### 1. 防缓存机制

**问题**: 部署后浏览器显示旧版本

**解决方案**:
- Docker 构建使用 `--no-cache` 参数
- 清理 Docker build 缓存
- Nginx 配置 HTML/JS/CSS 不缓存
- 部署前强制删除旧 dist 目录

### 2. 健康检查

**检查内容**:
- HTTP 状态码 200
- 响应时间 < 2秒
- 30次重试，每次间隔2秒
- 总超时时间 60秒

**失败处理**:
- 自动停止新环境容器
- 保持旧环境运行
- 不切换 Nginx 流量

### 3. 自动备份

**备份策略**:
- 每次部署前自动备份 dist
- 保留最近 3 个备份
- 备份命名: `dist-backup-YYYYMMDD-HHMMSS`
- 超过 3 个自动清理

### 4. 资源限制

**Docker 容器限制**:
- 内存: 1GB
- CPU: 2核
- 重启策略: unless-stopped

---

## 🆘 故障处理

### 常见问题速查

| 问题 | 可能原因 | 快速解决 |
|-----|---------|---------|
| 健康检查失败 | 容器启动慢 | 查看容器日志 |
| Nginx 切换失败 | 配置文件错误 | 自动恢复备份 |
| 浏览器显示旧版 | 浏览器缓存 | Ctrl+Shift+R |
| 容器无法启动 | 端口被占用 | 检查端口占用 |
| SSL 证书错误 | 证书过期 | 更新证书 |

### 紧急回滚命令

```bash
# 查看当前状态
./blue-green-deploy-production.sh status

# 回滚到蓝环境
./blue-green-deploy-production.sh rollback blue

# 回滚到绿环境
./blue-green-deploy-production.sh rollback green
```

---

## 📊 监控和日志

### 查看日志

```bash
# 容器日志
docker logs jihua-prod-blue -f --tail 100
docker logs jihua-prod-green -f --tail 100

# Nginx 访问日志
tail -f /var/log/nginx/jihuadz.xin.access.log

# Nginx 错误日志
tail -f /var/log/nginx/jihuadz.xin.error.log

# 系统日志
journalctl -u nginx -f
journalctl -u docker -f
```

### 性能监控

```bash
# 容器资源使用
docker stats jihua-prod-blue
docker stats jihua-prod-green

# 服务器资源
htop
df -h
free -h

# 网络连接
netstat -an | grep :443
```

---

## 🔐 安全建议

### 1. 证书管理

- ✅ 定期检查证书有效期
- ✅ 过期前 30 天续期
- ✅ 使用 Let's Encrypt 免费证书
- ✅ 启用 OCSP Stapling

### 2. 访问控制

- ✅ 使用 SSH 密钥认证
- ✅ 禁用 root 密码登录
- ✅ 配置防火墙规则
- ✅ 定期更新系统补丁

### 3. 数据备份

- ✅ 每天自动备份数据库
- ✅ 保留最近 7 天备份
- ✅ 异地备份重要数据
- ✅ 定期测试恢复流程

---

## 📈 性能优化

### Nginx 优化

```nginx
# Gzip 压缩
gzip on;
gzip_comp_level 6;
gzip_types text/plain text/css application/json;

# 连接优化
keepalive_timeout 65;
keepalive_requests 100;

# 缓存优化
proxy_cache_path /var/cache/nginx levels=1:2 keys_zone=cache:10m;
```

### Docker 优化

```json
{
  "log-driver": "json-file",
  "log-opts": {
    "max-size": "10m",
    "max-file": "3"
  },
  "storage-driver": "overlay2"
}
```

---

## 📞 技术支持

### 常用命令速查

```bash
# 部署相关
./blue-green-deploy-production.sh deploy
./blue-green-deploy-production.sh status
./blue-green-deploy-production.sh rollback blue

# Docker 相关
docker ps -a
docker logs <container-name> -f
docker stats <container-name>
docker exec -it <container-name> sh

# Nginx 相关
nginx -t
systemctl status nginx
systemctl reload nginx

# 系统相关
htop
df -h
free -h
netstat -tlnp
```

### 获取帮助

如遇问题，请按以下顺序排查：

1. 查看容器日志
2. 查看 Nginx 错误日志
3. 检查服务器资源
4. 参考故障处理文档
5. 联系技术支持

---

## 📚 相关文档

| 文档 | 说明 | 使用场景 |
|-----|------|---------|
| 生产环境蓝绿部署方案 v3.12.0 | 完整技术方案 | 需要深入了解时 |
| 生产环境快速部署指南 | 日常部署流程 | 日常部署时参考 |
| 生产环境首次配置指南 | 初始环境搭建 | 首次配置时使用 |
| nginx-production.conf | Nginx 配置详解 | 配置 Nginx 时 |
| blue-green-deploy-production.sh | 部署脚本 | 自动化部署工具 |

---

## 🎯 最佳实践

### 部署前检查

- [ ] 本地构建成功
- [ ] 代码已提交到 Git
- [ ] 版本号已更新
- [ ] 环境变量配置正确

### 部署中监控

- [ ] 关注健康检查结果
- [ ] 观察容器启动日志
- [ ] 检查 Nginx 切换状态

### 部署后验证

- [ ] 浏览器访问测试
- [ ] 核心功能测试
- [ ] 查看错误日志
- [ ] 监控服务器资源

---

## 🆕 版本信息

- **当前版本**: v3.12.0
- **发布日期**: 2026-01-08
- **主要更新**:
  - ✨ 生产环境蓝绿部署方案
  - ✨ 防缓存机制增强
  - ✨ 自动化部署脚本
  - ✨ 完整文档体系

---

## 📝 总结

### 方案优势

1. **零停机部署** - 用户体验无影响
2. **快速回滚** - 秒级切换保障安全
3. **自动化程度高** - 一键完成部署
4. **文档完善** - 覆盖各种场景
5. **安全可靠** - SSL 加密 + 健康检查

### 适用场景

- ✅ 生产环境常规版本发布
- ✅ 紧急 bug 修复部署
- ✅ 功能更新发布
- ✅ 配置变更部署

### 使用建议

1. **首次部署**: 严格按照首次配置指南执行
2. **日常部署**: 使用快速部署指南
3. **问题排查**: 参考完整方案文档
4. **脚本使用**: 熟悉各命令参数

---

**版本**: v3.12.0  
**最后更新**: 2026-01-08  
**维护者**: 际华协同办公平台开发团队  
**生产域名**: https://jihuadz.xin 🚀

---

## ⚠️ 重要提醒

1. **部署前备份**: 虽然有自动回滚，但建议手动备份重要数据
2. **低峰期部署**: 选择访问量低的时段部署
3. **充分测试**: 在测试环境充分验证后再部署生产
4. **监控告警**: 部署后密切关注系统状态
5. **文档更新**: 配置变更后及时更新文档

**祝部署顺利！** 🎉
