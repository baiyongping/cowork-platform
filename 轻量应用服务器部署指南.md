# 际华协同办公管理平台 - 轻量应用服务器部署指南

## 📋 目录
- [服务器环境要求](#服务器环境要求)
- [预备条件清单](#预备条件清单)
- [部署架构说明](#部署架构说明)
- [服务器配置推荐](#服务器配置推荐)
- [必需软件列表](#必需软件列表)
- [网络与安全配置](#网络与安全配置)
- [域名与SSL证书](#域名与ssl证书)
- [CloudBase配置要求](#cloudbase配置要求)

---

## 🖥️ 服务器环境要求

### 1. 操作系统
**推荐选择**:
- **Ubuntu 22.04 LTS** ⭐ (最推荐)
- Ubuntu 20.04 LTS
- CentOS 8 / Rocky Linux 8
- Debian 11

> 💡 **建议**: Ubuntu 22.04 LTS 具有最好的软件源支持和社区文档

### 2. 服务器配置推荐

#### 基础配置（小团队 10-30人）
```
CPU:     2核心
内存:    2GB
磁盘:    40GB SSD
带宽:    3Mbps
```
**价格参考**: ¥60-80/月

#### 标准配置（中等团队 30-100人）⭐ 推荐
```
CPU:     2核心
内存:    4GB
磁盘:    60GB SSD
带宽:    5Mbps
```
**价格参考**: ¥100-150/月

#### 高级配置（大团队 100-300人）
```
CPU:     4核心
内存:    8GB
磁盘:    80GB SSD
带宽:    10Mbps
```
**价格参考**: ¥200-300/月

> ⚠️ **注意**: 
> - 本系统前端为纯静态文件，占用资源很少
> - 主要性能消耗在并发用户数和网络带宽
> - 数据库使用 CloudBase，无需服务器承载

---

## ✅ 预备条件清单

### 必备项
- [ ] **云服务器实例** (腾讯云轻量应用服务器/CVM/阿里云ECS等)
- [ ] **公网IP地址** (服务器自带)
- [ ] **域名** (已备案，如: oa.jihua.com)
- [ ] **SSL证书** (Let's Encrypt免费证书或购买商业证书)
- [ ] **CloudBase环境** (腾讯云开发环境)
- [ ] **SSH访问权限** (用于服务器管理)

### 可选项
- [ ] **CDN加速** (提升全国访问速度)
- [ ] **对象存储COS** (备份数据)
- [ ] **监控告警** (云监控服务)

---

## 🏗️ 部署架构说明

### 系统架构图
```
┌─────────────────────────────────────────────────────────┐
│                      用户浏览器                          │
│                (https://oa.jihua.com)                   │
└──────────────────────┬──────────────────────────────────┘
                       │ HTTPS (443)
                       ↓
┌─────────────────────────────────────────────────────────┐
│                    腾讯云轻量应用服务器                   │
│  ┌─────────────────────────────────────────────────┐   │
│  │              Nginx Web Server                    │   │
│  │         (反向代理 + 静态文件服务)                  │   │
│  │  • 监听 80端口 (HTTP → HTTPS重定向)              │   │
│  │  • 监听 443端口 (HTTPS)                          │   │
│  │  • 托管 /usr/share/nginx/html/ 静态文件          │   │
│  │  • SPA路由支持                                   │   │
│  │  • Gzip压缩                                      │   │
│  │  • 静态资源缓存                                   │   │
│  └─────────────────────────────────────────────────┘   │
└──────────────────────┬──────────────────────────────────┘
                       │
                       ↓
┌─────────────────────────────────────────────────────────┐
│              腾讯云 CloudBase (云开发)                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  NoSQL数据库  │  │  用户认证     │  │  云存储      │  │
│  │  • users     │  │  • 登录注册   │  │  • 文件上传  │  │
│  │  • tasks     │  │  • 权限管理   │  │  • 图片存储  │  │
│  │  • projects  │  │  • 会话管理   │  │              │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### 技术说明
1. **前端**: React SPA应用，编译后为纯静态HTML/CSS/JS文件
2. **Web服务器**: Nginx托管静态文件，处理HTTPS和路由
3. **后端服务**: 完全由CloudBase提供（BaaS架构）
4. **数据存储**: CloudBase NoSQL数据库（MongoDB协议）
5. **用户认证**: CloudBase Auth服务
6. **文件存储**: CloudBase Storage（如需上传功能）

---

## 🛠️ 必需软件列表

### 1. Docker 与 Docker Compose
**用途**: 容器化部署（推荐方式）

```bash
# Docker Engine
版本: >= 20.10.0

# Docker Compose
版本: >= 2.0.0
```

**安装命令**:
```bash
# Ubuntu 22.04
curl -fsSL https://get.docker.com | bash
sudo usermod -aG docker $USER
```

### 2. Nginx (传统部署方式)
**用途**: Web服务器、反向代理

```bash
版本: >= 1.18.0
推荐: nginx/1.22+ (Ubuntu 22.04默认版本)
```

**安装命令**:
```bash
sudo apt update
sudo apt install nginx -y
```

### 3. SSL证书工具
**用途**: HTTPS加密通信

#### 方案A: Certbot (Let's Encrypt免费证书) ⭐ 推荐
```bash
sudo apt install certbot python3-certbot-nginx -y
```

#### 方案B: 商业证书
- 阿里云SSL证书
- 腾讯云SSL证书
- DigiCert等品牌证书

### 4. Node.js (本地构建用)
**用途**: 本地编译前端代码

```bash
版本: >= 18.0.0
推荐: Node.js 18 LTS
```

**安装命令**:
```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install nodejs -y
```

### 5. Git
**用途**: 代码拉取和版本管理

```bash
sudo apt install git -y
```

### 6. 其他工具
```bash
# 系统监控
sudo apt install htop -y

# 网络工具
sudo apt install net-tools curl wget -y

# 文本编辑
sudo apt install vim -y
```

---

## 🌐 网络与安全配置

### 1. 防火墙端口配置

#### 必须开放的端口
```bash
端口 22  (SSH)          - 服务器管理
端口 80  (HTTP)         - HTTP访问（自动跳转HTTPS）
端口 443 (HTTPS)        - HTTPS安全访问
```

#### 腾讯云安全组配置
```
规则方向: 入站
协议端口: TCP:22     来源: 0.0.0.0/0     策略: 允许
协议端口: TCP:80     来源: 0.0.0.0/0     策略: 允许
协议端口: TCP:443    来源: 0.0.0.0/0     策略: 允许
```

#### 服务器防火墙配置 (UFW)
```bash
# 启用UFW
sudo ufw enable

# 允许SSH
sudo ufw allow 22/tcp

# 允许HTTP和HTTPS
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# 查看状态
sudo ufw status
```

### 2. SSH安全加固

#### 禁用密码登录（推荐使用密钥）
```bash
sudo vim /etc/ssh/sshd_config

# 修改以下配置
PasswordAuthentication no
PubkeyAuthentication yes
PermitRootLogin no

# 重启SSH服务
sudo systemctl restart sshd
```

#### 配置SSH密钥
```bash
# 在本地生成密钥对
ssh-keygen -t rsa -b 4096 -C "your_email@example.com"

# 上传公钥到服务器
ssh-copy-id user@your-server-ip
```

### 3. Fail2Ban防暴力破解
```bash
# 安装
sudo apt install fail2ban -y

# 配置
sudo systemctl enable fail2ban
sudo systemctl start fail2ban
```

---

## 🌍 域名与SSL证书

### 1. 域名配置

#### 域名解析记录
```
记录类型: A记录
主机记录: oa (或 @ 代表根域名)
记录值:   你的服务器公网IP
TTL:      600
```

#### 验证域名解析
```bash
# 检查域名是否解析成功
ping oa.jihua.com
nslookup oa.jihua.com
```

> ⏱️ **等待时间**: DNS解析生效通常需要10分钟到2小时

### 2. SSL证书申请

#### 方案A: Let's Encrypt免费证书（推荐）

**优点**:
- ✅ 完全免费
- ✅ 自动续期
- ✅ 浏览器信任
- ✅ 3个月有效期（自动更新）

**申请步骤**:
```bash
# 1. 安装Certbot
sudo apt install certbot python3-certbot-nginx -y

# 2. 申请证书（自动配置Nginx）
sudo certbot --nginx -d oa.jihua.com

# 3. 测试自动续期
sudo certbot renew --dry-run
```

**自动续期配置**:
```bash
# Certbot会自动添加cron任务
# 查看自动续期任务
sudo systemctl status certbot.timer
```

#### 方案B: 商业SSL证书

**适用场景**:
- 需要更长有效期（1-2年）
- 企业级信任需求
- OV/EV级别证书

**步骤**:
1. 在云服务商购买SSL证书
2. 完成域名验证
3. 下载证书文件（.crt和.key）
4. 上传到服务器 `/etc/nginx/ssl/`
5. 配置Nginx

---

## ☁️ CloudBase配置要求

### 1. CloudBase环境信息

#### 当前环境ID
```json
{
  "envId": "jihua-oa-dev-3goht9irae4d949f",
  "region": "ap-shanghai",  // 或其他地区
  "env": "production"       // 生产环境
}
```

> ⚠️ **重要**: 
> - 确保生产环境与开发环境分离
> - 建议创建独立的生产环境ID

### 2. CloudBase服务开通清单

#### 必须开通的服务
- [x] **云数据库** (NoSQL Database)
  - users (用户集合)
  - tasks (任务集合)
  - opportunities (商机集合)
  - projects (项目集合)
  - goals (目标集合)
  - departments (部门集合)
  - systemSettings (系统设置)
  - operationLogs (操作日志)

- [x] **用户认证** (Authentication)
  - 邮箱登录
  - 手机号登录
  - 自定义用户系统

- [x] **云存储** (可选，如需文件上传)
  - 用户头像存储
  - 附件文件存储

#### 推荐开通的服务
- [ ] **云函数** (Cloud Functions) - 高级功能扩展
- [ ] **静态网站托管** (Static Hosting) - CDN加速
- [ ] **安全规则** (Security Rules) - 数据访问控制

### 3. CloudBase资源配额

#### 免费版配额 (按量计费)
```
数据库:
  - 存储: 2GB
  - 读操作: 50,000次/天
  - 写操作: 20,000次/天

云存储:
  - 容量: 5GB
  - 下载: 10GB/月
  - CDN: 5GB/月

云函数:
  - 调用: 100,000次/月
  - 资源使用: 40万GBs/月
```

#### 企业版推荐配置
```
数据库:
  - 存储: 按需扩展
  - 并发连接: 100+
  - QPS: 1000+

云存储:
  - 容量: 按需
  - CDN加速: 全国节点

云函数:
  - 无限制调用
  - 更高超时时间
```

### 4. 安全域名配置

#### Web SDK初始化配置
前端代码需要添加服务器域名到CloudBase安全域名列表：

```
CloudBase控制台 → 环境设置 → 安全配置 → 授权域名

添加域名:
- https://oa.jihua.com
- http://oa.jihua.com (可选)
```

#### CORS跨域配置
如果使用云函数或云托管，需配置CORS：
```
允许的源: https://oa.jihua.com
允许的方法: GET, POST, PUT, DELETE
允许的头: Content-Type, Authorization
```

---

## 📊 资源成本估算

### 月度成本预算（标准配置）

| 项目 | 服务 | 配置 | 费用 |
|-----|------|------|------|
| 服务器 | 轻量应用服务器 | 2核4G 5M带宽 | ¥100-150 |
| CloudBase | 数据库 | 小团队规模 | ¥0-50 (免费额度) |
| 域名 | 域名注册 | .com域名 | ¥60/年 ≈ ¥5/月 |
| SSL证书 | Let's Encrypt | 免费 | ¥0 |
| CDN | 腾讯云CDN | 可选 | ¥0-100 |
| **总计** | - | - | **¥105-300/月** |

### 企业级配置（100人以上）

| 项目 | 服务 | 配置 | 费用 |
|-----|------|------|------|
| 服务器 | CVM云服务器 | 4核8G 10M带宽 | ¥300-500 |
| CloudBase | 企业版 | 按需付费 | ¥100-500 |
| 域名 | 已有 | - | ¥5/月 |
| SSL证书 | OV证书 | 企业认证 | ¥1000/年 ≈ ¥83/月 |
| CDN | 全站加速 | 全国节点 | ¥200-500 |
| **总计** | - | - | **¥688-1588/月** |

---

## 🔍 环境检查清单

部署前请确认以下所有项目：

### 服务器环境
- [ ] 服务器已创建，系统为Ubuntu 22.04
- [ ] 拥有root或sudo权限
- [ ] 服务器可以访问公网
- [ ] 公网IP已分配
- [ ] 安全组已配置（22/80/443端口开放）

### 域名与证书
- [ ] 域名已购买并完成备案（中国大陆）
- [ ] DNS解析已配置（A记录指向服务器IP）
- [ ] 域名可以ping通
- [ ] SSL证书申请方式已确定

### CloudBase服务
- [ ] CloudBase环境已创建
- [ ] 环境ID已获取
- [ ] 数据库已开通
- [ ] 用户认证已开通
- [ ] 安全域名已配置
- [ ] 数据库集合已创建

### 本地环境
- [ ] Node.js >= 18.0.0 已安装
- [ ] npm可以正常使用
- [ ] 项目代码可以本地构建成功
- [ ] .env配置文件已准备

### 部署工具
- [ ] SSH密钥已配置
- [ ] Git已安装
- [ ] Docker已安装（如使用容器部署）
- [ ] Nginx已安装（如使用传统部署）

---

## 📝 后续步骤

完成以上环境准备后，可以继续：

1. **本地构建** - 编译React应用
2. **上传部署** - 将构建产物上传到服务器
3. **配置Nginx** - 设置Web服务器
4. **配置SSL** - 启用HTTPS
5. **测试验证** - 访问应用测试功能
6. **监控配置** - 设置性能监控

详细部署步骤请参考：
- 📄 [Docker部署方案文档](./Docker部署方案.md) (即将创建)
- 📄 [传统部署方案文档](./传统部署方案.md) (即将创建)
- 📄 [CloudBase配置指南](./CloudBase配置指南.md) (即将创建)

---

## 🆘 常见问题

### Q1: 必须使用腾讯云服务器吗？
**A**: 不必须。任何云服务商的服务器都可以，包括：
- 阿里云ECS
- 华为云ECS
- AWS EC2
- Azure VM

但建议使用腾讯云，与CloudBase同厂商网络延迟最低。

### Q2: 一定要用Docker部署吗？
**A**: 不一定。提供两种方案：
- **Docker方式**: 简单、标准化，推荐
- **传统方式**: 直接用Nginx托管dist目录

### Q3: 不使用CloudBase可以吗？
**A**: 不可以。本系统深度集成CloudBase：
- 数据库: CloudBase NoSQL
- 认证: CloudBase Auth
- 前端SDK: @cloudbase/js-sdk

若要更换后端，需要大量重构。

### Q4: 免费额度够用吗？
**A**: 小团队（10-30人）使用完全够用：
- 日均操作 < 1000次，远低于免费额度
- 数据量 < 100MB，远低于2GB限制

超出后按量付费，成本很低。

### Q5: SSL证书必须配置吗？
**A**: 强烈推荐：
- CloudBase SDK需要HTTPS
- 浏览器会警告HTTP不安全
- 用户密码传输需要加密
- Let's Encrypt免费证书完全够用

---

## 📞 技术支持

如有部署问题，请参考：
- 腾讯云CloudBase文档: https://cloud.tencent.com/document/product/876
- Nginx官方文档: https://nginx.org/en/docs/
- Let's Encrypt文档: https://letsencrypt.org/docs/

---

**准备完成后，请继续下一步：选择部署方式（Docker或传统）**
