# 际华协同办公平台 - 生产环境蓝绿部署方案

## 📋 部署架构

```
┌─────────────────────────────────────────────────────────┐
│                    生产环境部署架构                       │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  公网访问 (HTTPS 443) - jihuadz.xin                     │
│         ↓                                                │
│  ┌─────────────────┐                                    │
│  │  Nginx 反向代理  │  (SSL 已绑定)                      │
│  │  (SSL 终止)      │                                    │
│  └────────┬─────────┘                                    │
│           │                                              │
│           ├──→ 蓝环境 (Docker, 端口 3000) ←── 当前流量  │
│           │    └─ jihua-prod:blue                       │
│           │                                              │
│           └──→ 绿环境 (Docker, 端口 3003) ←── 升级时临时 │
│                └─ jihua-prod:green (新版本)             │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

## 🎯 核心特性

- ✅ **零停机部署** - 用户无感知升级
- ✅ **快速回滚** - 秒级切换到旧版本
- ✅ **健康检查** - 自动验证新版本可用性
- ✅ **统一入口** - HTTPS 443 端口访问
- ✅ **自动化脚本** - 一键完成部署
- ✅ **SSL 证书** - 生产域名已绑定
- ✅ **防缓存机制** - 强制无缓存构建

## 🚀 快速部署流程

### 步骤一：本地构建（在 Windows 本地执行）

```powershell
# 1. 确保环境变量正确
# 检查 .env.production 配置
cat .env.production

# 2. 执行生产环境构建
npm run build

# 3. 验证构建产物
ls dist/
# 应该看到: index.html, assets/, ...

# 4. 查看构建版本
cat VERSION.md
# 当前版本: v3.12.0
```

### 步骤二：上传到 Lighthouse 服务器

使用 Lighthouse MCP 工具上传构建产物：

```javascript
// 调用 MCP 工具
mcp_call_tool({
  serverName: "cloudbase",
  toolName: "deploy_project_preparation",
  arguments: JSON.stringify({
    FolderPath: "d:/project/cowork12-21/dist",
    InstanceId: "lhins-pnt984h2",  // 你的 Lighthouse 实例ID
    Region: "ap-beijing",
    ProjectName: "jihua-prod"
  })
})
```

**重要说明**：
- 工具会自动上传 dist 目录到服务器 `/var/www/jihua-deploy/dist/`
- 上传时间戳路径：`/root/dist_YYYYMMDD_HHMMSS/`

### 步骤三：执行蓝绿部署（在服务器上）

#### 方式一：自动化一键部署（推荐） ⭐

```bash
# 1. SSH 连接到 Lighthouse 服务器
ssh root@<your-lighthouse-ip>

# 2. 执行蓝绿部署脚本
/var/www/jihua-deploy/blue-green-deploy.sh deploy

# 脚本会自动执行以下步骤：
# ✓ 清理旧部署文件和缓存
# ✓ 验证上传文件完整性
# ✓ 强制无缓存构建 Docker 镜像
# ✓ 启动目标环境容器
# ✓ 健康检查（30次重试，60秒超时）
# ✓ 切换 Nginx 配置
# ✓ 清理旧环境容器
```

#### 方式二：分步部署（调试用）

```bash
# 1. 清理旧文件和验证上传
/var/www/jihua-deploy/blue-green-deploy.sh clean
/var/www/jihua-deploy/blue-green-deploy.sh validate

# 2. 查看当前状态
/var/www/jihua-deploy/blue-green-deploy.sh status

# 3. 执行部署
/var/www/jihua-deploy/blue-green-deploy.sh deploy

# 4. 如果部署失败，快速回滚
/var/www/jihua-deploy/blue-green-deploy.sh rollback blue
```

## 📂 服务器文件结构

```
/var/www/jihua-deploy/
├── dist/                      # 当前最新构建产物
│   ├── index.html
│   ├── assets/
│   └── .build-version
├── dist-backup-YYYYMMDD-HHMMSS/  # 自动备份 (保留最近3个)
├── Dockerfile                  # Docker 镜像构建文件
└── blue-green-deploy.sh       # 蓝绿部署脚本

/etc/nginx/
├── sites-available/
│   └── jihuadz.xin            # Nginx 反向代理配置
└── ssl/
    ├── jihuadz.xin.crt        # SSL 证书（已配置）
    └── jihuadz.xin.key        # SSL 私钥（已配置）
```

## 🔧 Nginx 生产环境配置

需要创建/更新的 Nginx 配置文件：`/etc/nginx/sites-available/jihuadz.xin`

```nginx
# 蓝绿部署后端配置
upstream jihua_backend {
    server 127.0.0.1:3000;  # 蓝环境（默认）
    # server 127.0.0.1:3003;  # 绿环境（部署时切换）
}

# HTTP 跳转到 HTTPS
server {
    listen 80;
    server_name jihuadz.xin www.jihuadz.xin;
    
    location / {
        return 301 https://$host$request_uri;
    }
}

# HTTPS 主站配置
server {
    listen 443 ssl http2;
    server_name jihuadz.xin www.jihuadz.xin;
    
    # SSL 证书配置（已绑定）
    ssl_certificate /etc/nginx/ssl/jihuadz.xin.crt;
    ssl_certificate_key /etc/nginx/ssl/jihuadz.xin.key;
    
    # SSL 安全配置
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers 'ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384';
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;
    
    # 安全头
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    
    # 反向代理到 Docker 容器
    location / {
        proxy_pass http://jihua_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        
        # 防缓存配置（重要）
        proxy_no_cache 1;
        proxy_cache_bypass 1;
    }
    
    # 静态资源防缓存（部署时）
    location ~* \.(js|css|html)$ {
        proxy_pass http://jihua_backend;
        expires -1;
        add_header Cache-Control "no-store, no-cache, must-revalidate";
        add_header Pragma "no-cache";
    }
    
    # 图片和字体缓存（7天）
    location ~* \.(jpg|jpeg|png|gif|ico|svg|woff|woff2|ttf|eot)$ {
        proxy_pass http://jihua_backend;
        expires 7d;
        add_header Cache-Control "public, immutable";
    }
    
    # 日志配置
    access_log /var/log/nginx/jihuadz.xin.access.log;
    error_log /var/log/nginx/jihuadz.xin.error.log;
}
```

## 🐳 Dockerfile（生产环境）

项目根目录的 `Dockerfile` 已经配置好，内容如下：

```dockerfile
# 使用 Nginx 作为 Web 服务器
FROM nginx:alpine

# 复制构建产物到 Nginx 默认目录
COPY dist/ /usr/share/nginx/html/

# 复制自定义 Nginx 配置
COPY nginx.conf /etc/nginx/conf.d/default.conf

# 暴露 80 端口
EXPOSE 80

# 启动 Nginx
CMD ["nginx", "-g", "daemon off;"]
```

## 📝 首次部署清单

### 1. 服务器准备（一次性）

```bash
# 1. 创建部署目录
mkdir -p /var/www/jihua-deploy
cd /var/www/jihua-deploy

# 2. 上传蓝绿部署脚本
# (使用 Lighthouse MCP 工具上传 blue-green-deploy.sh)

# 3. 设置脚本权限
chmod +x /var/www/jihua-deploy/blue-green-deploy.sh

# 4. 创建 Nginx 配置
nano /etc/nginx/sites-available/jihuadz.xin
# (复制上面的 Nginx 配置内容)

# 5. 启用站点
ln -s /etc/nginx/sites-available/jihuadz.xin /etc/nginx/sites-enabled/

# 6. 测试 Nginx 配置
nginx -t

# 7. 重启 Nginx
systemctl reload nginx
```

### 2. 防火墙配置（一次性）

```bash
# 确保 80 和 443 端口开放
firewall-cmd --permanent --add-port=80/tcp
firewall-cmd --permanent --add-port=443/tcp
firewall-cmd --reload

# 验证端口
firewall-cmd --list-ports
```

### 3. Docker 环境检查

```bash
# 检查 Docker 是否运行
systemctl status docker

# 查看现有容器
docker ps -a

# 清理旧容器（如果有）
docker stop $(docker ps -aq) 2>/dev/null
docker rm $(docker ps -aq) 2>/dev/null
```

## 🔄 日常升级流程

### 完整升级步骤

```bash
# ====================================
# 步骤 1: 本地构建（Windows）
# ====================================
npm run build

# ====================================
# 步骤 2: 上传到服务器（使用 MCP 工具）
# ====================================
# 通过 AI 工具调用 deploy_project_preparation

# ====================================
# 步骤 3: SSH 连接服务器
# ====================================
ssh root@<your-lighthouse-ip>

# ====================================
# 步骤 4: 执行蓝绿部署
# ====================================
cd /var/www/jihua-deploy
./blue-green-deploy.sh deploy

# ====================================
# 步骤 5: 验证部署
# ====================================
# 1. 检查容器状态
docker ps --filter "name=jihua-prod"

# 2. 访问网站
curl -I https://jihuadz.xin

# 3. 查看 Nginx 日志
tail -f /var/log/nginx/jihuadz.xin.access.log

# ====================================
# 步骤 6: 用户验证
# ====================================
# 浏览器访问: https://jihuadz.xin
# 强制刷新: Ctrl + Shift + R
```

## 🆘 故障回滚方案

### 自动回滚（脚本内置）

如果部署过程中健康检查失败，脚本会自动回滚：

```bash
# 自动回滚流程：
# 1. 停止新环境容器
# 2. 保持旧环境继续运行
# 3. 用户不受影响
```

### 手动回滚到旧版本

```bash
# 1. 查看当前状态
./blue-green-deploy.sh status
# 输出: 当前活跃环境: green

# 2. 回滚到蓝环境
./blue-green-deploy.sh rollback blue

# 3. 验证回滚
curl -I https://jihuadz.xin
docker ps --filter "name=jihua-prod"
```

## 📊 监控和验证

### 部署后验证清单

```bash
# ✓ 1. 容器状态
docker ps --filter "name=jihua-prod" --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"

# ✓ 2. Nginx 状态
systemctl status nginx

# ✓ 3. SSL 证书
echo | openssl s_client -connect jihuadz.xin:443 2>/dev/null | openssl x509 -noout -dates

# ✓ 4. 端口监听
netstat -tlnp | grep -E ':80|:443|:3000|:3003'

# ✓ 5. 网站访问
curl -I https://jihuadz.xin

# ✓ 6. 磁盘空间
df -h

# ✓ 7. Docker 镜像
docker images | grep jihua-prod
```

### 日志查看

```bash
# 容器日志
docker logs jihua-prod-blue -f
docker logs jihua-prod-green -f

# Nginx 访问日志
tail -f /var/log/nginx/jihuadz.xin.access.log

# Nginx 错误日志
tail -f /var/log/nginx/jihuadz.xin.error.log

# 系统日志
journalctl -u nginx -f
```

## ⚙️ 脚本命令参考

```bash
# 查看帮助
./blue-green-deploy.sh --help

# 执行完整部署（推荐）
./blue-green-deploy.sh deploy

# 仅清理旧文件和缓存
./blue-green-deploy.sh clean

# 仅验证上传文件
./blue-green-deploy.sh validate

# 查看当前环境状态
./blue-green-deploy.sh status

# 回滚到指定环境
./blue-green-deploy.sh rollback blue
./blue-green-deploy.sh rollback green
```

## 🔐 安全注意事项

1. **SSL 证书**：
   - ✅ 生产环境 SSL 证书已绑定
   - ⚠️ 定期检查证书有效期
   - 📅 建议在过期前 30 天续期

2. **防火墙规则**：
   - ✅ 仅开放 80、443 端口
   - ❌ 不要开放 3000、3003 端口
   - 🔒 使用 Nginx 反向代理保护后端

3. **敏感信息**：
   - ✅ 使用环境变量管理配置
   - ❌ 不要在代码中硬编码密钥
   - 🔐 定期更换 CloudBase 密钥

## 📈 性能优化建议

### 1. Nginx 优化

```nginx
# 启用 Gzip 压缩
gzip on;
gzip_vary on;
gzip_min_length 1024;
gzip_types text/plain text/css application/json application/javascript text/xml application/xml;

# 增加连接超时
keepalive_timeout 65;
keepalive_requests 100;

# 启用 HTTP/2
listen 443 ssl http2;
```

### 2. Docker 资源限制

```bash
# 启动容器时设置资源限制
docker run -d \
  --name jihua-prod-blue \
  --restart unless-stopped \
  --memory="512m" \
  --cpus="1.0" \
  -p 3000:80 \
  jihua-prod-app:blue
```

### 3. CDN 加速（可选）

考虑使用腾讯云 CDN 加速静态资源：
- JS、CSS、图片等静态文件
- 配置 CDN 源站为 jihuadz.xin
- 设置缓存策略

## 🎯 访问地址

- **生产环境**: https://jihuadz.xin
- **蓝环境直连** (调试用): http://<server-ip>:3000
- **绿环境直连** (调试用): http://<server-ip>:3003

**注意**：直连端口仅用于调试，不要暴露给外部访问。

## 📚 相关文档

- [蓝绿部署方案 - 测试环境](./部署方案-蓝绿部署.md)
- [部署方案总览](./部署方案总览.md)
- [防缓存部署参考](./防缓存部署-快速参考.md)
- [版本信息](./VERSION.md)

## 🆕 更新记录

| 版本 | 日期 | 更新内容 |
|------|------|---------|
| v3.12.0 | 2026-01-08 | 生产环境蓝绿部署方案 |
| v3.11.0 | 2026-01-08 | 测试环境蓝绿部署完成 |

---

**版本**: v3.12.0  
**最后更新**: 2026-01-08  
**维护者**: 际华协同办公平台开发团队  
**生产域名**: https://jihuadz.xin 🚀
