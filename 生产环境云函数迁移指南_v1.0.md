# 生产环境云函数迁移指南 v1.0

## 📋 概述

本指南将帮助您将开发环境（jihua-oa-dev-3goht9irae4d949f）的云函数迁移到生产环境（cowork-9gg9oocb516be5fb）。

---

## 🎯 新增云函数列表

以下云函数需要部署到生产环境：

### 会议管理相关（优先级最高）

1. **meeting-management** - 会议管理主云函数
2. **meeting-ai-generator** - AI 会议纪要生成
3. **task-from-meeting** - 会议任务转换
4. **issue-from-agenda** - 会议议题转问题

### 微信相关

5. **generate-miniprogram-qr** - 生成小程序二维码
6. **wechat-oauth** - 微信 OAuth 认证

---

## 🚀 部署方法（推荐方式）

### 方法 1：使用 CloudBase MCP 工具（自动化）

**步骤：**

1. **查询生产环境云函数列表**

```bash
# 在 AI 工具中执行
call_tcb_integration({
  toolName: "getFunctionList",
  arguments: { action: "list", limit: 100 }
})
```

2. **为每个新增云函数执行部署**

```bash
# 示例：部署 meeting-management
call_tcb_integration({
  toolName: "createFunction",
  arguments: {
    func: {
      name: "meeting-management",
      runtime: "Nodejs18.15",
      handler: "index.main",
      timeout: 60,
      envVariables: {}
    },
    functionRootPath: "d:\\project\\cowork12-21\\cloudfunctions",
    force: true
  }
})
```

3. **重复部署其他云函数**

按照相同方式部署：
- meeting-ai-generator
- task-from-meeting
- issue-from-agenda
- generate-miniprogram-qr
- wechat-oauth

---

### 方法 2：使用 CloudBase CLI（手动）

**前提条件：**
- 已安装 CloudBase CLI
- 已登录腾讯云账号

**步骤：**

1. **切换到生产环境**

```powershell
# PowerShell
cd d:\project\cowork12-21
tcb env:use cowork-9gg9oocb516be5fb
```

2. **批量部署云函数**

```powershell
# 部署所有新增云函数
$functions = @(
  "meeting-management",
  "meeting-ai-generator",
  "task-from-meeting",
  "issue-from-agenda",
  "generate-miniprogram-qr",
  "wechat-oauth"
)

foreach ($func in $functions) {
  Write-Host "📦 正在部署 $func..." -ForegroundColor Cyan
  tcb fn deploy $func --force
  if ($LASTEXITCODE -eq 0) {
    Write-Host "✅ $func 部署成功" -ForegroundColor Green
  } else {
    Write-Host "❌ $func 部署失败" -ForegroundColor Red
  }
}
```

---

## 📊 数据集合同步

### 需要检查/创建的集合

生产环境需要确保以下集合存在：

#### 新增集合（会议管理）

1. **meetings** - 会议基础信息
2. **meetingMinutes** - 会议纪要

#### 现有集合（确认存在）

3. **tasks** - 任务
4. **opportunities** - 商机
5. **projects** - 项目
6. **issues** - 问题
7. **users** - 用户
8. **budgetSubjects** - 预算科目
9. **budgetExecutionRecords** - 预算执行记录

### 集合权限配置

对于新增的集合，需要配置权限：

```javascript
// meetings 集合权限
{
  "read": "auth.openid != null",
  "write": "auth.openid != null"
}

// meetingMinutes 集合权限
{
  "read": "auth.openid != null",
  "write": "auth.openid != null"
}
```

### 索引配置

**meetings 集合索引：**
```javascript
// 创建索引
db.collection('meetings').createIndex({
  keys: { createdBy: 1, type: 1, status: 1 }
});

db.collection('meetings').createIndex({
  keys: { startTime: -1 }
});
```

**meetingMinutes 集合索引：**
```javascript
// 创建索引
db.collection('meetingMinutes').createIndex({
  keys: { meetingId: 1 }
});
```

---

## 🗄️ 存储桶配置

### CORS 配置验证

确保生产环境存储桶已配置 CORS：

**控制台路径：**
> CloudBase 控制台 → 云存储 → 设置 → CORS 配置

**配置内容：**
```json
[
  {
    "AllowedOrigin": ["https://jihuadz.xin"],
    "AllowedMethod": ["GET", "POST", "PUT", "DELETE"],
    "AllowedHeader": ["*"],
    "ExposeHeader": ["ETag"],
    "MaxAgeSeconds": 3600
  }
]
```

### 安全域名配置

**控制台路径：**
> CloudBase 控制台 → 环境设置 → 安全域名

**需要添加的域名：**
- https://jihuadz.xin

---

## ✅ 部署验证清单

完成部署后，逐项验证功能：

### 1. 前端访问验证
- [ ] 访问 https://jihuadz.xin
- [ ] 强制刷新浏览器（Ctrl + F5）
- [ ] 检查控制台无错误
- [ ] 验证页面加载正常

### 2. 云函数验证
- [ ] meeting-management 部署成功
- [ ] meeting-ai-generator 部署成功
- [ ] task-from-meeting 部署成功
- [ ] issue-from-agenda 部署成功
- [ ] generate-miniprogram-qr 部署成功
- [ ] wechat-oauth 部署成功

### 3. 数据库验证
- [ ] meetings 集合已创建
- [ ] meetingMinutes 集合已创建
- [ ] 集合权限配置正确
- [ ] 索引已创建

### 4. 功能测试
- [ ] 用户登录功能正常
- [ ] 会议管理模块可访问
- [ ] 可以创建会议
- [ ] 可以编辑会议
- [ ] 会议纪要功能正常
- [ ] 任务转换功能正常
- [ ] 问题转换功能正常

### 5. 存储桶验证
- [ ] CORS 配置正确
- [ ] 安全域名已添加
- [ ] 文件上传功能正常

---

## 🔧 常见问题处理

### 问题 1：云函数部署失败

**原因：** 依赖包未安装

**解决：**
```bash
cd d:\project\cowork12-21\cloudfunctions\[云函数名]
npm install
```

### 问题 2：数据库连接失败

**原因：** 环境 ID 配置错误

**解决：**
- 检查云函数中的环境 ID 是否为生产环境 ID
- 确认 `cloud.init()` 使用了正确的环境

### 问题 3：存储桶访问失败

**原因：** CORS 或安全域名未配置

**解决：**
1. 添加 CORS 配置
2. 添加安全域名 https://jihuadz.xin

### 问题 4：会议纪要生成失败

**原因：** AI 生成相关配置缺失

**解决：**
- 检查 meeting-ai-generator 云函数环境变量
- 确认 AI 服务配置正确

---

## 📞 技术支持

如遇到问题，请检查：

1. **CloudBase 控制台日志**
   - 路径：CloudBase 控制台 → 日志监控 → 云函数日志
   - 筛选对应的云函数查看错误信息

2. **浏览器控制台**
   - 按 F12 打开开发者工具
   - 查看 Console 和 Network 标签页

3. **Nginx 日志**（Lighthouse 服务器）
   ```bash
   tail -f /var/log/nginx/error.log
   tail -f /var/log/nginx/access.log
   ```

---

## 📝 部署记录

**部署时间：** 2026-01-02 22:48

**部署内容：**
- ✅ 前端：已部署到 /var/www/jihua/
- ⏳ 云函数：待手动部署
- ⏳ 数据集合：待验证/创建
- ⏳ 存储桶：待验证配置

**下一步操作：**
1. 使用本指南完成云函数迁移
2. 验证数据集合配置
3. 测试所有关键功能

---

## 🎉 完成标准

所有检查项通过后，生产环境部署完成：

- ✅ 前端访问正常
- ✅ 所有云函数已部署
- ✅ 数据集合配置完成
- ✅ 存储桶配置正确
- ✅ 关键功能测试通过

---

**版本：** v1.0  
**更新日期：** 2026-01-02  
**维护者：** 际华协同办公平台开发团队
