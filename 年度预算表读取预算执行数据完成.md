# 年度预算表读取预算执行数据 - 完成报告

## 修改时间
2025-12-28

## 修改目标
让年度预算管理表的季度完成数据从预算执行表中读取,而不是从年度预算表本身读取。

## 修改内容

### 1. 修改 `loadBudgetData` 函数

**文件**: `components/pages/AnnualBudgetManagement.tsx`

**核心逻辑**:
1. 同时查询年度预算和预算执行两个集合
2. 创建预算执行数据映射 `executionMap` (按科目ID+项目名称)
3. 合并数据时,优先使用预算执行表的月度数据
4. 重新计算实际完成总计和完成率

**关键代码**:
```typescript
// 同时查询年度预算和预算执行数据
const [annualResult, executionResult] = await Promise.all([
  db.collection('annual_budgets').where({ year: year }).get(),
  db.collection('budget_execution').where({ year: year }).get()
]);

// 创建预算执行数据映射(按科目ID和项目名称)
const executionMap = new Map<string, MonthlyActuals>();
if (executionResult.data && executionResult.data.length > 0) {
  const executionData = executionResult.data[0];
  if (executionData.items && Array.isArray(executionData.items)) {
    executionData.items.forEach((item: any) => {
      const key = `${item.categoryId}_${item.itemName}`;
      executionMap.set(key, item.monthlyActuals);
    });
  }
}

// 从预算执行表中读取月度实际数据
mergedItems = mergedItems.map(item => {
  const key = `${item.categoryId}_${item.itemName}`;
  const executionData = executionMap.get(key);
  
  if (executionData) {
    // 使用预算执行表的月度数据
    const monthlyActuals = executionData;
    // 重新计算实际完成总计
    const actualTotal = Object.values(monthlyActuals).reduce((sum, val) => sum + (val || 0), 0);
    // 重新计算完成率
    const percentage = item.annualBudget > 0 ? (actualTotal / item.annualBudget) * 100 : 0;
    
    return {
      ...item,
      monthlyActuals,
      actualTotal,
      percentage
    };
  }
  return item;
});
```

### 2. 季度完成数据自动计算

季度完成数据在渲染时从月度数据中自动计算:

```typescript
// Q1完成（1-3月）
const q1Completed = (item.monthlyActuals.m1 || 0) + 
                   (item.monthlyActuals.m2 || 0) + 
                   (item.monthlyActuals.m3 || 0);

// Q2完成（4-6月）
const q2Completed = (item.monthlyActuals.m4 || 0) + 
                   (item.monthlyActuals.m5 || 0) + 
                   (item.monthlyActuals.m6 || 0);

// Q3完成（7-9月）
const q3Completed = (item.monthlyActuals.m7 || 0) + 
                   (item.monthlyActuals.m8 || 0) + 
                   (item.monthlyActuals.m9 || 0);

// Q4完成（10-12月）
const q4Completed = (item.monthlyActuals.m10 || 0) + 
                   (item.monthlyActuals.m11 || 0) + 
                   (item.monthlyActuals.m12 || 0);
```

## 数据流向

```
预算执行表 (budget_execution)
  ↓
  提供月度实际完成数据 (monthlyActuals)
  ↓
年度预算表 (annual_budgets)
  ↓
  读取月度数据并计算:
  - 季度完成 (q1/q2/q3/q4)
  - 年度实际总计 (actualTotal)
  - 完成率 (percentage)
```

## 测试验证

### 验证步骤
1. 打开年度预算管理页面
2. 切换到有预算执行数据的年份 (如2025年)
3. 检查季度完成列的数据
4. 对比预算执行表中的月度数据

### 预期结果
- ✅ 季度完成数据应该等于预算执行表中对应月份的总和
- ✅ 年度实际总计应该等于12个月的总和
- ✅ 完成率应该自动重新计算

## 影响范围

### 修改的文件
- `components/pages/AnnualBudgetManagement.tsx`

### 影响的功能
- 年度预算管理的数据加载逻辑
- 季度完成数据的显示
- 年度实际总计的计算
- 完成率的计算

### 不影响的功能
- 季度预算的输入和保存
- 年度预算的计算
- 科目配置的加载
- 其他表格功能

## 注意事项

1. **数据一致性**: 确保预算执行表和年度预算表的科目ID和项目名称完全一致
2. **月度数据格式**: 月度数据必须是 `{m1, m2, ..., m12}` 格式
3. **计算精度**: 所有金额保留2位小数
4. **性能优化**: 使用 `Map` 进行数据映射查找,时间复杂度为 O(1)

## 后续优化建议

1. 可以考虑在预算执行表中直接存储季度总计,减少重复计算
2. 可以添加数据验证,确保预算执行表数据的准确性
3. 可以添加数据同步状态提示,告知用户数据来源

## 完成状态

✅ **已完成** - 年度预算表现在从预算执行表中读取季度完成数据
