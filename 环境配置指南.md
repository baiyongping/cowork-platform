# 际华协同办公平台 - 环境配置指南

## 📋 环境检查结果

### ✅ 已安装并正常运行

| 工具 | 版本 | 状态 |
|------|------|------|
| **Node.js** | v24.12.0 | ✅ 正常 |
| **npm** | 11.6.2 | ✅ 正常 |
| **项目依赖** | 520 packages | ✅ 已安装 |
| **开发服务器** | Vite 5.0.8 | ✅ 可用 |
| **winget** | v1.12.350 | ✅ 可用 |

### 🔄 正在安装

| 工具 | 状态 | 操作 |
|------|------|------|
| **Git** | 🔄 后台安装中 | 安装完成后需要**重启终端**才能使用 |

---

## 🔧 完整配置步骤

### 方法一：使用配置脚本（推荐）

在项目根目录打开 **PowerShell**（以管理员身份），运行：

```powershell
# 允许执行脚本
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# 运行配置脚本
.\环境配置.ps1
```

或使用批处理脚本：

```cmd
.\环境配置.bat
```

### 方法二：手动配置

#### 1. 等待 Git 安装完成

Git 正在通过 winget 后台安装，通常需要 1-2 分钟。

**验证安装**：
```bash
# 重启终端后运行
git --version
```

**如果安装失败**，可手动安装：
- 下载地址：https://git-scm.com/download/win
- 安装后重启终端

#### 2. 修复安全漏洞

项目检测到 **7 个安全漏洞**（2 个中等，5 个高危）。

**运行修复**：
```bash
cd d:\project\cowork12-21
npm audit fix
```

**查看详细报告**：
```bash
npm audit
```

**强制修复（可能有破坏性变更）**：
```bash
npm audit fix --force
```

#### 3. 验证环境

```bash
# 检查所有工具
node --version
npm --version
git --version

# 检查项目依赖
npm list --depth=0
```

---

## 🚀 启动开发

环境配置完成后，可以开始开发：

### 开发模式
```bash
npm run dev
```
访问：http://localhost:5173

### 生产构建
```bash
npm run build
```
构建输出：`dist/` 目录

### 预览生产版本
```bash
npm run preview
```

### 代码检查
```bash
npm run lint
```

### 类型检查
```bash
npm run type-check
```

---

## 📦 项目依赖说明

### 主要依赖

- **React 18.2.0** - UI 框架
- **TypeScript 5.2.2** - 类型系统
- **Vite 5.0.8** - 构建工具
- **Tailwind CSS 4.0.0** - CSS 框架
- **CloudBase SDK** - 腾讯云开发
- **Radix UI** - 无障碍组件库
- **Zustand** - 状态管理
- **React Hot Toast** - 通知组件

### 开发依赖

- **ESLint** - 代码检查
- **TypeScript ESLint** - TS 代码检查
- **Autoprefixer** - CSS 自动前缀
- **PostCSS** - CSS 处理

---

## ⚠️ 注意事项

### 安全漏洞

当前项目有一些依赖的安全漏洞，主要来自：
- `rimraf@3.0.2`（已过时，建议升级到 v4+）
- 其他传递依赖

**建议操作**：
1. 运行 `npm audit` 查看详情
2. 运行 `npm audit fix` 自动修复
3. 对于无法自动修复的，考虑手动升级依赖

### Git 配置

首次使用 Git 需要配置用户信息：

```bash
git config --global user.name "你的名字"
git config --global user.email "你的邮箱"
```

### Node.js 版本

项目要求：
- Node.js >= 18.0.0 ✅（当前 v24.12.0）
- npm >= 9.0.0 ✅（当前 v11.6.2）

---

## 🔍 故障排查

### 问题：npm install 失败

**解决方案**：
```bash
# 清除缓存
npm cache clean --force

# 删除 node_modules
rmdir /s /q node_modules

# 删除 package-lock.json
del package-lock.json

# 重新安装
npm install
```

### 问题：Git 命令不可用

**解决方案**：
1. 确认 Git 安装完成（检查"添加/删除程序"）
2. 重启终端（必须）
3. 检查环境变量 PATH 是否包含 Git 路径
4. 如仍不可用，手动下载安装

### 问题：端口占用

如果 5173 端口被占用：

```bash
# Windows 查找占用进程
netstat -ano | findstr :5173

# 杀死进程（需要管理员权限）
taskkill /F /PID <进程ID>
```

---

## 📞 技术支持

如遇到问题，可以：

1. 查看项目 README.md
2. 检查项目 Issues
3. 联系技术团队

---

## ✅ 配置完成检查清单

完成环境配置后，确认以下项目：

- [ ] Node.js 版本正确（>= 18.0.0）
- [ ] npm 版本正确（>= 9.0.0）
- [ ] Git 已安装并可用
- [ ] 项目依赖已安装（node_modules 存在）
- [ ] 安全漏洞已修复（或已知晓）
- [ ] 开发服务器可以启动（npm run dev）
- [ ] 构建成功（npm run build）
- [ ] Git 用户信息已配置

---

**最后更新时间**：2025-12-26
**项目版本**：v3.4.0
