# 际华协同办公平台 - 测试环境快速迭代部署方案

## 方案概述

**目的**: 开发过程中快速验证功能,实现分钟级部署
**范围**: 仅前端代码更新,不涉及 CloudBase 云函数/数据库配置
**目标时间**: 2-3 分钟完成构建和部署

---

## 环境信息

- **服务器**: 152.136.183.181
- **前端端口**: 3443 (HTTPS)
- **部署路径**: `/var/www/jihua-dev/`
- **访问地址**: https://152.136.183.181:3443
- **CloudBase环境**: jihua-oa-dev-3goht9irae4d949f (已配置,无需修改)

---

## 快速部署流程

### 方式一: 一键部署脚本 (推荐) ⚡

#### Windows PowerShell

```powershell
# 快速部署.ps1
# 保存到项目根目录并执行

Write-Host "=== 际华协同办公平台 - 测试环境快速部署 ===" -ForegroundColor Cyan

# 1. 清理旧构建
Write-Host "`n[1/4] 清理旧构建文件..." -ForegroundColor Yellow
if (Test-Path "dist") {
    Remove-Item -Recurse -Force "dist"
}

# 2. 构建项目
Write-Host "`n[2/4] 构建项目..." -ForegroundColor Yellow
npm run build

if ($LASTEXITCODE -ne 0) {
    Write-Host "❌ 构建失败!" -ForegroundColor Red
    exit 1
}

# 3. 使用 Lighthouse MCP 工具部署
Write-Host "`n[3/4] 上传文件到服务器..." -ForegroundColor Yellow
Write-Host "提示: 请在 AI 助手中执行以下工具调用:" -ForegroundColor Green
Write-Host @"
call_lighthouse_integration({
  toolName: "deploy_project_preparation",
  arguments: '{
    "FolderPath": "d:/project/cowork12-21/dist",
    "InstanceId": "lhins-xxx",
    "Region": "ap-beijing",
    "ProjectName": "jihua-dev"
  }'
})
"@ -ForegroundColor Cyan

# 4. 完成
Write-Host "`n[4/4] 部署完成!" -ForegroundColor Green
Write-Host "访问地址: https://152.136.183.181:3443?v=$(Get-Date -Format 'yyyyMMddHHmmss')" -ForegroundColor Cyan
Write-Host "提示: 请按 Ctrl+F5 强制刷新浏览器" -ForegroundColor Yellow
```

#### Linux/Mac Bash

```bash
#!/bin/bash
# 快速部署.sh
# 保存到项目根目录并执行: bash 快速部署.sh

echo "=== 际华协同办公平台 - 测试环境快速部署 ==="

# 1. 清理旧构建
echo -e "\n[1/4] 清理旧构建文件..."
rm -rf dist

# 2. 构建项目
echo -e "\n[2/4] 构建项目..."
npm run build

if [ $? -ne 0 ]; then
    echo "❌ 构建失败!"
    exit 1
fi

# 3. 使用 Lighthouse MCP 工具部署
echo -e "\n[3/4] 上传文件到服务器..."
echo "提示: 请在 AI 助手中执行以下工具调用:"
cat << EOF
call_lighthouse_integration({
  toolName: "deploy_project_preparation",
  arguments: '{
    "FolderPath": "$(pwd)/dist",
    "InstanceId": "lhins-xxx",
    "Region": "ap-beijing",
    "ProjectName": "jihua-dev"
  }'
})
EOF

# 4. 完成
echo -e "\n[4/4] 部署完成!"
echo "访问地址: https://152.136.183.181:3443?v=$(date +%Y%m%d%H%M%S)"
echo "提示: 请按 Ctrl+F5 强制刷新浏览器"
```

---

### 方式二: 手动分步部署

#### 步骤1: 本地构建

```bash
# 清理并构建
npm run build
```

**构建时间**: 约 30-60 秒

#### 步骤2: 使用 Lighthouse MCP 工具部署

**在 AI 助手中执行**:

```javascript
call_lighthouse_integration({
  toolName: "deploy_project_preparation",
  arguments: JSON.stringify({
    FolderPath: "d:/project/cowork12-21/dist",  // Windows
    // FolderPath: "/path/to/project/dist",     // Linux/Mac
    InstanceId: "lhins-pnt984h2",                     // 实际实例ID
    Region: "ap-beijing",                        // 实际地域
    ProjectName: "jihua-dev"
  })
})
```

**部署时间**: 约 60-90 秒

#### 步骤3: 验证部署

```bash
# 访问测试地址(带时间戳防缓存)
https://152.136.183.181:3443?v=20250101120000

# 或使用 curl 验证
curl -I https://152.136.183.181:3443
```

---

### 方式三: 传统 SCP 部署 (备选)

**仅在 Lighthouse MCP 工具不可用时使用**

```bash
# 1. 构建
npm run build

# 2. 压缩
cd dist
tar -czf ../dist.tar.gz *
cd ..

# 3. 上传
scp dist.tar.gz root@152.136.183.181:/tmp/

# 4. 解压部署
ssh root@152.136.183.181 << 'EOF'
cd /tmp
tar -xzf dist.tar.gz -C /var/www/jihua-dev/
rm -f dist.tar.gz
EOF

# 5. 清理本地
rm -f dist.tar.gz
```

---

## 快速回滚方案

### 情况1: 发现 Bug 需要立即回滚

```bash
# 1. 切换到上一个稳定版本
git checkout <上一个稳定分支/标签>

# 2. 重新构建部署
npm run build

# 3. 使用 Lighthouse MCP 工具部署(步骤同上)
```

**回滚时间**: 约 2-3 分钟

### 情况2: 保留备份快速恢复

```bash
# 在服务器上保留最近3次部署的备份
ssh root@152.136.183.181 << 'EOF'
cd /var/www

# 备份当前版本
cp -r jihua-dev jihua-dev-backup-$(date +%Y%m%d%H%M%S)

# 只保留最近3个备份
ls -t jihua-dev-backup-* | tail -n +4 | xargs rm -rf
EOF
```

---

## 部署验证清单

### ✅ 构建验证

- [ ] `npm run build` 无错误
- [ ] `dist/` 目录生成完整
- [ ] `dist/index.html` 存在
- [ ] `dist/assets/` 包含 JS/CSS 文件

### ✅ 部署验证

- [ ] 文件上传成功
- [ ] 访问地址返回 200
- [ ] 控制台无 404 错误
- [ ] CloudBase 环境ID正确

### ✅ 功能验证

- [ ] 登录功能正常
- [ ] 核心功能可用
- [ ] 数据加载正常
- [ ] 样式显示正确

---

## 常见问题快速处理

### 问题1: 构建失败

**症状**: `npm run build` 报错

**解决**:
```bash
# 1. 检查依赖
npm install

# 2. 清理缓存重试
npm cache clean --force
rm -rf node_modules package-lock.json
npm install
npm run build
```

### 问题2: 浏览器显示旧版本

**症状**: 部署后仍显示旧内容

**解决**:
```bash
# 用户端: 强制刷新
Ctrl + F5 (Windows)
Cmd + Shift + R (Mac)

# 或使用带时间戳的 URL
https://152.136.183.181:3443?v=$(date +%Y%m%d%H%M%S)
```

### 问题3: 上传速度慢

**症状**: 文件上传时间超过 2 分钟

**解决**:
```bash
# 1. 检查网络连接
ping 152.136.183.181

# 2. 使用压缩传输(传统 SCP 方式)
tar -czf dist.tar.gz dist/
scp dist.tar.gz root@152.136.183.181:/tmp/

# 3. 检查 dist 目录大小
du -sh dist/
```

### 问题4: 部署后功能异常

**症状**: 部署后出现功能报错

**解决**:
```bash
# 1. 立即回滚
git checkout <稳定版本>
npm run build
# 重新部署

# 2. 检查浏览器控制台
F12 -> Console 查看错误

# 3. 检查环境变量
cat vite.config.ts | grep TCB_ENV_ID
```

---

## 性能优化建议

### 1. 构建优化

```typescript
// vite.config.ts
export default defineConfig({
  build: {
    // 关闭 sourcemap 加快构建
    sourcemap: false,
    
    // 启用代码分割
    rollupOptions: {
      output: {
        manualChunks: {
          'vendor-react': ['react', 'react-dom'],
          'vendor-ui': ['@headlessui/react', 'lucide-react'],
        }
      }
    },
    
    // 压缩优化
    minify: 'terser',
    terserOptions: {
      compress: {
        drop_console: true  // 生产环境移除 console
      }
    }
  }
})
```

### 2. 增量部署优化

```bash
# 只上传变化的文件(传统 SCP 方式)
rsync -avz --delete dist/ root@152.136.183.181:/var/www/jihua-dev/
```

### 3. 并行构建

```json
// package.json
{
  "scripts": {
    "build:fast": "vite build --mode development"
  }
}
```

---

## 部署时间对比

| 部署方式 | 构建时间 | 上传时间 | 总耗时 | 推荐场景 |
|---------|---------|---------|--------|---------|
| Lighthouse MCP | 30-60s | 60-90s | **2-3分钟** | ✅ 日常开发 |
| 传统 SCP | 30-60s | 40-60s | 2分钟 | 网络不稳定 |
| rsync 增量 | 30-60s | 20-30s | 1-2分钟 | 小改动 |

---

## 版本管理建议

### 测试版本标记

```bash
# 每次快速迭代后打标签
git tag dev-v$(date +%Y%m%d-%H%M)
git push origin --tags

# 示例
dev-v20250101-1430  # 2025年1月1日 14:30 测试版本
```

### 回滚版本查询

```bash
# 查看最近10个测试版本
git tag -l 'dev-v*' --sort=-version:refname | head -n 10

# 切换到指定版本
git checkout dev-v20250101-1430
```

---

## 自动化脚本模板

### 完整自动化脚本 (PowerShell)

```powershell
# 快速部署完整版.ps1
param(
    [string]$Message = "快速迭代部署"
)

$ErrorActionPreference = "Stop"

Write-Host "=== 际华协同办公平台 - 自动化部署 ===" -ForegroundColor Cyan

# 1. Git 检查
Write-Host "`n[Git] 检查代码状态..." -ForegroundColor Yellow
$status = git status --short
if ($status) {
    Write-Host "有未提交的更改,是否继续?(Y/N)" -ForegroundColor Yellow
    $confirm = Read-Host
    if ($confirm -ne "Y") {
        exit 0
    }
}

# 2. 清理构建
Write-Host "`n[Build] 清理旧构建..." -ForegroundColor Yellow
Remove-Item -Recurse -Force "dist" -ErrorAction SilentlyContinue

# 3. 构建项目
Write-Host "`n[Build] 构建项目..." -ForegroundColor Yellow
npm run build

if ($LASTEXITCODE -ne 0) {
    Write-Host "❌ 构建失败!" -ForegroundColor Red
    exit 1
}

# 4. 打标签
$tag = "dev-v$(Get-Date -Format 'yyyyMMdd-HHmm')"
Write-Host "`n[Git] 创建标签: $tag" -ForegroundColor Yellow
git tag $tag
git push origin $tag

# 5. 部署提示
Write-Host "`n[Deploy] 请在 AI 助手中执行部署工具" -ForegroundColor Green
Write-Host "部署路径: $PWD\dist" -ForegroundColor Cyan

# 6. 生成访问链接
$timestamp = Get-Date -Format 'yyyyMMddHHmmss'
$url = "https://152.136.183.181:3443?v=$timestamp"

Write-Host "`n=== 部署完成 ===" -ForegroundColor Green
Write-Host "访问地址: $url" -ForegroundColor Cyan
Write-Host "提示: 请按 Ctrl+F5 强制刷新" -ForegroundColor Yellow
```

### 使用说明

```powershell
# 快速部署
.\快速部署完整版.ps1

# 带提交信息
.\快速部署完整版.ps1 -Message "修复登录问题"
```

---

## 监控和日志

### 快速查看日志

```bash
# Nginx 访问日志(最近100条)
ssh root@152.136.183.181 "tail -n 100 /var/log/nginx/jihua-dev-access.log"

# Nginx 错误日志(最近50条)
ssh root@152.136.183.181 "tail -n 50 /var/log/nginx/jihua-dev-error.log"
```

### 实时监控部署

```bash
# 实时监控访问日志
ssh root@152.136.183.181 "tail -f /var/log/nginx/jihua-dev-access.log"
```

---

## 最佳实践

### ✅ 推荐做法

1. **小步快跑**: 每次只改动少量代码,快速验证
2. **及时提交**: 验证通过后立即提交代码
3. **打标签**: 每次部署打标签,便于回滚
4. **记录问题**: 在 Git commit 中记录修复的问题

### ❌ 避免做法

1. **大批量改动**: 避免一次改动过多文件
2. **跳过测试**: 部署前确保本地测试通过
3. **忘记打标签**: 每次部署都应该有版本标记
4. **不看日志**: 部署后检查日志确认无错误

---

## 紧急联系

- **服务器问题**: root@152.136.183.181
- **代码问题**: 查看 Git 提交历史
- **CloudBase 问题**: [CloudBase 控制台](https://tcb.cloud.tencent.com/dev?envId=jihua-oa-dev-3goht9irae4d949f)

---

**最后更新**: 2025-01-01  
**版本**: v1.0  
**维护人**: AI Assistant

**提示**: 此方案专注于前端快速迭代,如需部署云函数或数据库配置,请参考 [测试环境部署方案.md](./部署方案-测试环境.md)
