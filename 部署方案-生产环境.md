# 际华协同办公平台 - 生产环境部署方案

## 📋 环境信息

| 项目 | 配置 |
|-----|------|
| **环境名称** | 生产环境 (Production) |
| **服务器地址** | 152.136.183.181 |
| **访问域名** | https://jihuadz.xin |
| **访问端口** | HTTPS 443 (通过Nginx代理) |
| **部署路径** | /var/www/jihua-deploy/ |
| **Docker端口** | 蓝环境:3000 / 绿环境:3003 |
| **CloudBase环境** | cowork-9gg9oocb516be5fb |
| **用途** | 正式对外服务 |
| **更新频率** | 每月1-2次 (需经理审批) |

---

## 🎯 部署特点

- ✅ **零停机部署**: 蓝绿部署策略
- ✅ **强制审批**: 必须经过经理审批
- ✅ **生产数据**: 使用生产CloudBase环境
- ✅ **完整测试**: 必须在测试环境验证通过
- ✅ **自动回滚**: 健康检查失败自动回滚
- ⚠️ **严格验证**: 不允许有任何bug

---

## 📦 部署前准备

### 1. 审批流程

```markdown
【生产环境部署申请】

申请人: xxx
申请时间: YYYY-MM-DD HH:mm
部署版本: v3.x.x
Git分支: main (必须)
Git Tag: v3.x.x (必须打标签)

改动内容:
- [ ] 新增xxx功能
- [ ] 修复xxx bug
- [ ] 优化xxx性能

测试环境验证:
- [x] 功能测试通过
- [x] 回归测试通过
- [x] 性能测试通过
- [x] UAT验收通过
- [x] 无严重bug

风险评估:
- 影响范围: xxx
- 回滚方案: 已准备
- 预计停机时间: 0分钟 (蓝绿部署)

部署计划:
- 部署时间: YYYY-MM-DD HH:mm (业务低峰期)
- 部署人员: xxx
- 值班人员: xxx
- 回滚负责人: xxx

审批人签字:
项目经理: ____________  日期: ________
技术总监: ____________  日期: ________ (重大变更需要)
```

### 2. 部署窗口选择

**最佳部署时间**:
- 工作日: 21:00 - 23:00 (用户访问量最低)
- 周末: 任意时间
- ⚠️ 避免: 工作时间 (9:00 - 18:00)
- ⚠️ 避免: 月末/季末/年末 (业务繁忙期)

### 3. 本地环境检查

```powershell
# Windows (PowerShell)
node -v          # 确认 >= 18.0.0
npm -v           # 确认 >= 9.0.0

# 切换到main分支
git checkout main
git pull origin main

# 确认分支干净
git status
# 输出: On branch main
#      Your branch is up to date with 'origin/main'.
#      nothing to commit, working tree clean

# 查看最新提交
git log -1

# 打版本标签 (如果还没打)
git tag v3.x.x
git push origin v3.x.x
```

### 4. CloudBase环境检查

```javascript
// 确认 src/config/cloudbase.ts 使用生产环境
export const config = {
  env: 'cowork-9gg9oocb516be5fb',  // ✓ 生产环境
  region: 'ap-shanghai'
}
```

---

## 🚀 完整部署流程

### 步骤1: 本地清理构建 (Windows)

```powershell
# 确认在main分支
git branch
# 输出: * main

# 执行清理构建脚本
.\build-clean.ps1

# 输出示例:
# ========================================
# 际华协同办公平台 - 清理构建脚本
# ========================================
# [1/5] 删除旧的 dist 目录...
# ✓ 已删除旧的 dist 目录
# [2/5] 清理 Vite 缓存...
# ✓ Vite 缓存已清理
# [3/5] 检查依赖变更...
# ✓ package.json 未变更
# [4/5] 验证环境变量...
# ✓ 环境变量验证通过
# [5/5] 开始构建...
# ✓ 构建完成!
# ========================================
# 📦 dist 目录大小: 3.45 MB
# 📄 文件总数: 23
# 🏷️ 构建版本: 20251231-201535
```

**⚠️ 构建验证检查清单 (CRITICAL):**
- [ ] 确认Git分支为main
- [ ] 确认Git工作区干净
- [ ] 确认已打版本标签 (v3.x.x)
- [ ] 确认CloudBase环境ID为生产环境
- [ ] 构建过程无警告/错误
- [ ] dist/ 目录已生成
- [ ] dist/index.html 存在
- [ ] dist/assets/ 目录存在
- [ ] 文件数量 > 10个
- [ ] 总大小 > 1MB
- [ ] 查看dist/index.html确认版本正确

---

### 步骤2: 上传到服务器

```powershell
# 1. 压缩dist目录 (带版本标识)
$timestamp = Get-Date -Format "yyyyMMdd-HHmmss"
$version = "v3.x.x"  # 替换为实际版本号
$archive = "jihua-prod-dist_${version}_$timestamp.tar.gz"

# 使用tar压缩
tar -czf $archive -C dist .

# 2. 验证压缩包
if (Test-Path $archive) {
    Write-Host "✓ 压缩包创建成功: $archive" -ForegroundColor Green
    $size = (Get-Item $archive).Length / 1MB
    Write-Host "📦 压缩包大小: $([math]::Round($size, 2)) MB" -ForegroundColor Cyan
} else {
    Write-Host "✗ 压缩包创建失败!" -ForegroundColor Red
    exit 1
}

# 3. 上传到服务器
Write-Host "开始上传到服务器..." -ForegroundColor Yellow
scp $archive root@152.136.183.181:/root/

# 4. 验证上传
Write-Host "验证上传是否成功..." -ForegroundColor Yellow
ssh root@152.136.183.181 "ls -lh /root/$archive"
```

---

### 步骤3: 服务器端蓝绿部署

**生产环境使用增强版蓝绿部署脚本 (防缓存版 v2.0)**

```bash
# ========================================
# 服务器端操作
# ========================================

# 1. SSH登录服务器
ssh root@152.136.183.181

# 2. 进入部署目录
cd /var/www/jihua-deploy

# 3. 移动上传的文件到dist/
mv /root/jihua-prod-dist_*.tar.gz ./jihua-prod-dist.tar.gz
rm -rf dist
mkdir dist
tar -xzf jihua-prod-dist.tar.gz -C ./dist/

# 4. 验证文件完整性
echo "=========================================="
echo "验证文件"
echo "=========================================="
ls -lh dist/
find dist/ -type f | wc -l
du -sh dist/

# 如果文件数量 < 10,停止部署
file_count=$(find dist/ -type f | wc -l)
if [ $file_count -lt 10 ]; then
    echo "✗ 文件数量异常: $file_count"
    exit 1
fi

# 5. 执行增强版蓝绿部署
./blue-green-deploy.sh deploy

# 部署输出示例:
# ======================================
# 开始增强版蓝绿部署
# ======================================
# [INFO] ======================================
# [INFO] 清理旧部署文件
# [INFO] ======================================
# [INFO] 备份当前 dist 到: dist-backup-20251231-201545
# [INFO] ✓ 旧 dist 目录已删除
# [INFO] ✓ 旧备份已清理 (保留最近3个)
# [INFO] ✓ Docker 缓存已清理
# [INFO] ======================================
# [INFO] 验证上传文件
# [INFO] ======================================
# [INFO] 文件总数: 23
# [INFO] 总大小: 3.5M
# [INFO] 构建版本: 20251231-201535
# [INFO] ✓ 文件验证通过
# [INFO] 当前活跃环境: blue
# [INFO] 目标部署环境: green (端口:3003)
# [INFO] ======================================
# [INFO] 构建 Docker 镜像 (无缓存)
# [INFO] ======================================
# [INFO] Step 1/5 : FROM nginx:alpine
# [INFO] ...
# [INFO] ✓ Docker 镜像构建完成
# [INFO] 启动新容器: jihua-prod-green (端口:3003)...
# [INFO] 健康检查中...
# [INFO] ✓ 健康检查通过
# [INFO] 切换Nginx到green环境...
# [INFO] ✓ Nginx配置已更新
# [INFO] ✓ Nginx已重新加载
# [INFO] 等待30秒后清理旧环境...
# [INFO] 停止旧容器: jihua-prod-blue...
# [INFO] 删除旧镜像: jihua-prod:blue...
# [INFO] ======================================
# [INFO] ✓ 部署完成!
# [INFO] ======================================
# [INFO] 活跃环境: green
# [INFO] 访问地址: https://jihuadz.xin
# [INFO]
# [INFO] 容器状态:
# [INFO] NAMES              STATUS              PORTS
# [INFO] jihua-prod-green   Up 2 minutes        0.0.0.0:3003->80/tcp
# [INFO]
# [WARN] ⚠ 重要提示:
# [WARN] 1. 请使用 Ctrl+Shift+R (硬刷新) 清理浏览器缓存
# [WARN] 2. 或使用隐私/无痕模式访问验证
# [WARN] 3. CDN缓存约需3-5分钟更新
```

**⚠️ 部署过程监控:**

在部署过程中,可以在另一个终端实时监控:
```bash
# 打开新的SSH连接
ssh root@152.136.183.181

# 实时查看Docker容器状态
watch -n 1 'docker ps | grep jihua-prod'

# 实时查看Nginx日志
tail -f /var/log/nginx/error.log

# 实时查看容器日志
docker logs -f jihua-prod-green
```

---

### 步骤4: 验证部署结果

#### 4.1 服务器端验证

```bash
# 1. 检查容器状态
docker ps | grep jihua-prod

# 应该看到新环境的容器在运行:
# jihua-prod-green   Up 5 minutes   0.0.0.0:3003->80/tcp

# 2. 检查Nginx配置
cat /etc/nginx/conf.d/jihua.conf | grep proxy_pass

# 应该看到:
# proxy_pass http://127.0.0.1:3003;  (绿环境)

# 3. 健康检查
curl -I https://jihuadz.xin

# 应该返回: HTTP/2 200

# 4. 检查Docker日志
docker logs --tail 50 jihua-prod-green

# 应该无错误日志
```

#### 4.2 浏览器端验证 (CRITICAL)

**方法1: 硬刷新 (必须)**
```
Windows/Linux: Ctrl + Shift + R
macOS: Cmd + Shift + R

连续按3次确保刷新成功
```

**方法2: 清除所有缓存 (强烈推荐)**
```
Chrome:
1. F12 打开开发者工具
2. 右键点击刷新按钮
3. 选择 "清空缓存并硬性重新加载"
4. 或者: Ctrl+Shift+Delete → 清除浏览数据
```

**方法3: 无痕模式验证 (强制要求)**
```
Chrome: Ctrl + Shift + N
Firefox: Ctrl + Shift + P
Edge: Ctrl + Shift + N

⚠️ 必须在无痕模式下完整验证一遍
这样可以确保没有任何缓存影响
```

**方法4: 不同浏览器交叉验证**
```
在以下浏览器都测试一遍:
- Chrome
- Edge
- Firefox
- Safari (如果有Mac)
```

#### 4.3 完整功能验证清单

```
访问地址: https://jihuadz.xin

【生产环境核心功能验证清单】(必须100%通过)

✓ 1. 页面基础
  - [ ] 页面能正常打开
  - [ ] 无白屏
  - [ ] 无404错误
  - [ ] 静态资源加载正常 (F12 Network查看)
  - [ ] 控制台无错误 (F12 Console查看)

✓ 2. 登录/注册
  - [ ] 邮箱登录成功
  - [ ] 微信扫码登录成功 (如启用)
  - [ ] 密码重置功能正常
  - [ ] 退出登录正常
  - [ ] 会话保持正常

✓ 3. 核心业务功能 (抽查)
  - [ ] 任务列表加载正常
  - [ ] 创建新任务成功
  - [ ] 商机管理正常
  - [ ] 项目管理正常
  - [ ] 数据显示正确

✓ 4. CloudBase连接
  - [ ] 数据库查询正常
  - [ ] 数据写入正常
  - [ ] 实时更新正常 (如有)
  - [ ] 文件上传正常 (如有)

✓ 5. 性能检查
  - [ ] 首页加载时间 < 3秒
  - [ ] 列表加载时间 < 2秒
  - [ ] API响应时间 < 1秒
  - [ ] 无明显卡顿

✓ 6. 多用户测试
  - [ ] 使用不同角色账号登录测试
  - [ ] 权限控制正常
  - [ ] 数据隔离正常

✓ 7. 兼容性检查
  - [ ] Chrome浏览器正常
  - [ ] Edge浏览器正常
  - [ ] Firefox浏览器正常
  - [ ] 移动端访问正常 (如需要)
```

---

### 步骤5: 部署后监控 (前2小时)

部署完成后,需要持续监控2小时:

```bash
# 1. 实时监控容器状态
watch -n 5 'docker ps | grep jihua-prod'

# 2. 实时监控Docker日志
docker logs -f --tail 100 jihua-prod-green

# 3. 实时监控Nginx日志
tail -f /var/log/nginx/access.log
tail -f /var/log/nginx/error.log

# 4. 监控服务器资源
htop
# 检查CPU和内存使用率

# 5. 定时健康检查
watch -n 10 'curl -I https://jihuadz.xin'
```

---

## ⚠️ 常见问题处理

### 问题1: 部署后用户反馈看到旧版本

**原因**: 浏览器缓存 / CDN缓存

**解决方案**:
```
1. 立即通知所有用户:
   "系统已更新到v3.x.x,请按Ctrl+Shift+R刷新页面"
   
2. 如果用户仍看到旧版本:
   - 指导清除浏览器缓存
   - 或使用无痕模式访问
   
3. 等待CDN缓存过期 (3-5分钟)

4. 如果仍有问题,检查Nginx缓存配置
```

### 问题2: 健康检查失败,部署自动回滚

**排查步骤**:
```bash
# 1. 查看容器日志
docker logs jihua-prod-green

# 2. 检查端口监听
netstat -nltp | grep 3003

# 3. 手动测试容器
curl -I http://localhost:3003

# 4. 检查Dockerfile配置
cat Dockerfile

# 5. 检查dist文件
docker exec jihua-prod-green ls -la /usr/share/nginx/html

# 6. 如果发现问题,修复后重新部署
./blue-green-deploy.sh deploy
```

### 问题3: Nginx切换失败

**排查步骤**:
```bash
# 1. 检查Nginx配置语法
nginx -t

# 2. 查看Nginx错误日志
tail -100 /var/log/nginx/error.log

# 3. 手动切换Nginx (如果脚本失败)
# 编辑配置
vim /etc/nginx/conf.d/jihua.conf

# 修改 proxy_pass 行:
# proxy_pass http://127.0.0.1:3003;  (绿环境)

# 测试配置
nginx -t

# 重新加载
nginx -s reload

# 4. 验证
curl -I https://jihuadz.xin
```

### 问题4: 新版本有严重Bug需要紧急回滚

**快速回滚步骤**:
```bash
# 方案1: 使用蓝绿部署脚本回滚 (推荐)
cd /var/www/jihua-deploy
./blue-green-deploy.sh rollback blue

# 输出:
# [INFO] 回滚到 blue 环境...
# [INFO] 切换Nginx到blue环境...
# [INFO] ✓ 回滚完成!

# 方案2: 手动回滚
# 1. 切换Nginx到旧环境
vim /etc/nginx/conf.d/jihua.conf
# 修改: proxy_pass http://127.0.0.1:3000;  (蓝环境)
nginx -s reload

# 2. 停止新环境容器
docker stop jihua-prod-green

# 3. 启动旧环境容器 (如果已停止)
docker start jihua-prod-blue

# 4. 验证
curl -I https://jihuadz.xin
docker ps | grep jihua-prod

# 预计回滚时间: 2-3分钟
```

---

## 📊 部署检查清单 (CRITICAL)

### 部署前检查 (必须100%通过)
- [ ] 获得项目经理审批
- [ ] 获得技术总监审批 (重大变更)
- [ ] Git分支为main
- [ ] Git工作区干净
- [ ] 已打版本标签 (v3.x.x)
- [ ] CloudBase环境ID为生产环境
- [ ] 测试环境验证100%通过
- [ ] 无未解决的严重bug
- [ ] 回滚方案已准备
- [ ] 值班人员已到位
- [ ] 部署时间在业务低峰期

### 部署中检查
- [ ] 文件压缩无错误
- [ ] 文件上传成功
- [ ] 服务器文件完整性验证通过
- [ ] Docker镜像构建成功 (无缓存)
- [ ] 新容器启动成功
- [ ] 健康检查通过
- [ ] Nginx切换成功
- [ ] 旧容器已停止

### 部署后检查 (必须100%通过)
- [ ] 页面能正常访问
- [ ] 无痕模式验证通过
- [ ] 多浏览器验证通过
- [ ] 控制台无错误
- [ ] 核心功能验证100%通过
- [ ] CloudBase连接正常
- [ ] 性能指标正常
- [ ] 无用户投诉
- [ ] 监控2小时无异常

---

## 📝 部署报告模板

```markdown
## 生产环境部署报告 - v3.x.x

### 基本信息
- **部署时间**: 2025-12-31 20:15 - 20:35
- **部署版本**: v3.x.x
- **Git Commit**: abc123def456
- **部署人员**: xxx
- **审批人员**: xxx (项目经理)
- **值班人员**: xxx
- **部署方式**: 蓝绿部署 (blue → green)

### 改动内容
1. 新增xxx功能
2. 修复xxx严重bug
3. 优化xxx性能

### 部署过程
- 20:15 开始构建
- 20:17 上传文件
- 20:20 执行蓝绿部署
- 20:25 健康检查通过
- 20:26 Nginx切换完成
- 20:30 清理旧环境
- 20:35 部署完成

### 验证结果
核心功能验证:
- [x] 登录/注册: 正常
- [x] 用户管理: 正常
- [x] 任务管理: 正常
- [x] 商机管理: 正常
- [x] 项目管理: 正常
- [x] 目标管理: 正常
- [x] 预算管理: 正常
- [x] 问题管理: 正常
- [x] 消息通知: 正常
- [x] 工作台: 正常

性能指标:
- 首页加载: 1.8s (✓ < 3s)
- 列表加载: 0.9s (✓ < 2s)
- API响应: 380ms (✓ < 1s)

浏览器兼容性:
- [x] Chrome: 正常
- [x] Edge: 正常
- [x] Firefox: 正常
- [x] Mobile: 正常

### 监控结果
监控时间: 20:35 - 22:35 (2小时)
- 容器状态: 稳定运行
- 错误日志: 无
- 用户反馈: 无异常
- 性能指标: 正常

### 用户反馈
- 共收到X条反馈
- 正面反馈: X条
- 问题反馈: 0条

### 遗留问题
- 无

### 总结
✅ 部署成功,系统运行稳定,无异常。

签字: ____________  日期: ________
```

---

## 🎯 生产环境最佳实践

### 1. 部署前准备
- ✅ 必须在测试环境100%验证通过
- ✅ 必须获得正式审批
- ✅ 必须选择业务低峰期
- ✅ 必须准备回滚方案
- ✅ 必须安排值班人员
- ⚠️ 不要在业务繁忙期部署
- ⚠️ 不要在下班后无人值班时部署

### 2. 部署中监控
- ✅ 实时监控部署过程
- ✅ 立即处理异常情况
- ✅ 保持沟通渠道畅通
- ⚠️ 不要同时部署多个服务

### 3. 部署后监控
- ✅ 持续监控2小时
- ✅ 及时响应用户反馈
- ✅ 记录所有异常情况
- ✅ 准备随时回滚
- ⚠️ 不要部署完就离开

### 4. 沟通协作
- ✅ 提前通知相关人员
- ✅ 部署完成通知用户
- ✅ 及时反馈部署结果
- ✅ 记录完整部署日志

---

## 📞 应急联系方式

### 技术团队
- **项目经理**: [姓名] - [电话]
- **技术总监**: [姓名] - [电话]
- **运维负责人**: [姓名] - [电话]
- **开发负责人**: [姓名] - [电话]

### 外部支持
- **服务器供应商**: [联系方式]
- **CloudBase技术支持**: [工单系统]
- **域名DNS服务**: [联系方式]

### 应急处理流程
```
1. 发现严重问题 → 立即通知项目经理
2. 评估影响范围 → 决定是否回滚
3. 执行回滚操作 → 验证系统恢复
4. 分析问题原因 → 制定修复方案
5. 修复后重新部署
```

---

## 📌 快速命令参考

```powershell
# ============ 本地构建 ============
git checkout main
git pull origin main
git status  # 确认工作区干净
git tag v3.x.x  # 打版本标签
git push origin v3.x.x
.\build-clean.ps1

# ============ 压缩上传 ============
$timestamp = Get-Date -Format "yyyyMMdd-HHmmss"
$version = "v3.x.x"
$archive = "jihua-prod-dist_${version}_$timestamp.tar.gz"
tar -czf $archive -C dist .
scp $archive root@152.136.183.181:/root/
```

```bash
# ============ 服务器部署 ============
ssh root@152.136.183.181
cd /var/www/jihua-deploy
mv /root/jihua-prod-dist_*.tar.gz ./jihua-prod-dist.tar.gz
rm -rf dist && mkdir dist
tar -xzf jihua-prod-dist.tar.gz -C ./dist/
./blue-green-deploy.sh deploy

# ============ 健康检查 ============
curl -I https://jihuadz.xin
docker ps | grep jihua-prod
docker logs --tail 50 jihua-prod-green

# ============ 紧急回滚 ============
./blue-green-deploy.sh rollback blue
# 或
./blue-green-deploy.sh rollback green

# ============ 查看状态 ============
./blue-green-deploy.sh status
```

---

## 📅 更新记录

| 版本 | 日期 | 更新内容 | 审批人 |
|-----|------|---------|-------|
| v2.0 | 2025-12-31 | 整合防缓存蓝绿部署方案 | xxx |
| v1.0 | 2025-12-15 | 初始版本 | xxx |

---

**⚠️ 严肃提醒**: 生产环境部署事关重大,必须严格遵守所有流程和规范,确保万无一失! 🔐
