# 数据完整性验证指南

## 概述

本指南介绍如何验证和修复际华协同办公平台的模块数据完整性，确保所有模块的 `parentId` 引用正确，层级结构清晰。

## 验证内容

### 1. 模块数据完整性检查
- ✅ 所有模块的 `parentId` 是否有效
- ✅ 顶级模块的 `parentId` 是否为 null
- ✅ 子模块的父模块是否存在
- ✅ 模块层级是否正确（一级/二级）
- ✅ 是否存在循环引用
- ✅ 排序字段是否合理

### 2. 标准模块配置

#### 一级模块（parentId: null）
```javascript
{
  _id: '工作台',
  name: '工作台',
  path: '/dashboard',
  parentId: null,
  level: 1,
  order: 1
}
```

#### 二级模块（parentId: 父模块ID）
```javascript
{
  _id: '商机管理',
  name: '商机管理',
  path: '/opportunities',
  parentId: '销售管理',
  level: 2,
  order: 1
}
```

## 验证方法

### 方法一：在线验证（推荐）

1. **访问验证页面**
   ```
   https://152.136.183.181:3443/test-data-integrity.html
   或
   https://jihuadz.xin/test-data-integrity.html
   ```

2. **执行验证**
   - 点击"验证数据完整性"按钮
   - 查看检查结果
   - 如果发现问题，点击"修复数据问题"
   - 重新验证确认修复成功

3. **查看验证报告**
   - ✅ 绿色：所有检查通过
   - ⚠️ 黄色：发现问题但可自动修复
   - ❌ 红色：发现严重问题需要手动处理

### 方法二：云函数验证

使用 CloudBase 控制台调用云函数：

```javascript
// 1. 进入云函数控制台
// https://tcb.cloud.tencent.com/dev?envId=cowork-9gg9oocb516be5fb#/scf

// 2. 选择 verify-module-integrity 函数

// 3. 测试调用
{
  "action": "verify"
}

// 4. 修复问题（如果验证失败）
{
  "action": "repair"
}
```

### 方法三：本地脚本验证

```powershell
# 1. 进入项目目录
cd d:/project/cowork12-21

# 2. 安装依赖（如果尚未安装）
npm install

# 3. 执行验证脚本
node scripts/verify-module-data-integrity.js
```

## 常见问题修复

### 问题 1: parentId 引用不存在的模块

**现象**：
```javascript
{
  name: '商机管理',
  parentId: '不存在的父模块ID'
}
```

**修复**：
```javascript
// 如果是二级模块，找到正确的父模块ID
{
  name: '商机管理',
  parentId: '销售管理'  // 修复为正确的父模块ID
}

// 如果是一级模块
{
  name: '商机管理',
  parentId: null  // 设置为 null
}
```

### 问题 2: 一级模块的 parentId 不为 null

**现象**：
```javascript
{
  name: '工作台',
  level: 1,
  parentId: 'some-id'  // ❌ 错误
}
```

**修复**：
```javascript
{
  name: '工作台',
  level: 1,
  parentId: null  // ✅ 正确
}
```

### 问题 3: 二级模块缺少父模块

**现象**：
```javascript
{
  name: '商机管理',
  level: 2,
  parentId: null  // ❌ 错误
}
```

**修复**：
```javascript
{
  name: '商机管理',
  level: 2,
  parentId: '销售管理'  // ✅ 添加父模块ID
}
```

## 验证清单

在每次修改模块配置后，请执行以下验证：

- [ ] 所有一级模块的 `parentId` 为 null
- [ ] 所有二级模块的 `parentId` 指向有效的父模块
- [ ] 没有模块的 `parentId` 指向不存在的模块
- [ ] 没有循环引用（A → B → A）
- [ ] `level` 字段与实际层级一致
- [ ] `order` 字段合理，用于排序
- [ ] 没有重复的模块ID

## 自动修复功能

验证工具提供以下自动修复功能：

1. **修复无效的 parentId**
   - 自动将不存在的 parentId 设置为 null
   - 将一级模块的 parentId 设置为 null

2. **修复 level 字段**
   - parentId 为 null 的模块设置为 level 1
   - 有 parentId 的模块设置为 level 2

3. **重置排序**
   - 根据模块创建时间重新分配 order
   - 确保同级模块的 order 唯一且连续

## 手动修复步骤

如果自动修复无法解决问题，可以手动修复：

### 1. 进入数据库控制台
```
https://tcb.cloud.tencent.com/dev?envId=cowork-9gg9oocb516be5fb#/db/doc
```

### 2. 打开 modulesConfig 集合

### 3. 查找问题模块
使用查询条件：
```javascript
// 查找 parentId 可能有问题的模块
{
  "parentId": { "$exists": true, "$ne": null }
}
```

### 4. 编辑并修复
根据上述修复示例修改字段值

### 5. 重新验证
使用在线工具或云函数重新验证

## 预防措施

### 1. 修改模块配置前的检查

```javascript
// ✅ 正确：添加二级模块前确认父模块存在
const parentExists = await db.collection('modulesConfig')
  .where({ _id: parentId })
  .count();

if (parentExists.total === 0) {
  throw new Error('父模块不存在');
}
```

### 2. 删除模块前的检查

```javascript
// ✅ 正确：删除前检查是否有子模块
const hasChildren = await db.collection('modulesConfig')
  .where({ parentId: moduleId })
  .count();

if (hasChildren.total > 0) {
  throw new Error('该模块下还有子模块，不能删除');
}
```

### 3. 定期验证

建议每周或在重大更新后执行一次完整性验证：

```javascript
// 添加到定时任务
setInterval(() => {
  callFunction({
    name: 'verify-module-integrity',
    data: { action: 'verify' }
  });
}, 7 * 24 * 60 * 60 * 1000); // 每周一次
```

## 测试数据完整性

### 测试场景 1: 正常层级结构

```javascript
// 一级模块
{ _id: 'A', name: '模块A', parentId: null, level: 1 }

// 二级模块
{ _id: 'A1', name: '模块A1', parentId: 'A', level: 2 }
{ _id: 'A2', name: '模块A2', parentId: 'A', level: 2 }
```

### 测试场景 2: 错误的 parentId

```javascript
// ❌ 错误：parentId 指向不存在的模块
{ _id: 'B', name: '模块B', parentId: 'NOT_EXISTS', level: 2 }

// ✅ 修复后
{ _id: 'B', name: '模块B', parentId: null, level: 1 }
```

### 测试场景 3: 一级模块有 parentId

```javascript
// ❌ 错误：一级模块不应该有 parentId
{ _id: 'C', name: '模块C', parentId: 'some-id', level: 1 }

// ✅ 修复后
{ _id: 'C', name: '模块C', parentId: null, level: 1 }
```

## 监控和告警

### 1. 数据完整性监控

```javascript
// 在系统设置中显示完整性状态
const integrityStatus = await callFunction({
  name: 'verify-module-integrity',
  data: { action: 'verify' }
});

// 如果发现问题，显示警告提示
if (!integrityStatus.allPassed) {
  showWarning('发现模块数据完整性问题，请及时修复');
}
```

### 2. 自动修复日志

所有自动修复操作都会记录日志：

```javascript
{
  timestamp: '2026-01-26 10:00:00',
  action: 'repair',
  changes: [
    {
      moduleId: '商机管理',
      field: 'parentId',
      oldValue: 'invalid-id',
      newValue: null
    }
  ]
}
```

## 总结

- ✅ 定期执行数据完整性验证
- ✅ 在修改模块配置前后都要验证
- ✅ 优先使用在线验证工具
- ✅ 自动修复简单问题
- ✅ 手动修复复杂问题
- ✅ 记录所有修复操作

## 快速访问

- **在线验证工具**: `https://jihuadz.xin/test-data-integrity.html`
- **云函数控制台**: `https://tcb.cloud.tencent.com/dev?envId=cowork-9gg9oocb516be5fb#/scf/detail?id=verify-module-integrity`
- **数据库控制台**: `https://tcb.cloud.tencent.com/dev?envId=cowork-9gg9oocb516be5fb#/db/doc/collection/modulesConfig`

---

**版本**: v1.0.0  
**更新日期**: 2026-01-26  
**维护者**: 际华协同办公平台开发团队
