# 会议纪要保存草稿功能 - 调试指南

## 📋 问题描述

用户反馈：点击"保存为草稿"按钮时，数据没有保存下来。

## 🔧 已完成的修复

### 1. 前端日志增强 (MeetingMinutesEditor.tsx)

在 `handleSaveDraft` 函数中添加了详细的日志输出：

```typescript
console.log('📝 保存草稿数据:', {
  meetingId: meeting._id,
  minutesData: saveData,
  agendas: agendas
});

console.log('📝 保存草稿响应:', res);
```

### 2. 后端日志增强 (meeting-management/index.js)

在云函数的 `handleUpdate` 函数中添加了详细的日志：

```javascript
console.log('[update] 📝 更新会议纪要数据:', minutesData);
console.log('[update] 📋 更新议题数据:', agendas);
console.log('[update] 🔄 准备更新会议数据:', {
  meetingId: id,
  hasMinutesData: !!minutesData,
  hasAgendas: !!agendas,
  updateDataKeys: Object.keys(updateData)
});
console.log('[update] ✅ Meeting updated successfully:', id);
```

### 3. 云函数已重新部署

meeting-management 云函数已成功部署最新版本。

## 🧪 测试步骤

### 步骤 1: 打开浏览器控制台

1. 打开 Chrome/Edge 浏览器
2. 按 `F12` 打开开发者工具
3. 切换到 **Console** 标签

### 步骤 2: 编辑会议纪要

1. 进入会议管理模块
2. 选择一个会议，打开会议详情
3. 切换到"会议纪要" Tab
4. 点击"编辑"按钮（如果是草稿）

### 步骤 3: 修改纪要内容

在编辑器中修改以下内容：

- **纪要内容** - 修改会议概述文本
- **上周工作总结** - 添加/修改工作项
- **本周工作计划** - 添加/修改计划项
- **会议结论** - 修改总结文本

### 步骤 4: 点击"保存为草稿"

点击"保存为草稿"按钮，然后查看控制台输出。

### 步骤 5: 检查控制台日志

#### 预期看到的日志输出：

**前端日志**：
```
📝 保存草稿数据: {
  meetingId: "会议ID",
  minutesData: {
    summary: "修改后的概述",
    lastWeekWorkSummary: [...],
    thisWeekWorkPlan: [...],
    conclusion: "修改后的结论",
    status: "draft",
    generatedAt: Date,
    generatedBy: "用户名"
  },
  agendas: [...]
}
```

```
📝 保存草稿响应: {
  result: {
    success: true,
    message: "会议更新成功"
  }
}
```

**后端日志**（在 CloudBase 控制台查看）：
```
[update] 📝 更新会议纪要数据: { summary: "...", ... }
[update] 📋 更新议题数据: [...]
[update] 🔄 准备更新会议数据: {
  meetingId: "会议ID",
  hasMinutesData: true,
  hasAgendas: true,
  updateDataKeys: ["updatedAt", "updatedBy", "minutesData", "agendas"]
}
[update] ✅ Meeting updated successfully: 会议ID
```

### 步骤 6: 验证数据持久化

1. **刷新页面** - 按 `F5` 刷新浏览器
2. 再次打开同一个会议详情
3. 切换到"会议纪要" Tab
4. **检查修改的内容是否保留**

## 🔍 可能的问题排查

### 问题 1: 前端没有日志输出

**原因**: `handleSaveDraft` 函数没有被调用

**排查方法**:
- 检查按钮的 onClick 绑定是否正确
- 使用 React DevTools 查看组件状态

### 问题 2: 前端日志显示成功，但后端无日志

**原因**: 云函数调用失败或网络问题

**排查方法**:
```javascript
// 查看网络请求
// 在 Chrome DevTools -> Network 标签
// 筛选 "tcb-function"
```

**检查项**:
- CloudBase 环境ID是否正确
- 网络连接是否正常
- 云函数是否正常运行

### 问题 3: 后端日志显示成功，但数据未保存

**原因**: 数据库更新失败

**排查方法**:
1. 打开 CloudBase 控制台
2. 进入"文档型数据库" -> `meetings` 集合
3. 查找对应的会议记录
4. 检查 `minutesData` 字段是否有值

### 问题 4: 弹窗显示"保存失败"

**查看错误信息**:
- 控制台会打印 `❌ 保存草稿失败: [错误信息]`
- 根据错误信息进行针对性修复

## 📊 CloudBase 控制台查看日志

### 访问云函数日志：

1. 打开 [CloudBase 控制台](https://tcb.cloud.tencent.com)
2. 选择环境
3. 进入"云函数" -> "meeting-management"
4. 点击"日志"标签
5. 查看最近的调用日志

### 查看数据库数据：

1. 进入"文档型数据库"
2. 选择 `meetings` 集合
3. 找到对应的会议记录
4. 查看 `minutesData` 字段内容

## 🎯 预期结果

保存草稿后：

1. ✅ 浏览器弹出"草稿已保存"提示
2. ✅ 控制台显示完整的日志链路
3. ✅ 数据库中 `minutesData` 字段已更新
4. ✅ `minutesData.status = "draft"`
5. ✅ 刷新页面后，修改的内容仍然存在

## 📞 如何报告问题

如果问题依然存在，请提供以下信息：

1. **前端控制台截图** - 包含完整的日志输出
2. **网络请求截图** - Network 标签中的 tcb-function 请求
3. **云函数日志截图** - CloudBase 控制台的日志
4. **数据库记录截图** - meetings 集合中对应记录的 minutesData 字段
5. **操作步骤描述** - 详细的操作流程

## 🚀 下一步

测试完成后，如果功能正常，可以：

1. 移除调试日志（可选）
2. 部署到测试环境验证
3. 部署到生产环境

---

**版本**: v1.9.1  
**日期**: 2025-01-26  
**修复内容**: 增强日志，便于排查保存草稿问题
