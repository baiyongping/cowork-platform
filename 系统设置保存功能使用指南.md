# 系统设置保存功能使用指南

## 功能概述

在系统设置的类型设置页面新增了"保存所有设置"功能，用于批量保存所有修改的类型数据，确保数据持久化到数据库。

## 功能特性

### 1. 智能保存按钮

位置：类型设置页面右上角

按钮状态：
- **蓝色"保存所有设置"** - 有未保存的更改，可以点击保存
- **灰色"暂无更改"** - 没有更改，按钮禁用

### 2. 自动追踪更改

系统会自动追踪以下操作：
- ✅ 新增类型项
- ✅ 编辑类型项
- ✅ 切换启用/禁用状态
- ✅ 删除类型项

### 3. 批量保存

点击"保存所有设置"按钮会批量保存以下所有类型：
- 任务类型
- 任务状态
- 商机阶段
- 商机跟进动作类型
- 项目准备期环节
- 项目生产期环节
- 项目交付期环节
- 项目状态
- 产品类型

## 使用步骤

### 步骤1：打开类型设置

1. 登录系统
2. 点击左侧导航栏"系统设置"
3. 点击顶部标签"类型设置"

### 步骤2：修改类型数据

**新增类型：**
1. 找到要修改的类型块（如"商机阶段设置"）
2. 点击右上角"+ 新增"按钮
3. 输入新类型名称
4. 按回车键或点击"添加"按钮

**编辑类型：**
1. 找到要编辑的类型项
2. 点击"编辑"按钮
3. 修改名称
4. 点击"确定保存"

**启用/禁用类型：**
1. 找到要修改的类型项
2. 点击左侧的切换开关
3. 开启 = 启用，关闭 = 禁用

**删除类型：**
1. 找到要删除的类型项
2. 点击"删除"按钮
3. 确认删除操作
4. 注意：如果该类型被引用，无法删除

### 步骤3：保存设置

1. 修改完成后，注意右上角的保存按钮变为蓝色"保存所有设置"
2. 点击"保存所有设置"按钮
3. 等待提示"所有设置已保存成功！"
4. 按钮变为灰色"暂无更改"状态，表示保存成功

## 注意事项

### ⚠️ 重要提示

1. **必须保存**  
   修改类型设置后，必须点击"保存所有设置"按钮，否则重启系统后数据会丢失

2. **引用检查**  
   如果某个类型项已被使用（如商机、任务等引用），删除时会提示无法删除

3. **启用状态**  
   只有启用状态的类型项才会在其他页面的下拉选择中显示

4. **批量操作**  
   一次保存会同时保存所有类型的修改，无需逐个保存

## 常见问题

### Q1: 为什么修改后重启系统数据又丢失了？

**A:** 很可能是修改后忘记点击"保存所有设置"按钮。请确保每次修改后都点击保存按钮。

### Q2: 保存按钮是灰色的，点击不了？

**A:** 这表示当前没有未保存的更改。只有进行了新增、编辑、删除或切换启用状态等操作后，保存按钮才会变成蓝色可点击。

### Q3: 删除某个类型项时提示"无法删除"？

**A:** 这是因为该类型项正在被系统中的其他记录使用。解决方法：
1. 先修改或删除引用该类型的记录
2. 或者将该类型项设为"禁用"而不是删除

### Q4: 禁用的类型项会怎样？

**A:** 禁用后：
- 该类型项不会在下拉选择中显示
- 已使用该类型的记录仍然有效，不受影响
- 可以随时重新启用

### Q5: 如何验证保存是否成功？

**A:** 有以下几种方法：
1. 查看提示信息"所有设置已保存成功！"
2. 保存按钮变为灰色"暂无更改"
3. 刷新页面，检查修改是否保留
4. 打开浏览器控制台，查看日志输出

## 数据库说明

### 存储位置

所有类型设置数据存储在 `type_settings` 集合中。

### 数据结构

```typescript
{
  _id: "唯一ID",
  type: "类型标识（如 'opportunity'）",
  values: [
    { value: "跟进线索", enabled: true },
    { value: "方案咨询", enabled: true },
    { value: "商务谈判", enabled: false }  // 已禁用
  ],
  createdAt: "创建时间",
  updatedAt: "更新时间"
}
```

## 测试验证

### 验证步骤

1. **修改数据**
   - 打开商机阶段设置
   - 添加新阶段"需求分析"
   - 添加新阶段"技术评估"

2. **保存数据**
   - 点击右上角"保存所有设置"按钮
   - 等待成功提示

3. **验证持久化**
   - 刷新浏览器页面
   - 再次查看商机阶段设置
   - 确认新增的阶段仍然存在

4. **验证功能使用**
   - 打开商机管理页面
   - 创建新商机
   - 检查商机阶段下拉列表是否包含新增的阶段

### 预期结果

- ✅ 新增的类型项保存成功
- ✅ 刷新页面后数据仍然存在
- ✅ 其他页面可以正常使用新增的类型项
- ✅ 禁用的类型项不在下拉列表中显示

## 技术实现

### 核心功能

**1. 状态追踪**
```typescript
const [hasUnsavedChanges, setHasUnsavedChanges] = useState(false);
```

**2. 批量保存**
```typescript
const handleSaveAllSettings = async () => {
  await Promise.all([
    saveTypeSettingsToDb('task', taskTypes),
    saveTypeSettingsToDb('taskStatus', taskStatuses),
    saveTypeSettingsToDb('opportunity', opportunityStages),
    // ... 其他类型
  ]);
  alert('所有设置已保存成功！');
  setHasUnsavedChanges(false);
};
```

**3. 自动标记**
- 每次修改操作后自动设置 `hasUnsavedChanges = true`
- 保存成功后自动设置 `hasUnsavedChanges = false`

## 相关文档

- 📄 [商机阶段数据丢失问题排查报告](./商机阶段数据丢失问题排查报告.md)
- 📄 [类型设置使用说明](./类型设置使用说明.md)
- 📄 [系统设置优化完成报告](./系统设置类型管理优化完成报告.md)

---

**文档版本：** v1.0  
**更新时间：** 2025-12-12  
**适用版本：** v1.3.0+
