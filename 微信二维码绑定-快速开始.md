
# 微信二维码绑定 - 快速开始指南

## 📋 当前状态

✅ **已实现功能：**
- 生成绑定 Scene ID
- 云函数轮询状态检查
- 绑定确认和解绑

⚠️ **待配置功能：**
- 生成真实的微信小程序二维码
- 配置微信开放平台 AppSecret

---

## 🚀 快速开始（3步完成）

### 步骤1：获取 AppSecret

1. 访问 [微信公众平台](https://mp.weixin.qq.com/)
2. 进入：**开发 → 开发管理 → 开发设置**
3. 找到"开发者ID"，点击"AppSecret"后的**重置**
4. 扫码验证，复制新的 AppSecret

> ⚠️ AppSecret 只显示一次，请立即保存！

---

### 步骤2：配置云函数

#### 方式A：使用配置脚本（推荐）

```bash
# 运行配置工具
node scripts/check-wechat-config.js

# 按提示输入 AppSecret
# 工具会自动生成配置文件并更新代码
```

#### 方式B：手动配置

1. 创建配置文件 `cloudfunctions/generate-miniprogram-qr/wechat-config.js`：

```javascript
// ⚠️ 此文件包含敏感信息，请勿提交到Git
module.exports = {
  APPID: 'wx79afc92f6fe01a31',
  APPSECRET: '你的AppSecret'
};
```

2. 更新 `cloudfunctions/generate-miniprogram-qr/index.js` 第5-6行：

```javascript
// 替换前
const APPID = 'wx79afc92f6fe01a31';
const APPSECRET = '你的小程序AppSecret';

// 替换后
const { APPID, APPSECRET } = require('./wechat-config');
```

3. 添加到 `.gitignore`：

```gitignore
# 微信配置（敏感信息）
**/wechat-config.js
```

---

### 步骤3：部署和测试

#### 1️⃣ 部署云函数

```bash
# 进入云函数目录
cd cloudfunctions/generate-miniprogram-qr

# 安装依赖
npm install

# 返回项目根目录
cd ../..

# 使用 CloudBase CLI 部署
tcb fn deploy generate-miniprogram-qr
```

或使用 CloudBase MCP 工具：

```javascript
updateFunctionCode({
  name: 'generate-miniprogram-qr',
  functionRootPath: 'd:/project/cowork12-21/cloudfunctions'
})
```

#### 2️⃣ 测试生成二维码

打开浏览器控制台，执行：

```javascript
const app = cloudbase.init({ env: 'jihua-oa-dev-3goht9irae4d949f' });
const auth = app.auth();

// 登录
await auth.signInWithUsernameAndPassword('admin', 'Jihua@2025!');

// 测试生成二维码
const res = await app.callFunction({
  name: 'generate-miniprogram-qr',
  data: {
    sceneId: 'test_' + Date.now(),
    type: 'unlimited'
  }
});

console.log('生成结果:', res.result);

// 如果成功，会返回 Base64 格式的图片
if (res.result.code === 200) {
  // 在新窗口打开二维码
  const win = window.open();
  win.document.write(`<img src="${res.result.data.qrCodeUrl}" />`);
}
```

**预期结果：**
- ✅ 返回 `code: 200`
- ✅ 包含 `qrCodeUrl`（Base64 图片）
- ✅ 在新窗口显示二维码

#### 3️⃣ 测试完整绑定流程

1. 登录系统：http://localhost:5173
2. 进入"个人信息" → 点击"微信绑定"
3. 等待生成真实的二维码
4. 使用微信扫描（如果有小程序）

---

## 🔍 问题排查

### ❌ 问题1: 生成二维码失败，提示"access_token无效"

**原因：** AppSecret 配置错误

**解决：**
1. 重新检查 AppSecret 是否正确
2. 确保没有多余空格或换行符
3. 重新运行配置脚本

---

### ❌ 问题2: 云函数调用超时

**原因：** 网络问题或微信API响应慢

**解决：**
1. 检查云函数日志（CloudBase 控制台 → 云函数 → 日志）
2. 增加云函数超时时间（`config.json` 中修改 `timeout: 20`）
3. 重新部署云函数

---

### ❌ 问题3: 二维码生成成功，但扫码无反应

**原因：** 小程序端未配置绑定页面

**解决：**
1. 确保小程序已上传 `pages/wechat-bind/index` 页面
2. 检查 `app.json` 中是否注册了该页面
3. 使用微信开发者工具调试

---

## 📊 完整架构流程

### 前端（Web系统）

```
用户点击"绑定微信"
  ↓
调用 wechat-bind 云函数
generateScene() → 生成 sceneId
  ↓
调用 generate-miniprogram-qr 云函数
生成真实的微信小程序二维码（Base64）
  ↓
显示二维码 <img src="data:image/png;base64,..." />
  ↓
轮询检查绑定状态（每2秒）
checkStatus() → 'pending' | 'scanned' | 'completed'
  ↓
绑定成功 → 更新UI
```

### 后端（云函数）

```
wechat-bind 云函数：
  - generateScene: 生成 sceneId，写入 wechat_sessions
  - checkStatus: 查询绑定状态
  - getSessionInfo: 获取用户信息（小程序端）
  - confirmBind: 确认绑定，更新 users 表
  - unbind: 解除绑定

generate-miniprogram-qr 云函数：
  - getAccessToken: 调用微信API获取 access_token
  - generateMiniProgramCode: 生成小程序码（推荐）
  - generateQRCode: 生成普通二维码（备用）
```

### 小程序端

```
扫描二维码 → 获取 scene 参数
  ↓
解析 sceneId
  ↓
调用 wechat-bind 云函数
getSessionInfo() → 获取要绑定的账号信息
  ↓
显示确认界面（姓名、部门、职位）
  ↓
用户点击"确认绑定"
  ↓
调用 wechat-bind 云函数
confirmBind() → 更新 users.wechatOpenId
  ↓
绑定成功
```

---

## ✅ 配置完成检查清单

- [ ] 已获取微信小程序 AppSecret
- [ ] 已配置 `wechat-config.js` 文件
- [ ] 已添加到 `.gitignore`
- [ ] 已部署 `generate-miniprogram-qr` 云函数
- [ ] 已测试生成二维码成功（返回 Base64 图片）
- [ ] 已在前端显示真实二维码
- [ ] （可选）已配置小程序绑定页面

---

## 🎯 当前进度

| 功能 | 状态 | 备注 |
|-----|------|------|
| 生成 Scene ID | ✅ 完成 | 云函数 `wechat-bind` |
| 生成真实二维码 | ⚠️ 待配置 | 需要 AppSecret |
| 轮询绑定状态 | ✅ 完成 | 前端轮询 |
| 小程序扫码 | ⚠️ 待开发 | 需要小程序页面 |
| 确认绑定 | ✅ 完成 | 云函数 `confirmBind` |
| 解绑功能 | ✅ 完成 | 云函数 `unbind` |

---

## 📚 参考文档

- [微信小程序二维码生成文档](https://developers.weixin.qq.com/miniprogram/dev/OpenApiDoc/qrcode-link/qr-code/getUnlimitedQRCode.html)
- [微信开放平台接入指南](https://developers.weixin.qq.com/doc/oplatform/Third-party_Platforms/2.0/getting_started/how_to_read.html)
- [CloudBase 身份认证文档](https://docs.cloudbase.net/authentication/introduce)

---

## 🆘 需要帮助？

如果配置过程中遇到问题，请检查：

1. **AppSecret 是否正确？** 重新获取并验证
2. **云函数是否部署成功？** 查看 CloudBase 控制台日志
3. **网络是否正常？** 测试访问 `https://api.weixin.qq.com/`
4. **小程序是否已审核通过？** 测试时可使用"体验版"

---

**祝配置顺利！🎉**
