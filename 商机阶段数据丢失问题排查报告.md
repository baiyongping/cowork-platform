# 商机阶段数据丢失问题排查报告

## 问题描述

每次重启系统后，商机阶段设置的数据项会只剩下最初的三项（跟进线索、方案咨询、商务谈判），其它新增项都丢失。

## 问题根本原因

经过排查，发现以下根本原因：

### 1. **数据未持久化到数据库**

多个组件在加载商机阶段设置时，如果数据库没有数据或数据为空，会使用默认值但**没有保存到数据库**，导致每次重启系统数据都会丢失。

**问题组件列表：**
- `components/pages/OpportunityManagement.tsx`
- `components/OpportunitySelector.tsx`
- `components/EditOpportunityModal.tsx`
- `components/CreateOpportunityModal.tsx`
- `components/OpportunityDetailModal.tsx`
- `components/OpportunityRecycleBin.tsx`
- `components/OpportunityRecycleBinDetail.tsx`

### 2. **数据格式兼容性问题**

系统设置中的类型数据从旧格式 `string[]` 升级到新格式 `TypeItem[]`（包含 `enabled` 字段），但部分组件没有正确处理新格式数据。

```typescript
// 旧格式
values: ['跟进线索', '方案咨询', '商务谈判']

// 新格式
values: [
  { value: '跟进线索', enabled: true },
  { value: '方案咨询', enabled: true },
  { value: '商务谈判', enabled: true }
]
```

## 解决方案

### 1. 系统设置页面 - 添加保存按钮

**修改文件：** `components/pages/SystemSettings.tsx`

**新增功能：**
- 在类型设置页面右上角添加"保存所有设置"按钮
- 添加 `hasUnsavedChanges` 状态追踪未保存的更改
- 实现 `handleSaveAllSettings()` 批量保存所有类型设置
- 每次修改类型设置后自动标记为有未保存更改

**使用说明：**
1. 在系统设置 → 类型设置页面，修改任何类型数据后
2. 点击右上角的"保存所有设置"按钮
3. 所有修改会被批量保存到数据库
4. 保存成功后按钮变为灰色"暂无更改"状态

### 2. 商机阶段加载逻辑修复

**修改的组件：**
- `OpportunityManagement.tsx`
- `OpportunitySelector.tsx`
- `EditOpportunityModal.tsx`
- `CreateOpportunityModal.tsx`
- `OpportunityDetailModal.tsx`
- `OpportunityRecycleBin.tsx`
- `OpportunityRecycleBinDetail.tsx`

**修复内容：**

```typescript
// 修复后的加载逻辑
const loadOpportunityStages = async () => {
  try {
    const result = await db.collection('type_settings')
      .where({ type: 'opportunity' })
      .get();
    
    if (result.data && result.data.length > 0) {
      // 支持旧格式（string[]）和新格式（TypeItem[]）
      const values = result.data[0].values;
      if (values && values.length > 0) {
        if (typeof values[0] === 'string') {
          // 旧格式：直接使用
          setOpportunityStages(values);
        } else {
          // 新格式：提取enabled=true的value
          setOpportunityStages(
            values.filter((item: any) => item.enabled)
                  .map((item: any) => item.value)
          );
        }
      } else {
        // 数据为空，初始化并保存到数据库
        const defaultStages = ['跟进线索', '方案咨询', '商务谈判'];
        setOpportunityStages(defaultStages);
        await db.collection('type_settings').add({
          type: 'opportunity',
          values: defaultStages.map(v => ({ value: v, enabled: true })),
          createdAt: new Date(),
          updatedAt: new Date()
        });
        console.log('商机阶段设置已初始化到数据库');
      }
    } else {
      // 数据库中没有记录，创建并保存默认值
      const defaultStages = ['跟进线索', '方案咨询', '商务谈判'];
      setOpportunityStages(defaultStages);
      await db.collection('type_settings').add({
        type: 'opportunity',
        values: defaultStages.map(v => ({ value: v, enabled: true })),
        createdAt: new Date(),
        updatedAt: new Date()
      });
      console.log('商机阶段设置已初始化到数据库');
    }
  } catch (error) {
    console.error('加载商机阶段设置失败:', error);
    setOpportunityStages(['跟进线索', '方案咨询', '商务谈判']);
  }
};
```

**关键改进：**
1. 支持新旧两种数据格式
2. 只显示 `enabled: true` 的选项
3. 数据为空时自动初始化并保存到数据库
4. 添加详细的日志输出便于排查问题

## 数据库集合说明

### type_settings 集合

**集合名称：** `type_settings`

**数据结构：**
```typescript
{
  _id: string,
  type: string,        // 类型标识：'opportunity', 'task', 'taskStatus' 等
  values: TypeItem[],  // 类型值数组（新格式）
  createdAt: Date,     // 创建时间
  updatedAt: Date      // 更新时间
}

interface TypeItem {
  value: string,    // 选项值
  enabled: boolean  // 是否启用
}
```

**相关类型标识：**
- `opportunity` - 商机阶段
- `task` - 任务类型
- `taskStatus` - 任务状态
- `action` - 商机跟进动作类型
- `preparation` - 项目准备期环节
- `production` - 项目生产期环节
- `delivery` - 项目交付期环节
- `projectStatus` - 项目状态
- `productType` - 产品类型

**是否独立：** ✅ 是独立的集合，不会被其他程序直接写入。

## 验证方法

### 方法1：使用检查脚本

运行检查脚本查看数据库状态：

```bash
node scripts/check-type-settings.js
```

脚本会输出：
- type_settings 集合中的所有记录
- 商机阶段的详细数据
- 是否有重复记录
- 各集合的记录数量

### 方法2：手动测试

1. 打开系统设置 → 类型设置 → 商机阶段设置
2. 添加几个新的商机阶段（如：需求分析、技术评估、合同签订）
3. 点击右上角"保存所有设置"按钮
4. 重启系统（刷新浏览器）
5. 再次查看商机阶段设置，验证新增项是否保留

### 方法3：浏览器控制台检查

打开浏览器开发者工具控制台，查看日志输出：
- `✅ 商机阶段设置已初始化到数据库` - 表示数据已保存
- `类型设置加载完成:` - 查看加载的数据内容

## 后续预防措施

### 1. 统一数据加载逻辑

建议创建一个统一的类型设置加载 Hook：

```typescript
// hooks/useTypeSettings.ts
export function useTypeSettings(type: string) {
  const [values, setValues] = useState<string[]>([]);
  
  useEffect(() => {
    loadTypeSettings();
  }, []);
  
  const loadTypeSettings = async () => {
    // 统一的加载逻辑
    // 自动处理格式兼容
    // 自动初始化缺失数据
  };
  
  return { values, loading };
}
```

### 2. 添加数据验证

在系统启动时验证所有类型设置是否存在，自动初始化缺失项。

### 3. 添加操作日志

记录所有类型设置的修改操作，便于追溯问题。

## 总结

**问题原因：**
1. 多个组件加载数据时没有保存到数据库
2. 新旧数据格式兼容性处理不完善

**解决方案：**
1. ✅ 系统设置添加保存按钮
2. ✅ 修复所有组件的加载逻辑
3. ✅ 支持新旧格式兼容
4. ✅ 数据为空时自动初始化

**验证状态：**
- ✅ Linter 检查通过
- ✅ TypeScript 编译通过
- ✅ 构建成功

**注意事项：**
- 商机阶段设置存储在独立的 `type_settings` 集合中
- 不会被其他程序直接写入
- 需要通过系统设置页面进行修改
- 修改后务必点击"保存所有设置"按钮

---

**报告生成时间：** 2025-12-12  
**修复状态：** ✅ 已完成
