# 快速部署参考卡 📋

## 🚀 一键部署

### Windows
```cmd
双击运行: 一键部署-最终版.bat
```

### Linux/Mac
```bash
chmod +x 快速部署.sh && ./快速部署.sh
```

---

## 📍 重要地址

**访问地址**:
- HTTP: http://152.136.183.181
- HTTPS: https://152.136.183.181 ✅
- 域名: https://jihuadz.xin ⏳（备案后）

**管理后台**:
- 腾讯云控制台: https://console.cloud.tencent.com/lighthouse
- SSL 证书: https://console.cloud.tencent.com/ssl

**服务器信息**:
- IP: `152.136.183.181`
- 地域: `ap-beijing`（北京）
- 实例 ID: `lhins-pnt984h2`

---

## 🔑 默认凭据

**管理员账号**:
- 用户名: `admin`
- 密码: `admin123`
- 重置密码: https://152.136.183.181/reset-admin.html

---

## 🛠️ 常用命令

### 服务器连接
```bash
ssh root@152.136.183.181
```

### 查看服务状态
```bash
# 后端服务
ssh root@152.136.183.181 "pm2 list"

# Nginx 服务
ssh root@152.136.183.181 "systemctl status nginx"
```

### 查看日志
```bash
# 后端日志
ssh root@152.136.183.181 "pm2 logs jihua-api"

# Nginx 日志
ssh root@152.136.183.181 "tail -50 /var/log/nginx/error.log"
```

### 重启服务
```bash
# 重启后端
ssh root@152.136.183.181 "pm2 restart jihua-api"

# 重启 Nginx
ssh root@152.136.183.181 "systemctl restart nginx"
```

---

## 📦 手动部署

### 快速版本
```bash
# 构建
npm run build

# 上传前端
scp -r dist/* root@152.136.183.181:/var/www/jihua/

# 上传后端
scp -r server/dist/* root@152.136.183.181:/root/jihua-backend/

# 重启服务
ssh root@152.136.183.181 "pm2 restart jihua-api"
```

### 完整版本
```bash
# 1. 清理
rm -rf dist server/dist

# 2. 安装依赖
npm install
cd server && npm install && cd ..

# 3. 构建
npm run build
cd server && npm run build && cd ..

# 4. 上传
scp -r dist/* root@152.136.183.181:/var/www/jihua/
scp -r server/dist/* root@152.136.183.181:/root/jihua-backend/

# 5. 重启
ssh root@152.136.183.181 "pm2 restart jihua-api"
```

---

## 🔍 快速故障排查

### 页面 404
```bash
# 检查文件
ssh root@152.136.183.181 "ls -la /var/www/jihua/"

# 重启 Nginx
ssh root@152.136.183.181 "nginx -t && systemctl restart nginx"
```

### API 错误
```bash
# 查看后端日志
ssh root@152.136.183.181 "pm2 logs jihua-api --lines 50"

# 重启后端
ssh root@152.136.183.181 "pm2 restart jihua-api"
```

### 登录失败
1. 确认使用 HTTPS: https://152.136.183.181
2. 重置密码: https://152.136.183.181/reset-admin.html
3. 使用 `admin` / `admin123` 登录

### 静态资源 404
```bash
# 重新构建和部署
npm run build
scp -r dist/* root@152.136.183.181:/var/www/jihua/
```

---

## ✅ 部署检查清单

### 部署前
- [ ] 代码已提交
- [ ] 版本号已更新
- [ ] 本地测试通过
- [ ] SSH 连接正常

### 部署后
- [ ] 页面能访问
- [ ] API 正常响应
- [ ] 登录功能正常
- [ ] 无控制台错误

---

## 🔐 HTTPS 配置

### 当前状态（自签名证书）
- ✅ IP 访问: https://152.136.183.181
- ⚠️ 浏览器警告: 点击"高级" → "继续前往"
- ✅ crypto.subtle API 可用

### 备案后（腾讯云证书）
1. 下载证书: https://console.cloud.tencent.com/ssl
2. 上传证书到服务器
3. 更新 Nginx 配置
4. 域名访问: https://jihuadz.xin

---

## 📞 紧急联系

**问题类型** | **解决方案**
---|---
服务器无法访问 | 检查防火墙和安全组
部署失败 | 查看构建日志和错误信息
登录失败 | 重置密码或检查 HTTPS
性能问题 | 查看服务器资源使用情况

---

## 📚 相关文档

- **详细方案**: `自动化部署完整方案.md`
- **检查清单**: `部署检查清单.md`
- **问题排查**: `问题排查手册.md`
- **项目说明**: `README.md`

---

**版本**: v1.0 | **更新**: 2025-12-14
