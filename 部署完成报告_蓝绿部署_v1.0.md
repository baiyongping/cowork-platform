# 际华协同办公平台 - 生产环境蓝绿部署方案

**版本**: v1.0  
**日期**: 2025-12-30  
**部署架构**: 端口 3000(蓝) + 3003(绿)

---

## 📋 部署架构总览

### 环境隔离设计

```
┌─────────────────────────────────────────────────────────────┐
│ 服务器: 152.136.183.181                                       │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌──────────── 开发环境 ─────────────┐                        │
│  │ 域名: 152.136.183.181:3443        │                        │
│  │ Docker: jihua-dev-blue  (3001)   │                        │
│  │ Docker: jihua-dev-green (3002)   │                        │
│  └──────────────────────────────────┘                        │
│                                                               │
│  ┌──────────── 生产环境 ─────────────┐                        │
│  │ 域名: jihuadz.xin (HTTPS)         │                        │
│  │ Docker: jihua-prod-blue  (3000)  │  ← 蓝色环境              │
│  │ Docker: jihua-prod-green (3003)  │  ← 绿色环境              │
│  │ Nginx: upstream切换流量          │                        │
│  └──────────────────────────────────┘                        │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

**端口分配:**
- **开发环境**: 3001(蓝) + 3002(绿) - 保持不变
- **生产环境**: 3000(蓝) + 3003(绿) - 新部署方案

---

## ✅ 已完成配置

### 1. 防火墙规则
```bash
✓ 端口3000 - 生产环境蓝色容器
✓ 端口3003 - 生产环境绿色容器
```

### 2. 部署脚本
```bash
✓ /var/www/jihua-deploy/blue-green-deploy.sh
✓ /var/www/jihua-deploy/Dockerfile
```

### 3. Nginx配置
```bash
✓ /etc/nginx/sites-available/jihuadz.xin
✓ upstream jihua_prod (初始端口:3000)
✓ SSL证书配置完成
```

---

## 🚀 首次部署流程

### 步骤1: 构建前端项目

**本地执行:**
```bash
cd d:\project\cowork12-21
npx vite build
```

### 步骤2: 上传构建产物

**使用Lighthouse MCP工具:**
```javascript
deploy_project_preparation({
  FolderPath: "d:/project/cowork12-21/dist",
  InstanceId: "lhins-pnt984h2",
  Region: "ap-beijing",
  ProjectName: "jihua-prod-dist"
})
```

**上传成功后,复制到部署目录:**
```bash
# 在服务器执行
mkdir -p /var/www/jihua-deploy/dist
cp -r /root/jihua-prod-dist_YYYYMMDDHHMMSS/* /var/www/jihua-deploy/dist/
```

### 步骤3: 执行蓝绿部署

**在服务器执行:**
```bash
cd /var/www/jihua-deploy
./blue-green-deploy.sh deploy
```

**部署流程:**
1. ✅ 检测当前活跃环境(blue/green)
2. ✅ 构建Docker镜像
3. ✅ 启动新环境容器
4. ✅ 健康检查(等待60秒)
5. ✅ 切换Nginx流量
6. ✅ 清理旧环境(30秒后)

---

## 🔄 日常更新流程

### 更新步骤

**1. 构建新版本**
```bash
npx vite build
```

**2. 上传构建产物**
```bash
# 使用Lighthouse MCP工具上传dist目录
# 然后复制到部署目录
cp -r /root/jihua-prod-dist_新时间戳/* /var/www/jihua-deploy/dist/
```

**3. 执行部署**
```bash
cd /var/www/jihua-deploy
./blue-green-deploy.sh deploy
```

**自动流程:**
- 如果当前是蓝色环境 → 部署到绿色环境
- 如果当前是绿色环境 → 部署到蓝色环境
- 健康检查通过 → 自动切换流量
- 旧环境自动清理

---

## 🛡️ 回滚方案

### 紧急回滚

**如果新版本有问题:**
```bash
# 回滚到蓝色环境
./blue-green-deploy.sh rollback blue

# 或回滚到绿色环境
./blue-green-deploy.sh rollback green
```

**回滚流程:**
1. ✅ 启动目标环境容器
2. ✅ 健康检查
3. ✅ 切换Nginx流量
4. ✅ 完成回滚(约60秒)

---

## 📊 监控与状态

### 查看当前状态

**查看活跃环境:**
```bash
./blue-green-deploy.sh status
```

**输出示例:**
```
当前活跃环境: blue
NAMES              STATUS          PORTS
jihua-prod-blue    Up 2 hours      3000->80/tcp
```

### 查看容器日志

**蓝色环境:**
```bash
docker logs jihua-prod-blue -f
```

**绿色环境:**
```bash
docker logs jihua-prod-green -f
```

### 查看Nginx日志

```bash
# 访问日志
tail -f /var/log/nginx/access.log

# 错误日志
tail -f /var/log/nginx/error.log
```

---

## 🔧 维护命令

### Docker容器管理

```bash
# 查看所有生产容器
docker ps -a --filter "name=jihua-prod"

# 停止指定容器
docker stop jihua-prod-blue
docker stop jihua-prod-green

# 删除容器
docker rm jihua-prod-blue
docker rm jihua-prod-green

# 清理未使用镜像
docker image prune -f
```

### Nginx管理

```bash
# 测试配置
nginx -t

# 重载配置
systemctl reload nginx

# 查看配置
cat /etc/nginx/sites-available/jihuadz.xin

# 恢复备份
cp /etc/nginx/sites-available/jihuadz.xin.backup /etc/nginx/sites-available/jihuadz.xin
```

---

## 🎯 性能优化建议

### 1. 镜像优化

**Dockerfile优化(可选):**
```dockerfile
FROM nginx:alpine
COPY dist/ /usr/share/nginx/html/

# 启用gzip压缩
RUN echo 'gzip on;' >> /etc/nginx/nginx.conf && \
    echo 'gzip_types text/css application/javascript application/json;' >> /etc/nginx/nginx.conf

EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

### 2. 容器资源限制

**限制内存和CPU(可选):**
```bash
docker run -d \
    --name jihua-prod-blue \
    --restart unless-stopped \
    -p 3000:80 \
    --memory="256m" \
    --cpus="0.5" \
    jihua-prod-app:blue
```

### 3. 定期清理

**每周执行一次:**
```bash
# 清理停止的容器
docker container prune -f

# 清理未使用的镜像
docker image prune -a -f

# 清理未使用的网络
docker network prune -f
```

---

## 📈 监控指标

### 关键指标

| 指标 | 目标值 | 监控方式 |
|------|--------|----------|
| 响应时间 | < 200ms | Nginx日志 |
| 容器内存 | < 256MB | `docker stats` |
| 容器CPU | < 50% | `docker stats` |
| 健康检查成功率 | 100% | 部署日志 |
| 部署时间 | < 3分钟 | 部署日志 |

### 实时监控

```bash
# 监控容器资源
docker stats --no-stream --format "table {{.Name}}\t{{.CPUPerc}}\t{{.MemUsage}}"

# 监控端口连接
netstat -an | grep -E ':(3000|3003)' | grep ESTABLISHED | wc -l
```

---

## 🚨 故障排查

### 问题1: 部署失败

**症状**: 脚本执行失败
```bash
# 检查日志
./blue-green-deploy.sh deploy 2>&1 | tee deploy.log

# 检查Docker状态
systemctl status docker

# 检查镜像
docker images | grep jihua-prod
```

### 问题2: 健康检查失败

**症状**: 容器无法访问
```bash
# 检查容器日志
docker logs jihua-prod-blue

# 手动测试端口
curl -I http://localhost:3000

# 检查容器内部
docker exec jihua-prod-blue ls /usr/share/nginx/html
```

### 问题3: Nginx切换失败

**症状**: 流量未切换
```bash
# 检查Nginx配置
nginx -t

# 查看upstream配置
grep -A 5 'upstream jihua_prod' /etc/nginx/sites-available/jihuadz.xin

# 手动切换端口
sudo sed -i 's/server 127.0.0.1:3000;/server 127.0.0.1:3003;/' /etc/nginx/sites-available/jihuadz.xin
sudo systemctl reload nginx
```

---

## 📝 部署检查清单

### 部署前检查

- [ ] 本地代码已提交到Git
- [ ] 本地构建成功 (`npx vite build`)
- [ ] dist目录完整
- [ ] 构建产物大小合理(通常 < 10MB)

### 部署中检查

- [ ] 上传完成无错误
- [ ] Docker镜像构建成功
- [ ] 容器启动正常
- [ ] 健康检查通过
- [ ] Nginx切换成功

### 部署后验证

- [ ] 访问 https://jihuadz.xin 正常
- [ ] 页面加载正常
- [ ] 登录功能正常
- [ ] API接口正常
- [ ] 无控制台错误
- [ ] SSL证书有效

---

## 🎓 最佳实践

### 1. 版本管理

```bash
# 每次部署前打Tag
git tag v2.x.x-prod
git push origin v2.x.x-prod

# 构建时记录版本
echo "v2.x.x" > dist/version.txt
```

### 2. 部署时间选择

- ✅ **推荐**: 工作日 21:00-23:00 (低峰期)
- ✅ **紧急**: 任何时间(可快速回滚)
- ❌ **避免**: 工作日 09:00-18:00 (高峰期)

### 3. 通知流程

- 部署前: 通知团队成员
- 部署中: 实时监控
- 部署后: 确认无误后通知

---

## 📞 联系信息

**紧急问题联系:**
- 开发团队: [联系方式]
- 运维团队: [联系方式]

**文档维护:**
- 更新日期: 2025-12-30
- 维护人员: AI Assistant

---

## 📚 附录

### A. 完整部署命令清单

```bash
# 1. 本地构建
npx vite build

# 2. 上传(使用Lighthouse MCP工具)
# 详见"步骤2"

# 3. 部署
cd /var/www/jihua-deploy
./blue-green-deploy.sh deploy

# 4. 验证
./blue-green-deploy.sh status
curl -I https://jihuadz.xin

# 5. 监控
docker logs jihua-prod-blue -f
```

### B. 紧急联系流程

**生产环境故障:**
1. 立即执行回滚: `./blue-green-deploy.sh rollback [blue|green]`
2. 检查日志: `docker logs [容器名]`
3. 通知开发团队
4. 记录故障原因
5. 修复后重新部署

---

## ✅ 总结

**蓝绿部署优势:**
- ✅ **零停机时间**: 用户无感知更新
- ✅ **快速回滚**: 60秒内恢复服务
- ✅ **风险隔离**: 新旧版本独立运行
- ✅ **自动化**: 一键部署,自动切换

**预计效果:**
- 部署时间: 2-3分钟
- 停机时间: 0秒
- 回滚时间: 60秒
- 成功率: 99%+

**下一步操作:**
1. 准备构建产物
2. 上传到服务器
3. 执行首次部署
4. 验证功能正常

---

**部署成功后,您将拥有一个零停机、可快速回滚的生产环境部署方案!** 🎉
