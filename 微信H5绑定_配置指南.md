# 微信 H5 绑定功能 - 配置指南

## 📋 配置清单

### 1️⃣ **微信公众号配置**（必需）

#### 📝 准备工作
1. **申请认证服务号**（未认证的服务号不支持网页授权）
   - 费用：¥300/年
   - 申请地址：https://mp.weixin.qq.com/

2. **获取配置信息**
   - 登录微信公众平台
   - 开发 → 基本配置
   - 记录：
     - `AppID`：wxxxxxxxxxxxxxxxxxxx
     - `AppSecret`：xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

#### ⚙️ 配置授权回调域名
1. 微信公众平台 → 设置与开发 → 接口权限 → 网页授权
2. 点击"修改"，填入授权回调域名：
   ```
   jihuadz.xin
   ```
3. ⚠️ **重要事项**：
   - 域名必须已备案
   - 不要加 `https://`
   - 不要加端口号
   - 域名需通过网站备案

---

### 2️⃣ **云函数环境变量配置**

#### 📁 文件位置
`cloudfunctions/wechat-oauth/config.json`

#### 🔧 配置内容
```json
{
  "permissions": {
    "openapi": []
  },
  "env": {
    "WECHAT_APPID": "wxxxxxxxxxxxxxxxxxxx",
    "WECHAT_SECRET": "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
  }
}
```

**替换说明：**
- `WECHAT_APPID`：从微信公众平台获取的 AppID
- `WECHAT_SECRET`：从微信公众平台获取的 AppSecret

---

### 3️⃣ **域名备案**（必需）

#### 📋 备案要求
- 域名：`jihuadz.xin`
- 备案类型：ICP 备案
- 服务商：腾讯云（推荐）

#### 🔗 备案流程
1. 登录腾讯云备案系统：https://beian.cloud.tencent.com/
2. 提交备案资料
3. 等待审核（约 1-2 周）

#### ⚠️ 未备案的影响
- 无法使用微信网页授权
- H5 绑定功能无法使用
- 建议先使用测试域名测试功能

---

## 🚀 部署流程

### 1️⃣ **部署云函数**

```bash
# 部署 wechat-oauth 云函数（新增）
tcb fn deploy wechat-oauth

# 更新 wechat-bind 云函数（已改造）
tcb fn deploy wechat-bind
```

### 2️⃣ **部署前端代码**

```bash
# 构建前端
npm run build

# 部署到测试环境
npm run deploy:dev

# 部署到生产环境（确认后）
npm run deploy:prod
```

### 3️⃣ **验证部署**

#### 检查 H5 页面路由
访问以下地址验证路由是否正常：

1. **授权页面**
   ```
   https://jihuadz.xin/wechat-bind?scene=test_scene_id
   ```
   - 预期：跳转到微信授权页面

2. **确认页面**
   ```
   https://jihuadz.xin/wechat-bind-confirm?scene=test_scene_id&code=test_code
   ```
   - 预期：显示"加载中"页面

3. **成功页面**
   ```
   https://jihuadz.xin/wechat-bind-success
   ```
   - 预期：显示"绑定成功"页面

---

## 🧪 测试流程

### 1️⃣ **生成二维码**
1. 登录系统
2. 进入"账户设置"
3. 点击"微信绑定" → "立即绑定"
4. 看到普通二维码（不是小程序码）

### 2️⃣ **扫码测试**
1. 使用微信扫描二维码
2. 微信内置浏览器打开 H5 页面
3. 自动跳转到微信授权页面
4. 点击"允许"授权

### 3️⃣ **确认绑定**
1. 看到确认页面，显示员工信息
2. 点击"确认绑定"
3. 绑定成功，显示成功页面

### 4️⃣ **验证绑定**
1. 关闭 H5 页面
2. PC 端自动检测到绑定成功
3. 显示"已绑定"状态

---

## 🐛 常见问题

### ❌ 问题1：扫码后显示"无法打开链接"

**原因：**
- 域名未备案
- 授权回调域名配置错误

**解决方案：**
1. 检查域名是否已备案
2. 检查微信公众平台的授权回调域名配置
3. 确保域名格式正确（不含 https:// 和端口号）

---

### ❌ 问题2：授权后返回"获取微信信息失败"

**原因：**
- AppID 或 AppSecret 配置错误
- code 已被使用（code 只能使用一次）

**解决方案：**
1. 检查云函数环境变量配置
2. 重新扫码获取新的 code

---

### ❌ 问题3：确认绑定后显示"该微信已绑定其他账号"

**原因：**
- 该 OpenID 已绑定其他员工账号

**解决方案：**
1. 先在其他账号解绑
2. 然后重新绑定

---

### ❌ 问题4：PC 端轮询检测不到绑定成功

**原因：**
- 网络延迟
- 云函数执行失败

**解决方案：**
1. 刷新页面
2. 检查云函数日志
3. 确认数据库更新成功

---

## 📊 数据库结构

### `wechat_sessions` 集合

| 字段 | 类型 | 说明 |
|-----|------|------|
| sceneId | String | 场景ID |
| type | String | 类型（bind） |
| userId | String | 用户ID |
| username | String | 用户名 |
| status | String | 状态（pending/completed） |
| openid | String | 微信 OpenID |
| createdAt | Date | 创建时间 |
| expiresAt | Date | 过期时间 |
| confirmedAt | Date | 确认时间 |

### `users` 集合（新增字段）

| 字段 | 类型 | 说明 |
|-----|------|------|
| wechatOpenId | String | 微信 OpenID |
| wechatBoundAt | Date | 绑定时间 |

---

## 🔐 安全性说明

### ✅ 已实现的安全措施

1. **场景过期**
   - 二维码 5 分钟后自动失效
   - 无法重复使用

2. **防止伪造**
   - sceneId 包含时间戳和随机字符串
   - 无法猜测或伪造

3. **防止重复绑定**
   - 检查 OpenID 是否已绑定其他账号
   - 一个微信只能绑定一个账号

4. **授权码一次性**
   - 微信 code 只能使用一次
   - 使用后立即失效

---

## 📝 后续扩展

### 🔔 微信消息推送（可选）

绑定成功后，可以扩展实现：
- 任务提醒推送到微信
- 会议通知推送到微信
- 审批消息推送到微信

### 📱 微信扫码登录（可选）

基于已绑定的 OpenID，实现：
- PC 端显示登录二维码
- 微信扫码直接登录
- 无需输入用户名密码

---

## ✅ 配置完成检查清单

- [ ] 微信公众号已认证
- [ ] AppID 和 AppSecret 已获取
- [ ] 授权回调域名已配置（jihuadz.xin）
- [ ] 域名已完成备案
- [ ] 云函数环境变量已配置
- [ ] 云函数已部署成功
- [ ] 前端代码已部署成功
- [ ] H5 页面路由可正常访问
- [ ] 扫码测试通过
- [ ] 绑定功能正常工作

---

**版本：** v1.0.0  
**更新时间：** 2026-01-02  
**维护者：** 际华定制协同办公平台开发团队
