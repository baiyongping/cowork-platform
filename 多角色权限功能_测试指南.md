# 多角色权限功能 - 测试指南

## 功能概述

本次更新实现了**多角色权限功能**，允许一个用户拥有多个角色，最终权限为所有角色权限的**并集**。

### 核心特性

✅ **多角色分配**：一个用户可以拥有最多**5个角色**  
✅ **权限并集计算**：用户的最终权限 = 所有角色权限的并集（后端计算）  
✅ **角色顺序**：按照赋予时间顺序显示  
✅ **最多5个限制**：超过5个后，无法继续选择新角色  

---

## 修改内容

### 1. 前端修改（SystemSettings.tsx）

**位置**：`components/pages/SystemSettings.tsx`

**修改内容**：
- ✅ 角色选择器改为多选（复选框）
- ✅ 添加角色数量限制（最多5个）
- ✅ 显示当前已选择角色数量（X/5）
- ✅ 超过限制后禁用未选角色的复选框
- ✅ 提示语更新："可选择多个角色（最多5个），最终权限为所有角色的并集"

**代码示例**：
```tsx
<label className="block text-sm text-gray-700 mb-2">
  角色（可多选，最多5个）
  <span className="ml-2 text-xs text-gray-500">
    已选择 {editForm.roles.length}/5
  </span>
</label>

{rolePermissions.map((roleItem) => {
  const isChecked = editForm.roles.includes(roleItem.role);
  const canSelect = isChecked || editForm.roles.length < 5;
  
  return (
    <input
      type="checkbox"
      checked={isChecked}
      disabled={!canSelect}
      onChange={...}
    />
  );
})}
```

### 2. 云函数修改（user-management）

**位置**：`cloudfunctions/user-management/index.js`

**新增功能**：`getUserPermissions` action

**功能说明**：
- 查询用户的所有角色（`roles` 数组）
- 查询每个角色的权限配置（`role_permissions` 集合）
- **合并权限（取并集）**：对于每个模块的每个操作，只要有一个角色允许就允许
- 返回合并后的权限对象

**请求参数**：
```javascript
{
  action: 'getUserPermissions',
  userId: '用户ID'
}
```

**返回数据**：
```javascript
{
  success: true,
  data: {
    userId: '用户ID',
    username: '用户名',
    roles: ['admin', 'manager'], // 用户的所有角色
    rolesDetail: [
      { role: 'admin', name: '管理员' },
      { role: 'manager', name: '部门经理' }
    ],
    permissions: {
      // 所有角色权限的并集
      dashboard: {
        view: true,
        edit: true,
        create: true,
        delete: true
      },
      opportunities: { ... },
      ...
    },
    calculatedAt: '2026-01-08T07:30:00.000Z'
  }
}
```

**权限并集计算逻辑**：
```javascript
// 遍历所有角色的权限配置
rolePermissionsResult.data.forEach(roleConfig => {
  const rolePermissions = roleConfig.permissions || {};
  
  Object.keys(rolePermissions).forEach(module => {
    if (!mergedPermissions[module]) {
      // 模块不存在，直接添加
      mergedPermissions[module] = { ...rolePermissions[module] };
    } else {
      // 模块已存在，合并权限（取并集）
      Object.keys(rolePermissions[module]).forEach(action => {
        // 只要有一个角色允许就允许
        mergedPermissions[module][action] = 
          mergedPermissions[module][action] || rolePermissions[module][action];
      });
    }
  });
});
```

### 3. 数据迁移

**数据兼容性处理**：
- ✅ **前端自动清理**：SystemSettings.tsx 中已有逻辑过滤掉无效角色（如 "user", 哈希值等）
- ✅ **向后兼容**：同时支持旧的 `role` 字段和新的 `roles` 数组
- ✅ **云函数兼容**：优先使用 `roles` 数组，如果为空则使用 `role` 字段

**用户数据结构**：
```javascript
// 旧格式（兼容）
{
  username: 'admin',
  role: 'admin'  // 单角色
}

// 新格式
{
  username: 'admin',
  role: 'admin',        // 保留旧字段（兼容）
  roles: ['admin']      // 新的多角色数组
}

// 多角色示例
{
  username: 'manager01',
  role: 'manager',      // 主要角色（第一个）
  roles: ['manager', 'employee']  // 所有角色
}
```

---

## 测试步骤

### 准备工作

1. **部署云函数**
```bash
# 使用MCP工具部署
mcp_call_tool createFunction {
  func: {
    name: "user-management",
    description: "用户管理云函数",
    runtime: "Nodejs16.13",
    ...
  }
}
```

2. **部署前端**
```bash
npm run build
# 部署到测试环境或生产环境
```

### 测试场景

#### 场景1：编辑员工角色（前端测试）

**步骤**：
1. 登录系统（管理员账号）
2. 进入 **系统设置** → **员工管理**
3. 点击任意员工卡片，打开详情页
4. 点击 **编辑** 按钮
5. 在角色选择区域：
   - ✅ 可以勾选多个角色
   - ✅ 显示"已选择 X/5"
   - ✅ 选择5个后，其他角色复选框被禁用
   - ✅ 取消勾选后可以重新选择
6. 点击 **保存** 按钮
7. 验证保存成功，数据库中 `roles` 数组已更新

**预期结果**：
- ✅ 可以选择1-5个角色
- ✅ 超过5个后无法继续选择
- ✅ 保存后数据正确

#### 场景2：权限并集计算（云函数测试）

**步骤**：
1. 打开测试页面：`test-multi-role-permissions.html`
2. 输入测试账号（如 admin / 123456）
3. 点击 **登录** 按钮
4. 登录成功后，点击 **获取权限信息** 按钮
5. 查看返回的权限数据

**预期结果**：
- ✅ 显示用户的所有角色
- ✅ 显示合并后的权限（所有角色权限的并集）
- ✅ 权限正确（只要有一个角色允许就允许）

**测试用例**：

| 角色组合 | 权限并集 |
|---------|---------|
| admin | 所有权限 |
| employee | 基础权限（查看和管理自己的数据） |
| admin + manager | 所有权限（admin的权限包含manager） |
| employee + manager | 基础权限 + 部门管理权限（并集） |

#### 场景3：数据库验证

**步骤**：
1. 在CloudBase控制台打开 `users` 集合
2. 查看测试用户的数据
3. 验证 `roles` 数组字段

**预期结果**：
```javascript
{
  username: 'test01',
  role: 'manager',           // 主要角色
  roles: ['manager', 'employee'],  // 所有角色（按时间顺序）
  ...
}
```

---

## 验收标准

### 功能验收

- ✅ 可以为用户分配多个角色（最多5个）
- ✅ 角色数量限制生效（超过5个无法选择）
- ✅ 已选择角色数量显示正确
- ✅ 保存后数据库更新正确
- ✅ 云函数正确计算权限并集
- ✅ 权限检查正确（只要有一个角色允许就允许）

### 数据验收

- ✅ `roles` 数组字段存在
- ✅ `roles` 数组最多包含5个角色
- ✅ 角色按赋予时间顺序排列
- ✅ 无效角色已被过滤（如 "user", 哈希值）

### 性能验收

- ✅ 权限计算响应时间 < 2秒
- ✅ 前端角色选择流畅（无卡顿）
- ✅ 保存操作正常（无报错）

---

## 常见问题

### Q1: 如何清理无效角色？

**A**: 前端已自动处理。在 `SystemSettings.tsx` 的 `loadEmployees` 函数中（第1090-1122行）：

```typescript
// ✅ 清理roles数组：只保留有效的角色ID
const validRoleIds = ['admin', 'manager', 'employee', 'test'];
let cleanedRoles = emp.roles || [];

cleanedRoles = cleanedRoles.filter((roleId: string) => {
  if (validRoleIds.includes(roleId)) return true;
  if (roleId.length > 20) {
    console.warn('过滤掉疑似哈希值的角色ID:', roleId);
    return false;
  }
  return true;
});
```

### Q2: 如何手动清理某个用户的无效角色？

**A**: 使用CloudBase控制台直接编辑：

1. 打开 `users` 集合
2. 找到目标用户
3. 编辑 `roles` 字段，删除无效角色
4. 保存

### Q3: 权限并集是如何计算的？

**A**: 后端云函数 `getUserPermissions` 中计算：

- 遍历用户的所有角色
- 查询每个角色的权限配置
- 对于每个模块的每个操作：**只要有一个角色允许就允许**
- 返回合并后的权限对象

### Q4: 如果用户没有任何角色会怎样？

**A**: 返回空权限对象：

```javascript
{
  success: true,
  data: {
    userId: 'xxx',
    roles: [],
    permissions: {}
  }
}
```

---

## 后续优化建议

1. **权限缓存**：在前端缓存权限数据，减少云函数调用次数
2. **角色继承**：支持角色之间的继承关系（如 manager 继承 employee）
3. **权限变更通知**：角色权限变更时通知相关用户
4. **权限审计日志**：记录权限变更历史

---

## 联系方式

如有问题，请联系开发团队或提交Issue。

**部署完成时间**：2026-01-08  
**版本**：v2.3.0
