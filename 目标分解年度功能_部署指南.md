# 目标分解年度功能 - 部署指南

## 📋 功能说明

为目标分解表添加年度维度，实现不同年度数据独立管理。

## 🎯 核心变更

### 1. 前端变更
- **GoalDecompositionMultiTable 组件**
  - 新增 `selectedYear` props（接收主页面传递的年度）
  - 查询/保存数据时携带年度参数
  - 表格标题显示年度：`2026年销售目标分解--收入渠道`

- **GoalManagement 主页面**
  - 向 `GoalDecompositionMultiTable` 组件传递 `selectedYear`
  - 使用主页面右上角已有的年度选择器

### 2. 云函数变更
- **goal-decomposition-data**
  - `query` 操作：添加 `year` 字段过滤
  - `batchSave` 操作：保存时记录 `year` 字段

### 3. 数据库变更
- **goalDecompositionData 集合**
  - 新增 `year` 字段（Number类型）
  - 现有数据迁移到 2026 年

---

## 🚀 部署步骤

### 第 1 步：前端代码构建
```bash
# 在项目根目录执行
npm run build:prod
```

### 第 2 步：部署云函数
```bash
# 方式1: 使用 CloudBase CLI
tcb fn deploy goal-decomposition-data

# 方式2: 使用 MCP 工具
# 调用 updateFunctionCode 工具
```

### 第 3 步：执行数据迁移
```bash
# 在项目根目录执行迁移脚本
node scripts/migrate-goal-decomposition-year.js
```

**迁移脚本说明：**
- 自动将所有没有 `year` 字段的数据迁移到 2026 年
- 显示迁移进度和统计信息
- 自动验证迁移结果

### 第 4 步：前端部署
```bash
# 使用 Lighthouse MCP 工具部署
deploy_project_preparation({
  FolderPath: "d:/project/cowork12-21/dist",
  InstanceId: "实例ID",
  Region: "地域",
  ProjectName: "jihua"
})
```

---

## ✅ 验证步骤

### 1. 年度切换验证
1. 登录系统，进入"目标策略管理" → "目标分解"
2. 在主页面右上角切换年度（2025 / 2026 / 2027）
3. 观察目标分解表：
   - 表格标题应显示对应年度：`2026年销售目标分解...`
   - 切换年度时，数据应随之变化

### 2. 数据隔离验证
1. 在 2026 年输入数据并保存
2. 切换到 2025 年，应该**看不到** 2026 年的数据（数据为空或默认值）
3. 切换回 2026 年，之前输入的数据应该**正常显示**

### 3. 新数据保存验证
1. 切换到 2027 年（新年度）
2. 输入数据并保存
3. 切换到其他年度再切回，数据应该正常显示

---

## 🔍 数据验证（CloudBase 控制台）

### 方法1: 通过控制台查询
1. 访问 [CloudBase 控制台 - 数据库](https://tcb.cloud.tencent.com/dev?envId=cowork-9gg9oocb516be5fb#/db/doc/collection/goalDecompositionData)
2. 查看 `goalDecompositionData` 集合
3. 检查数据是否包含 `year` 字段

### 方法2: 使用 MCP 工具
```javascript
// 查询 2026 年的数据
readNoSqlDatabaseContent({
  collectionName: 'goalDecompositionData',
  where: { year: 2026 }
})
```

### 预期结果
所有数据应包含 `year` 字段：
```json
{
  "_id": "xxx",
  "goalTypeId": "2026",
  "year": 2026,
  "tableId": "xxx",
  "tableName": "销售目标分解--收入渠道",
  "value": "1234567",
  ...
}
```

---

## ⚠️ 注意事项

### 1. 数据备份
- **部署前必须备份** `goalDecompositionData` 集合
- 备份方法：CloudBase 控制台 → 数据库 → 导出

### 2. 迁移脚本
- 迁移脚本会将**所有**没有 `year` 字段的数据迁移到 **2026 年**
- 如果需要迁移到其他年份，请修改脚本中的年份值
- 迁移脚本可以**多次执行**（幂等性），不会重复迁移

### 3. 云函数兼容性
- 新云函数向后兼容：即使没有 `year` 参数，查询也能正常工作
- 建议部署后立即执行数据迁移

### 4. 用户数据
- 现有用户的数据会自动迁移到 2026 年
- 用户切换到其他年度时，数据为空（需要重新填写）

---

## 🐛 常见问题

### Q1: 切换年度后，数据没有变化？
**A**: 浏览器缓存导致，按 Ctrl + F5 强制刷新

### Q2: 数据迁移失败？
**A**: 
1. 检查 CloudBase 环境 ID 是否正确
2. 检查网络连接
3. 查看 CloudBase 权限配置

### Q3: 年度选择器没有显示？
**A**: 年度选择器在主页面右上角，不在目标分解页面

### Q4: 保存失败？
**A**: 
1. 检查云函数是否成功部署
2. 查看浏览器控制台错误信息
3. 检查 CloudBase 数据库权限

---

## 📊 预期效果

### 界面效果
```
┌─────────────────────────────────────────────────┐
│  目标策略管理                    选择年度: [2026年 ▼] │
├─────────────────────────────────────────────────┤
│  [销售目标] [商机目标] [产品目标] [经营策略] [目标分解] │
├─────────────────────────────────────────────────┤
│  📍 目标分解多表管理                            │
│                                                 │
│  目标类型: [收入渠道 ▼]                         │
│                                                 │
│  2026年销售目标分解--收入渠道（单位：万元）      │
│  ┌───────────────────────────────────────┐     │
│  │     Q1      Q2      Q3      Q4   【合计】│     │
│  └───────────────────────────────────────┘     │
└─────────────────────────────────────────────────┘
```

### 数据结构
```javascript
// 2026 年数据
{
  _id: "doc1",
  goalTypeId: "2026",
  year: 2026,
  tableId: "table1",
  value: "1234567"
}

// 2027 年数据
{
  _id: "doc2",
  goalTypeId: "2027",
  year: 2027,
  tableId: "table1",
  value: "9876543"
}
```

---

## 📝 回滚方案

如果部署后出现问题，可以按以下步骤回滚：

### 1. 前端回滚
```bash
# 切换到上一个版本
git checkout <previous-version>
npm run build:prod
# 重新部署
```

### 2. 云函数回滚
```bash
# 重新部署上一个版本的云函数
tcb fn deploy goal-decomposition-data --code-secret <previous-version>
```

### 3. 数据库回滚
- 从备份中恢复 `goalDecompositionData` 集合
- 或手动删除 `year` 字段（不推荐）

---

## 📞 技术支持

如有问题，请联系开发团队或查看日志：
- 前端日志：浏览器控制台
- 云函数日志：CloudBase 控制台 → 云函数 → 日志
- 数据库日志：CloudBase 控制台 → 数据库 → 操作日志
