# 开发环境配置指南

## 📋 目录

- [环境概述](#环境概述)
- [前置要求](#前置要求)
- [快速开始](#快速开始)
- [开发工作流](#开发工作流)
- [CloudBase 资源管理](#cloudbase-资源管理)
- [常见问题](#常见问题)
- [开发规范](#开发规范)

---

## 环境概述

### 开发环境定义

**开发环境 = 本地开发环境**

```
前端: localhost:5173 (Vite dev server)
后端: CloudBase 云服务
数据库: CloudBase NoSQL (jihua-oa-dev-3goht9irae4d949f)
```

### 环境对比

| 环境 | 前端地址 | CloudBase 环境 | 用途 |
|------|---------|----------------|------|
| **开发** | `localhost:5173` | `jihua-oa-dev-3goht9irae4d949f` | 本地开发调试 |
| **测试** | `152.136.183.181:3443` | `jihua-oa-dev-3goht9irae4d949f` | 测试验证 |
| **生产** | `https://jihuadz.xin` | `cowork-9gg9oocb516be5fb` | 正式对外服务 |

---

## 前置要求

### 必需软件

1. **Node.js** (推荐 18.x 或更高)
   ```bash
   node -v  # 检查版本
   npm -v
   ```

2. **Git**
   ```bash
   git --version
   ```

3. **CloudBase CLI** (推荐安装)
   ```bash
   npm install -g @cloudbase/cli
   cloudbase -v
   ```

### 必需账号

1. **CloudBase 账号**
   - 已分配开发环境权限
   - 环境ID: `jihua-oa-dev-3goht9irae4d949f`

2. **Git 仓库访问权限**

---

## 快速开始

### 1. 克隆项目

```bash
# 克隆仓库
git clone <仓库地址>
cd cowork12-21

# 检查分支
git branch
```

### 2. 安装依赖

```bash
# 安装前端依赖
npm install
```

### 3. 配置环境

**检查 `cloudbaserc.json`:**
```json
{
  "envId": "jihua-oa-dev-3goht9irae4d949f"
}
```

**检查 `src/lib/cloudbase.ts`:**
```typescript
const app = cloudbase.init({
  env: 'jihua-oa-dev-3goht9irae4d949f'
});
```

### 4. 启动开发服务器

```bash
# 启动前端开发服务器
npm run dev

# 访问 http://localhost:5173
```

✅ **开发服务器启动成功!**

---

## 开发工作流

### 前端开发

```bash
# 1. 创建功能分支
git checkout -b feature/new-feature

# 2. 启动开发服务器
npm run dev

# 3. 修改代码 (自动热重载)

# 4. 提交代码
git add .
git commit -m "feat: 添加新功能"
git push origin feature/new-feature
```

### 后端开发 (云函数)

#### 方式1: CloudBase CLI (推荐)

```bash
# 登录 CloudBase
cloudbase login

# 部署单个云函数
cloudbase functions:deploy auth --envId jihua-oa-dev-3goht9irae4d949f

# 部署所有云函数
cloudbase functions:deploy --envId jihua-oa-dev-3goht9irae4d949f

# 查看云函数日志
cloudbase functions:log auth --envId jihua-oa-dev-3goht9irae4d949f
```

#### 方式2: CloudBase MCP 工具

```javascript
// 1. 查询现有云函数
getFunctionList({ action: 'list' })

// 2. 创建新云函数
createFunction({
  func: { 
    name: 'auth',
    runtime: 'Nodejs18.15',
    timeout: 20
  },
  functionRootPath: 'd:/project/cowork12-21/cloudfunctions',
  force: true
})

// 3. 更新云函数代码
updateFunctionCode({
  name: 'auth',
  functionRootPath: 'd:/project/cowork12-21/cloudfunctions'
})
```

### 数据库操作

#### 创建集合

**方式1: CloudBase 控制台**
1. 访问 [CloudBase 控制台](https://tcb.cloud.tencent.com)
2. 选择环境: `jihua-oa-dev-3goht9irae4d949f`
3. 进入"数据库" → "文档型数据库"
4. 点击"新建集合"

**方式2: CloudBase MCP 工具**
```javascript
// 创建集合
writeNoSqlDatabaseStructure({
  action: 'createCollection',
  collectionName: 'users'
})

// 设置权限
writeSecurityRule({
  resourceType: 'noSqlDatabase',
  resourceId: 'users',
  aclTag: 'PRIVATE'
})
```

#### 数据操作

```javascript
// 查询数据
readNoSqlDatabaseContent({
  collectionName: 'users',
  limit: 20
})

// 插入数据
writeNoSqlDatabaseContent({
  action: 'insert',
  collectionName: 'users',
  documents: [{ name: '测试用户' }]
})

// 更新数据
writeNoSqlDatabaseContent({
  action: 'update',
  collectionName: 'users',
  query: { _id: 'xxx' },
  update: { $set: { name: '新名称' } }
})
```

---

## CloudBase 资源管理

### 查看资源

```bash
# 查看云函数列表
cloudbase functions:list --envId jihua-oa-dev-3goht9irae4d949f

# 查看存储桶
cloudbase storage:list --envId jihua-oa-dev-3goht9irae4d949f

# 查看环境信息
cloudbase env:list
```

### 存储桶配置

**配置 CORS (控制台):**
1. 进入"云存储"
2. 选择存储桶
3. 点击"设置" → "跨域访问CORS"
4. 添加规则:
   ```json
   {
     "allowedOrigins": ["http://localhost:5173"],
     "allowedMethods": ["GET", "POST", "PUT", "DELETE"],
     "allowedHeaders": ["*"],
     "maxAgeSeconds": 3600
   }
   ```

### 权限配置

**设置集合权限:**
```javascript
// 开发环境建议使用宽松权限
writeSecurityRule({
  resourceType: 'noSqlDatabase',
  resourceId: 'users',
  aclTag: 'PRIVATE'  // 或 'READONLY'
})
```

---

## 常见问题

### Q1: 前端无法连接 CloudBase?

**检查清单:**
- [ ] `cloudbaserc.json` 环境ID正确
- [ ] `src/lib/cloudbase.ts` 环境ID正确
- [ ] CloudBase SDK 已正确初始化
- [ ] 网络连接正常

**解决方案:**
```bash
# 检查 SDK 版本
npm list @cloudbase/js-sdk

# 重新安装 SDK (如需要)
npm install @cloudbase/js-sdk@latest
```

### Q2: 云函数部署失败?

**常见原因:**
- 权限不足
- 代码语法错误
- 依赖包缺失
- 超时设置过小

**解决方案:**
```bash
# 检查云函数代码
cd cloudfunctions/auth
npm install

# 本地测试 (如果支持)
node index.js

# 查看详细日志
cloudbase functions:log auth --envId jihua-oa-dev-3goht9irae4d949f
```

### Q3: 数据库操作权限错误?

**检查权限配置:**
```javascript
// 查看当前权限
readSecurityRule({
  resourceType: 'noSqlDatabase',
  resourceId: 'users'
})

// 开发环境可临时放宽权限
writeSecurityRule({
  resourceType: 'noSqlDatabase',
  resourceId: 'users',
  aclTag: 'ADMINWRITE'
})
```

### Q4: 热重载不工作?

**解决方案:**
```bash
# 清除缓存
npm run clean

# 重启开发服务器
npm run dev
```

---

## 开发规范

### Git 提交规范

```bash
# 功能开发
git commit -m "feat: 添加用户管理功能"

# Bug修复
git commit -m "fix: 修复登录验证问题"

# 代码重构
git commit -m "refactor: 重构任务列表组件"

# 文档更新
git commit -m "docs: 更新开发环境配置指南"
```

### 代码规范

- 使用 TypeScript
- 遵循 ESLint 规则
- 使用 Prettier 格式化
- 添加必要的注释

### 测试规范

```bash
# 运行本地测试
npm run test

# 功能测试清单
- [ ] 登录/注册功能
- [ ] 数据增删改查
- [ ] 文件上传下载
- [ ] 权限验证
```

---

## 开发检查清单

### 开始开发前
- [ ] Git 仓库最新
- [ ] 依赖包已安装
- [ ] CloudBase 环境ID正确
- [ ] 开发服务器正常启动

### 开发过程中
- [ ] 代码符合规范
- [ ] 功能正常运行
- [ ] 无控制台错误
- [ ] Git 提交规范

### 开发完成后
- [ ] 本地测试通过
- [ ] 代码已提交
- [ ] 云函数已部署
- [ ] 文档已更新

---

## 相关文档

- [部署方案-测试环境.md](./部署方案-测试环境.md) - 测试环境部署指南
- [部署方案-生产环境.md](./部署方案-生产环境.md) - 生产环境部署指南
- [README.md](./README.md) - 项目总览

---

## 技术支持

**遇到问题?**
- 查看 [CloudBase 文档](https://docs.cloudbase.net/)
- 查看项目 Issues
- 联系团队技术负责人

**快速参考:**
- CloudBase 控制台: https://tcb.cloud.tencent.com
- 开发环境ID: `jihua-oa-dev-3goht9irae4d949f`
- 本地访问: http://localhost:5173

---

**最后更新:** 2025-12-31
**维护者:** 际华开发团队
