# 目标分解多表功能 - 修复指南 v1.1

## 🔍 问题分析

**症状**: 构建完成并启用的分解表,在目标分解页面看不到

**根本原因**:

1. **缺少关联字段**: `decompositionTableSettings` 集合缺少 `goalTypeId` 字段,无法与目标类型关联
2. **视图未集成**: 新的多表分解视图没有被集成到现有的目标分解管理器中
3. **数据流断裂**: 分解表配置和目标类型之间没有建立关联关系

## 🛠️ 修复步骤

### 步骤1: 更新数据模型(添加 goalTypeId 字段)

**修改文件**: `components/DecompositionTablesTab.tsx`

在构建分解表时,添加目标类型选择:

```typescript
// 在表单中添加目标类型选择
<div>
  <label className="block text-sm font-medium text-gray-700 mb-1">
    关联目标类型 *
  </label>
  <select
    value={formData.goalTypeId || ''}
    onChange={(e) => setFormData({ ...formData, goalTypeId: e.target.value })}
    className="w-full px-4 py-2 border border-gray-300 rounded-lg"
  >
    <option value="">请选择目标类型</option>
    <option value="sales">销售目标</option>
    <option value="opportunity">商机目标</option>
  </select>
</div>
```

### 步骤2: 更新云函数

**修改文件**: `cloudfunctions/decomposition-tables/index.js`

```javascript
// 在创建分解表时保存 goalTypeId
case 'create': {
  const tableData = {
    ...data,
    goalTypeId: data.goalTypeId, // 添加此字段
    createdAt: new Date(),
    updatedAt: new Date()
  };
  
  const result = await db.collection('decompositionTableSettings')
    .add(tableData);
  
  return {
    success: true,
    data: { _id: result.id, ...tableData }
  };
}

// 在查询时支持按 goalTypeId 筛选
case 'query': {
  const { goalTypeId } = data;
  let query = db.collection('decompositionTableSettings');
  
  if (goalTypeId) {
    query = query.where({ goalTypeId });
  }
  
  const result = await query
    .orderBy('sortOrder', 'asc')
    .get();
  
  return {
    success: true,
    data: result.data
  };
}
```

### 步骤3: 更新类型定义

**修改文件**: `types/decompositionTable.ts`

```typescript
export interface DecompositionTableConfig {
  _id?: string;
  name: string;
  goalTypeId: string; // 添加此字段: 'sales' | 'opportunity'
  goalTypeName: string; // 添加此字段: '销售目标' | '商机目标'
  horizontalDimensionId: string;
  horizontalDimensionName: string;
  verticalDimensionId: string;
  verticalDimensionName: string;
  columns: TableColumn[];
  sortOrder: number;
  isActive: boolean;
  createdAt?: Date;
  updatedAt?: Date;
}
```

### 步骤4: 更新多表分解组件查询逻辑

**修改文件**: `components/GoalDecompositionMultiTable.tsx`

确保查询时使用正确的 goalTypeId:

```typescript
const loadTables = async () => {
  try {
    setLoading(true);
    
    // 使用正确的云函数名称
    const res = await callFunction({
      name: 'decomposition-tables', // 注意云函数名称
      data: {
        action: 'query',
        goalTypeId: goalTypeId // 传入目标类型ID
      }
    });

    if (res.result.success) {
      setTables(res.result.data || []);
    } else {
      toast.error('加载分解表失败');
    }
  } catch (error) {
    console.error('加载分解表失败:', error);
    toast.error('加载分解表失败');
  } finally {
    setLoading(false);
  }
};
```

### 步骤5: 更新现有数据(数据迁移)

如果已经有构建好的分解表,需要手动添加 `goalTypeId` 字段:

**方法A: 通过控制台手动更新**
1. 登录 CloudBase 控制台
2. 进入数据库 - decompositionTableSettings 集合
3. 编辑每条记录,添加 `goalTypeId` 字段(值为 'sales' 或 'opportunity')

**方法B: 使用数据迁移脚本**

创建临时迁移脚本 `migrate-table-goal-type.js`:

```javascript
// 在 CloudBase 控制台 -> 云函数 -> 创建临时云函数
const cloud = require('wx-server-sdk');
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV });
const db = cloud.database();

exports.main = async (event, context) => {
  try {
    // 获取所有分解表
    const tables = await db.collection('decompositionTableSettings').get();
    
    // 更新每个表,添加 goalTypeId
    const updates = tables.data.map(table => {
      // 根据表名或其他特征判断目标类型
      const goalTypeId = table.name.includes('销售') ? 'sales' : 'opportunity';
      const goalTypeName = goalTypeId === 'sales' ? '销售目标' : '商机目标';
      
      return db.collection('decompositionTableSettings')
        .doc(table._id)
        .update({
          data: {
            goalTypeId,
            goalTypeName
          }
        });
    });
    
    await Promise.all(updates);
    
    return {
      success: true,
      message: `成功更新 ${updates.length} 条记录`
    };
  } catch (error) {
    console.error('迁移失败:', error);
    return {
      success: false,
      error: error.message
    };
  }
};
```

## 🧪 验证步骤

### 1. 验证数据模型

```javascript
// 在浏览器控制台执行
const { callFunction } = window.tcb;

const result = await callFunction({
  name: 'decomposition-tables',
  data: { 
    action: 'query',
    goalTypeId: 'sales' // 或 'opportunity'
  }
});

console.log('查询结果:', result);
// 应该返回该目标类型关联的所有分解表
```

### 2. 验证页面显示

1. 登录系统
2. 进入"目标管理"
3. 点击年度销售目标的"目标分解"按钮
4. 在弹出的管理器中,点击右上角"多表分解视图"按钮
5. **应该能看到**:
   - 该目标类型关联的所有启用的分解表
   - 每个表以卡片形式显示
   - 卡片头部显示表名和维度信息
   - 表格可以直接编辑

### 3. 验证功能完整性

- [ ] 能看到构建好的分解表
- [ ] 表格可以内联编辑
- [ ] 修改后单元格高亮显示
- [ ] 点击"保存全部"能保存数据
- [ ] 底部显示统计信息

## 📦 部署清单

### 云函数部署

```bash
# 1. 部署分解表管理云函数
tcb fn deploy decomposition-tables --force

# 2. 重新部署分解表配置云函数(如果有修改)
tcb fn deploy decomposition-table-configs --force
```

### 前端部署

```bash
# 1. 构建
npm run build

# 2. 部署到测试环境
# 使用 Lighthouse MCP 工具或 SCP

# 3. 访问测试
https://152.136.183.181:3443
```

## 🔧 故障排查

### 问题1: 点击"多表分解视图"后显示"暂无分解表"

**可能原因**:
- 分解表没有设置 `goalTypeId`
- 云函数查询逻辑错误
- goalTypeId 不匹配

**排查步骤**:
1. 检查数据库 `decompositionTableSettings` 集合
2. 确认记录有 `goalTypeId` 字段
3. 确认 `goalTypeId` 值为 'sales' 或 'opportunity'
4. 检查浏览器控制台错误

### 问题2: 表格显示但无法编辑

**可能原因**:
- 表格列配置不正确
- 单元格事件绑定失败

**排查步骤**:
1. 检查 `columns` 字段格式
2. 确认列定义包含 `name`, `type` 等必要字段
3. 查看浏览器控制台 React 错误

### 问题3: 保存数据失败

**可能原因**:
- 云函数权限配置
- 数据格式错误

**排查步骤**:
1. 检查云函数日志
2. 确认集合权限设置
3. 验证保存的数据格式

## 📝 完整工作流程

### 开发人员

1. **构建分解表**:
   - 进入"目标管理" -> "维度设置" -> "分解表构建"
   - 新增分解表
   - **关键**: 选择关联的目标类型(销售目标/商机目标)
   - 配置横纵轴维度
   - 保存并启用

2. **关联表到目标类型** (新增步骤):
   - 在"分解表配置"Tab 中
   - 为每个目标类型选择要使用的分解表
   - 设置表的显示顺序

3. **使用多表分解**:
   - 进入目标管理
   - 点击"目标分解"按钮
   - 点击"多表分解视图"
   - 在各个表中输入数据
   - 点击"保存全部"

### 用户

1. 进入"目标管理"
2. 点击年度目标的"目标分解"按钮
3. 点击右上角"多表分解视图"
4. 查看各个分解表
5. 内联编辑数据
6. 保存更改

## 🎯 成功标准

- [x] 代码已修改(GoalDecompositionManager 添加多表视图)
- [ ] 数据模型已更新(添加 goalTypeId)
- [ ] 云函数已部署
- [ ] 现有数据已迁移(如适用)
- [ ] 功能测试通过
- [ ] 用户可见分解表
- [ ] 数据可正常保存

## 📚 相关文档

- 分解表构建: `目标分解表管理_v1.0.0_完成报告.md`
- 多表功能: `目标分解多表功能_v1.0.0_完成报告.md`
- 快速部署: `目标分解多表_快速部署指南.md`

---

**版本**: v1.1  
**更新日期**: 2026-01-06  
**状态**: ✅ 代码已更新,等待数据模型更新和部署
