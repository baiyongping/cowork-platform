# 清除浏览器缓存指南 - 解决CORS问题

## ❌ 当前问题

虽然安全域名已经添加成功（`http://152.136.183.181:3000`），但浏览器仍报CORS错误。

这通常是因为：
1. 浏览器缓存了旧的CORS配置
2. CloudBase需要几分钟来传播新配置

## 🔧 解决方案

### 方法1: 完全清除浏览器缓存（推荐）

#### Chrome浏览器
1. 按 `Ctrl+Shift+Delete` 打开清除浏览器数据
2. 时间范围选择：**全部**
3. 勾选以下选项：
   - ✅ Cookie及其他网站数据
   - ✅ 缓存的图片和文件
4. 点击"清除数据"
5. 关闭浏览器，重新打开
6. 访问: `http://152.136.183.181:3000`

#### Edge浏览器
1. 按 `Ctrl+Shift+Delete`
2. 时间范围选择：**所有时间**
3. 勾选：
   - ✅ Cookie和其他站点数据
   - ✅ 缓存的图像和文件
4. 清除数据
5. 重启浏览器

### 方法2: 无痕/隐私模式（快速测试）

1. 按 `Ctrl+Shift+N`（Chrome）或 `Ctrl+Shift+P`（Edge）打开隐私窗口
2. 访问: `http://152.136.183.181:3000`
3. 如果能正常访问，说明是缓存问题，回到方法1清除缓存

### 方法3: 硬性重新加载（最快）

1. 打开 `http://152.136.183.181:3000`
2. 打开开发者工具（F12）
3. 右键点击刷新按钮（浏览器地址栏左边）
4. 选择"清空缓存并硬性重新加载"

### 方法4: 等待CloudBase配置生效（5-10分钟）

CloudBase的CORS配置可能需要几分钟传播到所有节点。

等待5-10分钟后：
1. 关闭浏览器
2. 重新打开
3. 访问测试环境

---

## 🔍 验证配置

### 检查安全域名是否添加

访问CloudBase控制台：
https://console.cloud.tencent.com/tcb/env/safety?envId=jihua-oa-dev-3goht9irae4d949f

应该能看到：
```
✅ http://152.136.183.181:3000
```

### 检查浏览器控制台

清除缓存后，打开 `http://152.136.183.181:3000`，按F12查看控制台：

**成功的日志**：
```
✅ 匿名登录成功 - 用户UID: xxx
✓ CloudBase 自动认证完成
✅ 检测到CloudBase登录态
✅ 查询微信用户成功  ← 这里应该成功
```

**失败的日志**：
```
❌ Access to XMLHttpRequest ... blocked by CORS policy
❌ 查询或创建微信用户失败
```

---

## 💡 如果仍然失败

### 临时方案：使用CloudBase默认域名

如果仍然无法访问，可以临时使用CloudBase的默认托管域名：

1. 先部署到CloudBase静态托管
2. 使用CloudBase提供的默认域名访问
3. 这个域名已经预配置好，不会有CORS问题

### 检查网络

```bash
# 测试能否访问CloudBase API
curl -v https://jihua-oa-dev-3goht9irae4d949f.ap-shanghai.tcb-api.tencentcloudapi.com/web
```

如果网络不通，可能是防火墙或网络问题。

---

## 📋 问题排查清单

完成以下检查：

- [ ] CloudBase控制台确认域名已添加
- [ ] 确认域名格式正确（包含http://和端口号）
- [ ] 完全清除浏览器缓存
- [ ] 尝试无痕模式访问
- [ ] 等待5-10分钟后重试
- [ ] 检查网络连接
- [ ] 尝试其他浏览器

---

## 🎯 推荐操作步骤

1. **立即操作**：清除浏览器缓存（方法1）
2. **等待5分钟**：让CloudBase配置传播
3. **重新访问**：`http://152.136.183.181:3000`
4. **验证成功**：能正常登录和使用

如果5分钟后仍有问题，可能需要检查：
- 防火墙设置
- CloudBase环境状态
- 网络连接
