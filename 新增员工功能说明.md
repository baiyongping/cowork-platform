# 新增员工功能说明

## 功能概述
在员工管理页面增加了"新增员工"按钮，可以直接创建新员工账号。

## 访问路径
**系统设置** → **员工管理** → **新增员工**（绿色按钮）

## 功能特性

### 1. 表单字段（简化版）

#### ✅ 基本信息
- **用户名**（必填）- 登录用户名，自动检查可用性
- **姓名**（必填）- 员工真实姓名
- **密码**（必填）- 登录密码，至少6位
- **确认密码**（必填）- 确认密码一致性

#### ✅ 联系信息
- **手机号**（必填）- 11位手机号，13-19开头

#### ✅ 状态设置
- **状态**（必填）- 在职/离职/休假

### 2. 自动设置的默认值
- **部门**: 空数组 `[]`
- **角色**: `employee`（普通员工）

### 3. 用户名验证
- ✅ 实时检查用户名是否可用
- ✅ 失焦时自动验证
- ✅ 视觉反馈：
  - 🔴 红色边框 = 已被使用
  - 🟢 绿色边框 = 可用
  - ⚪ 灰色边框 = 未验证

### 4. 密码功能
- ✅ 显示/隐藏密码切换
- ✅ 密码长度验证（≥6位）
- ✅ 密码一致性验证

### 5. 手机号验证
- ✅ 格式验证：11位数字
- ✅ 号段验证：13-19开头

### 6. 安全特性
- ✅ 防止重复提交
- ✅ 加载状态显示
- ✅ 用户名重复检查（前端+后端）
- ⚠️ **注意**: 密码当前未加密，生产环境需实现加密存储

### 7. 操作日志
- ✅ 自动记录创建操作
- ✅ 包含操作人、时间、详情

## 使用流程

1. **打开弹窗**
   - 进入"系统设置" → "员工管理"
   - 点击绿色的"新增员工"按钮

2. **填写表单**
   ```
   用户名: zhangsan (自动检查可用性)
   姓名: 张三
   密码: ******
   确认密码: ******
   手机号: 13812345678
   状态: 在职
   ```

3. **提交创建**
   - 点击"创建员工"按钮
   - 等待验证和创建完成

4. **自动刷新**
   - 创建成功后自动刷新员工列表
   - 显示成功提示

## 按钮布局

```
[新增员工] [邀请注册] [回收站]
  绿色      蓝色      橙色
```

## 权限要求
- 需要 `settings.employees.create` 权限
- 与"邀请注册"共享权限检查

## 字段对比（新旧版本）

### ✅ 保留字段
- 用户名
- 姓名
- 密码
- 确认密码
- 手机号
- 状态

### ❌ 已移除字段
- 邮箱
- 部门选择
- 角色选择

### 🔧 默认值设置
- `departments: []`
- `role: "employee"`

## 注意事项

1. **初始状态**
   - 姓名和密码默认为空（不预填充）
   - 用户需要手动输入所有信息

2. **密码安全**
   - 当前版本密码明文存储
   - 生产环境务必实现加密存储

3. **后续权限配置**
   - 创建后可在员工详情中修改部门和角色
   - 通过员工管理页面的编辑功能调整

4. **用户名唯一性**
   - 前端实时检查
   - 后端提交前二次验证
   - 确保不会创建重复用户名

## 相关文件

- **组件文件**: `components/CreateEmployeeModal.tsx`
- **页面文件**: `components/pages/SystemSettings.tsx`
- **数据集合**: `users`（CloudBase NoSQL）
- **日志集合**: `operation_logs`

## 下一步建议

1. 实现密码加密存储（bcrypt / 云开发密码加密）
2. 添加批量导入员工功能
3. 支持从Excel导入员工信息
4. 增加员工头像上传功能
