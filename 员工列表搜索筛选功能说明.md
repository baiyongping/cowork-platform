# 员工列表搜索筛选功能说明

## 功能概述

在员工管理页面新增了搜索和筛选功能，方便快速查找和过滤员工信息。

## 功能特性

### 1. 搜索功能
- **搜索范围**: 员工姓名、用户名、手机号
- **实时搜索**: 输入内容即时过滤结果
- **清空按钮**: 快速清除搜索内容
- **搜索提示**: 输入框提示"搜索姓名、用户名或手机号..."

### 2. 部门筛选
- **下拉选择**: 显示所有存在的部门列表
- **全部部门**: 默认选项，显示所有员工
- **精确匹配**: 只显示包含选定部门的员工
- **多部门员工**: 支持筛选有多个部门的员工

### 3. 角色筛选
- **角色选项**: 
  - 全部角色
  - 管理员
  - 普通员工
- **智能识别**: 自动识别员工的角色字段
- **默认角色**: 未设置角色的员工默认为普通员工

### 4. 组合筛选
- **多条件**: 支持同时使用搜索、部门筛选和角色筛选
- **实时统计**: 显示符合条件的员工数量
- **空结果提示**: 无匹配结果时显示友好提示

## 界面布局

```
┌─────────────────────────────────────────────────────────────┐
│  员工管理                                    [回收站]        │
│  管理已审核通过的员工信息                                    │
├─────────────────────────────────────────────────────────────┤
│  [🔍 搜索框]  [部门▼]  [角色▼]  找到 X 名员工              │
└─────────────────────────────────────────────────────────────┘
```

## 使用方法

### 搜索员工
1. 在搜索框输入关键词
2. 系统自动匹配姓名、用户名或手机号
3. 点击 ✕ 按钮清空搜索

### 按部门筛选
1. 点击"全部部门"下拉框
2. 选择要查看的部门
3. 列表只显示该部门的员工

### 按角色筛选
1. 点击"全部角色"下拉框
2. 选择"管理员"或"普通员工"
3. 列表只显示对应角色的员工

### 组合使用
1. 先选择部门和角色缩小范围
2. 再使用搜索框精确查找
3. 右侧显示符合条件的员工总数

## 技术实现

### 状态管理
```typescript
const [searchKeyword, setSearchKeyword] = useState('');
const [filterDepartment, setFilterDepartment] = useState('all');
const [filterRole, setFilterRole] = useState('all');
```

### 过滤逻辑
```typescript
const getFilteredEmployees = () => {
  let filtered = [...employees];
  
  // 搜索过滤
  if (searchKeyword.trim()) {
    filtered = filtered.filter(emp => 
      emp.name?.includes(keyword) ||
      emp.username?.includes(keyword) ||
      emp.phone?.includes(keyword)
    );
  }
  
  // 部门过滤
  if (filterDepartment !== 'all') {
    filtered = filtered.filter(emp => 
      emp.departments?.includes(filterDepartment)
    );
  }
  
  // 角色过滤
  if (filterRole !== 'all') {
    filtered = filtered.filter(emp => 
      emp.roles?.includes(filterRole)
    );
  }
  
  return filtered;
};
```

## 注意事项

1. **大小写**: 搜索不区分大小写
2. **空格处理**: 自动去除首尾空格
3. **多部门**: 员工可能属于多个部门
4. **旧数据兼容**: 兼容没有roles字段的员工记录
5. **实时更新**: 修改筛选条件后立即生效

## 性能优化

- ✅ 使用前端过滤，无需频繁请求后端
- ✅ 数组操作优化，避免重复遍历
- ✅ 组件只在必要时重新渲染
- ✅ 部门和角色列表动态生成

## 后续优化建议

1. **搜索高亮**: 在结果中高亮显示匹配的关键词
2. **历史记录**: 保存常用的搜索条件
3. **批量操作**: 支持勾选筛选结果进行批量操作
4. **导出功能**: 导出筛选后的员工列表
5. **高级筛选**: 增加在职状态、入职时间等筛选条件

## 测试场景

### 基础功能测试
- ✅ 搜索姓名
- ✅ 搜索用户名
- ✅ 搜索手机号
- ✅ 部门筛选
- ✅ 角色筛选
- ✅ 清空搜索

### 组合测试
- ✅ 搜索 + 部门筛选
- ✅ 搜索 + 角色筛选
- ✅ 部门 + 角色筛选
- ✅ 三项全部使用

### 边界测试
- ✅ 无搜索结果
- ✅ 空员工列表
- ✅ 特殊字符搜索
- ✅ 超长关键词

## 更新日期

2025-12-14

## 修改文件

- `components/pages/SystemSettings.tsx`: 新增搜索筛选功能
