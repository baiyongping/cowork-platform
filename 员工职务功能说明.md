# 员工职务功能说明

## 📅 更新时间
2025-12-14

## ✅ 完成内容

### 1. 员工管理模块 - 新增职务字段

#### 📝 编辑表单新增字段
在员工编辑模态框中，在"直属上级"字段之后新增**职务**输入框：

**字段配置：**
- **字段名称**：职务
- **字段位置**：直属上级字段之后、角色字段之前
- **输入类型**：文本输入框
- **提示文本**：请输入职务，如：总经理、部门主管、销售经理等
- **说明文字**：填写员工的职务名称
- **是否必填**：否（可选字段）

#### 💾 数据保存
- 新增 `position` 字段保存到users集合
- 在更新员工信息时自动保存职务字段
- 兼容旧数据（没有position字段的员工显示为"-"）

#### 🔧 表单状态管理
```typescript
const [editForm, setEditForm] = useState({
  name: '',
  phone: '',
  departments: [] as string[],
  roles: [] as string[],
  status: '在职',
  supervisorId: '',
  position: '' // 新增职务字段
});
```

### 2. 团队模块 - 显示职务替代角色

#### 📊 字段调整
在个人信息页面的"团队"标签中：

**修改前：**
- 显示3列：姓名、用户名、手机号
- 右侧显示角色标签（管理员/成员）

**修改后：**
- 显示4列：姓名、用户名、手机号、**职务**
- 移除角色标签显示
- 职务为空时显示"-"

#### 🎨 界面布局
```typescript
{/* 信息列 - 改为4列网格 */}
<div className="flex-1 grid grid-cols-1 md:grid-cols-4 gap-2">
  {/* 姓名 */}
  <div>
    <span className="text-xs text-gray-500">姓名</span>
    <p className="text-sm font-medium text-gray-900">
      {employee.name || '-'}
    </p>
  </div>
  
  {/* 用户名 */}
  <div>
    <span className="text-xs text-gray-500">用户名</span>
    <p className="text-sm text-gray-700">
      {employee.username}
    </p>
  </div>
  
  {/* 手机号 */}
  <div>
    <span className="text-xs text-gray-500">手机号</span>
    <p className="text-sm text-gray-700">
      {employee.phone || employee.username || '-'}
    </p>
  </div>
  
  {/* 职务 */}
  <div>
    <span className="text-xs text-gray-500">职务</span>
    <p className="text-sm text-gray-700">
      {employee.position || '-'}
    </p>
  </div>
</div>
```

## 📁 修改文件

### 1. `components/pages/SystemSettings.tsx`
- ✅ editForm状态添加position字段
- ✅ handleEditEmployee加载position数据
- ✅ handleSaveEdit保存position数据
- ✅ 编辑表单添加职务输入框

### 2. `components/pages/AccountSettings.tsx`
- ✅ 团队显示改为4列网格
- ✅ 添加职务列显示
- ✅ 移除角色标签

## 🔄 数据库字段

### users集合新增字段
```javascript
{
  position: String, // 职务名称，如：总经理、部门主管、销售经理等
  // ... 其他现有字段
}
```

## 🎯 功能特点

### 1. 职务字段特点
- ✅ **灵活输入**：不限制职务名称，可自由填写
- ✅ **可选字段**：不强制要求填写，为空显示"-"
- ✅ **兼容旧数据**：已有员工没有职务字段也能正常显示
- ✅ **即时保存**：与其他字段同步保存到数据库

### 2. 团队显示优化
- ✅ **更直观**：职务比角色更能体现员工实际岗位
- ✅ **信息完整**：4列显示涵盖员工关键信息
- ✅ **响应式布局**：移动端自动调整为单列显示
- ✅ **空值处理**：职务为空时友好显示"-"

## 🧪 测试建议

### 员工管理模块测试
1. 打开系统设置 > 员工管理
2. 点击任意员工卡片进入编辑
3. 在"职务"输入框中填写职务名称
4. 保存并验证数据是否正确保存
5. 重新打开编辑确认职务已保存

### 团队模块测试
1. 登录系统，进入个人信息页面
2. 点击"团队"标签
3. 展开任意部门查看员工列表
4. 确认显示4列：姓名、用户名、手机号、职务
5. 检查有职务的员工显示是否正确
6. 检查无职务的员工是否显示"-"
7. 测试响应式布局（缩放浏览器窗口）

### 数据兼容性测试
1. 测试已有员工（没有position字段）是否正常显示
2. 测试新建员工是否可以填写职务
3. 测试职务为空时的显示效果
4. 测试职务修改是否生效

## 📊 使用示例

### 职务命名建议
- **高层管理**：总经理、副总经理、总监
- **中层管理**：部门经理、部门主管、区域经理
- **基层管理**：组长、主管、班组长
- **专业岗位**：销售经理、技术经理、财务经理
- **普通岗位**：销售代表、工程师、会计

### 显示效果示例
```
部门：销售部
┌─────────┬──────────┬─────────────┬───────────┐
│ 姓名    │ 用户名   │ 手机号      │ 职务      │
├─────────┼──────────┼─────────────┼───────────┤
│ 张三    │ zhangsan │ 13800138000 │ 销售经理  │
│ 李四    │ lisi     │ 13900139000 │ 销售代表  │
│ 王五    │ wangwu   │ 13700137000 │ -         │
└─────────┴──────────┴─────────────┴───────────┘
```

## 💡 注意事项

1. **职务与角色的区别**
   - **角色**：系统权限相关，如管理员、普通员工（在角色权限管理中配置）
   - **职务**：实际工作岗位，如总经理、销售经理（自由填写）

2. **职务字段用途**
   - 主要用于团队展示和员工信息管理
   - 不影响系统权限控制
   - 便于了解员工实际岗位

3. **数据迁移**
   - 现有员工的职务字段为空
   - 需要手动编辑补充职务信息
   - 支持批量导入（如需要）

4. **响应式布局**
   - 大屏幕：4列横向显示
   - 中等屏幕：2列显示
   - 小屏幕：单列纵向显示

## 🎉 完成状态

- ✅ 员工管理添加职务字段
- ✅ 职务字段正常保存到数据库
- ✅ 团队显示替换为职务字段
- ✅ 移除角色标签显示
- ✅ 兼容旧数据
- ✅ 响应式布局适配
- ✅ 文档完善

功能开发完成，可以正常使用！
