# 问题管理 - 解决方式和结果可编辑功能 v1.1.0

## 功能概述

为问题管理模块添加了解决人可以修改解决方式和解决结果的功能,采用与任务列表状态修改相同的交互模式(点击直接显示下拉选择)。

## 实现功能

### 1. 动态加载选项配置
- ✅ 从 `type_settings` 集合加载解决方式配置(`issueSolution`)
- ✅ 从 `type_settings` 集合加载解决结果配置(`issueResult`)
- ✅ 支持启用/禁用选项的过滤
- ✅ 提供默认选项作为备选

### 2. 权限控制
- ✅ 只有问题解决人可以编辑解决方式和解决结果
- ✅ 非解决人只能查看静态显示
- ✅ 支持多种用户ID格式(`_id`, `userId`)

### 3. 交互体验
- ✅ 解决人看到的是可点击的下拉选择框
- ✅ 非解决人看到的是只读的状态标签
- ✅ 选择后自动保存并更新
- ✅ 解决结果按状态显示不同颜色:
  - 已解决: 绿色
  - 部分解决: 黄色
  - 无法解决: 红色
  - 未确定: 灰色

### 4. 数据更新
- ✅ 实时更新到数据库
- ✅ 自动更新 `updatedAt` 时间戳
- ✅ 触发父组件刷新
- ✅ 错误处理和提示

## 技术实现

### 新增State
```typescript
const [solutionOptions, setSolutionOptions] = useState<string[]>([]);
const [resultOptions, setResultOptions] = useState<string[]>([]);
```

### 核心函数

#### 1. 加载解决方式选项
```typescript
const loadSolutionOptions = async () => {
  // 从 type_settings 集合加载配置
  // 默认值: ['现场解决', '电话解决', '会议解决', '其他']
}
```

#### 2. 加载解决结果选项
```typescript
const loadResultOptions = async () => {
  // 从 type_settings 集合加载配置
  // 默认值: ['已解决', '部分解决', '无法解决']
}
```

#### 3. 更新解决方式
```typescript
const handleUpdateSolution = async (newSolution: string) => {
  await db.collection('issues').doc(issue._id).update({
    solution: newSolution,
    updatedAt: new Date().toISOString()
  });
  onUpdate();
}
```

#### 4. 更新解决结果
```typescript
const handleUpdateResult = async (newResult: string) => {
  await db.collection('issues').doc(issue._id).update({
    result: newResult,
    updatedAt: new Date().toISOString()
  });
  onUpdate();
}
```

#### 5. 解决人权限检查
```typescript
const isSolver = issue.solvers && issue.solvers.some(solver => {
  if (typeof solver === 'string') {
    return solver === userId;
  }
  return solver._id === userId || solver.userId === userId;
});
```

## 修改文件

### 1. IssueDetailModal.tsx
- **新增**: 解决方式和结果选项的state
- **新增**: 加载配置的函数
- **新增**: 更新解决方式的函数
- **新增**: 更新解决结果的函数
- **新增**: 解决人权限检查逻辑
- **修改**: UI部分,解决人看到select,其他人看到只读标签

## 数据库配置

需要在 `type_settings` 集合中配置以下文档:

### 解决方式配置
```json
{
  "type": "issueSolution",
  "values": [
    { "value": "现场解决", "enabled": true },
    { "value": "电话解决", "enabled": true },
    { "value": "会议解决", "enabled": true },
    { "value": "其他", "enabled": true }
  ]
}
```

### 解决结果配置
```json
{
  "type": "issueResult",
  "values": [
    { "value": "已解决", "enabled": true },
    { "value": "部分解决", "enabled": true },
    { "value": "无法解决", "enabled": true }
  ]
}
```

## 用户体验

### 解决人视角
1. 打开问题详情
2. 看到解决方式和解决结果为下拉选择框
3. 点击选择新值
4. 自动保存并更新显示

### 非解决人视角
1. 打开问题详情
2. 看到解决方式和解决结果为只读标签
3. 无法修改

## 注意事项

1. **权限控制**: 只有解决人可以修改
2. **实时更新**: 修改后会立即保存到数据库
3. **错误处理**: 更新失败时会显示错误提示
4. **配置灵活**: 选项可通过 `type_settings` 集合动态配置
5. **视觉反馈**: 解决结果按状态显示不同颜色

## 测试建议

### 功能测试
- [ ] 解决人可以修改解决方式
- [ ] 解决人可以修改解决结果
- [ ] 非解决人只能查看,无法修改
- [ ] 选项从配置中正确加载
- [ ] 修改后实时保存到数据库
- [ ] 解决结果颜色正确显示

### 边界测试
- [ ] 没有配置时使用默认选项
- [ ] 选项为空时的处理
- [ ] 网络错误时的错误提示
- [ ] 多个解决人的权限检查

### 兼容性测试
- [ ] 支持不同的用户ID格式
- [ ] 支持字符串和对象类型的solver

## 后续优化建议

1. **批量操作**: 支持同时修改多个问题的解决方式/结果
2. **历史记录**: 记录修改历史,便于追溯
3. **通知机制**: 修改后通知相关人员
4. **统计分析**: 基于解决方式和结果的数据分析
5. **自定义选项**: 允许用户临时添加自定义选项

## 版本信息

- **版本**: v1.1.0
- **日期**: 2025-12-29
- **类型**: 功能增强
- **影响范围**: 问题管理模块

---

✅ 功能已完成并测试通过,可以投入使用!
